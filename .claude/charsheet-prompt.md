The key visual is FINAL: `concepts/keyvisual/FINAL_key_visual.png` (attached as an image — study it: the tamer girl, pastel dawn palette, Korean webtoon semi-realistic rendering, soft light). From now on every asset must match this image. Use your built-in image generation tool (no APIs, no packages). Write only inside `concepts/character/` and `concepts/monsters/`.

## Part 1 — Main character reference sheet (`concepts/character/`)
Same girl exactly (face, tied-up black ponytail, ivory/sky-blue cloak, layered outfit with straps and pouches, lantern-cage, boots). Transparent background PNG, 1024×1024 unless stated, full body, feet at the bottom with ~8% padding, no text.
1. `tamer_front.png` — front view, relaxed stance, lantern held at her side.
2. `tamer_side.png` — side view facing RIGHT (this is the in-game battle orientation), walking stance.
3. `tamer_capture.png` — side view facing RIGHT, capture action: lantern thrust forward, cage glowing softly, cloak swept back.
4. `fox_companion.png` — her white fox companion, side view facing RIGHT, sitting; 768×768.
5. `sheet.png` — the four above composed on one pastel-dawn gradient board with labels (Pillow), for review.

## Part 2 — Monster style probe (`concepts/monsters/`)
Three monsters in the SAME rendering language and palette family (pastel, soft light, clean webtoon lines, semi-realistic creature anatomy — NOT chibi, NOT mascot-cute; think "illustrated field-guide creature that is charming but credible"). Transparent PNG 1024×1024, side view facing LEFT (enemies face the player's party), full body, no text.
1. `slime_pastel.png` — a translucent dawn-colored slime with soft internal glow.
2. `fox_wild.png` — a wild variant of the companion fox (untamed, slightly larger, dusk-purple tail tips).
3. `lakebat.png` — a mist-bat with pale lavender wings.
Then `sheet.png` — the three on one board with labels.

Rules: reject and regenerate anything that drifts into chibi, thick black outlines, saturated primary colors, or night scenes. Verify each PNG exists, has real transparency (alpha), and is > 50KB. Write `concepts/character/README.md` and `concepts/monsters/README.md` with the final prompts.
