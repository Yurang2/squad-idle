You are implementing phase **M3 — 캠프 (camp)** of `GAME_DESIGN.md` v0.2 §5. Read `GAME_DESIGN.md`, `MONSTERS.md`, `STYLE.md`, `AGENTS.md`, `CHANGELOG.md` first. M1–M2 are done and verified; keep structure and public names. Follow every rule in `AGENTS.md`. You may use your built-in image generation tool ONLY for the six facility illustrations described below (no APIs).

## M3 scope — the second screen where captured monsters live and produce
1. **Data** (`js/data-camp.js`): six facilities per the design doc table — 모닥불(center; camp level = facility cap; party slot +1 at camp Lv3 and Lv5), 우리(roster cap 40/60/80/100/120), 작업장(enhance cost −5%/level; produces 강화석), 제단(evolution bump chance +2%/level; catch rate +3%/level), 창고(offline cap 8/12/16/20/24h), 텃밭·광산 (production slots for placed monsters: 2/3/4/5/6). Levels 1–5, costs in 골드 + materials 나무/돌/정수. Materials: 나무/돌 drop from kills (8% each, region-weighted), 정수 from boss kills; placed monsters produce 나무/돌/강화석/골드 per hour based on `campJob` match (×2 when matching) and level.
2. **Logic** (`js/game-camp.js`, DOM-free): `Game.build(facilityId)`, `Game.assign(uid, facilityId)`, `Game.unassign(uid)`, production accrual on tick and in the offline 자루 (materials + camp output), `Game.collect()` for accrued production (auto-collected when opening the camp tab). Facility effects apply everywhere they are referenced (roster cap, party slots, enhance cost, evo bump, catch rate, offline cap). A monster is either in party, in a facility, or idle — never two at once. Construction is instant (no timers).
3. **Camp screen** (`js/ui-camp.js`): a new bottom tab **캠프** (5 tabs total: 탐험/몬스터/캠프/도감/설정). A horizontally scrollable pastel-dawn scene (about 2.2× viewport width, snap to the campfire on open) with the six facilities as illustrated stations; idle monsters wander slowly between stations (simple CSS translate animation using the species art at 64px, flipped as needed); assigned monsters sit at their facility. Tap a facility → sheet with level, effect, upgrade cost/button, assignment slots (tap a slot to pick an idle monster; show campJob match with a small ✦). Header shows materials (나무/돌/정수/강화석). Per `STYLE.md`: translucent panels, thin colored outlines, inline SVG line icons, slow motion, no emoji.
4. **Facility art**: generate six illustrations with your image tool in the pastel-dawn style of `concepts/keyvisual/FINAL_key_visual.png` (attached): `assets/camp/campfire.png`, `pen.png`, `workshop.png`, `altar.png`, `storehouse.png`, `garden.png` — each 768×768 RGBA transparent, a small cozy tamer-camp structure (canvas, wood, lantern light, mist), no characters, no text. Verify RGBA + transparent corners with Pillow; fall back to a magenta key if needed. Also one `assets/camp/ground.png` 2048×640 opaque: a soft meadow-by-the-lake ground strip, 3-layer simplified per STYLE.md §4.
5. **Save**: `schemaVersion: 6`, migration from v5 (camp defaults). Offline report adds camp production lines.
6. **Tests**: build costs/levels, facility effects on caps and rates, assign/unassign exclusivity with party, production accrual math (online and offline with the 창고 cap), collect idempotence, v5→v6 migration, all seven camp art files exist.
7. `CHANGELOG.md`: "0.7.0 · M3" with DECISION: notes.

## Definition of done
`node --check js/*.js`, `node test/run.js` exit 0; browser at 375px: 캠프 tab opens on the campfire, scroll reveals all six stations with art, assign a monster to 텃밭 and see production accrue after simulated ticks, upgrade 우리 raises the roster cap, 자루 after 3h offline lists camp output; no console errors; no horizontal scroll of the *page* (the camp scene scrolls inside its own container).
