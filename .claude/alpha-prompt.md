Re-export task. The seven images attached to this prompt (concepts/character/tamer_front.png, tamer_side.png, tamer_capture.png, fox_companion.png and concepts/monsters/slime_pastel.png, fox_wild.png, lakebat.png) are approved designs, but they were saved as opaque RGB files, several with a checkerboard baked into the pixels. Produce **the same designs with a real alpha channel**.

For each of the seven: use your built-in image generation tool with the corresponding attached image as the reference, ask for exactly the same character/creature, pose, colors and rendering, on a **transparent background** (request transparent output explicitly; if the tool ignores it, generate on a flat pure `#FF00FF` magenta background and convert magenta to alpha with Pillow, feathering the edge 1px). Save to the same paths, overwriting. Sizes: 1024×1024 (fox_companion 768×768 is fine). Facing directions must be preserved (tamer side/capture face RIGHT; monsters face LEFT).

Then verify with Pillow that every file has mode RGBA, that the corner pixels are fully transparent, and that no magenta remains; rebuild `concepts/character/sheet.png` and `concepts/monsters/sheet.png` on the pastel board. Update `validation.json` files accordingly. Write only inside `concepts/character/` and `concepts/monsters/`.
