OpenAI Codex v0.153.4
--------
workdir: D:\Git\Inv\squad-idle
model: gpt-6-astra
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 01a07b80-d9a0-7423-a12c-c871a77f7633
--------
user
You are doing the **ART/UI restyle pass** for this game. Read `STYLE.md` (the new visual spec — it fully replaces the current dark look), `ASSET_SPEC.md`, `assets/manifest.json`, `AGENTS.md`, and `CHANGELOG.md`. Keep all game logic, public function names, and tests intact. Follow every rule in `AGENTS.md`.

## Scope — implement ALL of it

1. **`style.css` rewrite** to STYLE.md: sky gradient background, cream panels with 3px dark-brown outlines, rounded 18px, hard bottom shadows, orange gradient primary buttons, tab bar with the active tab popping up, ribbon titles on sheets, rarity color variables, Jua/Gothic A1 font with system fallback (load via Google Fonts `<link>` — must degrade gracefully offline). Everything readable at 375px width, tap targets ≥ 44px, no horizontal scroll. Remove every dark-fantasy color.
2. **Sprites → images**: `DATA.assets` from `assets/manifest.json` (inline the mapping into `js/data.js`; do not fetch JSON at runtime). In the battle scene use SVG `<image href>` (or absolutely-positioned `<img>` — pick one and keep it simple) for mercenaries (idle/attack swap 120ms on attack), monsters (region-specific per ASSET_SPEC), and the region background. Keep the existing vector figures as a fallback when an asset is missing from the manifest or fails to load (`onerror`). Sprite base sizes: merc 96px tall, monster 96px, boss 140px; feet on the ground line; 2s breathing idle; white flash on hit via CSS filter; fade on death.
3. **Icons**: HUD gold/coin chips, equipment slot icons in the inventory grid and mercenary cards, skill book — use `icon/*.png`.
4. **Drop / fusion / level-up animations** per STYLE.md "모션" (pop with rotation and rarity glow, card flip, rainbow edge flash for unique+, LEVEL UP label, count-up numbers). Keep them short and non-blocking.
5. **Tab bar & sheets** restyled; make the 장비 tab badge (drop count) a red circle with white number.
6. Take care of the `index.html` head: theme-color `#8FD3FF`, favicon from `icon/weapon.png` if present.
7. **Tests**: `node test/run.js` must still pass unchanged. Add one test that `DATA.assets` contains all 28 manifest keys and that every path exists on disk.
8. `CHANGELOG.md`: "0.x · Restyle" section with DECISION: notes.

## Definition of done
- `node --check js/*.js` clean; `node test/run.js` exit 0.
- Browser at 375px: bright look per STYLE.md, image sprites visible and animating, background image per region, no console errors (a missing-asset fallback must not log errors either), no horizontal scroll.

2026-09-07T10:53:59.040535Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.cloudflare.com/.well-known/oauth-protected-resource/mcp\"" })
warning: Skill descriptions were shortened to fit the skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
codex
먼저 비주얼 명세와 현재 UI 구조를 확인한 뒤, 게임 로직을 유지하면서 스타일·이미지·모션을 적용하고 375px 브라우저 검증까지 진행하겠습니다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content AGENTS.md, STYLE.md, ASSET_SPEC.md, assets/manifest.json, CHANGELOG.md' in D:\Git\Inv\squad-idle
 succeeded in 63ms:
# squad-idle ??援ы쁽 洹쒖튃 (紐⑤뱺 ?먯씠?꾪듃 怨듯넻)

湲고쉷?쒕뒗 `GAME_DESIGN.md`. 湲고쉷?쒖? 異⑸룎?섎㈃ 湲고쉷?쒓? ?곗꽑. 紐⑦샇?섎㈃ 湲고쉷?쒖쓽 痍⑥?(戮묎린 ?щ?, ?ㅽ봽?쇱씤 ?섑솗)??留욊쾶 寃곗젙?섍퀬 肄붾뱶 二쇱꽍??"DECISION:"?쇰줈 ?④릿??

## ?ㅽ깮
- ?쒖닔 HTML/CSS/JS. **鍮뚮뱶 ?꾧뎄 ?놁쓬, ?꾨젅?꾩썙???놁쓬, npm ?섏〈???놁쓬.** `index.html`???뺤쟻 ?쒕쾭濡??대㈃ 諛붾줈 ?뚯븘媛???쒕떎.
- ?대옒??`<script>` ?쒓렇 ?쒖꽌 濡쒕뵫 (ES module ?꾨떂). ?꾩뿭 ?ㅼ엫?ㅽ럹?댁뒪???꾨옒 4媛쒕쭔:
  - `DATA` (`js/data.js`) ??紐⑤뱺 肄섑뀗痢?諛몃윴???곸닔. ?⑥닔 ?놁쓬, ?쒖닔 ?곗씠??+ ?ㅼ???怨듭떇 ?⑥닔 紐?媛?
  - `Game` (`js/game.js`) ???곹깭? 濡쒖쭅. DOM ?묎렐 湲덉?. ?뚮뜑?먮뒗 `Game.on(event, fn)` ?대깽?몃줈留??듭?.
  - `Battle` (`js/battle.js`) ???꾪닾 ?쒕??덉씠??(??湲곕컲, 10Hz). DOM ?묎렐 湲덉?.
  - `UI` (`js/ui.js`) ??DOM/SVG ?뚮뜑? ?낅젰. `Game`/`Battle`??怨듦컻 API留??몄텧.
- ?ㅽ??쇱? `style.css` ?섎굹. 湲곗? 酉고룷??390횞844 ?몃줈. `#app`??`max-width: 480px` 以묒븰 ?뺣젹, 洹??댁긽? 醫뚯슦 ?щ갚.
- ?쒓뎅??UI. ?대え吏???꾩씠肄???⑹쑝濡??덉슜.

## 肄붾뱶 洹쒖튃
- 寃곗젙??deterministic) ?쒕뜡: `Game.rng()` ?섎굹留??ъ슜 (?쒕뱶 ???媛?ν븯寃?. `Math.random()` 吏곸젒 ?몄텧 湲덉?.
- ??μ? `Game.save()`/`Game.load()`留? `localStorage` ??`squad_v1`. ???媛앹껜??`schemaVersion` ?꾩닔.
- ?レ옄 ?쒖떆??`UI.fmt(n)` ?섎굹濡??듭씪 (1234 ??"1,234", 12345 ??"12.3K", 1.2M ...).
- ?좊땲硫붿씠?섏? CSS ?대옒???좉? ?먮뒗 `requestAnimationFrame`. ?꾪닾 ?깆? `setInterval` 100ms?대ŉ ??씠 ?④꺼吏硫?硫덉텛怨? ?뚯븘?ㅻ㈃ 寃쎄낵 ?쒓컙??`Game.catchUp(elapsedMs)`濡?泥섎━?쒕떎.
- ?뚯씪 ?ш린 ?곹븳: ?뚯씪 ?섎굹 600以? ?섏쑝硫?履쇨컿??(?? `ui.js` ??`ui.js` + `ui-sheets.js`).
- 肄섏넄 ?먮윭 0. ?묒뾽 ?앹뿉 `node --check js/*.js`濡?臾몃쾿 ?뺤씤.

## ?묒뾽 諛⑹떇
- ?붿껌諛쏆? ?④퀎(P1/P2/P3)留?援ы쁽?쒕떎. ?ㅼ쓬 ?④퀎 寃껋쓣 誘몃━ 留뚮뱾吏 ?딅뒗??
- 湲곗〈 ?뚯씪???섏젙???뚮뒗 援ъ“瑜??좎??섍퀬, ?⑥닔 ?대쫫??諛붽씀吏 ?딅뒗??
- ?앸굹硫?`CHANGELOG.md`???대쾲 ?묒뾽?쇰줈 ?앷릿 ?뚯씪/?⑥닔/寃곗젙 ?ы빆??吏㏐쾶 ?곷뒗??
- ?뚯뒪?? `test/` ?대뜑??node濡??ㅽ뻾 媛?ν븳 ?쒖닔 濡쒖쭅 ?뚯뒪??(`node test/run.js`). DOM ?녿뒗 `Game`/`Battle`/`DATA`留???? 理쒖냼???꾪닾 ?쒕????ㅽ뀒?댁? 1-1???대━?댄븯?붿?, ???濡쒕뱶 ?뺣났???숈씪?쒖? ?뺤씤.
# UI ?ㅽ???媛?대뱶 ??諛앷퀬 ?뚮줉?щ줉??紐⑤컮??寃뚯엫??
紐⑺몴: 罹먯＜??紐⑤컮??RPG(荑좏궎?? ?몃툙?섏씠痢??ㅼ슦湲?怨꾩뿴)??"?먭퍖怨??숆??숆?, 諛섏쭩?대뒗" ?? 吏湲덉쓽 ?대몢???먰?吏 ?ㅼ? ?꾨? 踰꾨┛??

## ?붾젅??(CSS 蹂?섎줈 ?뺤쓽)
| 蹂??| 媛?| ?⑸룄 |
|---|---|---|
| `--bg-top` / `--bg-bottom` | `#8FD3FF` / `#EAF7FF` | ??諛곌꼍 洹몃씪?붿뼵??(?섎뒛) |
| `--panel` | `#FFF8E7` | 移대뱶/?쒗듃 諛뷀깢 (?щ┝) |
| `--panel-2` | `#FFE9B8` | 移대뱶 ?ㅻ뜑, 媛뺤“ ??|
| `--ink` | `#4A2C12` | 蹂몃Ц 湲?? ?뚮몢由?(吏꾪븳 媛덉깋) |
| `--ink-soft` | `#8A6A4A` | 蹂댁“ 湲??|
| `--primary` | `#FF9F1C` ??`#FF6B00` | 硫붿씤 踰꾪듉 洹몃씪?붿뼵??|
| `--accent` | `#3DDC97` | ?깃났/?뺤씤, 諛섎났 紐⑤뱶 |
| `--danger` | `#FF5C5C` | ?ㅽ뙣, ?먮ℓ, 珥덇린??|
| `--gold` | `#FFC93C` | 怨⑤뱶 |
| `--coin` | `#B388FF` | ?ㅼ옘??肄붿씤 |
| `--hp-ally` / `--hp-enemy` | `#5CE07A` / `#FF6B6B` | HP 諛?|
| rarity `--r-rare` / `--r-epic` / `--r-unique` / `--r-legendary` | `#3B82F6` / `#A855F7` / `#FACC15` / `#22C55E` | ?깃툒 ?뚮몢由?룰?濡쒖슦 |

## ?뺥깭
- 紐⑤뱺 移대뱶쨌踰꾪듉쨌?쒗듃: `border: 3px solid var(--ink)`, `border-radius: 18px`, ?꾨옒履?`box-shadow: 0 4px 0 var(--ink)` (?먭볼??留뚰솕??洹몃┝??. 踰꾪듉 ?뚮┝? `translateY(3px)` + 洹몃┝??0.
- 踰꾪듉 湲?? ?곗깋, 援듦쾶, `text-shadow: 0 2px 0 rgba(0,0,0,.25)`.
- ?곷떒 HUD: ?щ┝ ?⑤꼸 ?꾩뿉 ?ы솕 移?3媛??꾩씠肄??대?吏 + ?レ옄). CP?????レ옄??湲덉깋 湲濡쒖슦.
- ?꾪닾 ?? 諛곌꼍 ?대?吏 `bg/*.png`瑜?`object-fit: cover`, ?꾩뿉 ?ㅽ봽?쇱씠?? ???꾨젅?꾨룄 3px 媛덉깋 ?뚮몢由??쇱슫??
- ?섎떒 ?? 5媛? ?꾩씠肄??대?吏(?먮뒗 ?대え吏) + ?쇰꺼, ?쒖꽦 ??? ?꾨줈 6px ??댁삤瑜닿퀬 二쇳솴 諛곌꼍.
- 諛뷀??쒗듃: ?щ┝ ?⑤꼸, ?곷떒 洹몃┰, ?쒕ぉ 由щ낯(二쇳솴 諛뷀깢 ??湲??.

## ??댄룷
- ?쒓?: `"Jua", "Gothic A1", system-ui` (Google Fonts, ?ㅽ봽?쇱씤?대㈃ ?대갚). ?レ옄??`font-variant-numeric: tabular-nums`.
- ?쒕ぉ 22px, 蹂몃Ц 15px, 蹂댁“ 13px. 理쒖냼 12px.

## 紐⑥뀡
- ?쒕∼ 移대뱶: ???꾩튂?먯꽌 ?ㅼ???0??.2?? ??+ ?댁쭩 ?뚯쟾, ?깃툒 ??湲濡쒖슦, 0.9珥????λ퉬 ??쑝濡?鍮⑤젮 ?ㅼ뼱媛?
- ?⑹꽦 寃곌낵: 移대뱶 ?ㅼ쭛湲?`rotateY`, unique ?댁긽? ?붾㈃ 媛?μ옄由?臾댁?媛??뚮옒??0.4珥?
- ?レ옄 利앷?: 300ms 移댁슫?몄뾽. ?덈꺼?? ?⑸퀝 ??"LEVEL UP!" ?몃? ?쇰꺼 ??
- ?ㅽ봽?쇱씠??idle: 2珥?二쇨린 ?꾩븘??3px ?⑥돩湲?

## 湲덉?
- ?대몢??諛곌꼍, ?뉗? 1px ?뚮몢由? ?뚯깋 ?? ?몃━???고듃, ?묒? 湲??
# ?먯뀑 ?ㅽ럺 ??移섎퉬 移댄댆 ?대?吏

怨듯넻 ?ㅽ????ㅼ썙??(紐⑤뱺 ?꾨＼?꾪듃 ?욎뿉 遺숈엫):
`chibi, big head small body, cute, thick dark-brown outlines, cel-shaded, bright saturated colors, glossy, casual mobile game style, full body, transparent background, no text, no watermark`

?뚯씪 ?꾩튂 `assets/` ?꾨옒. PNG, ?щ챸 諛곌꼍. ?대쫫? ?꾨옒 ??洹몃?濡?(肄붾뱶媛 ???대쫫??李몄“).

## 1. ?⑸퀝 (facing RIGHT, 512횞512, 媛?2?? idle / attack)

| ?뚯씪 | ?꾨＼?꾪듃 ?붿? |
|---|---|
| `merc/warrior_idle.png`, `merc/warrior_attack.png` | ?대┛ 湲곗궗, 二쇳솴쨌湲덉깋 媛묒샆, ??諛⑺뙣? 寃. attack? 寃???섎몢瑜대뒗 ?쒓컙 |
| `merc/archer_idle.png`, `merc/archer_attack.png` | 珥덈줉 ?꾨뱶 ?뚮? 沅곸닔, ?섎Т ?? attack? ?쒖떆???밴? |
| `merc/mage_idle.png`, `merc/mage_attack.png` | 蹂대씪 怨좉퉼紐⑥옄 瑗щ쭏 留덈쾿?? 蹂?吏?≪씠. attack? 吏?≪씠 ?ㅺ퀬 鍮?|

## 2. 紐ъ뒪??(facing LEFT, 512횞512, 媛?1?? ??吏??퀎 ?붾젅??
| 吏??| ?뚯씪 | ?댁슜 |
|---|---|---|
| 1 ?딇엺 ??(珥덈줉쨌媛덉깋) | `mon/slime.png` | ?곕몢 ?ㅻ━ ?щ씪?? 硫랁븳 ??|
| | `mon/goblin.png` | 珥덈줉 怨좊툝由? ?섎Т 紐쎈뫁??|
| | `mon/mushroom.png` | 鍮④컙 ?먮컯??踰꾩꽢 愿대Ъ, ?붾궃 ?덉뜾 |
| | `mon/boss_treant.png` | 蹂댁뒪: ???섎Т ?뺣졊, ?쇨뎬 ?덈뒗 紐명넻 (640횞640) |
| 2 ?쇱뼱遺숈? 遊됱슦由?(?섎뒛쨌?곗깋) | `mon/ice_bat.png` | ?쇱쓬 諛뺤쪖, ?뚮? ?좉컻 |
| | `mon/yeti.png` | ?묒? ?덊떚, ????|
| | `mon/frost_wolf.png` | ?쒕━ ?묐?, ?섎뒛??|
| | `mon/boss_ice_golem.png` | 蹂댁뒪: ?쇱쓬 怨⑤젞, ?щ━?ㅽ깉 紐?(640횞640) |
| 3 ?용튆 ?붿궛 (鍮④컯쨌寃?? | `mon/lava_slime.png` | ?⑹븫 ?щ씪?? 二쇳솴 諛쒓킅 |
| | `mon/imp.png` | 鍮④컙 ?꾪봽, ?쇱?李?|
| | `mon/salamander.png` | 遺??꾨쭏諭 |
| | `mon/boss_dragon.png` | 蹂댁뒪: ?꾧린 ?쒕옒怨? 鍮④컯쨌湲?(640횞640) |

## 3. 諛곌꼍 (1080횞760, 遺덊닾紐? 吏?됱꽑???믪씠 55% ?꾩튂??

| ?뚯씪 | ?댁슜 |
|---|---|
| `bg/forest.png` | 諛앹? ????湲? ?뚮? ?섎뒛, ???섎Т, 移댄댆 |
| `bg/peak.png` | ????씤 ?곌만, 留묒? ?섎뒛, ?쇱쓬 ?щ━?ㅽ깉 |
| `bg/volcano.png` | ?붿궛 吏?, ?⑹븫 媛? 二쇳솴 ?섎뒛 (諛앹? ???좎?) |

## 4. ?꾩씠肄?(256횞256, 媛?1?? ?뺣㈃)

| ?뚯씪 | ?댁슜 |
|---|---|
| `icon/weapon.png` | 寃 |
| `icon/hat.png` | ?ш뎄/紐⑥옄 |
| `icon/gloves.png` | ?κ컩 |
| `icon/shoes.png` | 遺痢?|
| `icon/skillbook.png` | 諛섏쭩?대뒗 留덈쾿梨?|
| `icon/gold.png` | 湲덊솕 |
| `icon/coin.png` | 蹂대씪 蹂댁꽍 (?ㅼ옘??肄붿씤) |

?깃툒 ?됱? 肄붾뱶?먯꽌 ?뚮몢由щ줈 移좏븯誘濡??꾩씠肄섏? ?깃툒 臾닿? 1醫?

## 5. 肄붾뱶 ?곕룞 洹쒖튃
- `DATA.assets` 留ㅽ븨????寃쎈줈瑜??깅줉?섍퀬, ?뚮뜑 痢듭? `<image href>`(SVG) ?먮뒗 `<img>`濡??ъ슜. ?뚯씪???놁쑝硫?湲곗〈 踰≫꽣 ?꾪삎?쇰줈 ?대갚.
- ?⑸퀝 attack ?꾨젅?꾩? 怨듦꺽 ?쒓컙 120ms ?쒖떆 ??idle濡?蹂듦?. ?쇨꺽? CSS ?꾪꽣濡??곗깋 ?뚮옒??
- ?ㅽ봽?쇱씠??諛붾떏 ?뺣젹: ?대?吏 ?섎떒 5%媛 諛쒗뙋 洹몃┝???꾩튂.
{
  "merc.warrior.idle": "assets/merc/warrior_idle.png",
  "merc.archer.idle": "assets/merc/archer_idle.png",
  "merc.mage.idle": "assets/merc/mage_idle.png",
  "mon.slime": "assets/mon/slime.png",
  "mon.goblin": "assets/mon/goblin.png",
  "mon.mushroom": "assets/mon/mushroom.png",
  "mon.boss_treant": "assets/mon/boss_treant.png",
  "bg.forest": "assets/bg/forest.png",
  "icon.weapon": "assets/icon/weapon.png",
  "icon.hat": "assets/icon/hat.png",
  "icon.gloves": "assets/icon/gloves.png",
  "icon.shoes": "assets/icon/shoes.png",
  "icon.skillbook": "assets/icon/skillbook.png",
  "icon.gold": "assets/icon/gold.png",
  "icon.coin": "assets/icon/coin.png",
  "merc.warrior.attack": "assets/merc/warrior_attack.png",
  "merc.archer.attack": "assets/merc/archer_attack.png",
  "merc.mage.attack": "assets/merc/mage_attack.png",
  "mon.ice_bat": "assets/mon/ice_bat.png",
  "mon.yeti": "assets/mon/yeti.png",
  "mon.frost_wolf": "assets/mon/frost_wolf.png",
  "mon.boss_ice_golem": "assets/mon/boss_ice_golem.png",
  "bg.peak": "assets/bg/peak.png",
  "mon.lava_slime": "assets/mon/lava_slime.png",
  "mon.imp": "assets/mon/imp.png",
  "mon.salamander": "assets/mon/salamander.png",
  "mon.boss_dragon": "assets/mon/boss_dragon.png",
  "bg.volcano": "assets/bg/volcano.png"
}
# 蹂寃?湲곕줉

## 0.2.0 쨌 P2 ??2026-09-07

- `js/data.js`: ?λ퉬 4?щ’쨌8?곗뼱쨌4?깃툒 ?뺤젙, 諛⑹뼱???좎옱 異붽?, ?ㅽ뀒?댁? 6援ш컙 ?쒕∼?쒖? 移댁삤?ㅼ슜 ?곗씠???먮━, `fusionCost()`쨌`sellPrice()`, ?⑹꽦 ?곸듅 15%쨌?ъ꽕??20肄붿씤쨌?몃깽?좊━ 60移??곸닔.
- `js/game-equipment.js`: DOM ?녿뒗 ?λ퉬 ?뺤옣. `Game.rollItem(stageIndex, {forcedTier})`, `equip(uid, mercenaryId)`, `unequip(mercenaryId, slot)`, `fuse(uids)`, `autoFuse()`, `sell(uids)`, `toggleLock(uid)`, `rerollPotentials(uid)`. ?쒕∼쨌?⑹꽦쨌?ъ꽕?빧룹씤踰ㅽ넗由??ы솕 ?대깽?몄? ???寃利??ы븿. ?몃???諛섑솚?섎뒗 ?꾩씠?쒖? 蹂듭궗蹂?
- `js/game.js`쨌`js/battle.js`: `Game.mercenaryStats(id)`? CP쨌?꾩옱 ?꾪닾???λ퉬 諛섏쁺, 泥섏튂 ???쒕∼/怨⑤뱶 ?좎옱 ?곸슜. 湲곗〈 ?⑥닔 ?대쫫怨?10Hz ?꾪닾 援ъ“ ?좎?. ????ㅻ뒗 `squad_v1`, 遊됲닾? ?곹깭??`schemaVersion`? 2. v1? 鍮??몃깽?좊━쨌?⑸퀝蹂?鍮?4?щ’쨌UID ?쒕쾲쨌overflow瑜?異붽??섏뿬 ?댁쟾 ?꾪닾源뚯? 蹂듭썝.
- `js/ui-equipment.js`쨌`js/ui.js`쨌`style.css`: ???꾩튂?먯꽌 ?怨??λ퉬 ??쑝濡??좎븘媛??移대뱶, ?좉퇋 ?쒕∼ 諛곗?, ?좊땲???댁긽 ?뚮몢由??뚮옒?쑣룰?????30ms 吏꾨룞. 6???몃깽?좊━쨌?щ’ ?꾪꽣쨌3醫??뺣젹쨌?곸꽭쨌?μ갑/?댁젣쨌?좉툑/?먮ℓ쨌?좎옱 ?ъ꽕???꾪썑 怨듦컻, ?⑸퀝 ?щ’?먯꽌 吏???⑸퀝 ?μ갑 紐⑤뱶濡??대룞, CP 500ms 移댁슫?몄뾽. ?щ’횞?곗뼱 ?⑹꽦?쑣룸떒???먮룞 ?⑹꽦쨌?ㅼ쭛湲걔?50ms ?쒖감 怨듦컻쨌??嫄대꼫?곌린쨌?깃툒 ?곸듅 媛뺤“.
- `index.html`: 湲곗〈 ?대옒???ㅽ겕由쏀듃 ?쒖꽌???λ퉬 濡쒖쭅/UI ?뺤옣 異붽?. ?꾩뿭? DATA/Game/Battle/UI ??媛? ?몃? ?섏〈???놁쓬. `UI.init()`? ?뺤옣 ?깅줉???앸궃 ???ㅽ뻾.
- `test/run.js`: 湲곗〈 17媛?寃利??좎?(踰꾩쟾/鍮??몃깽?좊━ 湲곕?媛믩쭔 P2濡?媛깆떊), ?λ퉬 寃利?11媛?異붽?. `test/browser-equipment.js`? 湲곗〈 `test/browser.js`???ㅼ젣 釉뚮씪?곗? P2 寃利?異붽?. `test/balance.js`??20媛??쒕뱶??15遺??μ갑 ?뚮젅?대? ?ы쁽.

### DECISION: ?λ퉬쨌蹂댁긽 洹쒖튃

- ?щ’ 湲곕낯媛? 臾닿린 ATK **4**(P1 ?ㅽ뀅 10?먯꽌 ?섑뼢), 紐⑥옄 HP 30, ?κ컩 ATK 2 + 移섎챸? ?뺣쪧 1%, ?좊컻 HP 15 + DEF 3. 媛?湲곕낯媛믪뿉 `1.6^(tier-1) 횞 ?깃툒 諛곗쑉`???곸슜?섍퀬 ?뚯닔 ?룹㎏ ?먮━源뚯? ??? ?κ컩 移섎챸???媛숈? 怨듭떇?쇰줈 ?깆옣?섎릺 理쒖쥌 移섎챸? ?뺣쪧? 100% ?곹븳.
- ?쇰컲 泥섏튂 ?쒕∼瑜좎? 8%. 蹂댁뒪 援ш컙???쇰컲 ?곷룄 8%?대ŉ **蹂댁뒪 媛쒖껜 泥섏튂留?100%**, 1-10/2-10/3-10? 媛곴컖 T2/T3/T4 ?뺥솗???뺤젙. ?쇰컲 援ш컙 ?곗뼱??吏??퀎 T1~2 / T2~4 / T3~5. `forcedTier`???앹꽦 ??1~8 踰붿쐞??紐낆떆????뼱?곌린?대ŉ, 移댁삤???쒕뒗 P3???곗씠?곕쭔 議댁옱.
- ?좎옱?듭뀡? 以묐났 以??덉슜. 媛숈? 醫낅쪟??鍮꾩쑉? ?⑹궛 ???λ퉬 湲곕낯媛믨퉴吏 ?뷀븳 ?ㅽ꺈??怨깊븿. 移섎챸? ?뺣쪧/?쇳빐??媛?? 怨⑤뱶쨌?쒕∼ ?좎옱???곕윭吏??숇즺瑜??ы븿???⑸쪟 ?⑸퀝???μ갑遺꾩쓣 ?⑹궛?섍퀬 ?띾뱷??8% ?쒕∼瑜좎뿉 ?곷? 諛곗쑉濡??곸슜. `invincibleOnHit`????Β룹옱異붿꺼留??섎ŉ ?ㅼ젣 臾댁쟻 ?④낵??**P3**濡?蹂대쪟(?붾㈃???④낵 以鍮?以??쒖떆).
- ?μ갑쨌?댁젣쨌?μ갑 ?λ퉬 ?ъ꽕?뺤? ?꾩옱 ?꾪닾??利됱떆 諛섏쁺?섎릺 **HP 鍮꾩쑉怨?怨듦꺽 ?湲?吏꾪뻾瑜??좎?**, ?곕윭吏??⑸퀝 遺???놁쓬. ?좉툑? ?먮ℓ/?⑹꽦 蹂댄샇?대ŉ ?μ갑怨??좎옱 ?ъ꽕?뺤? 媛?? ?ㅻⅨ ?⑸퀝?먭쾶 ?대룞 ???댁쟾 ?щ’??鍮꾩?.
- **60移??ы솕 ?쒖뿉???대쾲 泥섏튂???쒕∼???앹꽦?섍퀬 `itemDrop`쨌`inventoryFull`??紐⑤몢 諛쒗뻾?섎ŉ `overflow`瑜?利앷?.** ???쒕∼???ы븿???좉툑/?μ갑?섏? ?딆? ?덉뼱 以?理쒖? ?곗뼱瑜??먮룞 ?먮ℓ(?숇쪧?대㈃ ?ㅻ옒??UID ?곗꽑). ?먮ℓ 媛?ν븳 ?덉뼱媛 ?꾪? ?놁쑝硫????쒕∼???대떦 ?깃툒???먮ℓ媛濡??섏쟾. ?좉툑쨌?μ갑쨌湲곗〈 ?곸쐞 ?깃툒 ?λ퉬??蹂댁〈?섎ŉ, ?먮ℓ ?대쫫/怨⑤뱶瑜??뚮┝?쇰줈 ?쒖떆?섏뿬 議곗슜???먯떎??諛⑹?.
- ?먮ℓ媛??`20 횞 2^(tier-1) 횞 ?깃툒 怨꾩닔(1/2/4/8)`. ?쇨큵 ?먮ℓ???꾨씫쨌以묐났쨌蹂댄샇 ?꾩씠?쒖씠 ?섎굹?쇰룄 ?덉쑝硫??꾨? 嫄곕?. ?⑹꽦? 媛숈? ?щ’쨌?곗뼱???쒕줈 ?ㅻⅨ 3媛쒕쭔 ?덉슜, T8? ?⑹꽦 遺덇?. ?먮룞 ?⑹꽦? 媛????? ?곗뼱遺???ш??됲븯???곗뇙 ?⑹꽦?섍퀬 怨⑤뱶媛 遺議깊븯硫??④?. 諛섑솚 寃곌낵?먮뒗 ?댄썑 ?⑹꽦???ъ슜??以묎컙 寃곌낵???앹꽦 ?쒖꽌?濡??ы븿.
- 諛곗????대쾲 ?몄뀡?먯꽌 ?꾩쭅 ?λ퉬 ??쑝濡??뺤씤?섏? ?딆? 蹂닿? ?쒕∼ ?? ?⑹꽦 寃곌낵??泥???쑝濡?紐⑤몢 怨듦컻, ?ㅼ쓬 ??쑝濡??リ린. 鍮좊Ⅴ寃??リ퀬 ?ㅼ떆 ?댁뼱???댁쟾 dialog ?リ린 ?대깽?멸? ??寃곌낵 ?곗텧??痍⑥냼?섏? ?딅룄濡?蹂댄샇.
- P1??理쒕? 60珥?`catchUp()` ?쒕??덉씠??寃쎄퀎???좎??섎ŉ 以묎컙 ?곗텧 ?놁씠 媛숈? ?λ퉬 濡쒖쭅???곸슜. ?ㅽ봽?쇱씤 蹂닿퀬?쑣룹뒪??룹뭅?ㅼ뒪 紐⑤뱶/?곸젏쨌JSON ?대낫?닿린??異붽??섏? ?딆쓬.

### 寃利?
- 紐⑤뱺 `js/*.js`??`node --check` ?듦낵(Windows PowerShell?먯꽌 ?뚯씪蹂??ㅽ뻾), `node test/run.js`: **28 PASS**, 醫낅즺 肄붾뱶 0. ?꾩껜 ?묒꽦 ?뚯씪? 600以?誘몃쭔, `git diff --check` ?듦낵.
- 媛??쒕∼ 援ш컙 2,000??珥?12,000?? ?앹꽦?쇰줈 ?곗뼱/蹂댁뒪 蹂댁옣/?좎옱 踰붿쐞 ?뺤씤. ?ㅼ젣 5,000???⑹꽦 ?곸듅瑜?**15.54%**. ?⑹꽦 ?щ즺/鍮꾩슜/?곗뇙/?곹븳, CP ?μ갑 ?뺣났, 紐⑤뱺 P2 ?좎옱 ?④낵, ?먮ℓ/?좉툑/?ъ꽕?? v1?뭭2? ?μ갑 ????뺣났쨌?먯긽 嫄곕?, 60移??먮룞 ?먮ℓ? ?꾨웾 蹂댄샇 ???섏쟾 寃利?
- Chrome 152 ?ㅼ젣 100ms ??대㉧????寃뚯엫 **120珥덉뿉 ?λ퉬 5媛?*, ?쒕∼ ?씲룸같吏 ?뺤씤. 375px 6??洹몃━?쒖? ?μ갑 CP ?곸듅쨌?⑸퀝 ?щ’ ?곕룞, T1?뭈2 ?ㅼ쭛湲걔룹옄??寃곌낵 ?쒖감 怨듦컻/嫄대꼫?곌린, ?좊땲?р넂?덉쟾?붾━ 媛뺥븳 ?뚮옒?? ?좎옱 ?꾪썑 怨듦컻쨌?좉툑, ?ъ젒?????寃利? 媛濡??섏묠 ?놁쓬(375px ?쒗듃 ?ы븿; 湲곗〈 320px/480px 理쒕? ??寃利??좎?), **釉뚮씪?곗? 肄섏넄쨌?고????ㅻ쪟 0**.
- ?뚯뒪???섍꼍??湲곕낯 Chrome/Edge renderer媛 ?쒖옉 ??異⑸룎?섏뿬 寃⑸━???꾩떆 ?꾨줈?꾩쓽 ?뚯뒪???ㅽ뻾?먮쭔 `BROWSER_NO_SANDBOX=1`???ъ슜. PowerShell ?ㅽ뻾: `$env:BROWSER_NO_SANDBOX='1'; node test/browser.js`. ?ㅼ젣 120珥??쒕∼ ?뺤씤 ???곗텧 ?섏젙 ?ш?利앹뿉??`BROWSER_FAST=1`濡??숈씪??1,200?깆쓣 ?ъ깮?덉쑝硫??꾩껜 釉뚮씪?곗? ?뚯뒪??醫낅즺 肄붾뱶 0. ???ъ슜??釉뚮씪?곗? ?ㅼ젙 蹂寃??놁쓬. OS 吏꾨룞 ?섎뱶?⑥뼱???ㅼ젣 珥됯컖 諛섏쓳? headless 寃利???곸씠 ?꾨떂.
- 諛몃윴?? 20媛??쒕뱶, 10珥덈쭏??CP媛 ?ㅻⅤ???쒕∼ ?μ갑, 15遺꾧컙 ?뚮젅?? **1-6: 123~167.9珥? 1-8: 156~213.4珥?以묒븰媛?176.4珥?, 紐⑤몢 20/20 ?듦낵**. 1-10? 229.9~650.9珥? 臾닿린 湲곕낯 ATK瑜??ㅽ뀅 10??濡?以꾩씠怨??쒕∼瑜좎? 8% ?좎?. P1???λ퉬 ?녿뒗 ?議곌뎔 5媛??쒕뱶??XP ?깆옣?쇰줈 1-8??209.5~363.1珥덉뿉 ?듦낵?섎?濡? P1 ?깆옣??諛붽씀吏 ?딄퀬 15遺??대궡 ?뚰뙆瑜?蹂댁옣?섎뒗 諛⑺뼢?쇰줈 寃곗젙. 15遺꾩뿉 洹쇱젒???뺤껜 援ш컙??媛뺤젣濡?留뚮뱾吏???딆쓬.

## 0.1.0 쨌 P1 ??2026-09-07

- `index.html`, `style.css`: ?쒓뎅??紐⑤컮???몃줈 UI, 以묒븰 理쒕? 480px ?덉씠?꾩썐, HUD쨌??대㉧쨌SVG ???꾪닾쨌HP쨌?쇳빐 ?レ옄쨌怨듦꺽/?쇨꺽/?щ쭩 ?곗텧, ?ㅽ뀒?댁? ?대룞怨?諛섎났/?꾩쟾 ?좉?, ?섎떒 ??紐⑤떖 ?쒗듃. ?⑸퀝 移대뱶???덈꺼쨌寃쏀뿕移샕?醫??ㅽ꺈쨌鍮??λ퉬 4移??쒖떆. ?ㅼ젙??踰꾩쟾쨌schemaVersion쨌?뺤씤 ??珥덇린???쒓났.
- `js/data.js`: ?⑸퀝 3醫? 吏??3媛쑣룹뒪?뚯씠吏 30媛쑣룸が?ㅽ꽣 ?붾젅?맞룹썾?대툕쨌蹂댁뒪, `enemyScale()`, `goldPerKill()`, `xpToNext()`, `mercenaryStats()`, CP 媛以묒튂. P2???λ퉬 ?щ’쨌?곗뼱 8媛쑣룸벑湲?4媛쑣룹옞???듭뀡 8醫낆? ?곗씠?곕쭔 異붽?.
- `js/battle.js`: `Battle.start()/tick()`??DOM ?녿뒗 10Hz ?쒕??덉씠?? ?좊큺 ?곗꽑 怨듦꺽, 移섎챸?쨌諛⑹뼱쨌?щ쭩쨌3?⑥씠釉뙿룸낫?ㅒ?0珥??쒗븳 諛?`wave/hit/kill/unitDeath/stageClear/stageFail` ?대깽?? ?좊떅??`skills`??鍮꾩뼱 ?덈뒗 ?뺤옣 ?먮━留?留덈젴.
- `js/game.js`: 寃곗젙??`rng()`, `on()`, `getState()/getCP()`, `selectStage()/setMode()`, 泥섏튂 蹂댁긽쨌XP쨌?덈꺼쨌沅곸닔/留덈쾿???닿툑, 諛섎났쨌?먮룞 吏꾪뻾쨌?ㅽ뙣 ?ъ떆?? `save()/load()`??`squad_v1`??schemaVersion 1怨??꾩껜 ?꾪닾 ?곹깭瑜???ν븯怨??섎せ???몄씠釉뚮? 嫄곕?. `init()/resume()/pause()/step()/catchUp()/reset()`怨?3珥??먮룞 ??? 理쒓렐 5遺?怨⑤뱶/泥섏튂 ?듦퀎 ?쒓났.
- `js/ui.js`: `UI.init()/fmt()`? ?대깽??湲곕컲 SVG/DOM ?뚮뜑留? ??쓣 ?④린嫄곕굹 ?섏씠吏瑜??좊궇 ????대㉧瑜?以묒??섍퀬 蹂듦? ??寃쎄낵 ?쒓컙??泥섎━. ?λ퉬/?⑹꽦/?ㅽ궗? ?쒖?鍮?以묅??쒗듃留??쒓났.
- `test/run.js`: Node ?댁옣 紐⑤뱢留??곕뒗 ?쒖닔 濡쒖쭅 ?뚯뒪??17媛? ?붿껌???대━???ㅼ??쇰쭅/????뺣났/RNG ?ы쁽???몄뿉 ?ㅼ젣 珥덇린 吏꾪뻾쨌蹂댁긽쨌?닿툑쨌?⑥씠釉뙿룹꽑遊됀룹튂紐낇?쨌??꾩븘?꺜룹옱?쒕룄쨌蹂듦? ?곹븳쨌CP쨌?듦퀎쨌?먯긽 ??Β룻??대㉧쨌?곗씠??寃利??ы븿.
- `test/browser.js`: 蹂꾨룄 ?⑦궎吏 ?놁씠 ?ㅼ튂??Chrome/Edge? Node 22+濡??ㅽ뻾?섎뒗 ?좏깮??釉뚮씪?곗? ?ㅻえ???뚯뒪?? ?꾩떆 ?꾨줈?꾧낵 濡쒖뺄 ?뺤쟻 ?쒕쾭瑜?留뚮뱾硫??덉씠?꾩썐쨌??룹큹湲고솕 ?뺤씤쨌?ㅼ떆媛?吏꾪뻾쨌??Β룹퐯???ㅻ쪟瑜?寃?? ?ㅽ뻾: `node test/browser.js` (`CHROME_PATH`濡??ㅽ뻾 ?뚯씪 吏??媛??.

### DECISION: 湲고쉷?쒖쓽 誘몄???媛믨낵 P1 寃쎄퀎

- 湲곕낯 ?꾩궗 HP/ATK/DEF/珥덈떦 怨듦꺽/移섎챸?/移섎챸? ?쇳빐??`170/14/12/0.85/8%/150%`. 沅곸닔??`140/12/5/1.4/15%/175%`, 留덈쾿?щ뒗 `115/23/3/0.7/10%/150%`. ?꾩궗留??⑸쪟???곹깭?먯꽌 湲곕낯 ?꾩쟾 紐⑤뱶濡??쒖옉?섍퀬, ?좉릿 ?숇즺???꾪닾 ?λ㈃???먮┛ ?ㅻ（?ｌ쑝濡??쒖떆.
- 珥덇린 ?ㅽ뿕???꾩궗 HP 260, 泥섏튂 XP 5???덈Т 鍮좊Ⅴ寃?1吏??쓣 ?듦낵?섏뿬 HP **170**, 泥섏튂 XP **2**濡?議곗젙. 湲곕낯 ?쒕뱶 `271828`?먯꽌 ?덈꺼 1 ?꾩궗??1-1??**18.9珥?*???대━?댄븯怨?1-5源뚯? ?대━?? 1-6 ?ㅽ뙣. ?먮룞 XP瑜??ы븿???먯뿰 吏꾪뻾??泥??ㅽ뙣媛 1-6?대ŉ ?ъ떆??XP濡?寃곌뎅 ?뚰뙆 媛?? ??HP `20 횞 1.22^index`, ATK `3 횞 1.18^index`, 怨⑤뱶 `5 횞 1.2^index`, ?덈꺼??XP `ceil(50 횞 1.15^level)`???뺥깭? 珥덇린媛믪? ?좎?.
- HP쨌怨듦꺽?Β룸갑?대젰???덈꺼??5% 蹂듬━ ?깆옣 ?곸슜. ?띾룄쨌移섎챸? ?섏튂??怨좎젙. 寃쏀뿕移섎뒗 ?곕윭吏??⑸퀝???ы븿???⑸쪟 ?몄썝?먭쾶 ?꾨웾 吏湲? ?덈꺼 ?곸듅 ?ㅽ꺈? ?ㅼ쓬 ?꾪닾遺???곸슜?섏뿬 ?꾪닾 以??덈꺼?낆씠 HP瑜??뚮났?쒗궎吏 ?딆쓬.
- 諛⑹뼱 怨듭떇? `max(1, round(ATK 횞 20 / (20 + DEF) 횞 移섎챸? 諛곗쑉))`. 媛숈? ?깆뿉?쒕뒗 ?⑸퀝 怨듦꺽??癒쇱? 泥섎━?섍퀬 ?щ쭩???좊떅??諛섍꺽? 痍⑥냼. ?곸? ?꾩궗?믨턿?섃넂留덈쾿???쒖꽌濡??댁븘 ?덈뒗 ?좊큺??怨듦꺽.
- ?쇰컲 ?⑥씠釉뚮뒗 ??2/3/3留덈━, 蹂댁뒪??2/3/蹂댁뒪 1留덈━. 蹂댁뒪 泥대젰/怨듦꺽 諛곗쑉 7/2.5. 理쒖큹 ?대━??肄붿씤? ?쇰컲 10, 蹂댁뒪 30, 蹂댁뒪 諛섎났 2. 泥섏튂 怨⑤뱶??紐ъ뒪??醫낅쪟? 臾닿???湲고쉷??怨듭떇???ъ슜.
- ?꾪닾 寃곌낵瑜?1.5珥??쒖떆????HP瑜??뚮났?섏뿬 ?ъ떆?? ?쒗븳?쒓컙 留덉?留??깆뿉 理쒖쥌 ?곸쓣 泥섏튂?섎㈃ ?깃났. ?ㅽ뙣??媛숈? ?ㅽ뀒?댁?, ?꾩쟾 ?깃났? ?ㅼ쓬 ?ㅽ뀒?댁?, 諛섎났 ?깃났? 媛숈? ?ㅽ뀒?댁?濡??대룞. 3-10 ?댄썑??P1?먯꽌 媛숈? ?ㅽ뀒?댁? ?좎?.
- ???遊됲닾 `{schemaVersion, savedAt, state}`??RNG쨌荑⑦??꽷텵P쨌吏꾪뻾 ?곹깭源뚯? ??? ?쒓컙 硫뷀??곗씠?곕뒗 ?곹깭 諛붽묑???먯뼱 ???濡쒕뱶??deep equality瑜?蹂댁옣. `load()`???쒓컙 吏꾪뻾 ?놁씠 蹂듭썝?섍퀬 `init()`?먯꽌留??ъ젒??寃쎄낵 ?쒓컙???곸슜.
- P1 蹂듦???理쒕? 600??60珥?留?媛숈? ?쒕??덉씠?곕줈 ?ㅽ뻾?섎ŉ ?⑤뒗 ?쒓컙? 踰꾨┝. 以묎컙 ?쒓컖 ?대깽?몄? ??μ쓣 ?듭젣?섍퀬 理쒖쥌 ?곹깭留???踰??쒖떆/??? ?ㅽ봽?쇱씤 蹂닿퀬?쑣룻슚??蹂댁긽쨌?λ퉬 ?쒕∼/?몃깽?좊━/?⑹꽦쨌?ㅼ젣 ?ㅽ궗쨌移댁삤?ㅒ텷SON 媛?몄삤湲??대낫?닿린??誘멸뎄??
- ?λ퉬 ?좎옱 ?곗씠?곗쓽 ?깃툒蹂?踰붿쐞? 媛以묒튂??rare/epic/unique/legendary ?쒖꽌?대ŉ, 媛믪? 鍮꾩쑉(0.05 = 5%)濡???? ?λ퉬 ?④낵??P1???곸슜?섏? ?딆쓬. 蹂꾨룄 Cloud ?꾩뿭? P1 踰붿쐞? 4媛??ㅼ엫?ㅽ럹?댁뒪 洹쒖튃???곕씪 異붽??섏? ?딆쓬.

### 寃利?
- `js/`??媛??뚯씪??`node --check` ?ㅽ뻾: ?듦낵. 紐⑤뱺 ?묒꽦 ?뚯씪 600以?誘몃쭔.
- `node test/run.js`: **17 PASS**, 醫낅즺 肄붾뱶 0.
- 釉뚮씪?곗? ?곌껐 ?꾧뎄???ъ슜 媛?ν븳 釉뚮씪?곗?媛 ?놁뿀?? ?ㅼ튂??Chrome???꾩떆 headless ?꾨줈?꾨줈 寃利앹쓣 ?쒕룄?덉쑝???꾩옱 ?ㅽ뻾 ?섍꼍?먯꽌 renderer `Target crashed`/GPU ?꾨줈?몄뒪 ?ㅻ쪟媛 諛쒖깮?섏뿬 ?ㅼ젣 ?붾㈃쨌肄섏넄 ?ㅻ쪟 0 寃利앹? ?꾨즺?섏? 紐삵븿. ?쒖닔 濡쒖쭅 寃利앷낵 援щ텇?섏뿬 湲곕줉.

## Art assets ??2026-09-07

- Generated all 28 PNGs listed in ASSET_SPEC.md using the built-in ChatGPT image_gen tool, with separate calls per asset and the required common style prefix. No HTTP API keys, package installations, or Blender were used.
- Added assets/merc (6), assets/mon (12), assets/bg (3), and assets/icon (7), plus assets/manifest.json and the standalone checkerboard review grid assets/contact_sheet.html.
- Attack poses were generated from their corresponding idle image references. Regenerated the slime to remove an unwanted outfit, the yeti to stand upright, and the peak background to expand its flat ground area.
- Transparency path: native generated alpha preserved for all 25 mercenary, monster, and icon files. No magenta removal was necessary. Preinstalled Pillow 12.1.0 normalized dimensions and padding; backgrounds are opaque RGB 1080x760. Icons are centered; sprite bottoms align at approximately 95% canvas height.
- Recorded final prompts in assets/prompts.json and source paths, processing methods, and sizes in assets/generation_log.jsonl. assets/prepare_asset.py provides the local normalization helper; assets/verify_assets.py checks files and writes assets/verification.json.
- Verification: 28/28 PNGs exist at the exact requested paths, match specified dimensions, exceed 10 KB, and pass alpha/opacity checks. Manifest keys and contact-sheet references are valid. JavaScript syntax checks passed. This art task did not change game code.
- Missing or ungenerated assets: none.

## 0.3.0 쨌 P3 ??2026-09-07

- `js/game-expedition.js`: `Game.harvest()`, `toggleSkill()`, `levelSkill()`, `setDifficulty()`, `exportSave()`쨌`importSave()`. ?먯젙 蹂닿퀬???앹꽦쨌誘몄닔????Β룹닔???몃옖??뀡, 吏곸뾽蹂??ㅽ궗遺? ?ㅽ궗 諛곗튂/?깆옣, ?낅┰ 移댁삤??吏꾪뻾?? v2?뭭3 留덉씠洹몃젅?댁뀡怨?P3 ???寃利앹쓣 異붽??덈떎.
- `js/data.js`쨌`js/battle.js`: ?⑸퀝蹂??ㅽ궗 4媛쒖? 湲곕낯 ?щ’ 3媛? ?곗꽑?쒖쐞/荑⑦??? ?꾨컻쨌諛⑹뼱쨌媛뺥?쨌?ъ깮쨌?곗궗쨌?뺤젙 移섎챸?쨌?끒룻쉶?셋룻뙆???뚮났쨌愿묒뿭湲걔룸??쑣룻씉?섎쭑 援ы쁽. ?쇨꺽 臾댁쟻怨?移댁삤??HP/ATK 諛곗쑉, ?ㅽ궗 諛쒕룞 ?대깽?몃? ?곸슜?덈떎.
- `js/game.js`쨌`js/game-equipment.js`: 湲곗〈 怨듦컻 ?⑥닔 ?좎?. `catchUp()`??60珥??댁긽 蹂듦????섑솗 ?꾩슜 蹂닿퀬?쒕줈 ?꾪솚?섍퀬 ?ㅽ뀒?댁?/?쒖씠?꾨퀎 理쒓렐 5遺??듦퀎瑜?蹂댁〈?쒕떎. ?λ퉬 ?섎졊 ?⑥닔瑜?遺꾨━??P2 ?ы솕 泥섎━ 洹몃?濡??ъ궗?⑺븳?? `schemaVersion: 3`, v1?뭭2?뭭3 ?곗뇙 蹂?? 鍮꾪뙆愿?`validateSave()`, ?좏깮??JSON ?몄옄瑜?諛쏅뒗 `load()`? ?덉슜???ㅼ꽢 踰덉㎏ ?꾩뿭 `Cloud` ?ㅽ뀅??異붽??덈떎. Cloud????硫붿꽌?쒕뒗 鍮꾨룞湲곕줈 `Error('v2')`瑜??섏?硫??듭떊?섏? ?딅뒗??
- `js/ui-expedition.js`쨌湲곗〈 UI쨌`style.css`쨌`index.html`: ?꾩껜 ?붾㈃ ?먯젙 蹂닿퀬?? 1珥?怨⑤뱶 移댁슫?몄뾽 ??200ms 媛꾧꺽 移대뱶 ?ㅼ쭛湲? ?꾩껜 怨듦컻/?섑솗, ?좊땲???댁긽 媛뺥븳 ?뚮옒?? ?ㅽ궗 移대뱶/?щ’/梨??뚮퉬, ?꾪닾 ?ㅽ궗 ?대쫫, 移댁삤???좉?, JSON textarea쨌蹂듭궗/?좏깮 ?泥는룰?利????뺤씤/?곸슜, ?명씉 ?좊땲硫붿씠?섏쓣 異붽??덈떎. 吏꾨룞? ?ㅼ젣 ?ъ슜???낅젰 ?댄썑?먮쭔 ?몄텧?쒕떎.
- `test/p3.js`, `test/long-run.js`, `test/browser-p3.js` 異붽?. 湲곗〈 濡쒖쭅/釉뚮씪?곗? ?뚯뒪?몄? ?낅┰ 諛몃윴???ㅽ겕由쏀듃??濡쒕뵫 ?쒖꽌瑜?媛깆떊?덈떎. P1 臾댁뒪??湲곗? 寃利앹? ?좎??섍퀬 P3濡?蹂寃쎈맂 踰꾩쟾쨌蹂듦?쨌臾댁쟻 湲곕?媛믩쭔 ?섏젙?덈떎.

### DECISION: ?먯젙쨌?ㅽ궗쨌移댁삤?ㅒ룹???
- 60珥?誘몃쭔? 湲곗〈 寃곗젙???꾪닾 鍮⑤━媛먭린, 洹??댁긽? 理쒕? 24?쒓컙 횞 ?⑥쑉 70%. 泥섏튂 ?섎뒗 ?뚯닔 湲곕?媛믪쑝濡?蹂닿??섍퀬 怨⑤뱶/XP???뺤닔 ?대┝?섎ŉ 遺?숈냼?섏젏 ?ㅼ감瑜?蹂댁젙?쒕떎. ?꾩쟾 以??섍???留덉?留됱쑝濡??ㅽ뻾???ㅽ뀒?댁?瑜?諛섎났 ?먯젙 ??곸쑝濡??쇱쑝硫??ㅽ봽?쇱씤 理쒖큹 ?대━???닿툑/肄붿씤? 吏湲됲븯吏 ?딅뒗??
- ?대떦 ?ㅽ뀒?댁????ㅼ륫 ?쒕낯???놁쑝硫?珥덈떦 0.1泥섏튂? ??怨⑤뱶(移댁삤???λ퉬 怨⑤뱶 蹂대꼫???ы븿)瑜??대떎. 蹂댁뒪 ?ㅽ뀒?댁????꾩껜 6留덈━ 以?蹂댁뒪 1留덈━ 鍮꾩쑉濡??뺤젙 ?쒕∼怨?XP 諛곗닔瑜?諛섏쁺?쒕떎. ?대? 痢≪젙??怨⑤뱶 ?섏씡?먮뒗 ?λ퉬 蹂대꼫?ㅻ? 以묐났 ?곸슜?섏? ?딅뒗??
- ?λ퉬 異붿꺼? 蹂닿퀬?쒕떦 理쒕? 500?? 珥덇낵 泥섏튂??媛?異붿꺼 ?뺣쪧?????泥섏튂 ?섎? 怨깊빐 諛섏쁺?섎릺 ?뺣쪧 100%, ?λ퉬 500媛쒓? ?곹븳?대떎. ?ㅽ궗遺곸? ?몃깽?좊━瑜??곗? ?딄퀬 吏곸뾽蹂?湲곕??됱쓣 ?뺣쪧 諛섏삱由쇳븳 理쒕? 3媛?臾띠쓬 移대뱶濡?蹂닿??쒕떎. ?⑤씪?몄? 泥섏튂??蹂꾨룄 3% 異붿꺼?쇰줈 ??吏곸뾽 以??섎굹瑜?洹좊벑 吏湲됲븯怨?利됱떆 ??ν븳??
- 誘몄닔??蹂닿퀬?쒕뒗 ?앹꽦 寃곌낵? RNG/UID瑜??④퍡 ??ν븯硫??ъ젒?????ъ텛泥⑦븯吏 ?딅뒗?? ?섑솗 ?꾩뿉???꾪닾? 異붽? ?먯젙 ?쒓컙 ?꾩쟻??硫덉텣?? ?섑솗? ??踰덈쭔 媛?ν븯硫?紐⑤뱺 蹂댁긽???곸슜??????ν븯怨??꾩옱 ?ㅽ뀒?댁? ?꾪닾瑜??덈줈 ?쒖옉?쒕떎. ?λ퉬???ы솕 ???좉툑/?μ갑 蹂댄샇쨌理쒖? ?곗뼱 ?덉뼱 ?먮ℓ쨌?泥??섏쟾? P2 洹쒖튃 洹몃?濡쒕떎.
- ?ㅽ궗 ?щ’ ?쒖꽌媛 ?곗꽑?쒖쐞?대ŉ ?댁젣 ???ㅼ떆 ??븯硫?留덉?留??щ’???ㅼ뼱媛꾨떎. ?⑸퀝? ?깅떦 以鍮꾨맂 ?ㅽ궗 ?섎굹瑜??곌퀬, ?놁쑝硫?怨듦꺽 ?湲곗떆媛꾩뿉 ?곕씪 湲곕낯 怨듦꺽?쒕떎. ?뚮났? 遺?곸옄媛 ?덉쓣 ?? 遺?쒖? ?곕윭吏??숇즺媛 ?덉쓣 ?뚮쭔 ?ъ슜?쒕떎. ?μ갑 蹂寃쎌? ?대? ?먮Ⅸ 荑⑦??꾩쓣 珥덇린?뷀븯吏 ?딅뒗??
- ?ㅽ궗遺?鍮꾩슜? ?꾩옱 ?ㅽ궗 ?덈꺼(1????1沅? 9??0? 9沅?, 理쒕? 10?덈꺼. ?꾨컲 吏꾪뻾 紐⑺몴??留욎떠 ?④낵 ?깆옣 諛곗쑉? `1 + 0.5 횞 (level - 1)`濡??뺥뻽?? ?꾨컻? 吏?띿떆媛? 遺?쒖? 荑⑦??꾩뿉 諛곗쑉???곸슜?섎ŉ 遺??HP????긽 30%, ?꾪닾????踰덉씠?? ?ㅻⅨ ?쇳빐/?뚮났/諛⑹뼱/?≪닔 ?④낵???꾨젰???곸슜?쒕떎. P1 ???깆옣 怨≪꽑怨?P2 ?λ퉬 湲곕낯媛??쒕∼ ?뺣쪧? ?좎??쒕떎.
- `invincibleOnHit` ?좎옱媛 ?섎굹?쇰룄 ?μ갑?섏뼱 ?덉쑝硫??쇨꺽留덈떎 怨좎젙 2% ?뺣쪧濡??대떦 ?寃⑸???1珥덇컙 臾댁쟻. 湲곗〈 ?좎옱 ?섏튂??????ъ꽕??踰붿쐞???명솚???꾪빐 蹂댁〈?섏?留?以묒꺽?쇰줈 諛쒕룞 ?뺣쪧???섎━吏 ?딅뒗??
- ?쇰컲 3-10 ?ㅼ젣 ?대━????移댁삤???닿툑. ?쒖씠?꾨퀎 吏꾪뻾?꾩? 留덉?留??좏깮 ?ㅽ뀒?댁?瑜?遺꾨━?쒕떎. 移댁삤??HP???쇰컲 ?곸쓽 諛섏삱由쇰맂 HP 횞8, ATK 횞5, 怨⑤뱶 횞4, 理쒖큹 ?대━??肄붿씤 횞3?대ŉ 蹂댁뒪??T5~8 移댁삤???쒕? ?ъ슜?쒕떎. 移댁삤???곸젏? 異붽??섏? ?딅뒗??
- v2???쇳빀 ?ㅽ뀒?댁? ?듦퀎???닿? ??鍮꾩썙 蹂댁닔?곸씤 ?먯젙 異붿젙移섎? ?ъ슜?쒕떎. 湲곗〈 ?⑸퀝/?λ퉬/吏꾪뻾???꾪닾 HP? 怨듦꺽 ?湲곕뒗 蹂댁〈?쒕떎. JSON 媛?몄삤湲곕뒗 ?꾩껜 寃利????뺤씤??諛쏄퀬 援먯껜?섎ŉ, 媛?몄삩 ?쒓컖遺????ν븳?? 媛?몄삤湲??먯껜??怨쇨굅 ?쒓컖?쇰줈 異붽? 蹂댁긽???앹꽦?섏? ?딆?留?誘몄닔??蹂닿퀬?쒕뒗 洹몃?濡?蹂듭썝?쒕떎.

### 寃利?
- 紐⑤뱺 `js/*.js`瑜??뚯씪蹂?`node --check`濡??뺤씤(Windows glob 誘명솗?????, `node test/run.js`: **37 PASS**, 醫낅즺 肄붾뱶 0. 媛?肄붾뱶/?뚯뒪???뚯씪 600以?誘몃쭔, 蹂寃?肄붾뱶??`git diff --check` ?듦낵.
- 濡쒖쭅: 70% 蹂닿퀬???섏떇쨌60珥?寃쎄퀎쨌24?쒓컙 ?곹븳쨌誘몄닔???ъ젒??以묐났 ?섑솗 諛⑹?쨌?ㅽ궗遺?3% 寃쎄퀎/利됱떆 ??Β?2媛??④낵쨌荑⑦????곗꽑?쒖쐞쨌????꾩뿉???꾪닾????踰?遺?쑣?0媛?移댁삤???ㅽ뀒?댁? 諛곗쑉/?닿툑쨌留덉씠洹몃젅?댁뀡쨌JSON ?뺣났쨌?먯긽???④낵/?щ’/蹂닿퀬?쒖쓽 ?먯옄??嫄곕?瑜?寃利앺뻽??
- ?κ린 吏꾪뻾: 10珥덈쭏???λ퉬 ?됯?/?μ갑쨌?먮룞 ?⑹꽦쨌?ㅽ궗遺??ъ슜, ??踰??ㅽ뙣?섎㈃ ???ㅽ뀒?댁? ?꾨옒?먯꽌 10遺?諛섎났 ???щ룄?? 留ㅼ떆媛????濡쒕뱶. 臾대즺 ?λ퉬/XP ?놁씠 ?쒕뱶 **271828 / 7919 / 42**媛 媛곴컖 **5.50 / 5.10 / 5.72?쒓컙**???쇰컲 3-10 ?대━??媛?6媛??몄뀡, ?⑹꽦 248 / 209 / 234??. 紐⑤몢 6?쒓컙 ?쒗븳 ?듦낵.
- Chrome 152: 湲곗〈 P1/P2 ?뚭?? P3 釉뚮씪?곗? 寃利??듦낵. savedAt??3?쒓컙 ?꾩쑝濡???ν븳 ?щ줈?⑹뿉??移댁슫?몄뾽쨌200ms ?ㅼ쭛湲걔룰컯???뚮옒?쑣룻꺆 怨듦컻쨌?섑솗/?ы솕 ?먮ℓ, 61珥??④? ??visibilitychange 蹂듦?, ?꾪닾 ?ㅽ궗 ?대쫫/?щ’ 蹂寃? 3-10 ?꾪썑 移댁삤???좉?, JSON 蹂듭궗/?섎せ???낅젰 嫄곕?/?뺤씤 ???뺣났???뺤씤?덈떎. 375px 媛濡??섏묠 ?놁쓬, 肄섏넄쨌?고????ㅻ쪟 0. ?ㅽ뻾? 湲곗〈 寃⑸━ headless ?꾨줈?꾩뿉??`BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` ?ъ슜; P2 120珥??꾪닾??1,200???ъ깮, P3 ?곗텧? ?ㅼ젣 ?꾨젅?꾩쑝濡?寃利앺뻽??
- ?숈떆 ?묒뾽??`assets/` ?뚯씪?먮뒗 ?묎렐?섏? ?딆븯?? 蹂寃??대젰? 吏곸쟾 ?댁슜???ㅼ떆 ?쎌뼱 ?⑥씪 ?먯옄??援먯껜濡????덉쓣 ?㏓텤???

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem; Get-ChildItem js; Get-Content index.html; Get-Content test/run.js' in D:\Git\Inv\squad-idle
 succeeded in 90ms:


    디렉터리: D:\Git\Inv\squad-idle


Mode                 LastWriteTime         Length Name                                                                 
----                 -------------         ------ ----                                                                 
d-----        2026-09-07   오후 7:53                .claude                                                              
d-----        2026-09-07   오후 7:31                assets                                                               
d-----        2026-09-07   오후 7:31                js                                                                   
d-----        2026-09-07   오후 7:39                test                                                                 
-a----        2026-09-07   오후 6:19             28 .gitignore                                                           
-a----        2026-09-07   오후 6:19           2553 AGENTS.md                                                            
-a----        2026-09-07   오후 6:56           3065 ASSET_SPEC.md                                                        
-a----        2026-09-07   오후 7:50          21391 CHANGELOG.md                                                         
-a----        2026-09-07   오후 6:18          10030 GAME_DESIGN.md                                                       
-a----        2026-09-07   오후 7:43           6783 index.html                                                           
-a----        2026-09-07   오후 7:43          25590 style.css                                                            
-a----        2026-09-07   오후 6:56           2607 STYLE.md                                                             


    디렉터리: D:\Git\Inv\squad-idle\js


Mode                 LastWriteTime         Length Name                                                                 
----                 -------------         ------ ----                                                                 
-a----        2026-09-07   오후 7:47           8664 battle.js                                                            
-a----        2026-09-07   오후 7:45          11536 data.js                                                              
-a----        2026-09-07   오후 7:43          12460 game-equipment.js                                                    
-a----        2026-09-07   오후 7:43          11891 game-expedition.js                                                   
-a----        2026-09-07   오후 7:47          15045 game.js                                                              
-a----        2026-09-07   오후 7:43          16127 ui-equipment.js                                                      
-a----        2026-09-07   오후 7:45          10108 ui-expedition.js                                                     
-a----        2026-09-07   오후 7:43          18360 ui.js                                                                
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="theme-color" content="#111916">
  <meta name="description" content="?덇컻 ?덈㉧濡??섏븘媛???묒? ?⑸퀝?? ?먮룞 ?꾪닾? ?깆옣??泥?踰덉㎏ ?먯젙.">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 40 40'%3E%3Crect width='40' height='40' rx='10' fill='%23111916'/%3E%3Ctext x='20' y='29' text-anchor='middle' font-size='28' fill='%23e4af65'%3E??3C/text%3E%3C/svg%3E">
  <link rel="stylesheet" href="style.css">
  <title>?⑸퀝??쨌 泥?踰덉㎏ ?먯젙</title>
</head>
<body>
  <main id="app">
    <header class="hud">
      <div class="brand-row"><h1><span class="brand-mark">??/span> ?⑸퀝??/h1><span class="chapter-label">泥?踰덉㎏ ?먯젙</span><span class="save-state" id="save-status">?먮룞 ???/span></div>
      <div class="resources">
        <div class="resource power"><span class="resource-label">?꾪닾??<small>CP</small></span><strong id="cp">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="gold-dot"></i> 怨⑤뱶</span><strong id="gold">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="coin-gem">??/i> 肄붿씤</span><strong id="coins">0</strong></div>
      </div>
    </header>
    <section class="expedition" aria-label="?먮룞 ?꾪닾">
      <div class="stage-header"><div><p id="region-name" class="eyebrow">?딇엺 ??/p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">?뀁쓽 ?낃뎄</span></h2></div><div class="clock"><span>?⑥? ?쒓컙</span><strong id="timer">60<small>珥?/small></strong></div></div>
      <div class="time-track"><div id="time-fill"></div></div>
      <div class="battle-scene">
        <div class="scene-topline"><span class="auto-badge"><i></i> ?먮룞 ?꾪닾 以?/span><span id="wave-label">?⑥씠釉?1 / 3</span></div>
        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="?⑸퀝怨?紐ъ뒪?곗쓽 ?먮룞 ?꾪닾 ?λ㈃">
          <defs>
            <linearGradient id="sky" x2="0" y2="1"><stop stop-color="#1b302c"/><stop offset="1" stop-color="#111e1c"/></linearGradient>
            <radialGradient id="moon-glow"><stop stop-color="#bdd0b0" stop-opacity=".2"/><stop offset="1" stop-color="#bdd0b0" stop-opacity="0"/></radialGradient>
            <linearGradient id="ground" x2="0" y2="1"><stop stop-color="#2c3b30"/><stop offset="1" stop-color="#14211c"/></linearGradient>
          </defs>
          <rect width="390" height="340" fill="url(#sky)"/>
          <circle cx="250" cy="81" r="107" fill="url(#moon-glow)"/><circle cx="250" cy="81" r="25" fill="#b6c6a4" opacity=".35"/><circle cx="263" cy="72" r="23" fill="#233830"/>
          <g fill="#142522"><path d="M11 246V45L1 22l15 10 9-32 4 65 26-27-17 42 10 166z"/><path d="M77 246l8-160-22-30 27 16 11-64 6 104 28-27-19 44 3 117z"/><path d="M315 252l1-155-24-42 33 22 13-62 6 78 33-39-25 69 17 129z"/></g>
          <g fill="none" stroke="#395043" stroke-width="2" opacity=".3"><path d="M158 238V121a37 37 0 0 1 74 0v117M150 234V120a45 45 0 0 1 90 0v115"/><path d="M158 141h-8m82 0h8m-82 34h-8m82 0h8"/></g>
          <path d="M0 247Q84 228 156 242T390 232V340H0Z" fill="url(#ground)"/>
          <path d="M135 340l49-92 21-1 59 93" fill="#9aab85" opacity=".045"/>
          <g fill="#0d1b18"><path d="M0 0h24l-7 115 25-22-22 45 4 118-24 8zM390 0h-22l6 88-31-26 29 49-7 145 25 9z"/><path d="M0 304l16-18-5 26 29-12-13 22 41-5-12 23H0zm390-13-23-15 8 30-30-8 12 21-39 4 10 17h62z"/></g>
          <g fill="#b9c8a4" opacity=".5"><circle cx="47" cy="135" r="1.3"/><circle cx="283" cy="176" r="1.5"/><circle cx="123" cy="189" r="1"/><circle cx="321" cy="118" r="1"/><circle cx="215" cy="203" r="1.4"/></g>
          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
        </svg>
        <div id="battle-result" class="battle-result" aria-live="polite"></div>
        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">?덇컻 ?덈㉧, 泥?踰덉㎏ ?먯젙</span><span class="caption-line"></span></div>
      </div>
      <div id="squad-strip" class="squad-strip" aria-label="?⑸퀝 ?곹깭"></div>
      <div class="harvest-row"><span>?대쾲 ?먯젙??諛쒓구??/span><span><b id="gold-rate">0</b> 怨⑤뱶/珥?<span class="rate-divider">쨌</span> <b id="kill-rate">0</b> 泥섏튂/珥?/span></div>
    </section>
    <section class="stage-controls" aria-label="?ㅽ뀒?댁? ?좏깮怨??꾪닾 紐⑤뱶">
      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="?먯젙 ?쒖씠?? hidden><button data-difficulty="normal" aria-pressed="true">?쇰컲</button><button data-difficulty="chaos" aria-pressed="false">移댁삤??/button></div>
      <div class="stage-navigation"><button id="previous-stage" aria-label="?댁쟾 ?ㅽ뀒?댁?">??/button><div><span>?ㅽ뀒?댁?</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="?ㅼ쓬 ?ㅽ뀒?댁?">??/button></div>
      <div class="mode-toggle" role="group" aria-label="?꾪닾 紐⑤뱶"><button id="repeat-mode" aria-pressed="false">??諛섎났</button><button id="challenge-mode" class="active" aria-pressed="true">?꾩쟾 ??/button></div>
      <p id="mode-hint">?대━?댄븯硫??ㅼ쓬 ?ㅽ뀒?댁?濡??섏븘媛묐땲??/p>
    </section>
    <nav class="bottom-tabs" aria-label="寃뚯엫 硫붾돱">
      <button data-tab="mercenaries" aria-haspopup="dialog"><span>??/span>?⑸퀝</button>
      <button data-tab="equipment" aria-haspopup="dialog"><span>??/span>?λ퉬<b id="equipment-badge" hidden>0</b></button>
      <button data-tab="fusion" aria-haspopup="dialog"><span>??/span>?⑹꽦</button>
      <button data-tab="skills" aria-haspopup="dialog"><span>??/span>?ㅽ궗</button>
      <button data-tab="settings" aria-haspopup="dialog"><span>??/span>?ㅼ젙</button>
    </nav>
    <div id="toast" role="status"></div>
    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">?⑸퀝?⑥쓽 湲곕줉</p><h2 id="sheet-title">?⑸퀝</h2></div><button id="close-sheet" aria-label="?リ린">횞</button></div><div id="sheet-content"></div></dialog>
  </main>
  <script src="js/data.js"></script>
  <script src="js/battle.js"></script>
  <script src="js/game.js"></script>
  <script src="js/game-equipment.js"></script>
  <script src="js/game-expedition.js"></script>
  <script src="js/ui.js"></script>
  <script src="js/ui-equipment.js"></script>
  <script src="js/ui-expedition.js"></script>
</body>
</html>
"use strict";

const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");
const vm = require("node:vm");

function context() {
  const storage = new Map();
  const intervals = new Map();
  let timerId = 0;
  const sandbox = vm.createContext({
    localStorage: { getItem: key => storage.get(key) || null, setItem: (key, value) => storage.set(key, value) },
    setInterval: (fn, ms) => { intervals.set(++timerId, { fn, ms }); return timerId; },
    clearInterval: id => intervals.delete(id)
  });
  for (const file of ["data", "battle", "game", "game-equipment", "game-expedition"]) {
    vm.runInContext(fs.readFileSync(path.join(__dirname, "../js/" + file + ".js"), "utf8"), sandbox, { filename: file + ".js" });
  }
  return { ...sandbox, storage, intervals };
}
function plain(value) { return JSON.parse(JSON.stringify(value)); }
function simulate(c, stage, squad = [{ id: "warrior", level: 1 }], emit) {
  const sim = c.Battle.start(stage, squad, emit);
  while (sim.status === "fighting") c.Battle.tick(sim, emit);
  return sim;
}
function untilResult(c) {
  for (let i = 0; i < 601 && c.Game.getState().battle.status === "fighting"; i++) c.Game.step();
  return c.Game.getState();
}
function fixture(c, stage, level = 1) {
  c.Game.reset();
  const state = c.Game.getState();
  state.currentStage = stage;
  state.unlockedStages = Array.from({ length: stage + 1 }, (_, i) => i);
  state.clearedStages = Array.from({ length: stage }, (_, i) => i);
  state.mercenaries.forEach((merc, i) => {
    merc.level = level;
    merc.unlocked = i === 0 || c.DATA.mercenaries[i].unlockStage < stage;
  });
  state.battle = c.Battle.start(stage, state.mercenaries.filter(m => m.unlocked));
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
  assert.equal(c.Game.load(), true);
}
let passed = 0;
function test(name, fn) {
  try { fn(); console.log("PASS " + name); passed++; }
  catch (error) { console.error("FAIL " + name); console.error(error); process.exitCode = 1; }
}

test("(a) fresh squad clears stage 1-1 within 60 seconds", () => {
  const c = context();
  const sim = simulate(c, 0);
  assert.equal(sim.status, "clear");
  assert.ok(sim.ticks <= 600);
  console.log("     Stage 1-1: " + sim.ticks / 10 + "s");
});
test("(b) stage 3-10 defeats a fresh level-1 squad, even with all classes", () => {
  const c = context();
  assert.equal(simulate(c, 29).status, "fail");
  assert.equal(simulate(c, 29, c.DATA.mercenaries.map(m => ({ id: m.id, level: 1 }))).status, "fail");
});
test("(c) save/load preserves the entire state and ongoing battle", () => {
  const c = context();
  for (let i = 0; i < 211; i++) c.Game.step();
  c.Game.setMode("repeat");
  const before = plain(c.Game.getState());
  c.Game.save();
  const stored = c.storage.get("squad_v1");
  c.Game.step();
  assert.equal(c.Game.load(), true);
  assert.deepEqual(plain(c.Game.getState()), before);
  const other = context();
  other.storage.set("squad_v1", stored);
  assert.equal(other.Game.load(), true);
  for (let i = 0; i < 70; i++) { c.Game.step(); other.Game.step(); }
  assert.deepEqual(plain(c.Game.getState()), plain(other.Game.getState()));
});
test("(d) equal RNG seeds produce equal sequences (including seed zero)", () => {
  const c = context();
  for (const seed of [0, 42, 4294967295]) {
    c.Game.reset(seed);
    const first = Array.from({ length: 100 }, () => c.Game.rng());
    c.Game.reset(seed);
    assert.deepEqual(Array.from({ length: 100 }, () => c.Game.rng()), first);
    assert.ok(first.every(n => n >= 0 && n < 1));
  }
});
test("balance: unupgraded level-1 warrior clears 1-1 through 1-5 and fails 1-6", () => {
  const c = context();
  for (let stage = 0; stage < 6; stage++) {
    c.Game.reset();
    const sim = simulate(c, stage);
    assert.equal(sim.status, stage < 5 ? "clear" : "fail", "stage " + (stage + 1));
  }
});
test("P1 baseline without active skills first fails at 1-6, then gains XP on retry", () => {
  const c = context();
  c.DATA.defaultSkills.warrior = []; c.Game.reset();
  let failure;
  c.Game.on("stageFail", e => { failure = e; });
  for (let i = 0; i < 2400 && !failure; i++) c.Game.step();
  assert.equal(failure.stageIndex, 5);
  assert.deepEqual(plain(c.Game.getState().clearedStages), [0, 1, 2, 3, 4]);
  const xp = c.Game.getState().mercenaries[0].xp;
  for (let i = 0; i < 100; i++) c.Game.step();
  assert.notEqual(c.Game.getState().mercenaries[0].xp, xp);
});
test("repeat keeps stage; first-clear coins cannot be farmed; challenge advances", () => {
  const c = context();
  c.Game.setMode("repeat");
  untilResult(c);
  assert.equal(c.Game.getState().squadCoins, 10);
  assert.deepEqual(plain(c.Game.getState().unlockedStages), [0, 1]);
  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
  assert.equal(c.Game.getState().currentStage, 0);
  untilResult(c);
  assert.equal(c.Game.getState().squadCoins, 10);
  c.Game.setMode("challenge");
  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
  assert.equal(c.Game.getState().currentStage, 1);
});
test("archer unlocks only on 1-6 clear; mage only on 2-6 clear", () => {
  const c = context();
  fixture(c, 5, 50);
  assert.equal(c.Game.getState().mercenaries[1].unlocked, false);
  untilResult(c);
  assert.equal(c.Game.getState().mercenaries[1].unlocked, true);
  assert.equal(c.Game.getState().mercenaries[2].unlocked, false);
  fixture(c, 15, 100);
  untilResult(c);
  assert.equal(c.Game.getState().mercenaries[2].unlocked, true);
});
test("wave, hit, kill, death, clear and fail events; boss is the last wave", () => {
  const c = context();
  const events = [];
  simulate(c, 9, [{ id: "warrior", level: 150 }], (name, data) => events.push({ name, data }));
  assert.deepEqual(events.filter(e => e.name === "wave").map(e => e.data.wave), [1, 2, 3]);
  assert.equal(events.filter(e => e.name === "kill").length, 6);
  assert.equal(events.filter(e => e.name === "unitDeath").length, 6);
  assert.ok(events.some(e => e.name === "kill" && e.data.type === "boss"));
  assert.equal(events.at(-1).name, "stageClear");
  const fails = [];
  simulate(c, 29, undefined, (name, data) => { if (name === "stageFail") fails.push(data); });
  assert.equal(fails.length, 1);
});
test("frontline priority survives reversed squad order and switches after death", () => {
  const c = context();
  const sim = c.Battle.start(0, [{ id: "mage", level: 1 }, { id: "archer", level: 1 }, { id: "warrior", level: 1 }]);
  sim.units.forEach(u => { u.cooldown = 999; });
  sim.units[0].hp = 1;
  sim.enemies.forEach(u => { u.cooldown = 0; });
  const targets = [];
  c.Battle.tick(sim, (name, event) => { if (name === "hit" && event.side === "enemy") targets.push(event.targetId); });
  assert.deepEqual(targets, ["warrior", "archer"]);
});
test("defense, forced critical damage, dead units and exact 60s timeout", () => {
  const c = context();
  const sim = c.Battle.start(0, [{ id: "warrior", level: 1 }]);
  sim.units[0].critChance = 1;
  sim.units[0].atk = 10;
  sim.units[0].critDamage = 2;
  sim.enemies[0].def = 20;
  let hit;
  c.Battle.tick(sim, (name, event) => { if (name === "hit") hit = event; });
  assert.equal(hit.amount, 10);
  assert.equal(hit.crit, true);
  sim.units.concat(sim.enemies).forEach(u => { u.cooldown = 999; });
  let reason;
  while (sim.status === "fighting") c.Battle.tick(sim, (name, e) => { if (name === "stageFail") reason = e.reason; });
  assert.equal(sim.ticks, 600);
  assert.equal(reason, "timeout");
  c.Battle.tick(sim, () => assert.fail("Finished battle emitted again"));
});
test("failure restores squad HP and retries the same stage without currency penalty", () => {
  const c = context();
  fixture(c, 29);
  const before = c.Game.getState().gold;
  assert.equal(untilResult(c).battle.status, "fail");
  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
  const state = c.Game.getState();
  assert.equal(state.currentStage, 29);
  assert.equal(state.battle.status, "fighting");
  assert.ok(state.battle.units.every(u => u.hp === u.maxHp));
  assert.ok(state.gold >= before);
});
test("catchUp below 60 seconds preserves deterministic fast-forward", () => {
  const a = context(); const b = context();
  assert.equal(a.Game.catchUp(59999), 599);
  for (let i = 0; i < 599; i++) b.Game.step();
  assert.deepEqual(plain(a.Game.getState()), plain(b.Game.getState()));
  assert.equal(a.Game.catchUp(-1), 0);
  assert.equal(a.Game.catchUp(NaN), 0);
  assert.equal(a.Game.catchUp(Infinity), 0);
  assert.equal(a.Game.catchUp(99), 0);
});
test("XP raises level and CP; the rolling five-minute window expires kills", () => {
  const c = context();
  const cp = c.Game.getCP();
  for (let i = 0; i < 1800; i++) c.Game.step();
  assert.ok(c.Game.getState().mercenaries[0].level > 1);
  assert.ok(c.Game.getCP() > cp);
  assert.ok(c.Game.getState().stats.goldPerSec > 0);
  fixture(c, 29);
  const state = c.Game.getState();
  state.stats = { elapsedMs: 300000, samples: [{ at: 0, gold: 5 }], goldPerSec: 1, killsPerSec: 1 };
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
  assert.equal(c.Game.load(), true);
  c.Game.step();
  assert.equal(c.Game.getState().stats.goldPerSec, 0);
  assert.equal(c.Game.getState().stats.killsPerSec, 0);
});
test("locked stages and invalid modes are rejected; corrupted saves preserve live state", () => {
  const c = context();
  assert.equal(c.Game.selectStage(1), false);
  assert.equal(c.Game.selectStage(-1), false);
  assert.equal(c.Game.setMode("chaos"), false);
  const before = plain(c.Game.getState());
  for (const value of ["{", "null", '{"schemaVersion":2}', '{"schemaVersion":1,"state":{}}']) {
    c.storage.set("squad_v1", value);
    assert.equal(c.Game.load(), false);
    assert.deepEqual(plain(c.Game.getState()), before);
  }
  const corrupt = plain(before);
  corrupt.battle.enemies[0].position = 99;
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: 1, savedAt: Date.now(), state: corrupt }));
  assert.equal(c.Game.load(), false);
  assert.deepEqual(plain(c.Game.getState()), before);
});
test("runtime installs only 100ms combat and 3s autosave timers; pause clears both", () => {
  const c = context();
  c.Game.resume(); c.Game.resume();
  assert.deepEqual([...c.intervals.values()].map(t => t.ms), [100, 3000]);
  c.Game.pause();
  assert.equal(c.intervals.size, 0);
  assert.equal(JSON.parse(c.storage.get("squad_v1")).schemaVersion, 3);
});
test("all 30 stages, equipment data and P3 default skills are defined", () => {
  const c = context();
  assert.equal(c.DATA.stages.length, 30);
  assert.equal(c.DATA.equipmentTiers.length, 8);
  c.DATA.stages.forEach((stage, i) => {
    assert.equal(stage.index, i);
    assert.equal(stage.timeLimit, 60);
    assert.equal(stage.boss, i % 10 === 9);
    assert.equal(stage.waves.length, 3);
    stage.waves.forEach((wave, n) => assert.ok(stage.boss && n === 2 ? wave.length === 1 : wave.length >= 2 && wave.length <= 4));
  });
  assert.equal(c.Game.getState().inventory.length, 0);
  assert.ok(c.Game.getState().battle.units.every(u => u.skills.length === 3 && u.skills.every(s => c.DATA.skills.some(d => d.id === s))));
});
function setState(c, state) {
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
  assert.equal(c.Game.load(), true);
}
function itemsOf(c, count, slot = "weapon", tier = 1, rarity = "rare") {
  const items = [];
  while (items.length < count) {
    const item = c.Game.rollItem(0, { forcedTier: tier });
    if (item.slot === slot && item.rarity === rarity) items.push(item);
  }
  return items;
}
function stock(c, items, gold = 1000000, coins = 100) {
  const state = c.Game.getState();
  state.inventory = items;
  state.gold = gold; state.squadCoins = coins;
  state.mercenaries.forEach(m => { m.equipment = { weapon: null, hat: null, gloves: null, shoes: null }; });
  setState(c, state);
}
test("P2: 2,000 rolls per stage band respect tier bounds, boss tiers, rarity lines and ranges", () => {
  const c = context();
  for (const band of c.DATA.dropTables) {
    for (let n = 0; n < 2000; n++) {
      const item = c.Game.rollItem(n % 2 ? band.min : band.max);
      assert.ok(band.tierWeights[item.tier] > 0);
      if (band.guaranteedTier) assert.equal(item.tier, band.guaranteedTier);
      assert.equal(item.potentials.length, c.DATA.equipmentRarities[item.rarity].lines);
      const rank = c.DATA.rarityOrder.indexOf(item.rarity);
      for (const p of item.potentials) {
        const option = c.DATA.potentialPool.find(o => o.id === p.id);
        assert.ok(option.weights[rank] > 0 && p.value >= option.ranges[rank][0] && p.value <= option.ranges[rank][1]);
      }
    }
  }
  for (let tier = 1; tier <= 8; tier++) assert.equal(c.Game.rollItem(0, { forcedTier: tier }).tier, tier);
  assert.equal(c.Game.rollItem(-1), null);
  assert.equal(c.Game.rollItem(30), null);
  assert.equal(c.Game.rollItem(0, { forcedTier: 9 }), null);
});
test("P2: every real boss kill drops its guaranteed tier, and fresh play drops in two minutes", () => {
  const c = context();
  for (const stage of [9, 19, 29]) {
    fixture(c, stage, 150);
    const drops = [];
    const off = c.Game.on("itemDrop", item => drops.push(item));
    untilResult(c); off();
    assert.ok(drops.some(i => i.enemyId === "enemy-2-0" && i.tier === Math.floor(stage / 10) + 2));
  }
  c.Game.reset();
  for (let n = 0; n < 1200; n++) c.Game.step();
  console.log("     Fresh 120s drops: " + c.Game.getState().inventory.length);
  assert.ok(c.Game.getState().inventory.length >= 3);
});
test("P2: fusion validates three distinct matching unlocked unequipped materials and gold", () => {
  const c = context();
  const items = itemsOf(c, 3).concat(itemsOf(c, 1, "hat"), itemsOf(c, 1, "weapon", 2));
  stock(c, items, 99);
  const uids = items.slice(0, 3).map(i => i.uid);
  assert.equal(c.Game.fuse(uids), null);
  stock(c, items);
  const before = plain(c.Game.getState());
  for (const bad of [[], uids.slice(0, 2), [uids[0], uids[0], uids[1]], [uids[0], uids[1], "missing"],
    [uids[0], uids[1], items[3].uid], [uids[0], uids[1], items[4].uid]]) assert.equal(c.Game.fuse(bad), null);
  assert.deepEqual(plain(c.Game.getState()), before);
  c.Game.toggleLock(uids[0]); assert.equal(c.Game.fuse(uids), null); c.Game.toggleLock(uids[0]);
  c.Game.equip(uids[0], "warrior"); assert.equal(c.Game.fuse(uids), null); c.Game.unequip("warrior", "weapon");
  const result = c.Game.fuse(uids);
  assert.equal(result.tier, 2); assert.equal(result.slot, "weapon");
  assert.equal(c.Game.getState().gold, 999900);
  assert.ok(!c.Game.getState().inventory.some(i => uids.includes(i.uid)));
  stock(c, itemsOf(c, 3, "weapon", 8));
  assert.equal(c.Game.fuse(c.Game.getState().inventory.map(i => i.uid)), null);
});
test("P2: rarity bumps at 0.15 짹 0.02 over 5,000 actual fusions; highest rarity is retained", () => {
  const c = context();
  let bumps = 0;
  for (let n = 0; n < 5000; n++) {
    const items = itemsOf(c, 3);
    stock(c, items);
    const item = c.Game.fuse(items.map(i => i.uid));
    if (item.rarity === "epic") bumps++;
    else assert.equal(item.rarity, "rare");
  }
  console.log("     Fusion bump rate: " + bumps / 5000);
  assert.ok(Math.abs(bumps / 5000 - 0.15) <= 0.02);
  const mixed = itemsOf(c, 2).concat(itemsOf(c, 1, "weapon", 1, "legendary"));
  stock(c, mixed);
  assert.equal(c.Game.fuse(mixed.map(i => i.uid)).rarity, "legendary");
});
test("P2: autoFuse cascades lowest tier first, respects protection, gold and tier 8", () => {
  const c = context();
  const items = itemsOf(c, 27).concat(itemsOf(c, 6, "hat"), itemsOf(c, 3, "shoes", 8));
  items[27].locked = true;
  stock(c, items);
  c.Game.equip(items[28].uid, "warrior");
  const results = c.Game.autoFuse();
  assert.equal(results.at(-1).tier, 4);
  assert.ok(results.slice(0, 10).every(i => i.tier === 2));
  const state = c.Game.getState();
  assert.equal(state.inventory.find(i => i.uid === items[27].uid).locked, true);
  assert.equal(state.inventory.find(i => i.uid === items[28].uid).equippedBy, "warrior");
  for (const slot of c.DATA.equipmentSlots) for (let t = 1; t < 8; t++) {
    assert.ok(state.inventory.filter(i => i.slot === slot.id && i.tier === t && !i.locked && !i.equippedBy).length < 3);
  }
  c.Game.unequip("warrior", "hat");
  stock(c, itemsOf(c, 9), 100);
  assert.equal(c.Game.autoFuse().length, 1);
  assert.equal(c.Game.getState().gold, 0);
});
test("P2: equip raises CP immediately, preserves HP ratio and unequip restores CP", () => {
  const c = context();
  const items = itemsOf(c, 1).concat(itemsOf(c, 1, "hat"));
  stock(c, items);
  for (let i = 0; i < 20; i++) c.Game.step();
  const cp = c.Game.getCP(), unit = c.Game.getState().battle.units[0];
  assert.equal(c.Game.equip(items[0].uid, "archer"), false);
  c.Game.equip(items[0].uid, "warrior"); c.Game.equip(items[1].uid, "warrior");
  assert.ok(c.Game.getCP() > cp);
  const after = c.Game.getState().battle.units[0];
  assert.ok(after.atk > unit.atk && after.maxHp > unit.maxHp);
  assert.ok(Math.abs(after.hp / after.maxHp - unit.hp / unit.maxHp) < 1e-12);
  c.Game.unequip("warrior", "weapon"); c.Game.unequip("warrior", "hat");
  assert.equal(c.Game.getCP(), cp);
  assert.equal(c.Game.getState().inventory[0].equippedBy, null);
});
test("P2: sell is atomic, lock protects materials, reroll charges 20 and works while locked/equipped", () => {
  const c = context();
  const items = itemsOf(c, 3);
  stock(c, items, 0, 20);
  c.Game.toggleLock(items[0].uid);
  assert.equal(c.Game.sell([items[0].uid, items[1].uid]), false);
  c.Game.equip(items[1].uid, "warrior");
  assert.equal(c.Game.sell([items[1].uid]), false);
  assert.equal(c.Game.sell([items[2].uid, items[2].uid]), false);
  assert.equal(c.Game.sell(["missing"]), false);
  assert.equal(c.Game.sell([items[2].uid]), c.DATA.sellPrice(1, "rare"));
  c.Game.equip(items[0].uid, "warrior");
  let event; c.Game.on("potentialReroll", e => { event = e; });
  const rolled = c.Game.rerollPotentials(items[0].uid);
  assert.equal(rolled.locked, true); assert.equal(rolled.equippedBy, "warrior");
  assert.equal(event.before.length, event.after.length);
  assert.equal(c.Game.getState().squadCoins, 0);
  assert.equal(c.Game.rerollPotentials(items[0].uid), null);
});
test("P2: schema v1 migration and populated v2 round-trip; bad equipment saves rejected", () => {
  const c = context();
  const legacy = c.Game.getState();
  legacy.schemaVersion = 1;
  delete legacy.inventory; delete legacy.overflow; delete legacy.nextItemUid;
  legacy.mercenaries.forEach(m => { delete m.equipment; });
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: 1, savedAt: Date.now(), state: legacy }));
  assert.equal(c.Game.load(), true);
  assert.equal(c.Game.getState().schemaVersion, 3);
  assert.deepEqual(plain(c.Game.getState().inventory), []);
  const item = itemsOf(c, 1)[0]; stock(c, [item]); c.Game.equip(item.uid, "warrior");
  c.Game.save(); const before = plain(c.Game.getState());
  assert.equal(c.Game.load(), true); assert.deepEqual(plain(c.Game.getState()), before);
  for (const corrupt of [s => { s.inventory.push(s.inventory[0]); }, s => { s.inventory[0].base.atk = 999; },
    s => { s.mercenaries[0].equipment.weapon = null; }, s => { s.inventory[0].potentials[0].value = 500; }]) {
    const state = plain(before); corrupt(state);
    c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
    assert.equal(c.Game.load(), false); assert.deepEqual(plain(c.Game.getState()), before);
  }
});
test("P2: inventory cap sells lowest eligible rare; fully protected bag monetizes incoming drop", () => {
  const c = context();
  fixture(c, 9, 150);
  const items = itemsOf(c, 60, "weapon", 3);
  items[0].locked = true;
  stock(c, items);
  c.Game.equip(items[1].uid, "warrior");
  const events = [], drops = [];
  c.Game.on("inventoryFull", e => events.push(e)); c.Game.on("itemDrop", e => drops.push(e));
  untilResult(c);
  assert.ok(events.length >= 1); assert.equal(events.length, drops.length);
  assert.equal(c.Game.getState().inventory.length, 60);
  assert.ok(events.every(e => e.sold.tier === 2 && !e.kept || e.sold.tier === 3 && e.kept));
  assert.ok(c.Game.getState().inventory.some(i => i.uid === items[0].uid));
  assert.ok(c.Game.getState().inventory.some(i => i.uid === items[1].uid));
  fixture(c, 9, 150);
  const protectedItems = itemsOf(c, 60); protectedItems.forEach(i => { i.locked = true; }); stock(c, protectedItems);
  events.length = 0; drops.length = 0; untilResult(c);
  assert.ok(events.length >= 1 && events.every(e => !e.kept && e.gold > 0));
  assert.equal(c.Game.getState().overflow, events.length);
  assert.equal(c.Game.getState().inventory.length, 60);
});
test("P2: all combat potentials apply, gold/drop bonuses work and invincibility is enabled", () => {
  const c = context();
  const items = c.DATA.equipmentSlots.map(s => itemsOf(c, 1, s.id, 1, "legendary")[0]);
  items[0].potentials = [{ id: "atkPct", value: .2 }, { id: "hpPct", value: .25 }, { id: "defPct", value: .25 }];
  items[1].potentials = [{ id: "attackSpeedPct", value: .15 }, { id: "critChance", value: .12 }, { id: "critDamage", value: .4 }];
  items[2].potentials = [{ id: "goldPct", value: .3 }, { id: "dropPct", value: .12 }, { id: "invincibleOnHit", value: .06 }];
  items[3].potentials = [{ id: "atkPct", value: .2 }, { id: "hpPct", value: .25 }, { id: "defPct", value: .25 }];
  stock(c, items, 0);
  items.forEach(i => c.Game.equip(i.uid, "warrior"));
  const base = c.DATA.mercenaryStats("warrior", 1), stats = c.Game.mercenaryStats("warrior");
  for (const key of ["atk", "hp", "def", "attackSpeed", "critChance", "critDamage"]) {
    const flat = base[key] + items.reduce((sum, i) => sum + (i.base[key] || 0), 0);
    const expected = ({ atk: flat * 1.4, hp: flat * 1.5, def: flat * 1.5, attackSpeed: flat * 1.15,
      critChance: flat + .12, critDamage: flat + .4 })[key];
    assert.ok(Math.abs(stats[key] - expected) < 1e-10, key);
  }
  assert.equal(stats.invincibleOnHit, true);
  const save = JSON.parse(c.Game.save());
  save.state.battle.enemies[0].hp = 1; save.state.battle.units[0].skills = [];
  setState(c, save.state);
  c.Game.rng = () => .085; // Above 8%, below the equipped 8.96% drop threshold.
  c.Game.step();
  assert.equal(c.Game.getState().gold, 7);
  assert.equal(c.Game.getState().inventory.length, 5);
  const plainGame = context();
  const plainState = plainGame.Game.getState(); plainState.battle.enemies[0].hp = 1; plainState.battle.units[0].skills = []; setState(plainGame, plainState);
  plainGame.Game.rng = () => .085;
  plainGame.Game.step();
  assert.equal(plainGame.Game.getState().inventory.length, 0);
  assert.equal(plainGame.Game.getState().gold, 5);
});
test("P2: transferring gear clears old owner, and changing gear never revives a downed ally", () => {
  const c = context(); fixture(c, 6, 1);
  const item = itemsOf(c, 1, "hat")[0]; stock(c, [item]);
  const before = c.Game.getCP(); c.Game.equip(item.uid, "warrior"); c.Game.equip(item.uid, "archer");
  let state = c.Game.getState();
  assert.equal(state.mercenaries[0].equipment.hat, null);
  assert.equal(state.mercenaries[1].equipment.hat, item.uid);
  assert.equal(state.inventory[0].equippedBy, "archer");
  c.Game.unequip("archer", "hat"); assert.equal(c.Game.getCP(), before);
  state = c.Game.getState(); state.battle.units[0].hp = 0; setState(c, state);
  c.Game.equip(item.uid, "warrior");
  assert.equal(c.Game.getState().battle.units[0].hp, 0);
  c.Game.unequip("warrior", "hat");
  assert.equal(c.Game.getState().battle.units[0].hp, 0);
});
require("./p3")({ test, context, plain, setState, fixture, untilResult, assert });
console.log("\n" + passed + " tests passed.");



exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 STYLE.md, ASSET_SPEC.md, GAME_DESIGN.md; git status --short; Get-ChildItem js | Select-Object Name,Length' in D:\Git\Inv\squad-idle
 succeeded in 135ms:
# UI 스타일 가이드 — 밝고 알록달록한 모바일 게임풍

목표: 캐주얼 모바일 RPG(쿠키런, 세븐나이츠 키우기 계열)의 "두껍고 동글동글, 반짝이는" 톤. 지금의 어두운 판타지 톤은 전부 버린다.

## 팔레트 (CSS 변수로 정의)
| 변수 | 값 | 용도 |
|---|---|---|
| `--bg-top` / `--bg-bottom` | `#8FD3FF` / `#EAF7FF` | 앱 배경 그라디언트 (하늘) |
| `--panel` | `#FFF8E7` | 카드/시트 바탕 (크림) |
| `--panel-2` | `#FFE9B8` | 카드 헤더, 강조 행 |
| `--ink` | `#4A2C12` | 본문 글자, 테두리 (진한 갈색) |
| `--ink-soft` | `#8A6A4A` | 보조 글자 |
| `--primary` | `#FF9F1C` → `#FF6B00` | 메인 버튼 그라디언트 |
| `--accent` | `#3DDC97` | 성공/확인, 반복 모드 |
| `--danger` | `#FF5C5C` | 실패, 판매, 초기화 |
| `--gold` | `#FFC93C` | 골드 |
| `--coin` | `#B388FF` | 스쿼드 코인 |
| `--hp-ally` / `--hp-enemy` | `#5CE07A` / `#FF6B6B` | HP 바 |
| rarity `--r-rare` / `--r-epic` / `--r-unique` / `--r-legendary` | `#3B82F6` / `#A855F7` / `#FACC15` / `#22C55E` | 등급 테두리·글로우 |

## 형태
- 모든 카드·버튼·시트: `border: 3px solid var(--ink)`, `border-radius: 18px`, 아래쪽 `box-shadow: 0 4px 0 var(--ink)` (두꺼운 만화식 그림자). 버튼 눌림은 `translateY(3px)` + 그림자 0.
- 버튼 글자: 흰색, 굵게, `text-shadow: 0 2px 0 rgba(0,0,0,.25)`.
- 상단 HUD: 크림 패널 위에 재화 칩 3개(아이콘 이미지 + 숫자). CP는 큰 숫자에 금색 글로우.
- 전투 씬: 배경 이미지 `bg/*.png`를 `object-fit: cover`, 위에 스프라이트. 씬 프레임도 3px 갈색 테두리 라운드.
- 하단 탭: 5개, 아이콘 이미지(또는 이모지) + 라벨, 활성 탭은 위로 6px 튀어오르고 주황 배경.
- 바텀시트: 크림 패널, 상단 그립, 제목 리본(주황 바탕 흰 글씨).

## 타이포
- 한글: `"Jua", "Gothic A1", system-ui` (Google Fonts, 오프라인이면 폴백). 숫자는 `font-variant-numeric: tabular-nums`.
- 제목 22px, 본문 15px, 보조 13px. 최소 12px.

## 모션
- 드롭 카드: 적 위치에서 스케일 0→1.2→1 팝 + 살짝 회전, 등급 색 글로우, 0.9초 후 장비 탭으로 빨려 들어감.
- 합성 결과: 카드 뒤집기 `rotateY`, unique 이상은 화면 가장자리 무지개 플래시 0.4초.
- 숫자 증가: 300ms 카운트업. 레벨업: 용병 위 "LEVEL UP!" 노란 라벨 팝.
- 스프라이트 idle: 2초 주기 위아래 3px 숨쉬기.

## 금지
- 어두운 배경, 얇은 1px 테두리, 회색 톤, 세리프 폰트, 작은 글씨.
# 에셋 스펙 — 치비 카툰 이미지

공통 스타일 키워드 (모든 프롬프트 앞에 붙임):
`chibi, big head small body, cute, thick dark-brown outlines, cel-shaded, bright saturated colors, glossy, casual mobile game style, full body, transparent background, no text, no watermark`

파일 위치 `assets/` 아래. PNG, 투명 배경. 이름은 아래 표 그대로 (코드가 이 이름을 참조).

## 1. 용병 (facing RIGHT, 512×512, 각 2장: idle / attack)

| 파일 | 프롬프트 요지 |
|---|---|
| `merc/warrior_idle.png`, `merc/warrior_attack.png` | 어린 기사, 주황·금색 갑옷, 큰 방패와 검. attack은 검을 휘두르는 순간 |
| `merc/archer_idle.png`, `merc/archer_attack.png` | 초록 후드 소녀 궁수, 나무 활. attack은 활시위 당김 |
| `merc/mage_idle.png`, `merc/mage_attack.png` | 보라 고깔모자 꼬마 마법사, 별 지팡이. attack은 지팡이 들고 빛 |

## 2. 몬스터 (facing LEFT, 512×512, 각 1장) — 지역별 팔레트

| 지역 | 파일 | 내용 |
|---|---|---|
| 1 잊힌 숲 (초록·갈색) | `mon/slime.png` | 연두 젤리 슬라임, 멍한 눈 |
| | `mon/goblin.png` | 초록 고블린, 나무 몽둥이 |
| | `mon/mushroom.png` | 빨간 점박이 버섯 괴물, 화난 눈썹 |
| | `mon/boss_treant.png` | 보스: 큰 나무 정령, 얼굴 있는 몸통 (640×640) |
| 2 얼어붙은 봉우리 (하늘·흰색) | `mon/ice_bat.png` | 얼음 박쥐, 파란 날개 |
| | `mon/yeti.png` | 작은 예티, 흰 털 |
| | `mon/frost_wolf.png` | 서리 늑대, 하늘색 |
| | `mon/boss_ice_golem.png` | 보스: 얼음 골렘, 크리스탈 몸 (640×640) |
| 3 잿빛 화산 (빨강·검정) | `mon/lava_slime.png` | 용암 슬라임, 주황 발광 |
| | `mon/imp.png` | 빨간 임프, 삼지창 |
| | `mon/salamander.png` | 불 도마뱀 |
| | `mon/boss_dragon.png` | 보스: 아기 드래곤, 빨강·금 (640×640) |

## 3. 배경 (1080×760, 불투명, 지평선을 높이 55% 위치에)

| 파일 | 내용 |
|---|---|
| `bg/forest.png` | 밝은 낮 숲 길, 파란 하늘, 큰 나무, 카툰 |
| `bg/peak.png` | 눈 덮인 산길, 맑은 하늘, 얼음 크리스탈 |
| `bg/volcano.png` | 화산 지대, 용암 강, 주황 하늘 (밝은 톤 유지) |

## 4. 아이콘 (256×256, 각 1장, 정면)

| 파일 | 내용 |
|---|---|
| `icon/weapon.png` | 검 |
| `icon/hat.png` | 투구/모자 |
| `icon/gloves.png` | 장갑 |
| `icon/shoes.png` | 부츠 |
| `icon/skillbook.png` | 반짝이는 마법책 |
| `icon/gold.png` | 금화 |
| `icon/coin.png` | 보라 보석 (스쿼드 코인) |

등급 색은 코드에서 테두리로 칠하므로 아이콘은 등급 무관 1종.

## 5. 코드 연동 규칙
- `DATA.assets` 매핑에 위 경로를 등록하고, 렌더 층은 `<image href>`(SVG) 또는 `<img>`로 사용. 파일이 없으면 기존 벡터 도형으로 폴백.
- 용병 attack 프레임은 공격 순간 120ms 표시 후 idle로 복귀. 피격은 CSS 필터로 흰색 플래시.
- 스프라이트 바닥 정렬: 이미지 하단 5%가 발판 그림자 위치.
# 용병단 (Squad Idle) — 게임 기획서 v0.1

> 레퍼런스: 메이플스토리 「울티마 스쿼드」
> 핵심 훅 (사용자 확정): **① 장비 드롭·합성 뽑기 ② 오프라인 보상 수확**
> 목표: 친구들과 테스트 가능한 수준 (APK 배포 가능, 세이브 안정적). 랭킹/클라우드 저장은 v2.
> 플랫폼: 모바일 세로 (HTML5 → Capacitor로 Android APK). 기준 해상도 390×844, CSS로 스케일.
> 원칙: 모든 콘텐츠(용병/스킬/스테이지/몬스터/장비/잠재옵션/드롭표)는 `js/data.js` 데이터로 정의. 기획 변경 = 데이터 수정.
> 온라인 없음 (v1). 저장은 localStorage + JSON 내보내기/불러오기.

---

## 1. 컨셉 한 줄

전사·궁수·마법사 용병 3명이 **알아서** 스테이지를 밀고, 플레이어는 **장비를 줍고 합성하고 세팅**만 한다. 앱을 꺼도 용병은 계속 싸우고, 다시 켜면 **원정 보고서**가 쌓여 있다.

## 2. 핵심 루프

```
자동 전투 → 골드·장비 드롭 → [합성/장착/잠재옵션] → 전투력 상승 → 막힌 스테이지 돌파 → 더 좋은 드롭
                      ↑                                                          ↓
            오프라인 원정 보고서 (앱 재접속 시 드롭 일괄 수확)  ←  현재 "반복 중인" 스테이지 기준
```

플레이어의 실제 조작은 세 가지뿐이다.
1. **어느 스테이지를 반복할지** 고르기 (반복 vs 다음 스테이지 도전)
2. **장비** 장착 / 합성 / 잠재옵션 재설정
3. **스킬** 슬롯 세팅

## 3. 용병

| 용병 | 역할 | 위치 | 해금 | 특징 |
|---|---|---|---|---|
| 전사 | 탱커 | 맨 앞 | 시작 | HP·방어 높음, 공격 느림, 적 공격을 우선 받음 |
| 궁수 | 딜러 | 중간 | 스테이지 1-6 클리어 | 공격 빠름, 공속·치명타 잠재옵션과 시너지 |
| 마법사 | 서포터 | 맨 뒤 | 스테이지 2-6 클리어 | 힐, 광역기, 부활(긴 쿨) |

- 각 용병은 **레벨**(전투 경험치로 상승, 기초 스탯 증가)과 **장비 4슬롯**(무기/모자/장갑/신발)을 가진다.
- 스탯: HP, 공격력, 방어력, 공격속도, 치명타 확률, 치명타 피해.
- 전투력(CP) = 스탯을 가중합한 단일 숫자. 화면 상단에 항상 표시. 숫자 오르는 게 보상감의 핵심.

## 4. 스테이지

- 구조: **3지역 × 10스테이지**. 1~9는 일반, 10은 보스.
- 각 스테이지는 **웨이브 3개**(보스 스테이지는 웨이브 2개 + 보스). 웨이브당 적 2~4마리.
- **제한시간 60초**. 시간 내 못 깨면 "실패", 용병 HP 회복 후 같은 스테이지 재시작. 죽어도 패널티 없음.
- 클리어 시 다음 스테이지 해금. 플레이어는 언제든 **해금된 스테이지 중 하나를 "반복"** 상태로 둘 수 있다.
- 하단에 토글 **[반복 / 도전]**: 도전이면 클리어 후 자동으로 다음 스테이지로, 반복이면 같은 스테이지 무한 반복(파밍).
- 적 스탯은 스테이지 번호로 계산되는 곡선(`data.js`의 `enemyScale`) × 몬스터 종류 배율. 지역별 몬스터 팔레트 교체.
- 3-10 클리어 시 **카오스 모드** 해금: 같은 30스테이지를 적 스탯 대폭 상향으로 다시 밈. 고티어 드롭 확률 크게 상승. (v1은 해금 + 스탯 배율 + 드롭표 상향만, 전용 상점은 v2)

## 5. 장비 (핵심 훅 ①)

### 5.1 정의
```
장비: { uid, slot(weapon|hat|gloves|shoes), tier(1~8), rarity(rare|epic|unique|legendary),
        base: {atk?, hp?, def?}, potentials: [{id, value}], locked: bool }
```
- **티어 1~8**: 기본 스탯 크기를 정한다. 티어가 오르면 기본 스탯이 지수적으로 상승.
- **등급**: rare(잠재 1줄) / epic(2줄) / unique(3줄) / legendary(3줄 + 값 상한 상향). 색 코드: 파랑/보라/노랑/초록.
- **잠재옵션**: `data.js`의 옵션 풀에서 등급별 가중치로 추첨. 예: 공격력 %, HP %, 공속 %, 치명타 확률, 치명타 피해, 골드 획득 %, 드롭률 %, 피격 시 확률 무적(1초).

### 5.2 드롭
- 적 처치마다 확률로 드롭. 드롭표는 **스테이지 구간별**로 정의: 나올 수 있는 티어 범위와 가중치, 등급 가중치.
- 일반 3-9까지 최대 티어 5. 보스 스테이지는 특정 티어 확정 드롭 (예: 1-10 → 2티어 확정, 2-10 → 3티어, 3-10 → 4티어).
- 카오스 모드는 티어 5~8 확률 상승.
- **드롭 연출이 중요**: 드롭 순간 화면에 아이템 카드가 튀어나오고, unique 이상은 화면 플래시 + 진동(가능 시). 이 연출이 뽑기 재미의 핵심.

### 5.3 합성
- **같은 슬롯·같은 티어 3개 → 티어 +1 장비 1개**. 골드 소모 (티어별 비용).
- 결과 등급: 재료 중 최고 등급을 기준으로, 일정 확률로 한 단계 상승 (`fuseUpgradeChance`). 잠재옵션은 새로 추첨.
- **자동 합성** 버튼: 잠금(locked) 안 된 아이템 중 조건 맞는 것 전부 합성. 노가다를 편하게 해주되, 결과 리스트를 카드로 하나씩 공개(뽑기 연출).
- 인벤토리 상한 (예: 60칸). 넘치면 자동 합성/판매 유도.
- 판매: 티어·등급별 골드.

### 5.4 잠재옵션 재설정
- 스쿼드 코인 소모. 줄 수는 유지, 값·종류 재추첨. 잠금 아이템도 가능.

## 6. 스킬

- 용병당 스킬 슬롯 3개. 스킬은 `data.js`에 정의: `{ id, owner, name, cooldown, effect, levelScale }`.
- 자동 발동: 쿨타임 찬 스킬 중 우선순위 높은 것부터. 없으면 기본 공격.
- 스킬 레벨은 **스킬북**(드롭 아이템)으로 상승. 슬롯 배치는 플레이어가 고름 (예: 전사 도발/방어태세, 궁수 연사/저격, 마법사 힐/메테오/부활).
- v1 스킬 수: 용병당 4개, 그중 3개 선택.

## 7. 재화

| 재화 | 획득 | 사용 |
|---|---|---|
| 골드 | 적 처치, 판매, 오프라인 보상 | 합성, 용병 레벨업 가속 |
| 스쿼드 코인 | 스테이지 최초 클리어 보상, 보스 반복 클리어 소량 | 잠재옵션 재설정, 상점(v1은 스킬북·인벤 확장만) |
| 스킬북 | 드롭 | 스킬 레벨업 |

## 8. 오프라인 원정 (핵심 훅 ②)

- 앱 종료 시각을 저장. 재접속 시 경과 시간 계산, **상한 24시간**.
- 보상 = 현재 "반복" 스테이지의 **초당 기대 골드·기대 드롭**을 경과 시간에 곱해 계산. 기대값은 최근 실제 전투 5분 통계에서 측정(`stats.goldPerSec`, `stats.killsPerSec`). 오프라인은 온라인 효율의 70%.
- 드롭 개수는 기대 처치 수 × 드롭률로 추첨. 결과는 실제 드롭표로 각각 뽑는다.
- **원정 보고서 화면**: "N시간 동안 용병단이 싸웠습니다" → 골드 카운트업 → 아이템 카드가 한 장씩 뒤집힘 (탭하면 빨리감기). 여기서 unique 뜨면 그 순간이 곧 리텐션이다.
- 앱이 백그라운드에 있다가 돌아와도 동일 로직 (visibilitychange).

## 9. 화면 구성 (세로 390×844)

```
┌──────────────────────────┐
│ CP 12,340   🪙 골드  🎟 코인 │  ← 상단 HUD
│ 지역1 · 스테이지 4  ⏱ 42s   │
├──────────────────────────┤
│                          │
│   [전투 씬: 용병 3 ← → 적]   │  ← SVG, 좌우 배치, 간단한 타격 애니메이션
│   HP바, 데미지 숫자, 드롭 카드 │
│                          │
├──────────────────────────┤
│ [◀ 스테이지 ▶]  [반복|도전]   │
├──────────────────────────┤
│ 용병 │ 장비 │ 합성 │ 스킬 │ 설정 │  ← 하단 탭 → 바텀시트
└──────────────────────────┘
```

- **용병 탭**: 3명 카드, 레벨, 스탯, 장착 장비 4칸.
- **장비 탭**: 인벤토리 그리드(슬롯/티어 필터, 정렬), 아이템 상세(잠재옵션, 장착/잠금/판매/재설정).
- **합성 탭**: 슬롯별 티어별 보유 수, [합성] [자동 합성].
- **스킬 탭**: 용병별 스킬 4개, 슬롯 3개 드래그 대신 탭으로 배치.
- **설정 탭**: 세이브 내보내기(JSON 텍스트 복사)/불러오기, 초기화, 버전.

## 10. 저장

- `localStorage` 키 `squad_v1`. 3초마다 + 주요 액션 후 저장. `schemaVersion` 포함, 마이그레이션 함수 자리 마련.
- JSON 내보내기/불러오기 (친구 테스트·버그 리포트·v2 클라우드 저장 대비).
- v2 대비: `Cloud` 인터페이스 스텁 (`save(json)`, `load()`, `submitScore(cp)`)만 두고 구현은 비움.

## 11. 밸런스 초기값 (전부 `data.js`)

- 용병 레벨업 경험치: `50 * 1.15^lvl`. 레벨당 기초 스탯 +5%.
- 적 HP: `20 * 1.22^stageIndex`, 적 공격: `3 * 1.18^stageIndex` (stageIndex 0~29, 카오스는 ×8/×5).
- 드롭률: 일반 8%, 보스 100%. 골드: `5 * 1.2^stageIndex` per kill.
- 장비 기본 스탯: `base * 1.6^(tier-1)`. 등급 배율 rare 1.0 / epic 1.15 / unique 1.35 / legendary 1.6.
- 합성 비용: `100 * 2^(tier-1)` 골드. 등급 상승 확률 15%.
- 잠재 재설정: 스쿼드 코인 20.
- 목표 체감: 1지역은 30분 내, 2지역은 첫날, 3지역은 3~5일, 카오스는 1~2주. (친구 테스트 중 조정)

## 12. 개발 단계

| 단계 | 내용 | 산출물 |
|---|---|---|
| P1 | 데이터 층, 전투 시뮬, 스테이지 진행, 반복/도전, 용병 레벨, 기본 UI, 저장 | 브라우저에서 "밀리는" 게임 |
| P2 | 장비 드롭·연출, 인벤토리, 장착, 합성(수동/자동), 잠재옵션·재설정, 판매 | 핵심 훅 ① 완성 |
| P3 | 오프라인 원정 보고서, 스킬 시스템·스킬북, 카오스 모드, JSON 내보내기, 설정 | 핵심 훅 ② 완성, 친구 테스트 배포 가능 |
| P4 | Capacitor Android 패키징, 아이콘/스플래시, 밸런스 조정 | APK |
?? .claude/restyle-log.txt

Name               Length
----               ------
battle.js            8664
data.js             11536
game-equipment.js   12460
game-expedition.js  11891
game.js             15045
ui-equipment.js     16127
ui-expedition.js    10108
ui.js               18360



exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 js/ui.js' in D:\Git\Inv\squad-idle
 succeeded in 44ms:
"use strict";

var UI = (function () {
  var svgNS = "http://www.w3.org/2000/svg";
  var activeSheet = null;
  var hiddenAt = null;
  var sheetSignature = "";
  var latestState;
  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
  function el(id) { return document.getElementById(id); }
  function fmt(n) {
    if (!Number.isFinite(n)) return "0";
    var abs = Math.abs(n);
    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
  }
  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
  function svgNode(tag, attrs, content) {
    var node = document.createElementNS(svgNS, tag);
    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
    if (content !== undefined) node.textContent = content;
    return node;
  }
  function figure(type, color) {
    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#674e38"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#abb5b0" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#b8c2bb"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#89968a" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#335e49"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#b3ad8f" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#638e6d"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#6f619b"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#8f7dbb"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#18251f" stroke-width="4"/><path d="M-7-6h8" stroke="#334b3d" stroke-width="2"/>';
    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#5b625b" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#526357"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#657461" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#26392c" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#716550" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#9da69a"/>';
  }
  function position(unit) {
    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
    if (unit.type === "boss") return { x: 284, y: 245 };
    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
  }
  function drawUnit(unit, color, locked) {
    var p = position(unit);
    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#07110d", opacity: ".4" }));
    var body = svgNode("g", { class: "unit-body", transform: unit.type === "boss" ? "scale(1.2)" : "scale(1)" });
    body.innerHTML = figure(unit.type, color);
    body.addEventListener("animationend", function (event) {
      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
    });
    group.appendChild(body);
    if (!locked) {
      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#07130e" }));
      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
        fill: unit.side === "enemy" ? "#c78778" : "#accb9b", class: "hp-fill" }));
    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
    return group;
  }
  function drawScene(state, clearEffects) {
    if (clearEffects) el("effect-layer").replaceChildren();
    el("mercenary-layer").replaceChildren();
    el("enemy-layer").replaceChildren();
    DATA.mercenaries.slice().reverse().forEach(function (merc) {
      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
    });
    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
  }
  function animate(node, name) {
    node.classList.remove(name);
    void node.getBoundingClientRect();
    node.classList.add(name);
  }
  function hit(event) {
    var attacker = el("sprite-" + event.attackerId);
    var target = el("sprite-" + event.targetId);
    if (attacker) animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right");
    if (!target) return;
    animate(target.querySelector(".unit-body"), "hit-flash");
    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
    (event.crit ? "✦ " : "") + fmt(event.amount));
    el("effect-layer").appendChild(text);
    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
  }
  function death(event) {
    var target = el("sprite-" + event.id);
    if (!target) return;
    var ghost = target.cloneNode(true);
    ghost.removeAttribute("id");
    ghost.classList.add("death-fade");
    el("effect-layer").appendChild(ghost);
    target.classList.add("fallen");
    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
  }
  function toast(message) {
    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
    surface.appendChild(el("toast"));
    el("toast").textContent = message;
    animate(el("toast"), "visible");
  }
  function update(state) {
    latestState = state;
    var stage = DATA.stages[state.currentStage];
    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
    if (UI.updateCP) UI.updateCP(Game.getCP());
    else el("cp").textContent = fmt(Game.getCP());
    el("gold").textContent = fmt(state.gold);
    el("coins").textContent = fmt(state.squadCoins);
    el("region-name").textContent = DATA.regions[stage.region].name;
    el("region-subtitle").textContent = DATA.regions[stage.region].subtitle;
    el("stage-id").textContent = stageLabel(state.currentStage);
    el("stage-name").textContent = stage.name;
    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
    el("timer").classList.toggle("urgent", remaining <= 10);
    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
    if (UI.updateExpedition) UI.updateExpedition(state);
    ["repeat", "challenge"].forEach(function (mode) {
      el(mode + "-mode").classList.toggle("active", state.mode === mode);
      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
    });
    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
    var result = el("battle-result");
    result.classList.toggle("shown", state.battle.status !== "fighting");
    result.classList.toggle("failed", state.battle.status === "fail");
    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
      var sprite = el("sprite-" + unit.id);
      if (sprite && sprite.querySelector(".hp-fill")) {
        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
        sprite.classList.toggle("fallen", unit.hp <= 0);
      }
    });
    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
      var definition = DATA.mercenaries[index];
      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
    }).join("");
    if (activeSheet) renderSheet(state);
  }
  function mercenaryCards(state) {
    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
      var def = DATA.mercenaries[index];
      var stats = Game.mercenaryStats(merc.id);
      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
          return UI.equipmentSlot(merc, slot, state);
        }).join("") + '</div></article>';
    }).join("");
  }
  function renderSheet(state) {
    if (!activeSheet) return;
    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
    if (signature === sheetSignature) return;
    sheetSignature = signature;
    el("sheet-title").textContent = titles[activeSheet];
    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
    else if (activeSheet === "skills") UI.renderSkills(state);
    else if (activeSheet === "settings") {
      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
      el("reset-game").addEventListener("click", function () {
        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
          Game.reset();
          el("sheet").close();
          toast("새로운 원정이 시작되었습니다");
        }
      });
      UI.renderSaveControls();
    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
  }
  function openSheet(name) {
    activeSheet = name;
    if (UI.sheetOpened) UI.sheetOpened(name);
    sheetSignature = "";
    renderSheet(latestState);
    if (!el("sheet").open) el("sheet").showModal();
    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
  }
  function init() {
    Game.on("update", update);
    Game.on("stageStart", function (state) { drawScene(state, true); });
    Game.on("wave", function () { drawScene(Game.getState(), false); });
    Game.on("hit", hit);
    Game.on("unitDeath", death);
    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
    el("sheet").addEventListener("close", function () {
      if (el("sheet").open) return;
      activeSheet = null;
      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
    });
    document.addEventListener("visibilitychange", function () {
      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
    });
    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
    window.addEventListener("pageshow", function (event) {
      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
    });
    Game.init();
    if (!document.hidden) Game.resume();
    else hiddenAt = Date.now();
  }
  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
})();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 style.css, index.html' in D:\Git\Inv\squad-idle
 succeeded in 57ms:
@charset "UTF-8";
:root { color-scheme: dark; font-family: "Malgun Gothic", "Apple SD Gothic Neo", system-ui, sans-serif; color: #e5e8da; background: #090f0d; font-synthesis: none; --muted: #929f92; --gold: #e4bd7e; --border: #ffffff12; }
* { box-sizing: border-box; }
body { margin: 0; min-width: 280px; -webkit-tap-highlight-color: transparent; }
button { font: inherit; color: inherit; cursor: pointer; touch-action: manipulation; min-width: 44px; min-height: 44px; border: 0; }
button:focus-visible { outline: 2px solid var(--gold); outline-offset: -3px; }
button:disabled { cursor: default; opacity: .25; }
button:active:not(:disabled) { filter: brightness(1.2); }
h1, h2, h3, p { margin: 0; }
button, .battle-scene { -webkit-user-select: none; user-select: none; }
#app { max-width: 480px; margin: 0 auto; min-height: 100vh; min-height: 100dvh; display: flex; flex-direction: column; position: relative; background: #111b17; overflow: clip; box-shadow: 0 0 100px #0006; }
.hud { padding: calc(16px + env(safe-area-inset-top)) 23px 18px; background: #151e19; border-bottom: 1px solid var(--border); }
.brand-row { display: flex; align-items: center; gap: 10px; margin-bottom: 22px; }
h1 { font-size: 17px; font-weight: 800; letter-spacing: -.6px; }
.brand-mark { color: var(--gold); margin-right: 5px; }
.chapter-label { font-size: 10px; color: var(--muted); border-left: 1px solid #536149; padding-left: 10px; }
.save-state { margin-left: auto; color: #a8b997; font-size: 9px; white-space: nowrap; }
.save-state::before { content: ""; display: inline-block; width: 4px; height: 4px; background: #99b888; border-radius: 100%; margin: 0 4px 2px 0; }
.resources { display: grid; grid-template-columns: 1.18fr 1fr .8fr; }
.resource { padding-left: 19px; border-left: 1px solid var(--border); display: flex; flex-direction: column; gap: 6px; }
.resource:first-child { padding-left: 0; border: 0; }
.resource-label { display: flex; gap: 6px; align-items: center; font-size: 11px; color: var(--muted); }
.resource-label small { font-size: 9px; opacity: .8; }
.resource strong { font-family: Georgia, "Malgun Gothic", serif; font-size: 25px; line-height: 1; letter-spacing: -.5px; font-weight: 500; font-variant-numeric: tabular-nums; }
.power strong { color: var(--gold); }
.gold-dot { width: 9px; height: 9px; border-radius: 50%; background: #d0ac62; box-shadow: inset 0 0 0 2px #7a693a; }
.coin-gem { font-size: 11px; color: #a09bc2; font-style: normal; }
.expedition { display: flex; flex: 1; flex-direction: column; }
.stage-header { display: flex; justify-content: space-between; align-items: center; padding: 22px 23px 17px; }
.eyebrow { color: #a2b497; font-size: 10px; letter-spacing: 1.5px; margin-bottom: 7px; }
.stage-header h2 { font-size: 17px; font-weight: 600; letter-spacing: -.7px; }
#stage-id { color: #cfb787; font-family: Georgia, serif; font-size: 19px; }
.title-divider { color: #526049; margin: 0 9px; font-weight: 400; }
.clock { text-align: right; }
.clock > span { display: block; font-size: 9px; color: var(--muted); margin-bottom: 3px; }
.clock strong { font-family: Georgia, serif; font-weight: 400; font-size: 25px; font-variant-numeric: tabular-nums; }
.clock small { font-family: sans-serif; font-size: 10px; padding-left: 4px; color: var(--muted); }
.urgent { color: #e99787; }
.time-track { height: 2px; background: #253127; margin: 0 23px; }
#time-fill { height: 100%; background: #b9bc8c; transition: width .1s linear; }
.battle-scene { position: relative; flex: 1; display: flex; flex-direction: column; justify-content: center; min-height: 310px; background: #14221e; isolation: isolate; }
.scene-topline { position: absolute; top: 20px; left: 23px; right: 23px; display: flex; justify-content: space-between; z-index: 1; color: #a5b2a0; font-size: 10px; }
.auto-badge { font-size: 9px; letter-spacing: .3px; }
.auto-badge i { display: inline-block; width: 5px; height: 5px; border-radius: 50%; background: #b3cc98; margin-right: 4px; box-shadow: 0 0 7px #b3cc9855; }
#battle-svg { width: 100%; display: block; max-height: 430px; }
.scene-caption { position: absolute; bottom: 21px; left: 0; right: 0; display: flex; align-items: center; justify-content: center; gap: 10px; font-size: 9px; color: #7e917d; letter-spacing: .8px; }
.caption-line { width: 22px; height: 1px; background: #62745a66; }
.locked-unit { opacity: .15; }
.locked-label { fill: #d5dfc9; font-size: 9px; }
.hp-fill { transition: width .1s linear; }
.fallen { visibility: hidden; }
.unit-body { transform-box: fill-box; transform-origin: center; animation: breathe 3s ease-in-out infinite; }
.lunge-right { animation: lunge-right .25s ease-out; }
.lunge-left { animation: lunge-left .25s ease-out; }
.hit-flash { animation: hit-flash .24s ease-out; }
.death-fade { animation: death-fade .6s ease-in forwards; pointer-events: none; }
.damage-number { fill: #f3edcb; stroke: #15201b; stroke-width: 3px; paint-order: stroke; font-size: 16px; font-weight: 800; animation: float-damage .8s ease-out forwards; pointer-events: none; }
.damage-number.critical { fill: #ffd279; font-size: 20px; }
.damage-number.incoming { fill: #e19b8b; font-size: 13px; }
.battle-result { display: none; position: absolute; z-index: 3; top: 28%; left: 16%; right: 16%; padding: 19px 12px; text-align: center; background: #17251bf2; border: 1px solid #b6b07d66; box-shadow: 0 10px 35px #0008; border-radius: 5px; }
.battle-result.shown { display: grid; gap: 7px; animation: result-in .3s ease-out; }
.battle-result > span { color: var(--gold); font-size: 22px; }
.battle-result strong { font-size: 17px; color: #e5d8b4; }
.battle-result small { font-size: 10px; color: #a6b299; }
.battle-result.failed { border-color: #aa766566; }
.battle-result.failed strong { color: #e0aea0; }
.squad-strip { display: grid; grid-template-columns: repeat(3, 1fr); gap: 7px; padding: 17px 18px 14px; background: #14201a; border-top: 1px solid var(--border); }
.squad-member { display: flex; align-items: center; gap: 5px; font-size: 10px; white-space: nowrap; }
.squad-member small { margin-left: auto; font-size: 9px; color: #b2b8a0; }
.member-dot { display: block; width: 4px; height: 12px; border-radius: 2px; }
.not-recruited { opacity: .5; }
.harvest-row { display: flex; justify-content: space-between; gap: 5px; padding: 12px 20px 17px; background: #14201a; color: #94a28e; font-size: 9px; }
.harvest-row b { font-weight: 500; color: #c3cba8; }
.rate-divider { color: #5d705d; margin: 0 4px; }
.stage-controls { display: grid; grid-template-columns: 1fr 1.07fr; gap: 12px 19px; padding: 19px 20px 15px; background: #1a241d; border-top: 1px solid var(--border); }
.stage-navigation { display: flex; align-items: center; justify-content: space-between; border: 1px solid #45503b; border-radius: 6px; }
.stage-navigation button { background: transparent; font-size: 25px; color: #e0d6bb; }
.stage-navigation div { display: flex; flex-direction: column; align-items: center; gap: 1px; }
.stage-navigation div span { font-size: 8px; color: var(--muted); }
.stage-navigation strong { font-family: Georgia, serif; font-weight: 400; font-size: 17px; }
.mode-toggle { display: flex; padding: 3px; border-radius: 6px; background: #101a14; border: 1px solid #2c3729; }
.mode-toggle button { flex: 1; background: transparent; color: #97a38e; font-size: 11px; border-radius: 3px; padding: 0 7px; white-space: nowrap; }
.mode-toggle button.active { color: #29291c; background: #d5bb82; box-shadow: 0 1px 5px #0005; font-weight: 700; }
#mode-hint { grid-column: 1/-1; text-align: center; font-size: 9px; color: #9caa90; letter-spacing: .1px; }
.bottom-tabs { display: grid; grid-template-columns: repeat(5, 1fr); background: #101913; padding: 7px 8px calc(10px + env(safe-area-inset-bottom)); border-top: 1px solid var(--border); }
.bottom-tabs button { background: transparent; color: #93a18e; display: flex; flex-direction: column; align-items: center; gap: 5px; font-size: 10px; min-height: 57px; border-radius: 5px; }
.bottom-tabs button span { font-family: Georgia, serif; font-size: 25px; line-height: 31px; color: #aeb49b; }
.bottom-tabs button.selected { background: #cfb98312; color: var(--gold); }
.bottom-tabs button.selected span { color: var(--gold); }
#toast { position: fixed; z-index: 10; width: max-content; max-width: min(88vw, 420px); left: 50%; bottom: 115px; transform: translateX(-50%); pointer-events: none; opacity: 0; background: #e0cf9e; color: #253021; padding: 12px 18px; border-radius: 5px; font-size: 12px; box-shadow: 0 5px 25px #0005; }
#toast.visible { animation: toast 3.4s ease both; }
dialog { color: #e5e8da; background: #19231c; border: 1px solid #68745655; border-bottom: 0; border-radius: 20px 20px 0 0; margin: auto auto 0; padding: 0 20px calc(25px + env(safe-area-inset-bottom)); width: 100%; max-width: 480px; max-height: 78vh; max-height: 78dvh; overflow-y: auto; overscroll-behavior: contain; box-shadow: 0 -20px 80px #0007; }
dialog::backdrop { background: #030a07b8; backdrop-filter: blur(3px); }
dialog[open] { animation: sheet-in .22s ease-out; }
.sheet-grip { height: 4px; width: 34px; background: #62715a; border-radius: 4px; margin: 10px auto 15px; }
.sheet-heading { display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; }
.sheet-heading h2 { font-size: 24px; font-weight: 600; }
.sheet-heading button { color: #bdc5ad; font-size: 25px; background: #ffffff06; border-radius: 50%; }
.sheet-heading .eyebrow { font-size: 9px; }
.sheet-intro { color: #a4b19a; font-size: 11px; margin-bottom: 18px; }
.merc-card { background: #111c15; border: 1px solid #7a866333; border-radius: 10px; padding: 14px; margin-bottom: 12px; }
.merc-card-heading { display: flex; gap: 12px; align-items: center; margin-bottom: 12px; }
.merc-card-heading svg { width: 54px; height: 62px; background: #283528; border-radius: 8px; }
.merc-card-heading p { color: #98a98e; font-size: 9px; margin-bottom: 5px; }
.merc-card-heading h3 { font-size: 18px; }
.merc-card-heading > strong { margin-left: auto; font-size: 14px; color: var(--gold); font-weight: 500; }
.xp-label { display: flex; justify-content: space-between; color: #95a18b; font-size: 9px; margin-bottom: 6px; }
.xp-track { height: 3px; background: #293526; border-radius: 2px; }
.xp-track > div { height: 100%; background: #b8bf84; border-radius: inherit; }
.merc-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px 8px; margin: 16px 0; }
.merc-stats dt { color: #96a38d; font-size: 9px; margin-bottom: 4px; }
.merc-stats dd { margin: 0; font-size: 13px; }
.equipment-slots { display: grid; grid-template-columns: repeat(4, 1fr); gap: 7px; }
.equipment-slot { min-height: 69px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; border: 1px dashed #59664b; border-radius: 6px; background: #1c291b55; }
.equipment-slot > span { font-size: 20px; color: #81916e; }
.equipment-slot small { font-size: 9px; color: #b0b9a0; }
.equipment-slot em { font-size: 8px; color: #829174; font-style: normal; }
.locked-card { opacity: .62; }
.unlock-note { font-size: 11px; color: #c8b894; padding: 7px 0; }
.placeholder { text-align: center; padding: 45px 0 60px; }
.placeholder > span { color: #b5a273; font-size: 50px; }
.placeholder h3 { font-size: 21px; margin: 15px 0 10px; }
.placeholder p { color: #b3bda6; font-size: 12px; }
.placeholder small { display: block; color: #87967b; font-size: 10px; margin-top: 8px; }
.settings-info { font-size: 12px; }
.settings-info > div { display: flex; justify-content: space-between; padding: 16px 0; border-bottom: 1px solid var(--border); gap: 10px; }
.settings-info dt { color: #a5b198; }
.settings-info dd { margin: 0; }
.reset-panel { margin: 28px 0 0; padding: 18px; background: #2a211b; border: 1px solid #9b6b4833; border-radius: 8px; }
.reset-panel h3 { font-size: 14px; }
.reset-panel p { color: #b5a597; font-size: 11px; line-height: 1.7; margin: 8px 0 15px; }
.danger-button { border: 1px solid #bc8a6555; background: #62403255; border-radius: 5px; color: #e6b49a; padding: 0 18px; font-size: 12px; }
@keyframes lunge-right { 35% { translate: 12px 0; rotate: 5deg; } }
@keyframes lunge-left { 35% { translate: -10px 0; rotate: -5deg; } }
@keyframes hit-flash { 35% { filter: brightness(2.1); } }
@keyframes death-fade { to { opacity: 0; translate: 0 12px; } }
@keyframes float-damage { 0% { opacity: 0; translate: 0 5px; } 15% { opacity: 1; } 100% { opacity: 0; translate: 0 -34px; } }
@keyframes result-in { from { opacity: 0; translate: 0 8px; } }
@keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
@keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
@media (max-width: 350px) { .hud { padding-inline: 15px; } .stage-header { padding-inline: 16px; } .stage-header h2 { font-size: 14px; } .stage-controls { padding-inline: 12px; gap: 12px 8px; } .squad-strip { padding-inline: 10px; gap: 6px; } .squad-member { gap: 3px; font-size: 9px; } .squad-member small { font-size: 8px; } .chapter-label { display: none; } }
.rarity-rare { --rarity: #659bff; }
.rarity-epic { --rarity: #b789f5; }
.rarity-unique { --rarity: #edd26a; }
.rarity-legendary { --rarity: #6cdc99; }
.equipment-slot[class*="rarity-"] { border: 1px solid var(--rarity); background: color-mix(in srgb, var(--rarity) 8%, #17251c); }
.equipment-slot[class*="rarity-"] > span, .equipment-slot[class*="rarity-"] em { color: var(--rarity); }
.bottom-tabs button { position: relative; }
#equipment-badge { position: absolute; right: 10px; top: 0; border-radius: 20px; min-width: 18px; padding: 2px 4px; font-size: 10px; background: #e4bd7e; color: #19231c; }
.inventory-heading, .inventory-sort, .fusion-heading, .equip-mode { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-bottom: 14px; }
.inventory-heading p, .fusion-heading p { font-size: 12px; color: #c0cbb4; }
.inventory-heading strong { color: var(--gold); font-size: 14px; font-variant-numeric: tabular-nums; }
.inventory-filters { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 5px; margin-bottom: 12px; }
.inventory-filters button, .detail-actions button, .equip-actions button, .fusion-table button, .fusion-heading button, .equip-mode button { background: #29372a; border: 1px solid #67795544; border-radius: 6px; padding: 7px 5px; font-size: 11px; }
.inventory-filters button.active, button.primary { background: #d5bb82; color: #20281c; border-color: #d5bb82; font-weight: 700; }
.inventory-sort { font-size: 11px; color: #9eae94; }
.inventory-sort select { font: inherit; color: #e5e8da; min-height: 44px; background: #111c15; border: 1px solid #59664b; border-radius: 5px; padding: 0 8px; }
.inventory-grid { display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: 6px; }
.inventory-cell { position: relative; min-width: 0; min-height: 64px; padding: 4px 1px 10px; border: 1px solid var(--rarity); border-radius: 7px; background: color-mix(in srgb, var(--rarity) 10%, #111c15); display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px; }
.inventory-cell > span { color: var(--rarity); font-size: 22px; }
.inventory-cell b { font-size: 10px; font-weight: 500; }
.inventory-cell em { position: absolute; right: 1px; top: 1px; font-size: 9px; font-style: normal; }
.inventory-cell i { position: absolute; bottom: 1px; font-size: 8px; font-style: normal; color: var(--rarity); }
.selected-item { box-shadow: 0 0 0 2px var(--rarity); }
.equipment-empty { color: #a4b19a; font-size: 12px; line-height: 1.9; text-align: center; padding: 35px 8px; }
.equipment-note { color: #93a18b; font-size: 10px; line-height: 1.8; margin: 15px 0; }
.equip-mode { font-size: 11px; padding: 8px; border: 1px solid #d5bb8266; background: #d5bb8211; border-radius: 5px; }
.item-detail { border: 1px solid var(--rarity); border-radius: 10px; padding: 12px; margin: 0 0 16px; background: #111c15; }
.detail-heading { display: flex; gap: 10px; align-items: center; }
.detail-heading > div { flex: 1; min-width: 0; }
.detail-heading h3 { color: var(--rarity); font-size: 14px; overflow-wrap: anywhere; }
.detail-heading p { color: #a4b19a; font-size: 10px; margin-top: 5px; }
.detail-heading > button { border-radius: 50%; background: #ffffff09; }
.item-card { width: 52px; height: 70px; flex: 0 0 auto; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; border: 1px solid var(--rarity); border-radius: 8px; background: color-mix(in srgb, var(--rarity) 15%, #111c15); color: var(--rarity); box-shadow: 0 3px 16px #0004; }
.item-card .item-icon { font-size: 26px; line-height: 1; }
.item-card b { font-size: 13px; }
.item-card small { font-size: 8px; }
.base-stats { display: flex; flex-wrap: wrap; gap: 20px; margin: 16px 0; font-size: 12px; }
.base-stats dt { color: #9fac93; font-size: 10px; margin-bottom: 5px; }
.base-stats dd { margin: 0; }
.item-detail h4 { font-size: 11px; margin: 14px 0 8px; color: #c7cfb9; }
.potential-lines { list-style: none; padding: 0; margin: 0; display: grid; gap: 8px; font-size: 11px; }
.potential-lines li { display: flex; justify-content: space-between; gap: 8px; }
.potential-lines b { white-space: nowrap; color: #c6d7a5; font-weight: 500; }
.equip-actions { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 5px; }
.detail-actions { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
.detail-actions button { flex: 1 1 42%; }
.reroll-preview { margin-top: 15px; padding: 0 10px 12px; border: 1px solid #b789f555; border-radius: 6px; background: #b789f509; }
.reroll-preview > div:first-child { opacity: .55; }
.reroll-after { animation: potential-in .6s ease-out both; }
.fusion-heading small { display: block; font-size: 10px; color: #a5b294; margin-top: 6px; }
.fusion-heading > button { padding-inline: 15px; }
.fusion-table { width: 100%; border-collapse: collapse; font-size: 12px; text-align: center; table-layout: fixed; }
.fusion-table th { font-weight: 500; color: #a8b69d; }
.fusion-table td, .fusion-table th { padding: 8px 2px; border-bottom: 1px solid #ffffff0b; }
.fusion-table th:last-child { width: 32%; }
.fusion-table button { width: 100%; }
.fusion-table small { display: block; font-size: 9px; margin-top: 3px; color: #b3be9e; }
.drop-card { position: fixed; pointer-events: none; z-index: 9; animation: drop-flight 1.25s both; }
.drop-card .item-card { width: 50px; height: 60px; box-shadow: 0 0 20px color-mix(in srgb, var(--rarity) 50%, transparent); }
.battle-scene::after { content: ""; position: absolute; inset: 0; pointer-events: none; border: 3px solid transparent; z-index: 4; }
.loot-flash::after { animation: loot-border .65s ease-out; }
.loot-flash-strong::after { animation: loot-border .9s ease-out; border-width: 6px; }
#cp.cp-jump { animation: cp-jump .5s ease-out; transform-origin: left center; }
#fusion-reveal { margin: auto; border-radius: 16px; border: 1px solid #d5bb8266; width: calc(100% - 36px); max-width: 430px; padding: 24px 16px 16px; text-align: center; background: #101b16; }
#fusion-reveal h2 { font-size: 22px; color: #e4bd7e; }
#reveal-progress { margin: 10px 0 18px; font-size: 11px; color: #a1b195; }
#reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
#reveal-cards:has(.fusion-card:only-child) { grid-template-columns: 1fr; }
.fusion-card { min-width: 0; perspective: 600px; }
.flip-inner { position: relative; width: 76px; height: 100px; margin: auto; transform-style: preserve-3d; animation: item-flip .6s ease-out both; }
.card-back, .card-front { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 10px; }
.card-back { display: grid; place-items: center; border: 1px solid #d5bb82; color: #d5bb82; font-size: 40px; background: repeating-linear-gradient(45deg, #283728, #283728 6px, #213021 6px, #213021 12px); transform: rotateY(180deg); }
.card-front .item-card { width: 100%; height: 100%; }
.card-front .item-icon { font-size: 34px; }
.fusion-card p { margin: 8px 0 0; font-size: 10px; color: #b7c5a7; animation: potential-in .6s both; }
.rarity-upgrade .card-front { animation: fusion-flash .8s .3s both; }
.rarity-upgrade p { color: #edd26a; }
#reveal-skip { background: transparent; color: #c9bc96; font-size: 11px; margin-top: 20px; width: 100%; }
.reveal-skipped .flip-inner, .reveal-skipped .fusion-card p, .reveal-skipped .card-front { animation: none; }
@keyframes drop-flight { 0% { transform: scale(.2) rotate(-18deg); opacity: 0; } 22% { transform: translateY(-60px) scale(1.12) rotate(8deg); opacity: 1; } 42% { transform: translateY(-15px) scale(.97); } 55% { transform: translateY(-30px) scale(1); } 72% { transform: translateY(-24px); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(.15); opacity: 0; } }
@keyframes loot-border { 15%, 40% { border-color: #edd26a; box-shadow: inset 0 0 35px #edd26a77, 0 0 20px #edd26a66; } 100% { border-color: transparent; } }
@keyframes cp-jump { 30% { transform: scale(1.15); color: #fff2b8; text-shadow: 0 0 18px #e4bd7eaa; } }
@keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
@keyframes fusion-flash { 35% { box-shadow: 0 0 36px 10px #edd26aaa; filter: brightness(2); } }
@keyframes potential-in { 0%, 20% { opacity: 0; transform: translateY(6px); } 100% { opacity: 1; transform: translateY(0); } }
@media (max-width: 350px) { dialog { padding-inline: 12px; } .inventory-grid { gap: 4px; } .flip-inner { width: 65px; height: 90px; } }
@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: .01s !important; transition-duration: .01s !important; } #toast.visible { animation-duration: 3.4s !important; } }
/* P3: report harvest, skill loadouts and portable saves. */
.difficulty-toggle { grid-column: 1 / -1; display: flex; gap: 8px; }
.difficulty-toggle[hidden] { display: none; }
.difficulty-toggle button, .skill-selector button { flex: 1; border: 1px solid #637355; border-radius: 8px; }
.difficulty-toggle .active, .skill-selector .active { background: #536246; color: #fff3c3; }
.skill-selector, .active-skills, .save-buttons { display: flex; gap: 8px; }
.skill-books { margin: 16px 0 12px; color: #d5bd86; }
.active-skills span { flex: 1; min-width: 0; background: #111c15; padding: 10px 4px; border-radius: 6px; text-align: center; font-size: 11px; }
.skill-card { margin-top: 12px; padding: 14px; border: 1px solid #536148; border-radius: 12px; background: #142018; }
.equipped-skill { border-color: #c5ae72; }
.skill-toggle { display: flex; width: 100%; justify-content: space-between; align-items: center; gap: 8px; text-align: left; background: transparent; }
.skill-toggle strong { font-size: 16px; color: #e4ce9b; }
.skill-toggle b, .skill-card small { font-size: 11px; color: #aebd9d; }
.skill-card p { font-size: 12px; line-height: 1.6; color: #c3cdb9; margin: 8px 0; }
.skill-level { display: block; width: 100%; margin-top: 12px; border-radius: 6px; background: #33442c; }
.save-controls { margin-top: 20px; }
.save-controls p { font-size: 12px; line-height: 1.6; color: #b3c0a5; }
.save-buttons button { flex: 1; border: 1px solid #74845e; border-radius: 8px; font-size: 12px; }
#save-editor label { display: block; margin: 16px 0 8px; font-size: 12px; }
#save-json { display: block; width: 100%; min-width: 0; max-width: 100%; padding: 10px; border: 1px solid #6b785b; border-radius: 6px; color: #d6dfce; background: #0e1711; font: 12px monospace; resize: vertical; overflow-wrap: anywhere; }
#save-editor > button { margin-top: 10px; width: 100%; background: #405536; border-radius: 6px; }
#offline-report { margin: auto; width: 100%; max-width: 480px; height: 100dvh; max-height: 100dvh; border-radius: 0; border: 0; padding: 32px 20px 0; text-align: center; background: radial-gradient(ellipse at top, #39432a, #101b16 60%); }
#offline-report::after { content: ''; position: fixed; inset: 0; border: 6px solid transparent; pointer-events: none; z-index: 10; }
.report-heading h2 { margin: 14px 0; font-size: 30px; color: #ead3a3; }
#report-duration { font-size: 13px; line-height: 1.7; color: #b9c6a8; }
.report-gold { padding: 26px 0 14px; }
.report-gold span { display: block; font-size: 12px; color: #bba978; }
.report-gold strong { display: block; margin-top: 10px; font-size: 44px; color: #f2d18a; font-variant-numeric: tabular-nums; }
#report-stats, #report-progress { font-size: 11px; color: #b6c2a5; margin-bottom: 18px; }
#report-items { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; padding: 8px 0 24px; }
.book-card { border: 1px solid #a8be8e; color: #bdcea9; }
.harvest-footer { position: sticky; bottom: 0; background: #101b16ed; padding: 12px 0 max(20px, env(safe-area-inset-bottom)); }
.harvest-footer p { font-size: 10px; color: #a4b395; margin-bottom: 10px; }
#harvest-report { width: 100%; min-height: 58px; border-radius: 12px; font-size: 18px; background: #c4ab71; color: #1a2417; font-weight: 700; }
.skill-label { fill: #f2db99; stroke: #152419; stroke-width: 3px; paint-order: stroke; font-size: 12px; font-weight: 700; animation: skill-rise 1s ease-out both; }
@keyframes skill-rise { from { opacity: 1; transform: translateY(0); } to { opacity: 0; transform: translateY(-20px); } }
@keyframes breathe { 50% { transform: translateY(-1px) scaleY(1.015); } }
.fallen .unit-body { animation: none; }
@media (prefers-reduced-motion: reduce) { .unit-body { animation: none; } }
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="theme-color" content="#111916">
  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 40 40'%3E%3Crect width='40' height='40' rx='10' fill='%23111916'/%3E%3Ctext x='20' y='29' text-anchor='middle' font-size='28' fill='%23e4af65'%3E⚔%3C/text%3E%3C/svg%3E">
  <link rel="stylesheet" href="style.css">
  <title>용병단 · 첫 번째 원정</title>
</head>
<body>
  <main id="app">
    <header class="hud">
      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
      <div class="resources">
        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="gold-dot"></i> 골드</span><strong id="gold">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
      </div>
    </header>
    <section class="expedition" aria-label="자동 전투">
      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
      <div class="time-track"><div id="time-fill"></div></div>
      <div class="battle-scene">
        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
          <defs>
            <linearGradient id="sky" x2="0" y2="1"><stop stop-color="#1b302c"/><stop offset="1" stop-color="#111e1c"/></linearGradient>
            <radialGradient id="moon-glow"><stop stop-color="#bdd0b0" stop-opacity=".2"/><stop offset="1" stop-color="#bdd0b0" stop-opacity="0"/></radialGradient>
            <linearGradient id="ground" x2="0" y2="1"><stop stop-color="#2c3b30"/><stop offset="1" stop-color="#14211c"/></linearGradient>
          </defs>
          <rect width="390" height="340" fill="url(#sky)"/>
          <circle cx="250" cy="81" r="107" fill="url(#moon-glow)"/><circle cx="250" cy="81" r="25" fill="#b6c6a4" opacity=".35"/><circle cx="263" cy="72" r="23" fill="#233830"/>
          <g fill="#142522"><path d="M11 246V45L1 22l15 10 9-32 4 65 26-27-17 42 10 166z"/><path d="M77 246l8-160-22-30 27 16 11-64 6 104 28-27-19 44 3 117z"/><path d="M315 252l1-155-24-42 33 22 13-62 6 78 33-39-25 69 17 129z"/></g>
          <g fill="none" stroke="#395043" stroke-width="2" opacity=".3"><path d="M158 238V121a37 37 0 0 1 74 0v117M150 234V120a45 45 0 0 1 90 0v115"/><path d="M158 141h-8m82 0h8m-82 34h-8m82 0h8"/></g>
          <path d="M0 247Q84 228 156 242T390 232V340H0Z" fill="url(#ground)"/>
          <path d="M135 340l49-92 21-1 59 93" fill="#9aab85" opacity=".045"/>
          <g fill="#0d1b18"><path d="M0 0h24l-7 115 25-22-22 45 4 118-24 8zM390 0h-22l6 88-31-26 29 49-7 145 25 9z"/><path d="M0 304l16-18-5 26 29-12-13 22 41-5-12 23H0zm390-13-23-15 8 30-30-8 12 21-39 4 10 17h62z"/></g>
          <g fill="#b9c8a4" opacity=".5"><circle cx="47" cy="135" r="1.3"/><circle cx="283" cy="176" r="1.5"/><circle cx="123" cy="189" r="1"/><circle cx="321" cy="118" r="1"/><circle cx="215" cy="203" r="1.4"/></g>
          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
        </svg>
        <div id="battle-result" class="battle-result" aria-live="polite"></div>
        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
      </div>
      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
    </section>
    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
    </section>
    <nav class="bottom-tabs" aria-label="게임 메뉴">
      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
    </nav>
    <div id="toast" role="status"></div>
    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
  </main>
  <script src="js/data.js"></script>
  <script src="js/battle.js"></script>
  <script src="js/game.js"></script>
  <script src="js/game-equipment.js"></script>
  <script src="js/game-expedition.js"></script>
  <script src="js/ui.js"></script>
  <script src="js/ui-equipment.js"></script>
  <script src="js/ui-expedition.js"></script>
</body>
</html>

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 js/ui-equipment.js, js/ui-expedition.js' in D:\Git\Inv\squad-idle
 succeeded in 59ms:
"use strict";

(function () {
  var filter = "all", sort = "tier", selected = null, equipFor = null, preview = null;
  var unread = 0, currentSheet = null, revealQueue = [], shown = 0, revealFrame = null, lastReveal = 0, skipped = false;
  var cpTarget = null, cpValue = 0, cpFrame = null;
  var ranks = DATA.rarityOrder;
  var labels = { hp: "HP", atk: "공격력", def: "방어력", critChance: "치명타 확률" };
  function el(id) { return document.getElementById(id); }
  function slot(id) { return DATA.equipmentSlots.find(function (s) { return s.id === id; }); }
  function name(item) { return "T" + UI.fmt(item.tier) + " " + DATA.equipmentRarities[item.rarity].name + " " + slot(item.slot).name; }
  function itemCard(item, extra) {
    return '<span class="item-card rarity-' + item.rarity + ' ' + (extra || "") + '"><span class="item-icon">' + slot(item.slot).icon +
      '</span><b>T' + UI.fmt(item.tier) + '</b><small>' + DATA.equipmentRarities[item.rarity].name + '</small></span>';
  }
  function lines(options) {
    return '<ul class="potential-lines">' + options.map(function (p) {
      var definition = DATA.potentialPool.find(function (d) { return d.id === p.id; });
      return '<li><span>' + definition.name + '</span><b>' + (p.id === "invincibleOnHit" ? UI.fmt(DATA.invincibleChance * 100) : '+' + UI.fmt(p.value * 100)) + '%</b></li>';
    }).join("") + '</ul>';
  }
  function badge() { el("equipment-badge").hidden = unread === 0; el("equipment-badge").textContent = UI.fmt(unread); }
  UI.sheetOpened = function (sheet) {
    currentSheet = sheet;
    if (sheet === "equipment") { unread = 0; badge(); }
  };
  UI.equipmentSignature = function (sheet, state) {
    if (sheet !== "equipment" && sheet !== "fusion") return "";
    return JSON.stringify([filter, sort, selected, equipFor, preview, state.inventory, state.gold, state.squadCoins, state.mercenaries]);
  };
  UI.equipmentSlot = function (merc, definition, state) {
    var item = state.inventory.find(function (i) { return i.uid === merc.equipment[definition.id]; });
    return '<button class="equipment-slot ' + (item ? 'rarity-' + item.rarity : '') + '" data-merc-slot="' + definition.id +
      '" data-merc="' + merc.id + '" ' + (merc.unlocked ? '' : 'disabled') + ' aria-label="' + definition.name + ' ' +
      (item ? name(item) : '빈 슬롯') + '"><span>' + definition.icon + '</span><small>' + definition.name + '</small><em>' +
      (item ? 'T' + UI.fmt(item.tier) + ' · ' + DATA.equipmentRarities[item.rarity].name : '비어 있음') + '</em></button>';
  };
  function detail(item, state) {
    return '<section class="item-detail rarity-' + item.rarity + '" aria-label="장비 상세"><div class="detail-heading">' + itemCard(item) +
      '<div><h3>' + name(item) + '</h3><p>' + (item.equippedBy ? DATA.mercenaries.find(function (m) { return m.id === item.equippedBy; }).name + ' 장착 중' : '장착 가능한 장비') +
      '</p></div><button data-action="back" aria-label="목록으로">←</button></div><dl class="base-stats">' + Object.keys(item.base).map(function (key) {
        return '<div><dt>' + labels[key] + '</dt><dd>+' + UI.fmt(item.base[key] * (key === "critChance" ? 100 : 1)) + (key === "critChance" ? '%' : '') + '</dd></div>';
      }).join("") + '</dl><h4>잠재옵션</h4>' + lines(item.potentials) +
      (preview && preview.uid === item.uid ? '<div class="reroll-preview"><div><h4>재설정 전</h4>' + lines(preview.before) + '</div><div class="reroll-after"><h4>재설정 후 ✦</h4>' + lines(preview.after) + '</div></div>' : '') +
      '<h4>' + (equipFor ? DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + '에게 장착' : '장착할 용병') + '</h4><div class="equip-actions">' + state.mercenaries.map(function (m, i) {
        return '<button data-equip="' + m.id + '" ' + (!m.unlocked || item.equippedBy === m.id ? 'disabled' : '') +
          ' class="' + (equipFor === m.id ? 'primary' : '') + '">' + DATA.mercenaries[i].name + (m.unlocked ? ' 장착' : ' 미합류') + '</button>';
      }).join("") + '</div><div class="detail-actions">' + (item.equippedBy ? '<button data-action="unequip">해제</button>' : '') +
      '<button data-action="lock">' + (item.locked ? '🔒 잠금 해제' : '잠금') + '</button><button data-action="sell" ' + (item.locked || item.equippedBy ? 'disabled' : '') +
      '>판매 · ' + UI.fmt(DATA.sellPrice(item.tier, item.rarity)) + ' 골드</button><button data-action="reroll" ' + (state.squadCoins < DATA.potentialRerollCost ? 'disabled' : '') +
      '>잠재 재설정 · 코인 ' + UI.fmt(DATA.potentialRerollCost) + '</button></div></section>';
  }
  function inventory(state) {
    var items = state.inventory.filter(function (i) { return filter === "all" || i.slot === filter; }).slice();
    items.sort(function (a, b) {
      var recent = Number(b.uid.slice(5)) - Number(a.uid.slice(5));
      return sort === "recent" ? recent : sort === "rarity" ? ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || b.tier - a.tier || recent :
        b.tier - a.tier || ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || recent;
    });
    var item = state.inventory.find(function (i) { return i.uid === selected; });
    return '<div class="inventory-heading"><p>전장에서 찾은 가능성</p><strong id="inventory-count">' + UI.fmt(state.inventory.length) + '/' + UI.fmt(DATA.inventoryCap) + '</strong></div>' +
      (equipFor ? '<div class="equip-mode">' + DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + ' · 이 용병에게 장착<button data-action="end-mode">모드 해제</button></div>' : '') +
      '<div class="inventory-filters" role="group" aria-label="장비 슬롯 필터">' + [{ id: "all", name: "전체" }].concat(DATA.equipmentSlots).map(function (s) {
        return '<button data-filter="' + s.id + '" class="' + (filter === s.id ? 'active' : '') + '" aria-pressed="' + (filter === s.id) + '">' + s.name + '</button>';
      }).join("") + '</div><div class="inventory-sort"><span>' + UI.fmt(items.length) + '개 장비</span><label>정렬 <select id="equipment-sort">' +
      [['tier', '티어↓'], ['rarity', '등급↓'], ['recent', '최근']].map(function (s) { return '<option value="' + s[0] + '" ' + (sort === s[0] ? 'selected' : '') + '>' + s[1] + '</option>'; }).join('') +
      '</select></label></div>' + (item ? detail(item, state) : '') + '<div class="inventory-grid">' + items.map(function (i) {
        return '<button class="inventory-cell rarity-' + i.rarity + (selected === i.uid ? ' selected-item' : '') + '" data-item="' + i.uid + '" aria-label="' + name(i) +
          (i.locked ? ' 잠금' : '') + (i.equippedBy ? ' 장착 중' : '') + '"><span>' + slot(i.slot).icon + '</span><b>T' + UI.fmt(i.tier) +
          '</b>' + (i.locked ? '<em>🔒</em>' : '') + (i.equippedBy ? '<i>장착</i>' : '') + '</button>';
      }).join("") + '</div>' + (!items.length ? '<p class="equipment-empty">적을 처치하면 장비가 떨어집니다.<br>같은 슬롯·티어 세 개를 모아 합성해 보세요.</p>' : '') +
      '<p class="equipment-note">가득 차면 잠금·장착을 제외한 최저 티어 레어를 자동 판매합니다. 판매할 레어가 없으면 새 드롭을 골드로 받습니다.</p>';
  }
  function fusion(state) {
    var rows = '';
    DATA.equipmentSlots.forEach(function (s) {
      for (var tier = 1; tier <= 8; tier++) {
        var count = state.inventory.filter(function (i) { return i.slot === s.id && i.tier === tier && !i.locked && !i.equippedBy; }).length;
        rows += '<tr><th>' + s.icon + ' ' + s.name + '</th><td>T' + UI.fmt(tier) + '</td><td>' + UI.fmt(count) + '개</td><td>' + (tier === 8 ? '<small>최대 티어</small>' :
          '<button data-fuse-slot="' + s.id + '" data-tier="' + tier + '" ' + (count < 3 || state.gold < DATA.fusionCost(tier) ? 'disabled' : '') +
          '>합성 ×' + UI.fmt(1) + '<small>' + UI.fmt(DATA.fusionCost(tier)) + ' 골드</small></button>') + '</td></tr>';
      }
    });
    return '<div class="fusion-heading"><div><p>셋을 모아, 한 단계 더.</p><small>보유 골드 ' + UI.fmt(state.gold) + '</small></div><button id="auto-fuse" class="primary">자동 합성</button></div>' +
      '<p class="equipment-note">잠금·장착 장비 제외 · 최저 티어부터 합성<br>최고 재료 등급 유지 · ' + UI.fmt(DATA.fuseUpgradeChance * 100) + '% 확률로 등급 상승</p>' +
      '<table class="fusion-table"><thead><tr><th>슬롯</th><th>티어</th><th>재료</th><th>세 개 합성</th></tr></thead><tbody>' + rows + '</tbody></table>';
  }
  UI.renderEquipmentSheet = function (sheet, state) { el("sheet-content").innerHTML = sheet === "equipment" ? inventory(state) : fusion(state); };
  UI.updateCP = function (value) {
    if (value === cpTarget) return;
    if (cpTarget === null) { cpTarget = cpValue = value; el("cp").textContent = UI.fmt(value); return; }
    cpTarget = value;
    cancelAnimationFrame(cpFrame);
    var from = cpValue, start = performance.now();
    el("cp").classList.remove("cp-jump");
    void el("cp").offsetWidth;
    el("cp").classList.add("cp-jump");
    function frame(now) {
      var t = Math.min(1, (now - start) / 500);
      cpValue = from + (value - from) * (1 - (1 - t) ** 3);
      el("cp").textContent = UI.fmt(Math.round(cpValue));
      if (t < 1) cpFrame = requestAnimationFrame(frame);
    }
    cpFrame = requestAnimationFrame(frame);
  };
  function flash(strong) {
    var scene = document.querySelector("#offline-report[open]") || document.querySelector(".battle-scene");
    scene.classList.remove("loot-flash", "loot-flash-strong");
    void scene.offsetWidth;
    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
  }
  function drop(item) {
    if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
    if (item.enemyId === null) return; // Already revealed in the expedition report.
    var source = el("sprite-" + item.enemyId);
    var svg = el("battle-svg");
    var point = svg.createSVGPoint();
    point.x = source ? Number(source.dataset.x) : 284;
    point.y = source ? Number(source.dataset.y) - 50 : 190;
    var start = point.matrixTransform(svg.getScreenCTM());
    var target = document.querySelector('[data-tab="equipment"] span').getBoundingClientRect();
    var card = document.createElement("div");
    card.className = "drop-card";
    card.innerHTML = itemCard(item);
    card.style.left = start.x - 25 + "px"; card.style.top = start.y - 30 + "px";
    card.style.setProperty("--drop-x", target.x + target.width / 2 - start.x + "px");
    card.style.setProperty("--drop-y", target.y + target.height / 2 - start.y + "px");
    el("app").appendChild(card);
    card.addEventListener("animationend", function (e) { if (e.target === card) card.remove(); });
    if (ranks.indexOf(item.rarity) >= 2) { flash(false); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
  }
  var reveal = document.createElement("dialog");
  reveal.id = "fusion-reveal";
  reveal.setAttribute("aria-label", "합성 결과");
  reveal.innerHTML = '<h2>합성의 순간</h2><p id="reveal-progress" aria-live="polite"></p><div id="reveal-cards"></div><button id="reveal-skip">탭하면 모두 공개</button>';
  el("app").appendChild(reveal);
  function revealOne(event) {
    var card = document.createElement("div");
    card.className = "fusion-card" + (event.upgraded ? " rarity-upgrade" : "");
    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + itemCard(event.item) +
      '</div></div><p>' + (event.upgraded ? '✦ 등급 상승!' : slot(event.item.slot).name) + '</p>';
    el("reveal-cards").appendChild(card);
    if (event.upgraded) flash(true);
  }
  function revealTick(now) {
    if (!reveal.open) { revealFrame = null; return; }
    if (shown < revealQueue.length && now - lastReveal >= 250) {
      revealOne(revealQueue[shown++]); lastReveal = now;
      el("reveal-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(revealQueue.length) + ' 공개';
      el("reveal-cards").lastElementChild.scrollIntoView({ block: "nearest" });
    }
    if (shown < revealQueue.length) revealFrame = requestAnimationFrame(revealTick);
    else { revealFrame = null; el("reveal-skip").textContent = '탭하면 모두 공개 · 다시 탭하면 닫기'; }
  }
  function enqueue(event) {
    if (!reveal.open) {
      revealQueue = []; shown = 0; skipped = false; lastReveal = -Infinity;
      el("reveal-cards").replaceChildren(); reveal.classList.remove("reveal-skipped"); reveal.showModal();
    }
    revealQueue.push(event);
    if (revealFrame === null) revealFrame = requestAnimationFrame(revealTick);
  }
  reveal.addEventListener("click", function () {
    if (skipped) { reveal.close(); return; }
    cancelAnimationFrame(revealFrame); revealFrame = null;
    while (shown < revealQueue.length) revealOne(revealQueue[shown++]);
    skipped = true; reveal.classList.add("reveal-skipped");
    el("reveal-progress").textContent = UI.fmt(shown) + '개 합성 완료';
    el("reveal-skip").textContent = '닫기';
  });
  reveal.addEventListener("close", function () { if (!reveal.open) { cancelAnimationFrame(revealFrame); revealFrame = null; } });
  el("sheet").addEventListener("close", function () { if (!el("sheet").open) { currentSheet = null; equipFor = null; } });
  el("sheet-content").addEventListener("change", function (event) {
    if (event.target.id === "equipment-sort") { sort = event.target.value; UI.refreshSheet(); }
  });
  el("sheet-content").addEventListener("click", function (event) {
    var button = event.target.closest("button");
    if (!button || button.disabled) return;
    var d = button.dataset, state = Game.getState(), item = state.inventory.find(function (i) { return i.uid === selected; });
    if (d.mercSlot) { filter = d.mercSlot; equipFor = d.merc; selected = null; preview = null; UI.openSheet("equipment"); }
    else if (d.filter) { filter = d.filter; selected = null; preview = null; UI.refreshSheet(); }
    else if (d.item) { selected = d.item; preview = null; UI.refreshSheet(); el("sheet").scrollTop = 0; }
    else if (d.equip && item) { Game.equip(item.uid, d.equip); UI.toast('장착 완료 · CP ' + UI.fmt(Game.getCP())); }
    else if (d.fuseSlot) {
      var materials = state.inventory.filter(function (i) { return i.slot === d.fuseSlot && i.tier === Number(d.tier) && !i.locked && !i.equippedBy; });
      if (!Game.fuse(materials.slice(0, 3).map(function (i) { return i.uid; }))) UI.toast('재료 또는 골드가 부족합니다');
    } else if (button.id === "auto-fuse") { if (!Game.autoFuse().length) UI.toast('합성할 재료 또는 골드가 부족합니다'); }
    else if (d.action === "back") { selected = null; preview = null; UI.refreshSheet(); }
    else if (d.action === "end-mode") { equipFor = null; UI.refreshSheet(); }
    else if (item) {
      if (d.action === "unequip") Game.unequip(item.equippedBy, item.slot);
      if (d.action === "lock") Game.toggleLock(item.uid);
      if (d.action === "sell") {
        var amount = Game.sell([item.uid]);
        if (amount !== false) { selected = null; preview = null; UI.refreshSheet(); UI.toast(UI.fmt(amount) + ' 골드에 판매했습니다'); }
      }
      if (d.action === "reroll") {
        if (!Game.rerollPotentials(item.uid)) UI.toast('스쿼드 코인이 부족합니다');
        else document.querySelector('.reroll-preview').scrollIntoView({ block: 'center' });
      }
    }
  });
  Game.on("itemDrop", drop);
  Game.on("fuseResult", enqueue);
  Game.on("potentialReroll", function (e) { preview = { uid: e.item.uid, before: e.before, after: e.after }; });
  Game.on("inventoryFull", function (e) {
    UI.toast('장비함 가득 참 · ' + name(e.sold) + ' 자동 판매 +' + UI.fmt(e.gold) + ' 골드');
  });
  Game.on("stageStart", function (state) {
    if (state.inventory.length === 0) { unread = 0; selected = null; preview = null; badge(); }
  });
  UI.itemCard = itemCard;
  UI.lootFlash = flash;
})();
"use strict";

(function () {
  var owner = "warrior", report = null, frame = null, started = 0, shown = 0;
  function el(id) { return document.getElementById(id); }
  function mercName(id) { return DATA.mercenaries.find(function (m) { return m.id === id; }).name; }
  UI.updateExpedition = function (state) {
    el("difficulty-toggle").hidden = !state.clearedStages.includes(29);
    document.querySelectorAll("[data-difficulty]").forEach(function (button) {
      button.classList.toggle("active", button.dataset.difficulty === state.difficulty);
      button.setAttribute("aria-pressed", String(button.dataset.difficulty === state.difficulty));
    });
  };
  document.querySelectorAll("[data-difficulty]").forEach(function (button) {
    button.addEventListener("click", function () { Game.setDifficulty(button.dataset.difficulty); });
  });
  UI.renderSkills = function (state) {
    var merc = state.mercenaries.find(function (m) { return m.id === owner; });
    el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
      '<div class="skill-selector">' + state.mercenaries.map(function (m) {
        return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
      }).join('') + '</div><p class="skill-books">' + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
      '<div class="active-skills">' + [0, 1, 2].map(function (index) {
        var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
        return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
      }).join('') + '</div>' + DATA.skills.filter(function (s) { return s.owner === owner; }).map(function (s) {
        var index = merc.skills.indexOf(s.id), level = merc.skillLevels[s.id], cost = DATA.skillBookCost(level);
        return '<article class="skill-card ' + (index >= 0 ? 'equipped-skill' : '') + '"><button class="skill-toggle" data-skill="' + s.id + '" aria-pressed="' + (index >= 0) + '" ' + (!merc.unlocked ? 'disabled' : '') +
          '><span><strong>' + s.name + '</strong> <small>Lv. ' + UI.fmt(level) + '</small></span><b>' + (index >= 0 ? '슬롯 ' + UI.fmt(index + 1) : '배치 +') + '</b></button>' +
          '<p>' + s.desc + '</p><small>쿨타임 ' + UI.fmt(s.cooldown / (s.effect.type === "resurrect" ? 1 + (level - 1) * s.levelScale : 1)) + '초 · 성장 배율 ' + UI.fmt((1 + (level - 1) * s.levelScale) * 100) + '%</small>' +
          '<button class="skill-level" data-level-skill="' + s.id + '" ' + (!merc.unlocked || level >= s.maxLevel || state.skillBooks[owner] < cost ? 'disabled' : '') + '>' +
          (level >= s.maxLevel ? '최대 레벨' : '레벨업 (스킬북 ' + UI.fmt(cost) + ')') + '</button></article>';
      }).join('') + (!merc.unlocked ? '<p class="equipment-note">이 용병이 합류하면 스킬을 변경할 수 있습니다.</p>' : '');
  };
  UI.renderSaveControls = function () {
    var panel = document.createElement("section");
    panel.className = "save-controls";
    panel.innerHTML = '<h3>원정 기록 보관</h3><p>JSON으로 진행 상황을 옮기거나 백업하세요.</p>' +
      '<div class="save-buttons"><button id="export-save">세이브 내보내기</button><button id="import-save">세이브 불러오기</button></div>' +
      '<div id="save-editor" hidden><label for="save-json" id="save-json-label">세이브 JSON</label><textarea id="save-json" spellcheck="false" rows="6"></textarea>' +
      '<button id="copy-save" hidden>복사</button><button id="apply-save" hidden>적용</button><p id="save-message" role="status"></p></div>';
    el("sheet-content").insertBefore(panel, el("sheet-content").querySelector(".reset-panel"));
  };
  el("sheet-content").addEventListener("click", async function (event) {
    var b = event.target.closest("button");
    if (!b || b.disabled) return;
    if (b.dataset.skillOwner) { owner = b.dataset.skillOwner; UI.refreshSheet(); }
    if (b.dataset.skill && !Game.toggleSkill(owner, b.dataset.skill)) UI.toast("먼저 슬롯의 스킬 하나를 해제하세요");
    if (b.dataset.levelSkill) Game.levelSkill(b.dataset.levelSkill);
    if (b.id === "export-save" || b.id === "import-save") {
      var exporting = b.id === "export-save";
      el("save-editor").hidden = false;
      el("save-json").value = exporting ? Game.exportSave() : "";
      el("save-json").readOnly = exporting;
      el("copy-save").hidden = !exporting; el("apply-save").hidden = exporting;
      el("save-message").textContent = exporting ? "복사한 JSON을 안전한 곳에 보관하세요." : "불러올 JSON을 붙여 넣으세요.";
    }
    if (b.id === "copy-save") {
      try {
        if (!navigator.clipboard) throw new Error("clipboard unavailable");
        await navigator.clipboard.writeText(el("save-json").value);
        if (el("save-message")) el("save-message").textContent = "복사했습니다.";
      } catch (error) {
        if (el("save-json")) {
          el("save-json").focus(); el("save-json").select();
          el("save-message").textContent = "텍스트를 선택했습니다. 복사 메뉴 또는 Ctrl+C를 사용하세요.";
        }
      }
    }
    if (b.id === "apply-save") {
      var json = el("save-json").value;
      try { JSON.parse(json); } catch (error) { el("save-message").textContent = "올바른 JSON을 입력하세요."; return; }
      if (!Game.validateSave(json)) { el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다."; return; }
      if (!window.confirm("현재 진행 상황을 이 세이브로 교체할까요?")) return;
      if (Game.importSave(json)) { el("sheet").close(); UI.toast("세이브를 불러왔습니다"); }
      else el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다.";
    }
  });
  var overlay = document.createElement("dialog");
  overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
  overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
    '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
    '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
  el("app").appendChild(overlay);
  function revealOne(item) {
    var card = document.createElement("div");
    card.className = "fusion-card";
    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
    el("report-items").appendChild(card);
    if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
  }
  function progress() { el("report-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(report.items.length) + ' 공개 · 탭하면 모두 공개'; }
  function tick(now) {
    if (!overlay.open) return;
    var elapsed = now - started;
    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 1000)));
    var count = Math.min(report.items.length, Math.max(0, Math.floor((elapsed - 1000) / 200) + 1));
    while (shown < count) revealOne(report.items[shown++]);
    progress();
    if (elapsed < 1000 || shown < report.items.length) frame = requestAnimationFrame(tick);
    else frame = null;
  }
  function skip() {
    cancelAnimationFrame(frame); frame = null;
    while (shown < report.items.length) revealOne(report.items[shown++]);
    el("report-gold").textContent = UI.fmt(report.gold);
    overlay.classList.add("reveal-skipped"); progress();
  }
  Game.on("offlineReport", function (reward) {
    if (overlay.open) return;
    report = reward; shown = 0; started = performance.now();
    el("report-items").replaceChildren(); overlay.classList.remove("reveal-skipped");
    var minutes = Math.floor(reward.elapsedMs / 60000);
    el("report-duration").textContent = UI.fmt(Math.floor(minutes / 60)) + '시간 ' + UI.fmt(minutes % 60) + '분 동안 용병단이 싸웠습니다';
    el("report-stats").textContent = UI.fmt(reward.kills) + ' 처치 · 용병별 경험치 +' + UI.fmt(reward.xp);
    el("report-gold").textContent = '0';
    overlay.showModal(); frame = requestAnimationFrame(tick);
  });
  overlay.addEventListener("cancel", function (event) { event.preventDefault(); skip(); });
  overlay.addEventListener("click", function (event) {
    if (event.target.closest("#harvest-report")) {
      skip(); overlay.close(); Game.harvest(); report = null;
    } else skip();
  });
  Game.on("skill", function (event) {
    var sprite = el("sprite-" + event.id);
    if (!sprite) return;
    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 94);
    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "skill-label"); label.textContent = event.name;
    el("effect-layer").appendChild(label);
    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
  });
  Game.on("skillBook", function (event) { UI.toast(mercName(event.owner) + ' 스킬북 획득'); });
  UI.init();
})();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 js/data.js, test/browser.js; Get-Content -Encoding utf8 CHANGELOG.md | Select-Object -Last 45' in D:\Git\Inv\squad-idle
 succeeded in 97ms:
"use strict";

var DATA = {
  version: "0.3.0 · P3",
  schemaVersion: 3,
  offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
  chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
  skillBookChance: 0.03,
  invincibleChance: 0.02,
  invincibleDuration: 1,
  skillBookCost: function (level) { return level; },
  defaultSkills: { warrior: ["taunt", "guard", "smash"], archer: ["volley", "snipe", "poison"], mage: ["heal", "meteor", "resurrect"] },
  // DECISION: Slot order is priority. Power/duration scales 50% per level; revival scales its cooldown instead.
  skills: [
    { id: "taunt", owner: "warrior", name: "도발", desc: "4초간 적의 공격을 자신에게 집중시킵니다.", cooldown: 12, effect: { type: "taunt", duration: 4 }, levelScale: 0.5, maxLevel: 10 },
    { id: "guard", owner: "warrior", name: "방어태세", desc: "5초간 방어력이 100% 증가합니다.", cooldown: 12, effect: { type: "guard", duration: 5, power: 1 }, levelScale: 0.5, maxLevel: 10 },
    { id: "smash", owner: "warrior", name: "강타", desc: "공격력 300%의 일격을 가합니다.", cooldown: 6, effect: { type: "hit", power: 3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "regen", owner: "warrior", name: "재생", desc: "5초간 매초 최대 HP의 4%를 회복합니다.", cooldown: 14, effect: { type: "regen", duration: 5, power: 0.04 }, levelScale: 0.5, maxLevel: 10 },
    { id: "volley", owner: "archer", name: "연사", desc: "공격력 90%로 세 번 연속 공격합니다.", cooldown: 5, effect: { type: "multi", power: 0.9, hits: 3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "snipe", owner: "archer", name: "저격", desc: "공격력 200%의 확정 치명타를 가합니다.", cooldown: 9, effect: { type: "crit", power: 2 }, levelScale: 0.5, maxLevel: 10 },
    { id: "poison", owner: "archer", name: "독화살", desc: "일격 후 6초간 매초 공격력 45%의 독 피해를 줍니다.", cooldown: 10, effect: { type: "dot", power: 0.45, duration: 6 }, levelScale: 0.5, maxLevel: 10 },
    { id: "dodge", owner: "archer", name: "회피", desc: "4초간 50% 확률로 적의 공격을 회피합니다.", cooldown: 12, effect: { type: "dodge", power: 0.5, duration: 4 }, levelScale: 0.5, maxLevel: 10 },
    { id: "heal", owner: "mage", name: "힐", desc: "모든 생존 동료의 최대 HP를 25% 회복합니다.", cooldown: 8, effect: { type: "heal", power: 0.25 }, levelScale: 0.5, maxLevel: 10 },
    { id: "meteor", owner: "mage", name: "메테오", desc: "모든 적에게 공격력 250%의 피해를 줍니다.", cooldown: 10, effect: { type: "aoe", power: 2.5 }, levelScale: 0.5, maxLevel: 10 },
    { id: "resurrect", owner: "mage", name: "천국의 문", desc: "전투당 한 번, 쓰러진 동료 한 명을 HP 30%로 부활시킵니다.", cooldown: 45, effect: { type: "resurrect", power: 0.3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "shield", owner: "mage", name: "마나실드", desc: "6초간 최대 HP의 40%만큼 피해를 흡수합니다.", cooldown: 12, effect: { type: "shield", duration: 6, power: 0.4 }, levelScale: 0.5, maxLevel: 10 }
  ],
  tickMs: 100,
  autosaveMs: 3000,
  catchUpMaxMs: 60000,
  statsWindowMs: 300000,
  resultTicks: 15,
  defaultSeed: 271828,
  // DECISION: Unspecified base stats favor a durable, slow frontline warrior.
  mercenaries: [
    { id: "warrior", name: "전사", role: "선봉 · 탱커", unlockStage: null, position: 0,
      color: "#e4af65", base: { hp: 170, atk: 14, def: 12, attackSpeed: 0.85, critChance: 0.08, critDamage: 1.5 }, growth: 0.05 },
    { id: "archer", name: "궁수", role: "중위 · 딜러", unlockStage: 5, position: 1,
      color: "#88c8a2", base: { hp: 140, atk: 12, def: 5, attackSpeed: 1.4, critChance: 0.15, critDamage: 1.75 }, growth: 0.05 },
    { id: "mage", name: "마법사", role: "후위 · 서포터", unlockStage: 15, position: 2,
      color: "#b7a0ee", base: { hp: 115, atk: 23, def: 3, attackSpeed: 0.7, critChance: 0.1, critDamage: 1.5 }, growth: 0.05 }
  ],
  balance: { enemyHp: 20, enemyHpGrowth: 1.22, enemyAtk: 3, enemyAtkGrowth: 1.18,
    enemyDef: 1, enemyDefGrowth: 1.12, defenseConstant: 20, goldBase: 5, goldGrowth: 1.2,
    xpBase: 50, xpGrowth: 1.15, xpPerKill: 2, bossXpMultiplier: 5,
    firstClearCoins: 10, bossFirstClearCoins: 30, bossRepeatCoins: 2 },
  cpWeights: { hp: 0.2, atk: 5, def: 2, attackSpeed: 20, critChance: 100, critDamage: 10 },
  monsterTypes: {
    slime: { name: "슬라임", hp: 1, atk: 0.9, def: 1, attackSpeed: 0.65 },
    goblin: { name: "척후병", hp: 0.85, atk: 1, def: 0.8, attackSpeed: 0.8 },
    brute: { name: "파수꾼", hp: 1.4, atk: 1.25, def: 1.5, attackSpeed: 0.6 },
    boss: { name: "수호자", hp: 7, atk: 2.5, def: 2, attackSpeed: 0.75 }
  },
  regions: [
    { name: "잊힌 숲", subtitle: "안개 너머, 첫 번째 원정", palette: { slime: "#91b69b", goblin: "#9ba979", brute: "#b09b7d", boss: "#c5b88d" },
      stages: ["숲의 입구", "이끼 오솔길", "버려진 야영지", "뒤틀린 뿌리", "안개 공터", "궁수의 흔적", "가시 덤불", "오래된 제단", "수호자의 길", "고목의 심장"], bossName: "고목 수호자" },
    { name: "잿빛 협곡", subtitle: "꺼지지 않는 불씨를 따라", palette: { slime: "#c69169", goblin: "#c27b68", brute: "#a58a81", boss: "#e09a68" },
      stages: ["재의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "무너진 광산", "마법사의 봉화", "검은 용광로", "불씨의 다리", "왕의 무덤", "잿불 왕좌"], bossName: "잿불 거인" },
    { name: "서리 성채", subtitle: "마지막 성문을 향하여", palette: { slime: "#8fbdd1", goblin: "#91a2c9", brute: "#a9a3c8", boss: "#c2dded" },
      stages: ["눈 덮인 고개", "얼어붙은 호수", "침묵의 초소", "푸른 회랑", "망각의 정원", "서리 감옥", "백야의 탑", "봉인된 서고", "최후의 성문", "영원의 군주"], bossName: "서리 군주" }
  ],
  // DECISION: Fixed waves keep balance reproducible; the boss replaces the last wave.
  waves: [["slime", "goblin"], ["slime", "goblin", "slime"], ["goblin", "slime", "brute"]],
  bossWaves: [["goblin", "brute"], ["slime", "goblin", "brute"], ["boss"]],
  stages: [],
  equipmentSlots: [
    { id: "weapon", name: "무기", icon: "⚔", base: { atk: 4 } },
    { id: "hat", name: "모자", icon: "♜", base: { hp: 30 } },
    { id: "gloves", name: "장갑", icon: "✧", base: { atk: 2, critChance: 0.01 } },
    { id: "shoes", name: "신발", icon: "♧", base: { hp: 15, def: 3 } }
  ],
  equipmentTiers: [
    { tier: 1, multiplier: 1 }, { tier: 2, multiplier: 1.6 },
    { tier: 3, multiplier: 2.56 }, { tier: 4, multiplier: 4.096 },
    { tier: 5, multiplier: 6.5536 }, { tier: 6, multiplier: 10.48576 },
    { tier: 7, multiplier: 16.777216 }, { tier: 8, multiplier: 26.8435456 }
  ],
  equipmentRarities: {
    rare: { name: "레어", multiplier: 1, lines: 1, color: "#659bff" },
    epic: { name: "에픽", multiplier: 1.15, lines: 2, color: "#b789f5" },
    unique: { name: "유니크", multiplier: 1.35, lines: 3, color: "#edd26a" },
    legendary: { name: "레전더리", multiplier: 1.6, lines: 3, color: "#6cdc99" }
  },
  inventoryCap: 60,
  fuseUpgradeChance: 0.15,
  potentialRerollCost: 20,
  rarityOrder: ["rare", "epic", "unique", "legendary"],
  sellMultipliers: { rare: 1, epic: 2, unique: 4, legendary: 8 },
  // DECISION: Boss-band trash uses normal chance; only the boss guarantees the exact tier.
  dropTables: [
    { min: 0, max: 8, chance: 0.08, tierWeights: { 1: 85, 2: 15 }, rarityWeights: [80, 17, 2.7, 0.3] },
    { min: 9, max: 9, chance: 0.08, bossChance: 1, guaranteedTier: 2, tierWeights: { 2: 100 }, rarityWeights: [65, 28, 6, 1] },
    { min: 10, max: 18, chance: 0.08, tierWeights: { 2: 65, 3: 30, 4: 5 }, rarityWeights: [74, 21, 4.4, 0.6] },
    { min: 19, max: 19, chance: 0.08, bossChance: 1, guaranteedTier: 3, tierWeights: { 3: 100 }, rarityWeights: [55, 34, 9, 2] },
    { min: 20, max: 28, chance: 0.08, tierWeights: { 3: 60, 4: 32, 5: 8 }, rarityWeights: [68, 25, 6, 1] },
    { min: 29, max: 29, chance: 0.08, bossChance: 1, guaranteedTier: 4, tierWeights: { 4: 100 }, rarityWeights: [45, 39, 13, 3] }
  ],
  // DECISION: Chaos bosses use the same T5–8 table without a normal-mode forced tier.
  chaosDropTable: { chance: 0.08, bossChance: 1, tierWeights: { 5: 55, 6: 30, 7: 12, 8: 3 }, rarityWeights: [40, 40, 16, 4] },
  fusionCost: function (tier) { return 100 * 2 ** (tier - 1); },
  sellPrice: function (tier, rarity) { return 20 * 2 ** (tier - 1) * DATA.sellMultipliers[rarity]; },
  // DECISION: Option values are fractions; each rarity lists [min, max] and selection weight.
  potentialPool: [
    { id: "atkPct", name: "공격력", ranges: [[0.02, 0.05], [0.04, 0.08], [0.06, 0.12], [0.08, 0.2]], weights: [25, 25, 25, 25] },
    { id: "hpPct", name: "HP", ranges: [[0.03, 0.06], [0.05, 0.1], [0.08, 0.15], [0.1, 0.25]], weights: [25, 25, 20, 20] },
    { id: "defPct", name: "방어력", ranges: [[0.03, 0.06], [0.05, 0.1], [0.08, 0.15], [0.1, 0.25]], weights: [15, 15, 15, 15] },
    { id: "attackSpeedPct", name: "공격속도", ranges: [[0.02, 0.04], [0.03, 0.07], [0.05, 0.1], [0.07, 0.15]], weights: [15, 15, 15, 15] },
    { id: "critChance", name: "치명타 확률", ranges: [[0.01, 0.03], [0.02, 0.05], [0.03, 0.08], [0.05, 0.12]], weights: [10, 10, 12, 12] },
    { id: "critDamage", name: "치명타 피해", ranges: [[0.05, 0.1], [0.08, 0.15], [0.1, 0.25], [0.15, 0.4]], weights: [10, 10, 12, 12] },
    { id: "goldPct", name: "골드 획득", ranges: [[0.03, 0.08], [0.05, 0.12], [0.08, 0.2], [0.12, 0.3]], weights: [10, 10, 8, 8] },
    { id: "dropPct", name: "드롭률", ranges: [[0.01, 0.03], [0.02, 0.05], [0.03, 0.08], [0.05, 0.12]], weights: [5, 4, 5, 5] },
    { id: "invincibleOnHit", name: "피격 시 1초 무적", duration: 1, ranges: [[0.01, 0.01], [0.01, 0.02], [0.02, 0.04], [0.03, 0.06]], weights: [0, 1, 3, 3] }
  ],
  enemyScale: function (stageIndex) {
    var b = DATA.balance;
    return { hp: b.enemyHp * b.enemyHpGrowth ** stageIndex,
      atk: b.enemyAtk * b.enemyAtkGrowth ** stageIndex,
      def: b.enemyDef * b.enemyDefGrowth ** stageIndex };
  },
  goldPerKill: function (stageIndex) { return Math.round(DATA.balance.goldBase * DATA.balance.goldGrowth ** stageIndex); },
  xpToNext: function (level) { return Math.ceil(DATA.balance.xpBase * DATA.balance.xpGrowth ** level); },
  mercenaryStats: function (id, level) {
    var merc = DATA.mercenaries.find(function (m) { return m.id === id; });
    var growth = (1 + merc.growth) ** (level - 1);
    // DECISION: HP/ATK/DEF grow 5% compounded; speed and critical stats stay at base values.
    return { hp: Math.round(merc.base.hp * growth), atk: Math.round(merc.base.atk * growth),
      def: Math.round(merc.base.def * growth), attackSpeed: merc.base.attackSpeed,
      critChance: merc.base.critChance, critDamage: merc.base.critDamage };
  }
};

DATA.regions.forEach(function (region, regionIndex) {
  region.stages.forEach(function (name, localIndex) {
    DATA.stages.push({ index: regionIndex * 10 + localIndex, id: (regionIndex + 1) + "-" + (localIndex + 1),
      name: name, region: regionIndex, palette: region.palette, boss: localIndex === 9,
      bossName: region.bossName, timeLimit: 60, waves: localIndex === 9 ? DATA.bossWaves : DATA.waves,
      firstClearCoins: localIndex === 9 ? DATA.balance.bossFirstClearCoins : DATA.balance.firstClearCoins });
  });
});
"use strict";

// Optional integration smoke test. Node 22+ and an existing Chrome/Edge install; no packages.
const assert = require("node:assert/strict");
const fs = require("node:fs");
const os = require("node:os");
const path = require("node:path");
const http = require("node:http");
const { spawn } = require("node:child_process");
const { once } = require("node:events");
const root = path.resolve(__dirname, "..");
const delay = ms => new Promise(resolve => setTimeout(resolve, ms));
const browserPath = [process.env.CHROME_PATH,
  "C:/Program Files/Google/Chrome/Application/chrome.exe",
  "C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe",
  "/usr/bin/google-chrome", "/usr/bin/chromium", "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
].filter(Boolean).find(file => fs.existsSync(file));
if (!browserPath) throw new Error("Set CHROME_PATH to an existing Chrome or Edge executable.");

async function main() {
  const profile = fs.mkdtempSync(path.join(os.tmpdir(), "p1-browser-"));
  const artifacts = fs.mkdtempSync(path.join(os.tmpdir(), "p1-screenshots-"));
  const server = http.createServer((req, res) => {
    const pathname = decodeURIComponent(new URL(req.url, "http://localhost").pathname);
    const file = path.resolve(root, "." + (pathname === "/" ? "/index.html" : pathname));
    if (!file.startsWith(root + path.sep)) { res.writeHead(403); res.end(); return; }
    fs.readFile(file, (error, data) => {
      if (error) { res.writeHead(404); res.end(); return; }
      const types = { ".html": "text/html", ".css": "text/css", ".js": "application/javascript" };
      res.setHeader("Content-Type", (types[path.extname(file)] || "application/octet-stream") + "; charset=utf-8");
      res.end(data);
    });
  });
  server.listen(0, "127.0.0.1");
  await once(server, "listening");
  const browser = spawn(browserPath, ["--headless=new", "--remote-debugging-port=0", "--user-data-dir=" + profile,
    "--no-first-run", "--no-default-browser-check", "--disable-gpu", "--in-process-gpu",
    ...(process.env.BROWSER_NO_SANDBOX === "1" ? ["--no-sandbox"] : []),
    ...(process.env.BROWSER_SINGLE_PROCESS === "1" ? ["--single-process"] : []),
    "about:blank"], { stdio: ["ignore", "ignore", "pipe"], windowsHide: true });
  let browserLog = "";
  browser.stderr.on("data", data => { browserLog += data.toString(); });
  let socket;
  try {
    const portFile = path.join(profile, "DevToolsActivePort");
    for (let i = 0; i < 100 && !fs.existsSync(portFile); i++) await delay(100);
    assert.ok(fs.existsSync(portFile), "Browser did not start its debugging endpoint");
    const endpoint = fs.readFileSync(portFile, "utf8").trim().split(/\r?\n/);
    socket = new WebSocket("ws://127.0.0.1:" + endpoint[0] + endpoint[1]);
    await Promise.race([once(socket, "open"), delay(10000).then(() => { if (socket.readyState !== WebSocket.OPEN) throw new Error("Browser WebSocket did not open"); })]);
    let id = 0;
    let sessionId;
    const pending = new Map();
    const events = [];
    socket.addEventListener("message", event => {
      const message = JSON.parse(event.data);
      if (message.id) {
        const task = pending.get(message.id);
        pending.delete(message.id);
        if (message.error) task.reject(new Error(JSON.stringify(message.error)));
        else task.resolve(message.result);
      } else events.push(message);
    });
    function command(method, params = {}) {
      return new Promise((resolve, reject) => {
        const key = ++id;
        const timeout = setTimeout(() => { pending.delete(key); reject(new Error("Timed out: " + method)); }, 10000);
        pending.set(key, { resolve: data => { clearTimeout(timeout); resolve(data); }, reject: error => { clearTimeout(timeout); reject(error); } });
        socket.send(JSON.stringify({ id: key, method, params, sessionId }));
      });
    }
    async function evaluate(expression) {
      const result = await command("Runtime.evaluate", { expression, returnByValue: true, awaitPromise: true });
      assert.equal(result.exceptionDetails, undefined, JSON.stringify(result.exceptionDetails));
      return result.result.value;
    }
    async function screenshot(name) {
      const result = await command("Page.captureScreenshot", { format: "png" });
      fs.writeFileSync(path.join(artifacts, name + ".png"), Buffer.from(result.data, "base64"));
    }
    const browserVersion = await command("Browser.getVersion");
    console.log("Browser: " + browserVersion.product);
    const target = await command("Target.createTarget", { url: "http://127.0.0.1:" + server.address().port });
    const session = await command("Target.attachToTarget", { targetId: target.targetId, flatten: true });
    sessionId = session.sessionId;
    await command("Page.enable");
    await command("Page.navigate", { url: "http://127.0.0.1:" + server.address().port });
    await command("Runtime.enable");
    await command("Page.enable");
    await command("Log.enable");
    await command("Emulation.setDeviceMetricsOverride", { width: 390, height: 844, deviceScaleFactor: 1, mobile: true });
    const url = "http://127.0.0.1:" + server.address().port;
    await command("Page.navigate", { url });
    for (let i = 0; i < 100; i++) {
      if (await evaluate("document.readyState === 'complete' && typeof UI !== 'undefined'")) break;
      await delay(100);
    }
    const initial = await evaluate("({stage:Game.getState().currentStage,status:Game.getState().battle.status,mode:Game.getState().mode})");
    assert.deepEqual(initial, { stage: 0, status: "fighting", mode: "challenge" });
    await screenshot("mobile-battle");
    const layout = await evaluate(`({width:innerWidth, scroll:document.documentElement.scrollWidth, height:innerHeight,
      app:document.getElementById('app').getBoundingClientRect().height,
      smallButtons:[...document.querySelectorAll('button')].filter(b=>b.getBoundingClientRect().height>0 && (b.getBoundingClientRect().width<44 || b.getBoundingClientRect().height<44)).map(b=>b.id)})`);
    assert.equal(layout.width, 390);
    assert.ok(layout.scroll <= 390, JSON.stringify(layout));
    assert.ok(layout.app <= 844, JSON.stringify(layout));
    assert.deepEqual(layout.smallButtons, []);
    console.log("PASS mobile 390×844 layout, tap sizes, initial auto-fight");

    await evaluate("document.querySelector('[data-tab=mercenaries]').click()");
    assert.equal(await evaluate("document.querySelectorAll('.merc-card').length"), 3);
    assert.equal(await evaluate("document.querySelectorAll('.equipment-slot').length"), 12);
    await screenshot("mercenaries");
    await evaluate("document.getElementById('close-sheet').click()");
    for (const tab of ["equipment", "fusion", "skills"]) {
      await evaluate("document.querySelector('[data-tab=" + tab + "]').click()");
      const selector = { equipment: ".inventory-grid", fusion: ".fusion-table", skills: ".skill-card" }[tab];
      assert.ok(await evaluate("document.getElementById('sheet').open && !!document.querySelector('" + selector + "')"));
      await evaluate("document.getElementById('close-sheet').click()");
    }
    await evaluate("document.querySelector('[data-tab=settings]').click()");
    assert.ok(await evaluate("document.getElementById('sheet-content').textContent.includes('schemaVersion')"));
    const confirmClick = evaluate("document.getElementById('reset-game').click()");
    for (let i = 0; i < 30 && !events.some(e => e.method === "Page.javascriptDialogOpening"); i++) await delay(100);
    assert.ok(events.some(e => e.method === "Page.javascriptDialogOpening" && e.params.type === "confirm"));
    await command("Page.handleJavaScriptDialog", { accept: false });
    await confirmClick;
    await evaluate("document.getElementById('close-sheet').click()");
    console.log("PASS mercenary cards, equipment slots, placeholders, settings and reset confirmation");

    if (process.env.BROWSER_FAST === "1") await evaluate("while(Game.getState().currentStage === 0) Game.step()");
    for (let i = 0; i < 240; i++) {
      if (await evaluate("Game.getState().currentStage === 1")) break;
      await delay(100);
    }
    assert.equal(await evaluate("Game.getState().currentStage"), 1);
    assert.ok(await evaluate("Game.getState().gold > 0 && Game.getState().squadCoins === 10"));
    await screenshot("challenge-advanced");
    console.log("PASS real 100ms timer clears 1-1 and advances to 1-2 without input");
    await evaluate("document.getElementById('repeat-mode').click();document.getElementById('previous-stage').click()");
    assert.ok(await evaluate("Game.getState().currentStage === 0 && Game.getState().mode === 'repeat'"));
    await evaluate("Game.catchUp(30000)");
    assert.equal(await evaluate("Game.getState().currentStage"), 0);
    assert.equal(await evaluate("Game.getState().squadCoins"), 10);
    await command("Emulation.setDeviceMetricsOverride", { width: 320, height: 640, deviceScaleFactor: 1, mobile: true });
    assert.ok(await evaluate("document.documentElement.scrollWidth <= 320"));
    await command("Emulation.setDeviceMetricsOverride", { width: 1280, height: 900, deviceScaleFactor: 1, mobile: false });
    assert.equal(await evaluate("document.getElementById('app').getBoundingClientRect().width"), 480);
    console.log("PASS repeat, arrows, 320px no overflow and desktop 480px maximum width");

    await require("./browser-equipment")({ evaluate, command, screenshot, delay });

    await require("./browser-p3")({ evaluate, command, screenshot, delay, events });

    await evaluate("Game.pause();Game.save()");
    const before = await evaluate("({gold:Game.getState().gold,seed:Game.getState().rngSeed})");
    await command("Page.reload");
    await delay(250);
    assert.ok(await evaluate("Game.getState().gold >= " + before.gold));
    assert.equal(await evaluate("Game.getState().mode"), "challenge");
    await evaluate("Game.pause()");
    const errors = events.filter(e => e.method === "Runtime.exceptionThrown" ||
      (e.method === "Runtime.consoleAPICalled" && e.params.type === "error") ||
      (e.method === "Log.entryAdded" && e.params.entry.level === "error"));
    assert.deepEqual(errors, []);
    console.log("PASS reload persistence; zero browser console/runtime errors");
    console.log("Screenshots: " + artifacts);
  } catch (error) {
    console.log("Screenshots: " + artifacts);
    console.error(browserLog.slice(-2500));
    throw error;
  } finally {
    if (socket) socket.close();
    browser.kill();
    server.close();
    await delay(500);
    const resolved = path.resolve(profile);
    if (path.dirname(resolved) === path.resolve(os.tmpdir()) && path.basename(resolved).startsWith("p1-browser-")) {
      fs.rmSync(resolved, { recursive: true, force: true, maxRetries: 5, retryDelay: 200 });
    }
  }
}
main().catch(error => { console.error(error); process.exitCode = 1; });
- 장비 잠재 데이터의 등급별 범위와 가중치는 rare/epic/unique/legendary 순서이며, 값은 비율(0.05 = 5%)로 저장. 장비 효과는 P1에 적용하지 않음. 별도 Cloud 전역은 P1 범위와 4개 네임스페이스 규칙에 따라 추가하지 않음.

### 검증

- `js/`의 각 파일에 `node --check` 실행: 통과. 모든 작성 파일 600줄 미만.
- `node test/run.js`: **17 PASS**, 종료 코드 0.
- 브라우저 연결 도구에 사용 가능한 브라우저가 없었음. 설치된 Chrome의 임시 headless 프로필로 검증을 시도했으나 현재 실행 환경에서 renderer `Target crashed`/GPU 프로세스 오류가 발생하여 실제 화면·콘솔 오류 0 검증은 완료하지 못함. 순수 로직 검증과 구분하여 기록.

## Art assets — 2026-09-07

- Generated all 28 PNGs listed in ASSET_SPEC.md using the built-in ChatGPT image_gen tool, with separate calls per asset and the required common style prefix. No HTTP API keys, package installations, or Blender were used.
- Added assets/merc (6), assets/mon (12), assets/bg (3), and assets/icon (7), plus assets/manifest.json and the standalone checkerboard review grid assets/contact_sheet.html.
- Attack poses were generated from their corresponding idle image references. Regenerated the slime to remove an unwanted outfit, the yeti to stand upright, and the peak background to expand its flat ground area.
- Transparency path: native generated alpha preserved for all 25 mercenary, monster, and icon files. No magenta removal was necessary. Preinstalled Pillow 12.1.0 normalized dimensions and padding; backgrounds are opaque RGB 1080x760. Icons are centered; sprite bottoms align at approximately 95% canvas height.
- Recorded final prompts in assets/prompts.json and source paths, processing methods, and sizes in assets/generation_log.jsonl. assets/prepare_asset.py provides the local normalization helper; assets/verify_assets.py checks files and writes assets/verification.json.
- Verification: 28/28 PNGs exist at the exact requested paths, match specified dimensions, exceed 10 KB, and pass alpha/opacity checks. Manifest keys and contact-sheet references are valid. JavaScript syntax checks passed. This art task did not change game code.
- Missing or ungenerated assets: none.

## 0.3.0 · P3 — 2026-09-07

- `js/game-expedition.js`: `Game.harvest()`, `toggleSkill()`, `levelSkill()`, `setDifficulty()`, `exportSave()`·`importSave()`. 원정 보고서 생성·미수확 저장·수확 트랜잭션, 직업별 스킬북, 스킬 배치/성장, 독립 카오스 진행도, v2→v3 마이그레이션과 P3 저장 검증을 추가했다.
- `js/data.js`·`js/battle.js`: 용병별 스킬 4개와 기본 슬롯 3개, 우선순위/쿨타임, 도발·방어·강타·재생·연사·확정 치명타·독·회피·파티 회복·광역기·부활·흡수막 구현. 피격 무적과 카오스 HP/ATK 배율, 스킬 발동 이벤트를 적용했다.
- `js/game.js`·`js/game-equipment.js`: 기존 공개 함수 유지. `catchUp()`의 60초 이상 복귀는 수확 전용 보고서로 전환하고 스테이지/난이도별 최근 5분 통계를 보존한다. 장비 수령 함수를 분리해 P2 포화 처리 그대로 재사용한다. `schemaVersion: 3`, v1→v2→v3 연쇄 변환, 비파괴 `validateSave()`, 선택적 JSON 인자를 받는 `load()`와 허용된 다섯 번째 전역 `Cloud` 스텁을 추가했다. Cloud의 세 메서드는 비동기로 `Error('v2')`를 던지며 통신하지 않는다.
- `js/ui-expedition.js`·기존 UI·`style.css`·`index.html`: 전체 화면 원정 보고서, 1초 골드 카운트업 후 200ms 간격 카드 뒤집기, 전체 공개/수확, 유니크 이상 강한 플래시, 스킬 카드/슬롯/책 소비, 전투 스킬 이름, 카오스 토글, JSON textarea·복사/선택 대체·검증 후 확인/적용, 호흡 애니메이션을 추가했다. 진동은 실제 사용자 입력 이후에만 호출한다.
- `test/p3.js`, `test/long-run.js`, `test/browser-p3.js` 추가. 기존 로직/브라우저 테스트와 독립 밸런스 스크립트의 로딩 순서를 갱신했다. P1 무스킬 기준 검증은 유지하고 P3로 변경된 버전·복귀·무적 기대값만 수정했다.

### DECISION: 원정·스킬·카오스·저장

- 60초 미만은 기존 결정적 전투 빨리감기, 그 이상은 최대 24시간 × 효율 70%. 처치 수는 소수 기대값으로 보관하고 골드/XP는 정수 내림하며 부동소수점 오차를 보정한다. 도전 중 나가도 마지막으로 실행한 스테이지를 반복 원정 대상으로 삼으며 오프라인 최초 클리어/해금/코인은 지급하지 않는다.
- 해당 스테이지의 실측 표본이 없으면 초당 0.1처치와 적 골드(카오스/장비 골드 보너스 포함)를 쓴다. 보스 스테이지는 전체 6마리 중 보스 1마리 비율로 확정 드롭과 XP 배수를 반영한다. 이미 측정된 골드 수익에는 장비 보너스를 중복 적용하지 않는다.
- 장비 추첨은 보고서당 최대 500회. 초과 처치는 각 추첨 확률에 대표 처치 수를 곱해 반영하되 확률 100%, 장비 500개가 상한이다. 스킬북은 인벤토리를 쓰지 않고 직업별 기대량을 확률 반올림한 최대 3개 묶음 카드로 보관한다. 온라인은 처치당 별도 3% 추첨으로 세 직업 중 하나를 균등 지급하고 즉시 저장한다.
- 미수확 보고서는 생성 결과와 RNG/UID를 함께 저장하며 재접속 시 재추첨하지 않는다. 수확 전에는 전투와 추가 원정 시간 누적을 멈춘다. 수확은 한 번만 가능하며 모든 보상을 적용한 뒤 저장하고 현재 스테이지 전투를 새로 시작한다. 장비함 포화 시 잠금/장착 보호·최저 티어 레어 판매·대체 환전은 P2 규칙 그대로다.
- 스킬 슬롯 순서가 우선순위이며 해제 후 다시 탭하면 마지막 슬롯에 들어간다. 용병은 틱당 준비된 스킬 하나를 쓰고, 없으면 공격 대기시간에 따라 기본 공격한다. 회복은 부상자가 있을 때, 부활은 쓰러진 동료가 있을 때만 사용한다. 장착 변경은 이미 흐른 쿨타임을 초기화하지 않는다.
- 스킬북 비용은 현재 스킬 레벨(1→2는 1권, 9→10은 9권), 최대 10레벨. 후반 진행 목표에 맞춰 효과 성장 배율은 `1 + 0.5 × (level - 1)`로 정했다. 도발은 지속시간, 부활은 쿨타임에 배율을 적용하며 부활 HP는 항상 30%, 전투당 한 번이다. 다른 피해/회복/방어/흡수 효과는 위력에 적용한다. P1 적 성장 곡선과 P2 장비 기본값/드롭 확률은 유지한다.
- `invincibleOnHit` 잠재가 하나라도 장착되어 있으면 피격마다 고정 2% 확률로 해당 타격부터 1초간 무적. 기존 잠재 수치의 저장/재설정 범위는 호환을 위해 보존하지만 중첩으로 발동 확률을 늘리지 않는다.
- 일반 3-10 실제 클리어 후 카오스 해금. 난이도별 진행도와 마지막 선택 스테이지를 분리한다. 카오스 HP는 일반 적의 반올림된 HP ×8, ATK ×5, 골드 ×4, 최초 클리어 코인 ×3이며 보스도 T5~8 카오스 표를 사용한다. 카오스 상점은 추가하지 않는다.
- v2의 혼합 스테이지 통계는 이관 시 비워 보수적인 원정 추정치를 사용한다. 기존 용병/장비/진행도/전투 HP와 공격 대기는 보존한다. JSON 가져오기는 전체 검증 후 확인을 받고 교체하며, 가져온 시각부터 저장한다. 가져오기 자체는 과거 시각으로 추가 보상을 생성하지 않지만 미수확 보고서는 그대로 복원한다.

### 검증

- 모든 `js/*.js`를 파일별 `node --check`로 확인(Windows glob 미확장 대응), `node test/run.js`: **37 PASS**, 종료 코드 0. 각 코드/테스트 파일 600줄 미만, 변경 코드의 `git diff --check` 통과.
- 로직: 70% 보고서 수식·60초 경계·24시간 상한·미수확 재접속/중복 수확 방지·스킬북 3% 경계/즉시 저장·12개 효과·쿨타임/우선순위·저장 후에도 전투당 한 번 부활·30개 카오스 스테이지 배율/해금·마이그레이션·JSON 왕복·손상된 효과/슬롯/보고서의 원자적 거부를 검증했다.
- 장기 진행: 10초마다 장비 평가/장착·자동 합성·스킬북 사용, 두 번 실패하면 세 스테이지 아래에서 10분 반복 후 재도전, 매시간 저장/로드. 무료 장비/XP 없이 시드 **271828 / 7919 / 42**가 각각 **5.50 / 5.10 / 5.72시간**에 일반 3-10 클리어(각 6개 세션, 합성 248 / 209 / 234회). 모두 6시간 제한 통과.
- Chrome 152: 기존 P1/P2 회귀와 P3 브라우저 검증 통과. savedAt을 3시간 전으로 저장한 재로딩에서 카운트업·200ms 뒤집기·강한 플래시·탭 공개·수확/포화 판매, 61초 숨김 후 visibilitychange 복귀, 전투 스킬 이름/슬롯 변경, 3-10 전후 카오스 토글, JSON 복사/잘못된 입력 거부/확인 후 왕복을 확인했다. 375px 가로 넘침 없음, 콘솔·런타임 오류 0. 실행은 기존 격리 headless 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; P2 120초 전투는 1,200틱 재생, P3 연출은 실제 프레임으로 검증했다.
- 동시 작업의 `assets/` 파일에는 접근하지 않았다. 변경 이력은 직전 내용을 다시 읽어 단일 원자적 교체로 이 절을 덧붙였다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 index.html; Get-Content -Encoding utf8 js/ui-equipment.js' in D:\Git\Inv\squad-idle
 succeeded in 61ms:
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="theme-color" content="#111916">
  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 40 40'%3E%3Crect width='40' height='40' rx='10' fill='%23111916'/%3E%3Ctext x='20' y='29' text-anchor='middle' font-size='28' fill='%23e4af65'%3E⚔%3C/text%3E%3C/svg%3E">
  <link rel="stylesheet" href="style.css">
  <title>용병단 · 첫 번째 원정</title>
</head>
<body>
  <main id="app">
    <header class="hud">
      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
      <div class="resources">
        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="gold-dot"></i> 골드</span><strong id="gold">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
      </div>
    </header>
    <section class="expedition" aria-label="자동 전투">
      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
      <div class="time-track"><div id="time-fill"></div></div>
      <div class="battle-scene">
        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
          <defs>
            <linearGradient id="sky" x2="0" y2="1"><stop stop-color="#1b302c"/><stop offset="1" stop-color="#111e1c"/></linearGradient>
            <radialGradient id="moon-glow"><stop stop-color="#bdd0b0" stop-opacity=".2"/><stop offset="1" stop-color="#bdd0b0" stop-opacity="0"/></radialGradient>
            <linearGradient id="ground" x2="0" y2="1"><stop stop-color="#2c3b30"/><stop offset="1" stop-color="#14211c"/></linearGradient>
          </defs>
          <rect width="390" height="340" fill="url(#sky)"/>
          <circle cx="250" cy="81" r="107" fill="url(#moon-glow)"/><circle cx="250" cy="81" r="25" fill="#b6c6a4" opacity=".35"/><circle cx="263" cy="72" r="23" fill="#233830"/>
          <g fill="#142522"><path d="M11 246V45L1 22l15 10 9-32 4 65 26-27-17 42 10 166z"/><path d="M77 246l8-160-22-30 27 16 11-64 6 104 28-27-19 44 3 117z"/><path d="M315 252l1-155-24-42 33 22 13-62 6 78 33-39-25 69 17 129z"/></g>
          <g fill="none" stroke="#395043" stroke-width="2" opacity=".3"><path d="M158 238V121a37 37 0 0 1 74 0v117M150 234V120a45 45 0 0 1 90 0v115"/><path d="M158 141h-8m82 0h8m-82 34h-8m82 0h8"/></g>
          <path d="M0 247Q84 228 156 242T390 232V340H0Z" fill="url(#ground)"/>
          <path d="M135 340l49-92 21-1 59 93" fill="#9aab85" opacity=".045"/>
          <g fill="#0d1b18"><path d="M0 0h24l-7 115 25-22-22 45 4 118-24 8zM390 0h-22l6 88-31-26 29 49-7 145 25 9z"/><path d="M0 304l16-18-5 26 29-12-13 22 41-5-12 23H0zm390-13-23-15 8 30-30-8 12 21-39 4 10 17h62z"/></g>
          <g fill="#b9c8a4" opacity=".5"><circle cx="47" cy="135" r="1.3"/><circle cx="283" cy="176" r="1.5"/><circle cx="123" cy="189" r="1"/><circle cx="321" cy="118" r="1"/><circle cx="215" cy="203" r="1.4"/></g>
          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
        </svg>
        <div id="battle-result" class="battle-result" aria-live="polite"></div>
        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
      </div>
      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
    </section>
    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
    </section>
    <nav class="bottom-tabs" aria-label="게임 메뉴">
      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
    </nav>
    <div id="toast" role="status"></div>
    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
  </main>
  <script src="js/data.js"></script>
  <script src="js/battle.js"></script>
  <script src="js/game.js"></script>
  <script src="js/game-equipment.js"></script>
  <script src="js/game-expedition.js"></script>
  <script src="js/ui.js"></script>
  <script src="js/ui-equipment.js"></script>
  <script src="js/ui-expedition.js"></script>
</body>
</html>
"use strict";

(function () {
  var filter = "all", sort = "tier", selected = null, equipFor = null, preview = null;
  var unread = 0, currentSheet = null, revealQueue = [], shown = 0, revealFrame = null, lastReveal = 0, skipped = false;
  var cpTarget = null, cpValue = 0, cpFrame = null;
  var ranks = DATA.rarityOrder;
  var labels = { hp: "HP", atk: "공격력", def: "방어력", critChance: "치명타 확률" };
  function el(id) { return document.getElementById(id); }
  function slot(id) { return DATA.equipmentSlots.find(function (s) { return s.id === id; }); }
  function name(item) { return "T" + UI.fmt(item.tier) + " " + DATA.equipmentRarities[item.rarity].name + " " + slot(item.slot).name; }
  function itemCard(item, extra) {
    return '<span class="item-card rarity-' + item.rarity + ' ' + (extra || "") + '"><span class="item-icon">' + slot(item.slot).icon +
      '</span><b>T' + UI.fmt(item.tier) + '</b><small>' + DATA.equipmentRarities[item.rarity].name + '</small></span>';
  }
  function lines(options) {
    return '<ul class="potential-lines">' + options.map(function (p) {
      var definition = DATA.potentialPool.find(function (d) { return d.id === p.id; });
      return '<li><span>' + definition.name + '</span><b>' + (p.id === "invincibleOnHit" ? UI.fmt(DATA.invincibleChance * 100) : '+' + UI.fmt(p.value * 100)) + '%</b></li>';
    }).join("") + '</ul>';
  }
  function badge() { el("equipment-badge").hidden = unread === 0; el("equipment-badge").textContent = UI.fmt(unread); }
  UI.sheetOpened = function (sheet) {
    currentSheet = sheet;
    if (sheet === "equipment") { unread = 0; badge(); }
  };
  UI.equipmentSignature = function (sheet, state) {
    if (sheet !== "equipment" && sheet !== "fusion") return "";
    return JSON.stringify([filter, sort, selected, equipFor, preview, state.inventory, state.gold, state.squadCoins, state.mercenaries]);
  };
  UI.equipmentSlot = function (merc, definition, state) {
    var item = state.inventory.find(function (i) { return i.uid === merc.equipment[definition.id]; });
    return '<button class="equipment-slot ' + (item ? 'rarity-' + item.rarity : '') + '" data-merc-slot="' + definition.id +
      '" data-merc="' + merc.id + '" ' + (merc.unlocked ? '' : 'disabled') + ' aria-label="' + definition.name + ' ' +
      (item ? name(item) : '빈 슬롯') + '"><span>' + definition.icon + '</span><small>' + definition.name + '</small><em>' +
      (item ? 'T' + UI.fmt(item.tier) + ' · ' + DATA.equipmentRarities[item.rarity].name : '비어 있음') + '</em></button>';
  };
  function detail(item, state) {
    return '<section class="item-detail rarity-' + item.rarity + '" aria-label="장비 상세"><div class="detail-heading">' + itemCard(item) +
      '<div><h3>' + name(item) + '</h3><p>' + (item.equippedBy ? DATA.mercenaries.find(function (m) { return m.id === item.equippedBy; }).name + ' 장착 중' : '장착 가능한 장비') +
      '</p></div><button data-action="back" aria-label="목록으로">←</button></div><dl class="base-stats">' + Object.keys(item.base).map(function (key) {
        return '<div><dt>' + labels[key] + '</dt><dd>+' + UI.fmt(item.base[key] * (key === "critChance" ? 100 : 1)) + (key === "critChance" ? '%' : '') + '</dd></div>';
      }).join("") + '</dl><h4>잠재옵션</h4>' + lines(item.potentials) +
      (preview && preview.uid === item.uid ? '<div class="reroll-preview"><div><h4>재설정 전</h4>' + lines(preview.before) + '</div><div class="reroll-after"><h4>재설정 후 ✦</h4>' + lines(preview.after) + '</div></div>' : '') +
      '<h4>' + (equipFor ? DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + '에게 장착' : '장착할 용병') + '</h4><div class="equip-actions">' + state.mercenaries.map(function (m, i) {
        return '<button data-equip="' + m.id + '" ' + (!m.unlocked || item.equippedBy === m.id ? 'disabled' : '') +
          ' class="' + (equipFor === m.id ? 'primary' : '') + '">' + DATA.mercenaries[i].name + (m.unlocked ? ' 장착' : ' 미합류') + '</button>';
      }).join("") + '</div><div class="detail-actions">' + (item.equippedBy ? '<button data-action="unequip">해제</button>' : '') +
      '<button data-action="lock">' + (item.locked ? '🔒 잠금 해제' : '잠금') + '</button><button data-action="sell" ' + (item.locked || item.equippedBy ? 'disabled' : '') +
      '>판매 · ' + UI.fmt(DATA.sellPrice(item.tier, item.rarity)) + ' 골드</button><button data-action="reroll" ' + (state.squadCoins < DATA.potentialRerollCost ? 'disabled' : '') +
      '>잠재 재설정 · 코인 ' + UI.fmt(DATA.potentialRerollCost) + '</button></div></section>';
  }
  function inventory(state) {
    var items = state.inventory.filter(function (i) { return filter === "all" || i.slot === filter; }).slice();
    items.sort(function (a, b) {
      var recent = Number(b.uid.slice(5)) - Number(a.uid.slice(5));
      return sort === "recent" ? recent : sort === "rarity" ? ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || b.tier - a.tier || recent :
        b.tier - a.tier || ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || recent;
    });
    var item = state.inventory.find(function (i) { return i.uid === selected; });
    return '<div class="inventory-heading"><p>전장에서 찾은 가능성</p><strong id="inventory-count">' + UI.fmt(state.inventory.length) + '/' + UI.fmt(DATA.inventoryCap) + '</strong></div>' +
      (equipFor ? '<div class="equip-mode">' + DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + ' · 이 용병에게 장착<button data-action="end-mode">모드 해제</button></div>' : '') +
      '<div class="inventory-filters" role="group" aria-label="장비 슬롯 필터">' + [{ id: "all", name: "전체" }].concat(DATA.equipmentSlots).map(function (s) {
        return '<button data-filter="' + s.id + '" class="' + (filter === s.id ? 'active' : '') + '" aria-pressed="' + (filter === s.id) + '">' + s.name + '</button>';
      }).join("") + '</div><div class="inventory-sort"><span>' + UI.fmt(items.length) + '개 장비</span><label>정렬 <select id="equipment-sort">' +
      [['tier', '티어↓'], ['rarity', '등급↓'], ['recent', '최근']].map(function (s) { return '<option value="' + s[0] + '" ' + (sort === s[0] ? 'selected' : '') + '>' + s[1] + '</option>'; }).join('') +
      '</select></label></div>' + (item ? detail(item, state) : '') + '<div class="inventory-grid">' + items.map(function (i) {
        return '<button class="inventory-cell rarity-' + i.rarity + (selected === i.uid ? ' selected-item' : '') + '" data-item="' + i.uid + '" aria-label="' + name(i) +
          (i.locked ? ' 잠금' : '') + (i.equippedBy ? ' 장착 중' : '') + '"><span>' + slot(i.slot).icon + '</span><b>T' + UI.fmt(i.tier) +
          '</b>' + (i.locked ? '<em>🔒</em>' : '') + (i.equippedBy ? '<i>장착</i>' : '') + '</button>';
      }).join("") + '</div>' + (!items.length ? '<p class="equipment-empty">적을 처치하면 장비가 떨어집니다.<br>같은 슬롯·티어 세 개를 모아 합성해 보세요.</p>' : '') +
      '<p class="equipment-note">가득 차면 잠금·장착을 제외한 최저 티어 레어를 자동 판매합니다. 판매할 레어가 없으면 새 드롭을 골드로 받습니다.</p>';
  }
  function fusion(state) {
    var rows = '';
    DATA.equipmentSlots.forEach(function (s) {
      for (var tier = 1; tier <= 8; tier++) {
        var count = state.inventory.filter(function (i) { return i.slot === s.id && i.tier === tier && !i.locked && !i.equippedBy; }).length;
        rows += '<tr><th>' + s.icon + ' ' + s.name + '</th><td>T' + UI.fmt(tier) + '</td><td>' + UI.fmt(count) + '개</td><td>' + (tier === 8 ? '<small>최대 티어</small>' :
          '<button data-fuse-slot="' + s.id + '" data-tier="' + tier + '" ' + (count < 3 || state.gold < DATA.fusionCost(tier) ? 'disabled' : '') +
          '>합성 ×' + UI.fmt(1) + '<small>' + UI.fmt(DATA.fusionCost(tier)) + ' 골드</small></button>') + '</td></tr>';
      }
    });
    return '<div class="fusion-heading"><div><p>셋을 모아, 한 단계 더.</p><small>보유 골드 ' + UI.fmt(state.gold) + '</small></div><button id="auto-fuse" class="primary">자동 합성</button></div>' +
      '<p class="equipment-note">잠금·장착 장비 제외 · 최저 티어부터 합성<br>최고 재료 등급 유지 · ' + UI.fmt(DATA.fuseUpgradeChance * 100) + '% 확률로 등급 상승</p>' +
      '<table class="fusion-table"><thead><tr><th>슬롯</th><th>티어</th><th>재료</th><th>세 개 합성</th></tr></thead><tbody>' + rows + '</tbody></table>';
  }
  UI.renderEquipmentSheet = function (sheet, state) { el("sheet-content").innerHTML = sheet === "equipment" ? inventory(state) : fusion(state); };
  UI.updateCP = function (value) {
    if (value === cpTarget) return;
    if (cpTarget === null) { cpTarget = cpValue = value; el("cp").textContent = UI.fmt(value); return; }
    cpTarget = value;
    cancelAnimationFrame(cpFrame);
    var from = cpValue, start = performance.now();
    el("cp").classList.remove("cp-jump");
    void el("cp").offsetWidth;
    el("cp").classList.add("cp-jump");
    function frame(now) {
      var t = Math.min(1, (now - start) / 500);
      cpValue = from + (value - from) * (1 - (1 - t) ** 3);
      el("cp").textContent = UI.fmt(Math.round(cpValue));
      if (t < 1) cpFrame = requestAnimationFrame(frame);
    }
    cpFrame = requestAnimationFrame(frame);
  };
  function flash(strong) {
    var scene = document.querySelector("#offline-report[open]") || document.querySelector(".battle-scene");
    scene.classList.remove("loot-flash", "loot-flash-strong");
    void scene.offsetWidth;
    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
  }
  function drop(item) {
    if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
    if (item.enemyId === null) return; // Already revealed in the expedition report.
    var source = el("sprite-" + item.enemyId);
    var svg = el("battle-svg");
    var point = svg.createSVGPoint();
    point.x = source ? Number(source.dataset.x) : 284;
    point.y = source ? Number(source.dataset.y) - 50 : 190;
    var start = point.matrixTransform(svg.getScreenCTM());
    var target = document.querySelector('[data-tab="equipment"] span').getBoundingClientRect();
    var card = document.createElement("div");
    card.className = "drop-card";
    card.innerHTML = itemCard(item);
    card.style.left = start.x - 25 + "px"; card.style.top = start.y - 30 + "px";
    card.style.setProperty("--drop-x", target.x + target.width / 2 - start.x + "px");
    card.style.setProperty("--drop-y", target.y + target.height / 2 - start.y + "px");
    el("app").appendChild(card);
    card.addEventListener("animationend", function (e) { if (e.target === card) card.remove(); });
    if (ranks.indexOf(item.rarity) >= 2) { flash(false); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
  }
  var reveal = document.createElement("dialog");
  reveal.id = "fusion-reveal";
  reveal.setAttribute("aria-label", "합성 결과");
  reveal.innerHTML = '<h2>합성의 순간</h2><p id="reveal-progress" aria-live="polite"></p><div id="reveal-cards"></div><button id="reveal-skip">탭하면 모두 공개</button>';
  el("app").appendChild(reveal);
  function revealOne(event) {
    var card = document.createElement("div");
    card.className = "fusion-card" + (event.upgraded ? " rarity-upgrade" : "");
    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + itemCard(event.item) +
      '</div></div><p>' + (event.upgraded ? '✦ 등급 상승!' : slot(event.item.slot).name) + '</p>';
    el("reveal-cards").appendChild(card);
    if (event.upgraded) flash(true);
  }
  function revealTick(now) {
    if (!reveal.open) { revealFrame = null; return; }
    if (shown < revealQueue.length && now - lastReveal >= 250) {
      revealOne(revealQueue[shown++]); lastReveal = now;
      el("reveal-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(revealQueue.length) + ' 공개';
      el("reveal-cards").lastElementChild.scrollIntoView({ block: "nearest" });
    }
    if (shown < revealQueue.length) revealFrame = requestAnimationFrame(revealTick);
    else { revealFrame = null; el("reveal-skip").textContent = '탭하면 모두 공개 · 다시 탭하면 닫기'; }
  }
  function enqueue(event) {
    if (!reveal.open) {
      revealQueue = []; shown = 0; skipped = false; lastReveal = -Infinity;
      el("reveal-cards").replaceChildren(); reveal.classList.remove("reveal-skipped"); reveal.showModal();
    }
    revealQueue.push(event);
    if (revealFrame === null) revealFrame = requestAnimationFrame(revealTick);
  }
  reveal.addEventListener("click", function () {
    if (skipped) { reveal.close(); return; }
    cancelAnimationFrame(revealFrame); revealFrame = null;
    while (shown < revealQueue.length) revealOne(revealQueue[shown++]);
    skipped = true; reveal.classList.add("reveal-skipped");
    el("reveal-progress").textContent = UI.fmt(shown) + '개 합성 완료';
    el("reveal-skip").textContent = '닫기';
  });
  reveal.addEventListener("close", function () { if (!reveal.open) { cancelAnimationFrame(revealFrame); revealFrame = null; } });
  el("sheet").addEventListener("close", function () { if (!el("sheet").open) { currentSheet = null; equipFor = null; } });
  el("sheet-content").addEventListener("change", function (event) {
    if (event.target.id === "equipment-sort") { sort = event.target.value; UI.refreshSheet(); }
  });
  el("sheet-content").addEventListener("click", function (event) {
    var button = event.target.closest("button");
    if (!button || button.disabled) return;
    var d = button.dataset, state = Game.getState(), item = state.inventory.find(function (i) { return i.uid === selected; });
    if (d.mercSlot) { filter = d.mercSlot; equipFor = d.merc; selected = null; preview = null; UI.openSheet("equipment"); }
    else if (d.filter) { filter = d.filter; selected = null; preview = null; UI.refreshSheet(); }
    else if (d.item) { selected = d.item; preview = null; UI.refreshSheet(); el("sheet").scrollTop = 0; }
    else if (d.equip && item) { Game.equip(item.uid, d.equip); UI.toast('장착 완료 · CP ' + UI.fmt(Game.getCP())); }
    else if (d.fuseSlot) {
      var materials = state.inventory.filter(function (i) { return i.slot === d.fuseSlot && i.tier === Number(d.tier) && !i.locked && !i.equippedBy; });
      if (!Game.fuse(materials.slice(0, 3).map(function (i) { return i.uid; }))) UI.toast('재료 또는 골드가 부족합니다');
    } else if (button.id === "auto-fuse") { if (!Game.autoFuse().length) UI.toast('합성할 재료 또는 골드가 부족합니다'); }
    else if (d.action === "back") { selected = null; preview = null; UI.refreshSheet(); }
    else if (d.action === "end-mode") { equipFor = null; UI.refreshSheet(); }
    else if (item) {
      if (d.action === "unequip") Game.unequip(item.equippedBy, item.slot);
      if (d.action === "lock") Game.toggleLock(item.uid);
      if (d.action === "sell") {
        var amount = Game.sell([item.uid]);
        if (amount !== false) { selected = null; preview = null; UI.refreshSheet(); UI.toast(UI.fmt(amount) + ' 골드에 판매했습니다'); }
      }
      if (d.action === "reroll") {
        if (!Game.rerollPotentials(item.uid)) UI.toast('스쿼드 코인이 부족합니다');
        else document.querySelector('.reroll-preview').scrollIntoView({ block: 'center' });
      }
    }
  });
  Game.on("itemDrop", drop);
  Game.on("fuseResult", enqueue);
  Game.on("potentialReroll", function (e) { preview = { uid: e.item.uid, before: e.before, after: e.after }; });
  Game.on("inventoryFull", function (e) {
    UI.toast('장비함 가득 참 · ' + name(e.sold) + ' 자동 판매 +' + UI.fmt(e.gold) + ' 골드');
  });
  Game.on("stageStart", function (state) {
    if (state.inventory.length === 0) { unread = 0; selected = null; preview = null; badge(); }
  });
  UI.itemCard = itemCard;
  UI.lootFlash = flash;
})();

codex
기존 P3 기능까지 포함된 UI를 확인했습니다. 전투 좌표와 이벤트는 유지하고, 렌더링에만 이미지와 모션을 연결하겠습니다. 좁은 화면에서는 인벤토리를 5열로 바꿔 44px 터치 영역과 글자 크기를 확보합니다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 js/ui-expedition.js; Get-Content -Encoding utf8 style.css | Select-Object -Skip 240; Get-Content -Encoding utf8 js/data.js | Select-Object -First 100; Get-Content test/run.js | Select-Object -First 45; Get-Content test/browser-equipment.js | Select-Object -First 70' in D:\Git\Inv\squad-idle
 succeeded in 104ms:
"use strict";

(function () {
  var owner = "warrior", report = null, frame = null, started = 0, shown = 0;
  function el(id) { return document.getElementById(id); }
  function mercName(id) { return DATA.mercenaries.find(function (m) { return m.id === id; }).name; }
  UI.updateExpedition = function (state) {
    el("difficulty-toggle").hidden = !state.clearedStages.includes(29);
    document.querySelectorAll("[data-difficulty]").forEach(function (button) {
      button.classList.toggle("active", button.dataset.difficulty === state.difficulty);
      button.setAttribute("aria-pressed", String(button.dataset.difficulty === state.difficulty));
    });
  };
  document.querySelectorAll("[data-difficulty]").forEach(function (button) {
    button.addEventListener("click", function () { Game.setDifficulty(button.dataset.difficulty); });
  });
  UI.renderSkills = function (state) {
    var merc = state.mercenaries.find(function (m) { return m.id === owner; });
    el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
      '<div class="skill-selector">' + state.mercenaries.map(function (m) {
        return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
      }).join('') + '</div><p class="skill-books">' + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
      '<div class="active-skills">' + [0, 1, 2].map(function (index) {
        var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
        return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
      }).join('') + '</div>' + DATA.skills.filter(function (s) { return s.owner === owner; }).map(function (s) {
        var index = merc.skills.indexOf(s.id), level = merc.skillLevels[s.id], cost = DATA.skillBookCost(level);
        return '<article class="skill-card ' + (index >= 0 ? 'equipped-skill' : '') + '"><button class="skill-toggle" data-skill="' + s.id + '" aria-pressed="' + (index >= 0) + '" ' + (!merc.unlocked ? 'disabled' : '') +
          '><span><strong>' + s.name + '</strong> <small>Lv. ' + UI.fmt(level) + '</small></span><b>' + (index >= 0 ? '슬롯 ' + UI.fmt(index + 1) : '배치 +') + '</b></button>' +
          '<p>' + s.desc + '</p><small>쿨타임 ' + UI.fmt(s.cooldown / (s.effect.type === "resurrect" ? 1 + (level - 1) * s.levelScale : 1)) + '초 · 성장 배율 ' + UI.fmt((1 + (level - 1) * s.levelScale) * 100) + '%</small>' +
          '<button class="skill-level" data-level-skill="' + s.id + '" ' + (!merc.unlocked || level >= s.maxLevel || state.skillBooks[owner] < cost ? 'disabled' : '') + '>' +
          (level >= s.maxLevel ? '최대 레벨' : '레벨업 (스킬북 ' + UI.fmt(cost) + ')') + '</button></article>';
      }).join('') + (!merc.unlocked ? '<p class="equipment-note">이 용병이 합류하면 스킬을 변경할 수 있습니다.</p>' : '');
  };
  UI.renderSaveControls = function () {
    var panel = document.createElement("section");
    panel.className = "save-controls";
    panel.innerHTML = '<h3>원정 기록 보관</h3><p>JSON으로 진행 상황을 옮기거나 백업하세요.</p>' +
      '<div class="save-buttons"><button id="export-save">세이브 내보내기</button><button id="import-save">세이브 불러오기</button></div>' +
      '<div id="save-editor" hidden><label for="save-json" id="save-json-label">세이브 JSON</label><textarea id="save-json" spellcheck="false" rows="6"></textarea>' +
      '<button id="copy-save" hidden>복사</button><button id="apply-save" hidden>적용</button><p id="save-message" role="status"></p></div>';
    el("sheet-content").insertBefore(panel, el("sheet-content").querySelector(".reset-panel"));
  };
  el("sheet-content").addEventListener("click", async function (event) {
    var b = event.target.closest("button");
    if (!b || b.disabled) return;
    if (b.dataset.skillOwner) { owner = b.dataset.skillOwner; UI.refreshSheet(); }
    if (b.dataset.skill && !Game.toggleSkill(owner, b.dataset.skill)) UI.toast("먼저 슬롯의 스킬 하나를 해제하세요");
    if (b.dataset.levelSkill) Game.levelSkill(b.dataset.levelSkill);
    if (b.id === "export-save" || b.id === "import-save") {
      var exporting = b.id === "export-save";
      el("save-editor").hidden = false;
      el("save-json").value = exporting ? Game.exportSave() : "";
      el("save-json").readOnly = exporting;
      el("copy-save").hidden = !exporting; el("apply-save").hidden = exporting;
      el("save-message").textContent = exporting ? "복사한 JSON을 안전한 곳에 보관하세요." : "불러올 JSON을 붙여 넣으세요.";
    }
    if (b.id === "copy-save") {
      try {
        if (!navigator.clipboard) throw new Error("clipboard unavailable");
        await navigator.clipboard.writeText(el("save-json").value);
        if (el("save-message")) el("save-message").textContent = "복사했습니다.";
      } catch (error) {
        if (el("save-json")) {
          el("save-json").focus(); el("save-json").select();
          el("save-message").textContent = "텍스트를 선택했습니다. 복사 메뉴 또는 Ctrl+C를 사용하세요.";
        }
      }
    }
    if (b.id === "apply-save") {
      var json = el("save-json").value;
      try { JSON.parse(json); } catch (error) { el("save-message").textContent = "올바른 JSON을 입력하세요."; return; }
      if (!Game.validateSave(json)) { el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다."; return; }
      if (!window.confirm("현재 진행 상황을 이 세이브로 교체할까요?")) return;
      if (Game.importSave(json)) { el("sheet").close(); UI.toast("세이브를 불러왔습니다"); }
      else el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다.";
    }
  });
  var overlay = document.createElement("dialog");
  overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
  overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
    '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
    '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
  el("app").appendChild(overlay);
  function revealOne(item) {
    var card = document.createElement("div");
    card.className = "fusion-card";
    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
    el("report-items").appendChild(card);
    if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
  }
  function progress() { el("report-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(report.items.length) + ' 공개 · 탭하면 모두 공개'; }
  function tick(now) {
    if (!overlay.open) return;
    var elapsed = now - started;
    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 1000)));
    var count = Math.min(report.items.length, Math.max(0, Math.floor((elapsed - 1000) / 200) + 1));
    while (shown < count) revealOne(report.items[shown++]);
    progress();
    if (elapsed < 1000 || shown < report.items.length) frame = requestAnimationFrame(tick);
    else frame = null;
  }
  function skip() {
    cancelAnimationFrame(frame); frame = null;
    while (shown < report.items.length) revealOne(report.items[shown++]);
    el("report-gold").textContent = UI.fmt(report.gold);
    overlay.classList.add("reveal-skipped"); progress();
  }
  Game.on("offlineReport", function (reward) {
    if (overlay.open) return;
    report = reward; shown = 0; started = performance.now();
    el("report-items").replaceChildren(); overlay.classList.remove("reveal-skipped");
    var minutes = Math.floor(reward.elapsedMs / 60000);
    el("report-duration").textContent = UI.fmt(Math.floor(minutes / 60)) + '시간 ' + UI.fmt(minutes % 60) + '분 동안 용병단이 싸웠습니다';
    el("report-stats").textContent = UI.fmt(reward.kills) + ' 처치 · 용병별 경험치 +' + UI.fmt(reward.xp);
    el("report-gold").textContent = '0';
    overlay.showModal(); frame = requestAnimationFrame(tick);
  });
  overlay.addEventListener("cancel", function (event) { event.preventDefault(); skip(); });
  overlay.addEventListener("click", function (event) {
    if (event.target.closest("#harvest-report")) {
      skip(); overlay.close(); Game.harvest(); report = null;
    } else skip();
  });
  Game.on("skill", function (event) {
    var sprite = el("sprite-" + event.id);
    if (!sprite) return;
    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 94);
    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "skill-label"); label.textContent = event.name;
    el("effect-layer").appendChild(label);
    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
  });
  Game.on("skillBook", function (event) { UI.toast(mercName(event.owner) + ' 스킬북 획득'); });
  UI.init();
})();
.skill-toggle b, .skill-card small { font-size: 11px; color: #aebd9d; }
.skill-card p { font-size: 12px; line-height: 1.6; color: #c3cdb9; margin: 8px 0; }
.skill-level { display: block; width: 100%; margin-top: 12px; border-radius: 6px; background: #33442c; }
.save-controls { margin-top: 20px; }
.save-controls p { font-size: 12px; line-height: 1.6; color: #b3c0a5; }
.save-buttons button { flex: 1; border: 1px solid #74845e; border-radius: 8px; font-size: 12px; }
#save-editor label { display: block; margin: 16px 0 8px; font-size: 12px; }
#save-json { display: block; width: 100%; min-width: 0; max-width: 100%; padding: 10px; border: 1px solid #6b785b; border-radius: 6px; color: #d6dfce; background: #0e1711; font: 12px monospace; resize: vertical; overflow-wrap: anywhere; }
#save-editor > button { margin-top: 10px; width: 100%; background: #405536; border-radius: 6px; }
#offline-report { margin: auto; width: 100%; max-width: 480px; height: 100dvh; max-height: 100dvh; border-radius: 0; border: 0; padding: 32px 20px 0; text-align: center; background: radial-gradient(ellipse at top, #39432a, #101b16 60%); }
#offline-report::after { content: ''; position: fixed; inset: 0; border: 6px solid transparent; pointer-events: none; z-index: 10; }
.report-heading h2 { margin: 14px 0; font-size: 30px; color: #ead3a3; }
#report-duration { font-size: 13px; line-height: 1.7; color: #b9c6a8; }
.report-gold { padding: 26px 0 14px; }
.report-gold span { display: block; font-size: 12px; color: #bba978; }
.report-gold strong { display: block; margin-top: 10px; font-size: 44px; color: #f2d18a; font-variant-numeric: tabular-nums; }
#report-stats, #report-progress { font-size: 11px; color: #b6c2a5; margin-bottom: 18px; }
#report-items { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; padding: 8px 0 24px; }
.book-card { border: 1px solid #a8be8e; color: #bdcea9; }
.harvest-footer { position: sticky; bottom: 0; background: #101b16ed; padding: 12px 0 max(20px, env(safe-area-inset-bottom)); }
.harvest-footer p { font-size: 10px; color: #a4b395; margin-bottom: 10px; }
#harvest-report { width: 100%; min-height: 58px; border-radius: 12px; font-size: 18px; background: #c4ab71; color: #1a2417; font-weight: 700; }
.skill-label { fill: #f2db99; stroke: #152419; stroke-width: 3px; paint-order: stroke; font-size: 12px; font-weight: 700; animation: skill-rise 1s ease-out both; }
@keyframes skill-rise { from { opacity: 1; transform: translateY(0); } to { opacity: 0; transform: translateY(-20px); } }
@keyframes breathe { 50% { transform: translateY(-1px) scaleY(1.015); } }
.fallen .unit-body { animation: none; }
@media (prefers-reduced-motion: reduce) { .unit-body { animation: none; } }
"use strict";

var DATA = {
  version: "0.3.0 · P3",
  schemaVersion: 3,
  offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
  chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
  skillBookChance: 0.03,
  invincibleChance: 0.02,
  invincibleDuration: 1,
  skillBookCost: function (level) { return level; },
  defaultSkills: { warrior: ["taunt", "guard", "smash"], archer: ["volley", "snipe", "poison"], mage: ["heal", "meteor", "resurrect"] },
  // DECISION: Slot order is priority. Power/duration scales 50% per level; revival scales its cooldown instead.
  skills: [
    { id: "taunt", owner: "warrior", name: "도발", desc: "4초간 적의 공격을 자신에게 집중시킵니다.", cooldown: 12, effect: { type: "taunt", duration: 4 }, levelScale: 0.5, maxLevel: 10 },
    { id: "guard", owner: "warrior", name: "방어태세", desc: "5초간 방어력이 100% 증가합니다.", cooldown: 12, effect: { type: "guard", duration: 5, power: 1 }, levelScale: 0.5, maxLevel: 10 },
    { id: "smash", owner: "warrior", name: "강타", desc: "공격력 300%의 일격을 가합니다.", cooldown: 6, effect: { type: "hit", power: 3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "regen", owner: "warrior", name: "재생", desc: "5초간 매초 최대 HP의 4%를 회복합니다.", cooldown: 14, effect: { type: "regen", duration: 5, power: 0.04 }, levelScale: 0.5, maxLevel: 10 },
    { id: "volley", owner: "archer", name: "연사", desc: "공격력 90%로 세 번 연속 공격합니다.", cooldown: 5, effect: { type: "multi", power: 0.9, hits: 3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "snipe", owner: "archer", name: "저격", desc: "공격력 200%의 확정 치명타를 가합니다.", cooldown: 9, effect: { type: "crit", power: 2 }, levelScale: 0.5, maxLevel: 10 },
    { id: "poison", owner: "archer", name: "독화살", desc: "일격 후 6초간 매초 공격력 45%의 독 피해를 줍니다.", cooldown: 10, effect: { type: "dot", power: 0.45, duration: 6 }, levelScale: 0.5, maxLevel: 10 },
    { id: "dodge", owner: "archer", name: "회피", desc: "4초간 50% 확률로 적의 공격을 회피합니다.", cooldown: 12, effect: { type: "dodge", power: 0.5, duration: 4 }, levelScale: 0.5, maxLevel: 10 },
    { id: "heal", owner: "mage", name: "힐", desc: "모든 생존 동료의 최대 HP를 25% 회복합니다.", cooldown: 8, effect: { type: "heal", power: 0.25 }, levelScale: 0.5, maxLevel: 10 },
    { id: "meteor", owner: "mage", name: "메테오", desc: "모든 적에게 공격력 250%의 피해를 줍니다.", cooldown: 10, effect: { type: "aoe", power: 2.5 }, levelScale: 0.5, maxLevel: 10 },
    { id: "resurrect", owner: "mage", name: "천국의 문", desc: "전투당 한 번, 쓰러진 동료 한 명을 HP 30%로 부활시킵니다.", cooldown: 45, effect: { type: "resurrect", power: 0.3 }, levelScale: 0.5, maxLevel: 10 },
    { id: "shield", owner: "mage", name: "마나실드", desc: "6초간 최대 HP의 40%만큼 피해를 흡수합니다.", cooldown: 12, effect: { type: "shield", duration: 6, power: 0.4 }, levelScale: 0.5, maxLevel: 10 }
  ],
  tickMs: 100,
  autosaveMs: 3000,
  catchUpMaxMs: 60000,
  statsWindowMs: 300000,
  resultTicks: 15,
  defaultSeed: 271828,
  // DECISION: Unspecified base stats favor a durable, slow frontline warrior.
  mercenaries: [
    { id: "warrior", name: "전사", role: "선봉 · 탱커", unlockStage: null, position: 0,
      color: "#e4af65", base: { hp: 170, atk: 14, def: 12, attackSpeed: 0.85, critChance: 0.08, critDamage: 1.5 }, growth: 0.05 },
    { id: "archer", name: "궁수", role: "중위 · 딜러", unlockStage: 5, position: 1,
      color: "#88c8a2", base: { hp: 140, atk: 12, def: 5, attackSpeed: 1.4, critChance: 0.15, critDamage: 1.75 }, growth: 0.05 },
    { id: "mage", name: "마법사", role: "후위 · 서포터", unlockStage: 15, position: 2,
      color: "#b7a0ee", base: { hp: 115, atk: 23, def: 3, attackSpeed: 0.7, critChance: 0.1, critDamage: 1.5 }, growth: 0.05 }
  ],
  balance: { enemyHp: 20, enemyHpGrowth: 1.22, enemyAtk: 3, enemyAtkGrowth: 1.18,
    enemyDef: 1, enemyDefGrowth: 1.12, defenseConstant: 20, goldBase: 5, goldGrowth: 1.2,
    xpBase: 50, xpGrowth: 1.15, xpPerKill: 2, bossXpMultiplier: 5,
    firstClearCoins: 10, bossFirstClearCoins: 30, bossRepeatCoins: 2 },
  cpWeights: { hp: 0.2, atk: 5, def: 2, attackSpeed: 20, critChance: 100, critDamage: 10 },
  monsterTypes: {
    slime: { name: "슬라임", hp: 1, atk: 0.9, def: 1, attackSpeed: 0.65 },
    goblin: { name: "척후병", hp: 0.85, atk: 1, def: 0.8, attackSpeed: 0.8 },
    brute: { name: "파수꾼", hp: 1.4, atk: 1.25, def: 1.5, attackSpeed: 0.6 },
    boss: { name: "수호자", hp: 7, atk: 2.5, def: 2, attackSpeed: 0.75 }
  },
  regions: [
    { name: "잊힌 숲", subtitle: "안개 너머, 첫 번째 원정", palette: { slime: "#91b69b", goblin: "#9ba979", brute: "#b09b7d", boss: "#c5b88d" },
      stages: ["숲의 입구", "이끼 오솔길", "버려진 야영지", "뒤틀린 뿌리", "안개 공터", "궁수의 흔적", "가시 덤불", "오래된 제단", "수호자의 길", "고목의 심장"], bossName: "고목 수호자" },
    { name: "잿빛 협곡", subtitle: "꺼지지 않는 불씨를 따라", palette: { slime: "#c69169", goblin: "#c27b68", brute: "#a58a81", boss: "#e09a68" },
      stages: ["재의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "무너진 광산", "마법사의 봉화", "검은 용광로", "불씨의 다리", "왕의 무덤", "잿불 왕좌"], bossName: "잿불 거인" },
    { name: "서리 성채", subtitle: "마지막 성문을 향하여", palette: { slime: "#8fbdd1", goblin: "#91a2c9", brute: "#a9a3c8", boss: "#c2dded" },
      stages: ["눈 덮인 고개", "얼어붙은 호수", "침묵의 초소", "푸른 회랑", "망각의 정원", "서리 감옥", "백야의 탑", "봉인된 서고", "최후의 성문", "영원의 군주"], bossName: "서리 군주" }
  ],
  // DECISION: Fixed waves keep balance reproducible; the boss replaces the last wave.
  waves: [["slime", "goblin"], ["slime", "goblin", "slime"], ["goblin", "slime", "brute"]],
  bossWaves: [["goblin", "brute"], ["slime", "goblin", "brute"], ["boss"]],
  stages: [],
  equipmentSlots: [
    { id: "weapon", name: "무기", icon: "⚔", base: { atk: 4 } },
    { id: "hat", name: "모자", icon: "♜", base: { hp: 30 } },
    { id: "gloves", name: "장갑", icon: "✧", base: { atk: 2, critChance: 0.01 } },
    { id: "shoes", name: "신발", icon: "♧", base: { hp: 15, def: 3 } }
  ],
  equipmentTiers: [
    { tier: 1, multiplier: 1 }, { tier: 2, multiplier: 1.6 },
    { tier: 3, multiplier: 2.56 }, { tier: 4, multiplier: 4.096 },
    { tier: 5, multiplier: 6.5536 }, { tier: 6, multiplier: 10.48576 },
    { tier: 7, multiplier: 16.777216 }, { tier: 8, multiplier: 26.8435456 }
  ],
  equipmentRarities: {
    rare: { name: "레어", multiplier: 1, lines: 1, color: "#659bff" },
    epic: { name: "에픽", multiplier: 1.15, lines: 2, color: "#b789f5" },
    unique: { name: "유니크", multiplier: 1.35, lines: 3, color: "#edd26a" },
    legendary: { name: "레전더리", multiplier: 1.6, lines: 3, color: "#6cdc99" }
  },
  inventoryCap: 60,
  fuseUpgradeChance: 0.15,
  potentialRerollCost: 20,
  rarityOrder: ["rare", "epic", "unique", "legendary"],
  sellMultipliers: { rare: 1, epic: 2, unique: 4, legendary: 8 },
  // DECISION: Boss-band trash uses normal chance; only the boss guarantees the exact tier.
  dropTables: [
    { min: 0, max: 8, chance: 0.08, tierWeights: { 1: 85, 2: 15 }, rarityWeights: [80, 17, 2.7, 0.3] },
    { min: 9, max: 9, chance: 0.08, bossChance: 1, guaranteedTier: 2, tierWeights: { 2: 100 }, rarityWeights: [65, 28, 6, 1] },
    { min: 10, max: 18, chance: 0.08, tierWeights: { 2: 65, 3: 30, 4: 5 }, rarityWeights: [74, 21, 4.4, 0.6] },
    { min: 19, max: 19, chance: 0.08, bossChance: 1, guaranteedTier: 3, tierWeights: { 3: 100 }, rarityWeights: [55, 34, 9, 2] },
    { min: 20, max: 28, chance: 0.08, tierWeights: { 3: 60, 4: 32, 5: 8 }, rarityWeights: [68, 25, 6, 1] },
    { min: 29, max: 29, chance: 0.08, bossChance: 1, guaranteedTier: 4, tierWeights: { 4: 100 }, rarityWeights: [45, 39, 13, 3] }
  ],
  // DECISION: Chaos bosses use the same T5–8 table without a normal-mode forced tier.
  chaosDropTable: { chance: 0.08, bossChance: 1, tierWeights: { 5: 55, 6: 30, 7: 12, 8: 3 }, rarityWeights: [40, 40, 16, 4] },
  fusionCost: function (tier) { return 100 * 2 ** (tier - 1); },
"use strict";

const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");
const vm = require("node:vm");

function context() {
  const storage = new Map();
  const intervals = new Map();
  let timerId = 0;
  const sandbox = vm.createContext({
    localStorage: { getItem: key => storage.get(key) || null, setItem: (key, value) => storage.set(key, value) },
    setInterval: (fn, ms) => { intervals.set(++timerId, { fn, ms }); return timerId; },
    clearInterval: id => intervals.delete(id)
  });
  for (const file of ["data", "battle", "game", "game-equipment", "game-expedition"]) {
    vm.runInContext(fs.readFileSync(path.join(__dirname, "../js/" + file + ".js"), "utf8"), sandbox, { filename: file + ".js" });
  }
  return { ...sandbox, storage, intervals };
}
function plain(value) { return JSON.parse(JSON.stringify(value)); }
function simulate(c, stage, squad = [{ id: "warrior", level: 1 }], emit) {
  const sim = c.Battle.start(stage, squad, emit);
  while (sim.status === "fighting") c.Battle.tick(sim, emit);
  return sim;
}
function untilResult(c) {
  for (let i = 0; i < 601 && c.Game.getState().battle.status === "fighting"; i++) c.Game.step();
  return c.Game.getState();
}
function fixture(c, stage, level = 1) {
  c.Game.reset();
  const state = c.Game.getState();
  state.currentStage = stage;
  state.unlockedStages = Array.from({ length: stage + 1 }, (_, i) => i);
  state.clearedStages = Array.from({ length: stage }, (_, i) => i);
  state.mercenaries.forEach((merc, i) => {
    merc.level = level;
    merc.unlocked = i === 0 || c.DATA.mercenaries[i].unlockStage < stage;
  });
  state.battle = c.Battle.start(stage, state.mercenaries.filter(m => m.unlocked));
  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
  assert.equal(c.Game.load(), true);
}
"use strict";
const assert = require("node:assert/strict");

module.exports = async function ({ evaluate, command, screenshot, delay }) {
  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
  await evaluate(`Game.reset(); Game.setMode("repeat"); window.p2Drops = []; window.p2Animations = [];
    window.p2Off = Game.on('itemDrop', item => {
      p2Drops.push(item);
      requestAnimationFrame(() => {
        const card = document.querySelector('.drop-card');
        p2Animations.push(card && getComputedStyle(card).animationName === 'drop-flight');
      });
    }); Game.resume();`);
  if (process.env.BROWSER_FAST === "1") await evaluate("Game.pause(); for(let n=0;n<1200;n++) Game.step()");
  let captured = false;
  for (let n = 0; n < 125; n++) {
    const state = await evaluate("({elapsed:Game.getState().stats.elapsedMs,drops:p2Drops.length,anim:!!document.querySelector('.drop-card')})");
    if (state.anim && !captured) { await screenshot("p2-drop-pop"); captured = true; }
    if (state.elapsed >= 120000) break;
    if (n % 30 === 0) console.log("P2 real-time play: " + Math.round(state.elapsed / 1000) + "s, " + state.drops + " drops");
    await delay(1000);
  }
  await evaluate("Game.pause(); p2Off()");
  assert.ok(await evaluate("Game.getState().stats.elapsedMs >= 120000 && p2Drops.length >= 3"));
  assert.ok(await evaluate("p2Animations.some(Boolean)"));
  assert.ok(await evaluate("!document.getElementById('equipment-badge').hidden"));
  console.log("PASS fresh 120s " + (process.env.BROWSER_FAST === "1" ? "replay" : "real timer") + ": " + await evaluate("p2Drops.length") + " drops, pop animation and tab badge");
  await evaluate("document.querySelector('[data-tab=equipment]').click()");
  await delay(250);
  assert.equal(await evaluate("document.querySelectorAll('.inventory-cell').length"), await evaluate("Game.getState().inventory.length"));
  assert.equal(await evaluate("getComputedStyle(document.querySelector('.inventory-grid')).gridTemplateColumns.split(' ').length"), 6);
  assert.ok(await evaluate("document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth"));
  await screenshot("p2-inventory-375");
  await evaluate("window.p2BeforeCP = Game.getCP(); document.querySelector('.inventory-cell').click(); document.querySelector('[data-equip=warrior]').click()");
  await delay(600);
  assert.ok(await evaluate("Game.getCP() > p2BeforeCP && document.getElementById('cp').textContent === UI.fmt(Game.getCP())"));
  await screenshot("p2-equipped-detail");
  await evaluate("document.getElementById('close-sheet').click(); document.querySelector('[data-tab=mercenaries]').click(); document.querySelector('[data-merc-slot=weapon][data-merc=warrior]').click()");
  assert.ok(await evaluate("document.getElementById('sheet-title').textContent === '?λ퉬' && !!document.querySelector('.equip-mode') && document.querySelector('[data-filter=weapon]').getAttribute('aria-pressed') === 'true'"));
  console.log("PASS inventory grid/filter, equip CP animation and mercenary-slot equip mode at 375px");
  // Controlled save fixtures exercise UI actions while all production logic remains unchanged.
  await evaluate(`document.getElementById('close-sheet').click(); Game.reset(); Game.pause();
    const items = []; while(items.length < 12) { const i = Game.rollItem(0,{forcedTier:1}); if(i.slot==='weapon' && i.rarity==='rare') items.push(i); }
    const save = JSON.parse(Game.save()); save.state.inventory = items; save.state.gold = 10000; save.state.squadCoins = 100;
    localStorage.setItem('squad_v1',JSON.stringify(save)); Game.load();
    document.querySelector('[data-tab=fusion]').click(); document.querySelector('[data-fuse-slot=weapon][data-tier="1"]').click();`);
  await delay(80);
  assert.ok(await evaluate("document.getElementById('fusion-reveal').open && !!document.querySelector('.card-back')"));
  await screenshot("p2-fusion-face-down");
  await delay(650);
  assert.ok(await evaluate("Game.getState().inventory.some(i=>i.tier===2) && document.querySelector('#reveal-cards .item-card b').textContent === 'T2'"));
  await screenshot("p2-fusion-face-up");
  await evaluate("document.getElementById('reveal-skip').click();document.getElementById('reveal-skip').click();document.getElementById('auto-fuse').click()");
  await delay(80);
  const first = await evaluate("document.querySelectorAll('.fusion-card').length");
  await delay(280);
  assert.ok(await evaluate("document.querySelectorAll('.fusion-card').length") > first);
  await evaluate("document.getElementById('reveal-skip').click()");
  assert.ok(await evaluate("document.getElementById('fusion-reveal').classList.contains('reveal-skipped') && document.querySelectorAll('.fusion-card').length === 4"));
  await screenshot("p2-auto-fusion-results");
  await evaluate("document.getElementById('reveal-skip').click();document.getElementById('close-sheet').click();document.querySelector('[data-tab=equipment]').click();document.querySelector('.inventory-cell').click();document.querySelector('[data-action=lock]').click();document.querySelector('[data-action=reroll]').click()");
  assert.ok(await evaluate("!!document.querySelector('.reroll-preview') && Game.getState().squadCoins === 80 && document.querySelector('[data-action=sell]').disabled"));
  await delay(650);
  await screenshot("p2-potential-reroll");
  assert.ok(await evaluate("document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth"));
  await evaluate("document.getElementById('close-sheet').click()");
  console.log("PASS single fusion T1?뭈2 flip, auto-fusion stagger/skip, lock and before/after potential reroll");
  await evaluate(`Game.reset(); Game.pause();
    const unique = []; while(unique.length < 3) { const item = Game.rollItem(29,{forcedTier:1}); if(item.slot==='weapon' && item.rarity==='unique') unique.push(item); }
    const upgradedSave = JSON.parse(Game.save()); upgradedSave.state.inventory = unique; upgradedSave.state.gold = 1000; upgradedSave.state.rngSeed = 2000;

apply patch
patch: completed
D:\Git\Inv\squad-idle\js\ui-art.js
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();

diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
import json,re
p=Path('js/data.js');s=p.read_text(encoding='utf-8');mapping=json.loads(Path('assets/manifest.json').read_text())
s=s.replace('var DATA = {','var DATA = {\\n  // DECISION: Inline manifest; static hosting and offline play need no JSON request.\\n  assets: '+json.dumps(mapping,ensure_ascii=False,indent=4)+' ,')
p.write_text(s,encoding='utf-8')
p=Path('index.html');s=p.read_text(encoding='utf-8').replace('#111916','#8FD3FF')
s=re.sub(r'  <link rel=\"icon\"["'^'"\\n]+', '  <link rel=\"icon\" type=\"image/png\" href=\"assets/icon/weapon.png\">\\n  <link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap\">',s)
s=s.replace('전투력 <small>CP</small>','<span data-icon=\"weapon\">⚔</span> 전투력').replace('<i class=\"gold-dot\"></i>','<span data-icon=\"gold\">🪙</span>').replace('<i class=\"coin-gem\">◆</i>','<span data-icon=\"coin\">◆</span>')
start=s.index('          <defs>');end=s.index('          <g id=\"mercenary-layer\">',start)
s=s[:start]+'''          <g id=\"scene-background\"><g class=\"asset-fallback\">
            <defs><linearGradient id=\"sky\" x2=\"0\" y2=\"1\"><stop stop-color=\"#8FD3FF\"/><stop offset=\"1\" stop-color=\"#EAF7FF\"/></linearGradient></defs>
            <rect width=\"390\" height=\"340\" fill=\"url(#sky)\"/><circle cx=\"300\" cy=\"72\" r=\"30\" fill=\"#FFC93C\"/>
            <path d=\"M0 187Q80 150 160 185T390 175V340H0Z\" fill=\"#95E888\"/>
            <path d=\"M140 340l30-145h50l75 145\" fill=\"#FFE9B8\"/>
            <g fill=\"#3DDC97\" stroke=\"#4A2C12\" stroke-width=\"3\"><path d=\"M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z\"/></g>
          </g></g>
'''+s[end:]
s=s.replace('<span>♜</span>장비','<span data-icon=\"hat\">♜</span>장비').replace('<span>✧</span>스킬','<span data-icon=\"skillbook\">✧</span>스킬')
s=s.replace('  <script src=\"js/ui-equipment.js\">','  <script src=\"js/ui-art.js\"></script>\\n  <script src=\"js/ui-equipment.js\">')
p.write_text(s,encoding='utf-8')
p=Path('js/ui.js');s=p.read_text(encoding='utf-8')
s=s.replace('fill: \"#07110d\"','fill: \"#4A2C12\"').replace('fill: \"#07130e\"','fill: \"#FFE9B8\"').replace('\"#c78778\" : \"#accb9b\"','\"#FF6B6B\" : \"#5CE07A\"')
s=s.replace('var body = svgNode(\"g\", { class: \"unit-body\", transform: unit.type === \"boss\" ? \"scale(1.2)\" : \"scale(1)\" });\\n    body.innerHTML = figure(unit.type, color);','''var body = svgNode(\"g\", { class: \"unit-body\" });
    var fallback = svgNode(\"g\", { class: \"asset-fallback\", transform: unit.type === \"boss\" ? \"scale(1.6)\" : \"scale(1.15)\" });
    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
    var region = DATA.stages[Game.getState().currentStage].region;
    var key = unit.side === \"mercenary\" ? \"merc.\" + unit.type + \".idle\" : \"mon.\" + UI.artRegion(region).monsters[unit.type];
    UI.spriteImage(body, key, unit.type === \"boss\" ? 140 : 96);''')
s=s.replace('    if (clearEffects) el(\"effect-layer\").replaceChildren();','    UI.sceneBackground(DATA.stages[state.currentStage].region);\\n    if (clearEffects) el(\"effect-layer\").replaceChildren();')
s=s.replace('    if (attacker) animate(attacker.querySelector(\".unit-body\"), event.side === \"enemy\" ? \"lunge-left\" : \"lunge-right\");','    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(\".unit-body\"), event.side === \"enemy\" ? \"lunge-left\" : \"lunge-right\"); }')
s=s.replace('el(\"gold\").textContent = fmt(state.gold);','UI.countUp(\"gold\", state.gold);').replace('el(\"coins\").textContent = fmt(state.squadCoins);','UI.countUp(\"coins\", state.squadCoins);')
s=s.replace('DATA.regions[stage.region].name','UI.artRegion(stage.region).name').replace('DATA.regions[stage.region].subtitle','UI.artRegion(stage.region).subtitle')
s=s.replace('function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + \" 레벨','function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + \" 레벨')
# Preserve the existing fallback shapes, recolor their dark/desaturated fills.
colors={'#674e38':'#FF9F1C','#abb5b0':'#FFC93C','#b8c2bb':'#FFC93C','#89968a':'#FF9F1C','#335e49':'#22C55E','#b3ad8f':'#FFE9B8','#638e6d':'#3DDC97','#6f619b':'#A855F7','#8f7dbb':'#B388FF','#18251f':'#4A2C12','#334b3d':'#4A2C12','#5b625b':'#FF9F1C','#526357':'#3DDC97','#657461':'#3DDC97','#26392c':'#4A2C12','#716550':'#8A6A4A','#9da69a':'#FFC93C'}
for a,b in colors.items():s=s.replace(a,b)
p.write_text(s,encoding='utf-8')
p=Path('js/ui-equipment.js');s=p.read_text(encoding='utf-8')
s=s.replace('slot(item.slot).icon','UI.icon(item.slot, slot(item.slot).icon)').replace('definition.icon','UI.icon(definition.id, definition.icon)').replace('slot(i.slot).icon','UI.icon(i.slot, slot(i.slot).icon)').replace(\"s.icon + ' ' + s.name\",\"UI.icon(s.id, s.icon) + ' ' + s.name\")
s=s.replace('(now - start) / 500','(now - start) / 300')
s=s.replace('document.querySelector(\"#offline-report[open]\") || document.querySelector(\".battle-scene\")','document.querySelector(\"#offline-report[open]\") || document.querySelector(\"#fusion-reveal[open]\") || document.querySelector(\".battle-scene\")')
s=s.replace('if (event.upgraded) flash(true);','if (event.upgraded || ranks.indexOf(event.item.rarity) >= 2) flash(true);')
p.write_text(s,encoding='utf-8')
p=Path('js/ui-expedition.js');s=p.read_text(encoding='utf-8')
s=s.replace(\"'<div class=\\\"skill-selector\\\">'\",\"'<div class=\\\"skill-selector\\\">'\")
s=s.replace(\"'</div><p class=\\\"skill-books\\\">' + mercName(owner)\",\"'</div><p class=\\\"skill-books\\\">' + UI.icon('skillbook', '▤') + mercName(owner)\")
s=s.replace('<span class=\"item-icon\">▤</span>', '<span class=\"item-icon\">\\' + UI.icon(\"skillbook\", \"▤\") + \\'</span>')
s=s.replace('<span>획득 골드</span>','<span>\\' + UI.icon(\"gold\", \"🪙\") + \\'획득 골드</span>')
s=s.replace('elapsed / 1000','elapsed / 300').replace('(elapsed - 1000)','(elapsed - 300)').replace('elapsed < 1000','elapsed < 300')
s=s.replace('    if ("'!sprite) return;'"','    if ("'!sprite) return;'"\\n    UI.attackSprite(sprite);')
p.write_text(s,encoding='utf-8')
p=Path('test/run.js');s=p.read_text(encoding='utf-8');i=s.index('require(\"./p3\")')
s=s[:i]+'''test(\"Art: DATA.assets matches all 28 manifest keys and files exist\", () => {
  const c = context();
  const manifest = JSON.parse(fs.readFileSync(path.join(__dirname, \"../assets/manifest.json\"), \"utf8\"));
  assert.equal(Object.keys(manifest).length, 28);
  assert.deepEqual(plain(c.DATA.assets), manifest);
  for (const file of Object.values(c.DATA.assets)) assert.ok(fs.statSync(path.join(__dirname, \"..\", file)).isFile(), file);
});
'''+s[i:];p.write_text(s,encoding='utf-8')
'@ | python -" in D:\Git\Inv\squad-idle
 succeeded in 123ms:
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new(); Get-Content -Encoding utf8 index.html | Select-Object -First 24; Get-Content -Encoding utf8 js/ui-expedition.js | Select-String '"'UI.icon|report-gold|skill-books'; Get-Content -Encoding utf8 js/ui.js | Select-String 'levelUp|UI.countUp|UI.artRegion'; Get-Content -Encoding utf8 style.css | Select-Object -Skip 192 -First 48" in D:\Git\Inv\squad-idle
 succeeded in 163ms:
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="theme-color" content="#8FD3FF">
  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
  <link rel="stylesheet" href="style.css">
  <title>용병단 · 첫 번째 원정</title>
</head>
<body>
  <main id="app">
    <header class="hud">
      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
      <div class="resources">
        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
      </div>
    </header>
    <section class="expedition" aria-label="자동 전투">
      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>

      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt
(state.skillBooks[owner]) + '</b></p>' +
    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 300)));
    el("report-gold").textContent = UI.fmt(report.gold);
    el("report-gold").textContent = '0';
    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.t
ype];
    UI.countUp("gold", state.gold);
    UI.countUp("coins", state.squadCoins);
    el("region-name").textContent = UI.artRegion(stage.region).name;
    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name 
+ " 레벨 " + fmt(event.level) + " 달성"); });
.fusion-table { width: 100%; border-collapse: collapse; font-size: 12px; text-align: center; table-layout: fixed; }
.fusion-table th { font-weight: 500; color: #a8b69d; }
.fusion-table td, .fusion-table th { padding: 8px 2px; border-bottom: 1px solid #ffffff0b; }
.fusion-table th:last-child { width: 32%; }
.fusion-table button { width: 100%; }
.fusion-table small { display: block; font-size: 9px; margin-top: 3px; color: #b3be9e; }
.drop-card { position: fixed; pointer-events: none; z-index: 9; animation: drop-flight 1.25s both; }
.drop-card .item-card { width: 50px; height: 60px; box-shadow: 0 0 20px color-mix(in srgb, var(--rarity) 50%, transparent); }
.battle-scene::after { content: ""; position: absolute; inset: 0; pointer-events: none; border: 3px solid transparent; z-index: 4; }
.loot-flash::after { animation: loot-border .65s ease-out; }
.loot-flash-strong::after { animation: loot-border .9s ease-out; border-width: 6px; }
#cp.cp-jump { animation: cp-jump .5s ease-out; transform-origin: left center; }
#fusion-reveal { margin: auto; border-radius: 16px; border: 1px solid #d5bb8266; width: calc(100% - 36px); max-width: 430px; padding: 24px 16px 16px; text-align: center; background: #101b16; }
#fusion-reveal h2 { font-size: 22px; color: #e4bd7e; }
#reveal-progress { margin: 10px 0 18px; font-size: 11px; color: #a1b195; }
#reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
#reveal-cards:has(.fusion-card:only-child) { grid-template-columns: 1fr; }
.fusion-card { min-width: 0; perspective: 600px; }
.flip-inner { position: relative; width: 76px; height: 100px; margin: auto; transform-style: preserve-3d; animation: item-flip .6s ease-out both; }
.card-back, .card-front { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 10px; }
.card-back { display: grid; place-items: center; border: 1px solid #d5bb82; color: #d5bb82; font-size: 40px; background: repeating-linear-gradient(45deg, #283728, #283728 6px, #213021 6px, #213021 12px); transform: rotateY(180deg); }
.card-front .item-card { width: 100%; height: 100%; }
.card-front .item-icon { font-size: 34px; }
.fusion-card p { margin: 8px 0 0; font-size: 10px; color: #b7c5a7; animation: potential-in .6s both; }
.rarity-upgrade .card-front { animation: fusion-flash .8s .3s both; }
.rarity-upgrade p { color: #edd26a; }
#reveal-skip { background: transparent; color: #c9bc96; font-size: 11px; margin-top: 20px; width: 100%; }
.reveal-skipped .flip-inner, .reveal-skipped .fusion-card p, .reveal-skipped .card-front { animation: none; }
@keyframes drop-flight { 0% { transform: scale(.2) rotate(-18deg); opacity: 0; } 22% { transform: translateY(-60px) scale(1.12) rotate(8deg); opacity: 1; } 42% { transform: translateY(-15px) scale(.97); } 55% { transform: translateY(-30px) scale(1); } 72% { transform: translateY(-24px); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(.15); opacity: 0; } }
@keyframes loot-border { 15%, 40% { border-color: #edd26a; box-shadow: inset 0 0 35px #edd26a77, 0 0 20px #edd26a66; } 100% { border-color: transparent; } }
@keyframes cp-jump { 30% { transform: scale(1.15); color: #fff2b8; text-shadow: 0 0 18px #e4bd7eaa; } }
@keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
@keyframes fusion-flash { 35% { box-shadow: 0 0 36px 10px #edd26aaa; filter: brightness(2); } }
@keyframes potential-in { 0%, 20% { opacity: 0; transform: translateY(6px); } 100% { opacity: 1; transform: translateY(0); } }
@media (max-width: 350px) { dialog { padding-inline: 12px; } .inventory-grid { gap: 4px; } .flip-inner { width: 65px; height: 90px; } }
@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: .01s !important; transition-duration: .01s !important; } #toast.visible { animation-duration: 3.4s !important; } }
/* P3: report harvest, skill loadouts and portable saves. */
.difficulty-toggle { grid-column: 1 / -1; display: flex; gap: 8px; }
.difficulty-toggle[hidden] { display: none; }
.difficulty-toggle button, .skill-selector button { flex: 1; border: 1px solid #637355; border-radius: 8px; }
.difficulty-toggle .active, .skill-selector .active { background: #536246; color: #fff3c3; }
.skill-selector, .active-skills, .save-buttons { display: flex; gap: 8px; }
.skill-books { margin: 16px 0 12px; color: #d5bd86; }
.active-skills span { flex: 1; min-width: 0; background: #111c15; padding: 10px 4px; border-radius: 6px; text-align: center; font-size: 11px; }
.skill-card { margin-top: 12px; padding: 14px; border: 1px solid #536148; border-radius: 12px; background: #142018; }
.equipped-skill { border-color: #c5ae72; }
.skill-toggle { display: flex; width: 100%; justify-content: space-between; align-items: center; gap: 8px; text-align: left; background: transparent; }
.skill-toggle strong { font-size: 16px; color: #e4ce9b; }



diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
Path('style.css').write_text('''@charset \"UTF-8\";
:root {
  color-scheme: light; font-family: \"Jua\", \"Gothic A1\", system-ui, sans-serif; font-synthesis: none;
  --bg-top: #8FD3FF; --bg-bottom: #EAF7FF; --panel: #FFF8E7; --panel-2: #FFE9B8;
  --ink: #4A2C12; --ink-soft: #8A6A4A; --primary: #FF9F1C; --primary-end: #FF6B00;
  --accent: #3DDC97; --danger: #FF5C5C; --gold: #FFC93C; --coin: #B388FF;
  --hp-ally: #5CE07A; --hp-enemy: #FF6B6B;
  --r-rare: #3B82F6; --r-epic: #A855F7; --r-unique: #FACC15; --r-legendary: #22C55E;
  color: var(--ink); background: linear-gradient(var(--bg-top), var(--bg-bottom)) fixed;
  font-size: 15px; font-variant-numeric: tabular-nums;
}
* { box-sizing: border-box; }
body { margin: 0; min-width: 280px; -webkit-tap-highlight-color: transparent; }
[hidden] { display: none "'!important; }
h1, h2, h3, h4, p { margin: 0; }
h1, h2 { font-size: 22px; } h3 { font-size: 18px; } small { font-size: 12px; }
button, select, textarea { font: inherit; color: inherit; }
button { cursor: pointer; touch-action: manipulation; min-width: 44px; min-height: 44px; padding: 8px;
  border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink);
  background: linear-gradient(var(--primary), var(--primary-end)); color: white; font-weight: 700;
  text-shadow: 0 2px 0 rgba(0,0,0,.25); transition: transform .12s, box-shadow .12s; }
button:active:not(:disabled) { transform: translateY(3px); box-shadow: none; }
button:disabled { cursor: default; opacity: .55; }
button:focus-visible, select:focus-visible, textarea:focus-visible { outline: 3px solid var(--r-rare); outline-offset: 3px; }
button, .battle-scene { -webkit-user-select: none; user-select: none; }
#app { max-width: 480px; margin: auto; min-height: 100vh; min-height: 100dvh; display: flex; flex-direction: column;
  position: relative; background: linear-gradient(var(--bg-top), var(--bg-bottom)); overflow: clip; }
.hud { margin: 10px 10px 0; padding: calc(10px + env(safe-area-inset-top)) 10px 12px; background: var(--panel);
  border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink); }
.brand-row { display: flex; align-items: center; gap: 8px; margin-bottom: 10px; }
.brand-mark { color: var(--primary-end); } .chapter-label { font-size: 12px; color: var(--ink-soft); }
.save-state { margin-left: auto; font-size: 12px; white-space: nowrap; color: var(--ink); }
.save-state::before { content: '"''; display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
.resources { display: grid; grid-template-columns: 1.15fr 1fr 1fr; gap: 6px; }
.resource { min-width: 0; padding: 6px; border: 3px solid var(--ink); border-radius: 18px; background: var(--panel-2); }
.resource-label { display: flex; align-items: center; gap: 3px; font-size: 12px; white-space: nowrap; }
.resource strong { display: block; font-size: 23px; line-height: 1.1; margin-top: 2px; overflow-wrap: anywhere; }
.power strong { text-shadow: 0 0 8px var(--gold), 0 2px var(--gold); font-size: 26px; }
.asset-icon { display: inline-grid; place-items: center; position: relative; width: 30px; height: 30px; vertical-align: middle; flex: 0 0 auto; }
.asset-icon img, .asset-icon .asset-fallback { grid-area: 1 / 1; width: 100%; height: 100%; object-fit: contain; }
.asset-icon .asset-fallback { display: grid; place-items: center; }
.resource .asset-icon { width: 24px; height: 24px; }
.expedition { display: flex; flex: 1; flex-direction: column; min-height: 0; }
.stage-header { display: flex; justify-content: space-between; align-items: center; gap: 6px; padding: 16px 16px 9px; }
.stage-header h2 { font-size: 22px; } .stage-header > div:first-child { min-width: 0; }
.eyebrow { font-size: 13px; color: var(--ink-soft); margin-bottom: 4px; }
#stage-id { color: var(--ink); } .title-divider { margin: 0 5px; color: var(--ink-soft); }
.clock { text-align: right; flex-shrink: 0; } .clock > span { display: block; font-size: 12px; }
.clock strong { font-size: 26px; } .clock small { padding-left: 3px; }
.urgent { color: #B52B20; }
.time-track { height: 8px; background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; margin: 0 12px 9px; overflow: hidden; }
#time-fill { height: 100%; background: var(--gold); transition: width .1s linear; }
.battle-scene { position: relative; flex: 1; display: flex; flex-direction: column; justify-content: center; min-height: 290px;
  margin: 0 10px; border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink); background: var(--bg-top); isolation: isolate; overflow: hidden; }
.scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
.scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
.auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
#battle-svg { width: 100%; display: block; max-height: 430px; }
.scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
#region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
.locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
.hp-fill { transition: width .1s linear; } .fallen { visibility: hidden; }
.unit-body { transform-box: fill-box; transform-origin: center bottom; animation: breathe 2s ease-in-out infinite; }
.lunge-right { animation: lunge-right .12s ease-out; } .lunge-left { animation: lunge-left .12s ease-out; }
.hit-flash { animation: hit-flash .18s ease-out; }
.death-fade { animation: death-fade .45s ease-in forwards; pointer-events: none; } .death-fade .unit-body { animation: none; }
.damage-number, .skill-label, .level-up-label { fill: white; stroke: var(--ink); stroke-width: 3px; paint-order: stroke; font-size: 18px; font-weight: 800; animation: float-damage .8s ease-out forwards; pointer-events: none; }
.damage-number.critical { fill: var(--gold); font-size: 23px; } .damage-number.incoming { fill: var(--hp-enemy); font-size: 16px; }
.skill-label { fill: var(--panel); font-size: 13px; animation: skill-rise .8s ease-out both; }
.level-up-label { fill: var(--gold); font-size: 19px; transform-box: fill-box; transform-origin: center; animation: level-pop .85s ease-out both; }
.battle-result { display: none; position: absolute; z-index: 3; top: 28%; left: 10%; right: 10%; padding: 16px 10px; text-align: center;
  background: var(--panel); border: 3px solid var(--ink); box-shadow: 0 4px 0 var(--ink); border-radius: 18px; }
.battle-result.shown { display: grid; gap: 5px; animation: result-in .3s ease-out; }
.battle-result > span { color: var(--primary-end); font-size: 22px; } .battle-result strong { font-size: 22px; }
.battle-result.failed strong { color: #B52B20; }
.squad-strip { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 5px; padding: 12px 10px 4px; }
.squad-member { display: grid; grid-template-columns: 5px 1fr; column-gap: 5px; padding: 6px; font-size: 13px;
  border: 3px solid var(--ink); border-radius: 18px; background: var(--panel); }
.squad-member small { grid-column: 2; font-size: 12px; white-space: nowrap; } .member-dot { width: 5px; height: 14px; border-radius: 3px; }
.not-recruited { opacity: .7; }
.harvest-row { display: flex; justify-content: space-between; gap: 6px; padding: 6px 13px 8px; font-size: 12px; }
.harvest-row > span:first-child { display: none; } .harvest-row > span:last-child { margin: auto; }
.rate-divider { margin: 0 4px; }
.stage-controls { display: grid; grid-template-columns: 1fr 1.1fr; gap: 8px; padding: 9px 10px 12px; }
.stage-navigation { display: flex; align-items: center; justify-content: space-between; gap: 3px; }
.stage-navigation button { font-size: 24px; padding: 0; }
.stage-navigation div { text-align: center; } .stage-navigation div span { display: block; font-size: 12px; }
.stage-navigation strong { font-size: 17px; }
.mode-toggle { display: flex; gap: 5px; } .mode-toggle button { flex: 1; padding: 5px 2px; font-size: 13px; white-space: nowrap; }
#repeat-mode.active { background: var(--accent); } #challenge-mode.active { outline: 3px solid var(--gold); outline-offset: 1px; }
#mode-hint { grid-column: 1/-1; text-align: center; font-size: 12px; margin-top: 2px; }
.bottom-tabs { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 5px; padding: 10px 10px calc(10px + env(safe-area-inset-bottom)); background: var(--panel); border-top: 3px solid var(--ink); }
.bottom-tabs button { position: relative; background: var(--panel-2); color: var(--ink); text-shadow: none; display: flex; flex-direction: column; align-items: center; gap: 2px; font-size: 13px; padding: 3px 0; min-height: 64px; }
.bottom-tabs button > span { font-size: 25px; height: 30px; line-height: 30px; }
.bottom-tabs button.selected { transform: translateY(-6px); background: linear-gradient(var(--primary), var(--primary-end)); color: white; text-shadow: 0 2px 0 #0004; }
#equipment-badge { position: absolute; right: -4px; top: -9px; display: grid; place-items: center; border: 3px solid var(--ink); border-radius: 50%; min-width: 25px; height: 25px; padding: 0 3px; font-size: 12px; background: var(--danger); color: white; text-shadow: none; }
#toast { position: fixed; z-index: 20; width: max-content; max-width: min(90vw, 430px); left: 50%; bottom: 110px; transform: translateX(-50%); pointer-events: none; opacity: 0;
  background: var(--panel-2); color: var(--ink); padding: 12px 16px; border: 3px solid var(--ink); border-radius: 18px; font-size: 13px; box-shadow: 0 4px 0 var(--ink); }
#toast.visible { animation: toast 3.4s ease both; }
dialog { color: var(--ink); background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; margin: auto auto 6px; padding: 0 12px calc(20px + env(safe-area-inset-bottom));
  width: calc(100% - 12px); max-width: 468px; max-height: 78vh; max-height: 78dvh; overflow-y: auto; overscroll-behavior: contain; box-shadow: 0 4px 0 var(--ink); }
dialog::backdrop { background: #8FD3FF80; backdrop-filter: blur(3px); } dialog[open] { animation: sheet-in .22s ease-out; }
.sheet-grip { height: 5px; width: 38px; background: var(--ink-soft); border-radius: 18px; margin: 9px auto 12px; }
.sheet-heading { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin-bottom: 16px; }
.sheet-heading h2, #fusion-reveal h2, .report-heading h2 { display: inline-block; background: linear-gradient(var(--primary), var(--primary-end)); color: white;
  text-shadow: 0 2px #0004; padding: 7px 20px; border: 3px solid var(--ink); border-radius: 5px 18px 18px 5px; box-shadow: 0 4px 0 var(--ink); font-size: 22px; }
.sheet-heading button { font-size: 24px; padding: 0; } .sheet-intro { font-size: 13px; line-height: 1.6; margin-bottom: 16px; }
.merc-card, .skill-card, .reset-panel { background: var(--panel-2); border: 3px solid var(--ink); border-radius: 18px; padding: 12px; margin-bottom: 15px; box-shadow: 0 4px 0 var(--ink); }
.merc-card-heading { display: flex; gap: 10px; align-items: center; margin-bottom: 12px; }
.merc-card-heading svg { width: 64px; height: 70px; background: var(--panel); border-radius: 18px; }
.merc-card-heading p { font-size: 12px; } .merc-card-heading > strong { margin-left: auto; font-size: 17px; }
.xp-label { display: flex; justify-content: space-between; font-size: 12px; margin-bottom: 5px; }
.xp-track { height: 8px; background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; overflow: hidden; }
.xp-track > div { height: 100%; background: var(--accent); }
.merc-stats { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 10px 5px; margin: 14px 0; }
.merc-stats dt { font-size: 12px; margin-bottom: 3px; } .merc-stats dd { margin: 0; font-size: 15px; }
.equipment-slots { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 5px; }
.equipment-slot { min-width: 44px; min-height: 92px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px; padding: 4px 1px; background: var(--panel); color: var(--ink); text-shadow: none; }
.equipment-slot em { font-size: 12px; font-style: normal; overflow-wrap: anywhere; }
.locked-card { opacity: .65; } .unlock-note { font-size: 13px; padding: 7px 0; }
.placeholder, .equipment-empty { text-align: center; padding: 30px 5px; line-height: 1.8; } .placeholder > span { font-size: 40px; }
.settings-info { font-size: 13px; } .settings-info > div { display: flex; justify-content: space-between; gap: 8px; padding: 12px 0; border-bottom: 3px solid var(--panel-2); }
.settings-info dd { margin: 0; } .reset-panel { margin-top: 22px; } .reset-panel p { font-size: 13px; line-height: 1.7; margin: 8px 0 12px; }
.danger-button, button[data-action=\"sell\"] { background: var(--danger); }
.rarity-rare { --rarity: var(--r-rare); } .rarity-epic { --rarity: var(--r-epic); } .rarity-unique { --rarity: var(--r-unique); } .rarity-legendary { --rarity: var(--r-legendary); }
.equipment-slot[class*=\"rarity-\"], .inventory-cell, .item-detail, .item-card { border-color: var(--rarity); background: color-mix(in srgb, var(--rarity) 12%, var(--panel)); }
.inventory-heading, .inventory-sort, .fusion-heading, .equip-mode { display: flex; align-items: center; justify-content: space-between; gap: 8px; margin-bottom: 14px; }
.inventory-heading p, .fusion-heading p { font-size: 15px; } .inventory-heading strong { font-size: 17px; }
.inventory-filters { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 5px; margin-bottom: 14px; }
.inventory-filters button, .equip-actions button, .fusion-table button { font-size: 13px; padding: 5px 2px; }
.inventory-filters button.active, .skill-selector .active, .difficulty-toggle .active { background: var(--accent); }
.inventory-sort { font-size: 13px; } select { min-height: 44px; background: var(--panel-2); border: 3px solid var(--ink); border-radius: 18px; padding: 0 8px; }
/* DECISION: Compact sheet padding keeps all six existing inventory columns at least 44px at 375px. */
.inventory-grid { display: grid; grid-template-columns: repeat(6, minmax(44px, 1fr)); gap: 5px; }
.inventory-cell { position: relative; min-width: 44px; min-height: 80px; padding: 4px 0 16px; display: flex; flex-direction: column; align-items: center; justify-content: center; color: var(--ink); text-shadow: none; }
.inventory-cell b { font-size: 13px; } .inventory-cell em { position: absolute; right: 0; top: 0; font-size: 12px; font-style: normal; }
.inventory-cell i { position: absolute; bottom: 1px; font-size: 12px; font-style: normal; }
.selected-item { outline: 3px solid var(--ink); outline-offset: 1px; }
.equipment-note { font-size: 13px; line-height: 1.7; margin: 14px 0; } .equip-mode { font-size: 13px; padding: 8px; background: var(--panel-2); border-radius: 18px; }
.item-detail { border: 3px solid var(--rarity); border-radius: 18px; padding: 10px; margin-bottom: 16px; box-shadow: 0 4px 0 var(--ink); }
.detail-heading { display: flex; gap: 8px; align-items: center; } .detail-heading > div { flex: 1; min-width: 0; }
.detail-heading h3 { font-size: 17px; overflow-wrap: anywhere; } .detail-heading p { font-size: 12px; margin-top: 5px; }
.item-card { width: 62px; height: 88px; flex: 0 0 auto; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px;
  border: 3px solid var(--rarity); border-radius: 18px; color: var(--ink); box-shadow: 0 4px 0 var(--ink); }
.item-card .asset-icon { width: 36px; height: 36px; } .item-card b { font-size: 15px; } .item-card small { font-size: 12px; }
.base-stats { display: flex; flex-wrap: wrap; gap: 15px; margin: 16px 0; } .base-stats dt { font-size: 12px; margin-bottom: 3px; } .base-stats dd { margin: 0; }
.item-detail h4 { font-size: 15px; margin: 14px 0 8px; } .potential-lines { list-style: none; padding: 0; margin: 0; display: grid; gap: 8px; font-size: 13px; }
.potential-lines li { display: flex; justify-content: space-between; gap: 8px; } .potential-lines b { white-space: nowrap; }
.equip-actions { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 5px; } .detail-actions { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 12px; }
.detail-actions button { flex: 1 1 42%; font-size: 13px; }
.reroll-preview { margin-top: 15px; padding: 0 8px 12px; border: 3px solid var(--r-epic); border-radius: 18px; background: var(--panel); }
.reroll-after { animation: potential-in .4s ease-out both; }
.fusion-heading small { display: block; margin-top: 6px; } .fusion-table { width: 100%; border-collapse: collapse; font-size: 13px; text-align: center; table-layout: fixed; }
.fusion-table td, .fusion-table th { padding: 8px 2px; border-bottom: 3px solid var(--panel-2); } .fusion-table th:last-child { width: 32%; }
.fusion-table button { width: 100%; } .fusion-table small { display: block; font-size: 12px; margin-top: 2px; } .fusion-table .asset-icon { width: 25px; height: 25px; }
.drop-card { position: fixed; pointer-events: none; z-index: 9; animation: drop-flight 1.25s both; }
.drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
.battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
#fusion-reveal::after, #offline-report::after { position: fixed; }
.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
#cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
#fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
#reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
#reveal-cards:has(.fusion-card:only-child) { grid-template-columns: 1fr; } .fusion-card { min-width: 0; perspective: 600px; }
.flip-inner { position: relative; width: 82px; height: 110px; margin: auto; transform-style: preserve-3d; animation: item-flip .5s ease-out both; }
.card-back, .card-front { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 18px; }
.card-back { display: grid; place-items: center; border: 3px solid var(--ink); color: white; font-size: 40px;
  background: repeating-linear-gradient(45deg, var(--primary), var(--primary) 8px, var(--gold) 8px, var(--gold) 16px); transform: rotateY(180deg); box-shadow: 0 4px 0 var(--ink); }
.card-front .item-card { width: 100%; height: 100%; } .card-front .asset-icon { width: 44px; height: 44px; }
.fusion-card p { margin-top: 8px; font-size: 13px; animation: potential-in .5s both; }
.rarity-upgrade .card-front { animation: fusion-flash .4s .2s both; } #reveal-skip { margin-top: 20px; width: 100%; }
.reveal-skipped .flip-inner, .reveal-skipped .fusion-card p, .reveal-skipped .card-front { animation: none; }
.difficulty-toggle { grid-column: 1/-1; display: flex; gap: 8px; } .difficulty-toggle button, .skill-selector button { flex: 1; }
.skill-selector, .active-skills, .save-buttons { display: flex; gap: 6px; } .skill-books { margin: 16px 0 12px; }
.active-skills span { flex: 1; min-width: 0; background: var(--panel-2); padding: 8px 3px; border: 3px solid var(--ink); border-radius: 18px; text-align: center; font-size: 12px; }
.skill-card { margin-top: 14px; } .equipped-skill { border-color: var(--accent); }
.skill-toggle { display: flex; width: 100%; justify-content: space-between; align-items: center; gap: 6px; text-align: left; }
.skill-toggle strong { font-size: 17px; } .skill-toggle b { font-size: 12px; white-space: nowrap; }
.skill-card p { font-size: 15px; line-height: 1.6; margin: 10px 0; } .skill-level { display: block; width: 100%; margin-top: 12px; }
.save-controls { margin-top: 20px; } .save-controls p { font-size: 13px; line-height: 1.7; margin: 8px 0; } .save-buttons button { flex: 1; font-size: 13px; }
#save-editor label { display: block; margin: 16px 0 8px; } #save-json { display: block; width: 100%; min-width: 0; max-width: 100%; padding: 10px;
  border: 3px solid var(--ink); border-radius: 18px; background: white; font: 13px monospace; resize: vertical; overflow-wrap: anywhere; }
#save-editor > button { margin-top: 10px; width: 100%; }
#offline-report { margin: auto; width: calc(100% - 8px); max-width: 480px; height: calc(100dvh - 8px); max-height: calc(100dvh - 8px); padding: 24px 12px 0; text-align: center; background: linear-gradient(var(--panel), var(--panel-2)); }
.report-heading h2 { margin: 10px 0 16px; } #report-duration { font-size: 15px; line-height: 1.7; }
.report-gold { padding: 22px 0 14px; } .report-gold > span { display: block; } .report-gold strong { display: block; margin-top: 10px; font-size: 44px; text-shadow: 0 0 8px var(--gold); }
#report-stats, #report-progress { font-size: 13px; margin-bottom: 16px; } #report-items { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; padding: 8px 0 24px; }
.book-card { --rarity: var(--coin); } .harvest-footer { position: sticky; bottom: 0; background: var(--panel); padding: 12px 0 max(20px, env(safe-area-inset-bottom)); z-index: 2; }
.harvest-footer p { font-size: 12px; margin-bottom: 10px; } #harvest-report { width: 100%; min-height: 58px; font-size: 18px; }
@keyframes breathe { 50% { translate: 0 -3px; } }
@keyframes lunge-right { 50% { translate: 10px 0; rotate: 4deg; } }
@keyframes lunge-left { 50% { translate: -8px 0; rotate: -4deg; } }
@keyframes hit-flash { 20%, 60% { filter: brightness(0) invert(1); } }
@keyframes death-fade { to { opacity: 0; translate: 0 10px; } }
@keyframes float-damage { 0% { opacity: 0; translate: 0 5px; } 15% { opacity: 1; } 100% { opacity: 0; translate: 0 -34px; } }
@keyframes skill-rise { to { opacity: 0; translate: 0 -25px; } }
@keyframes level-pop { 0% { scale: 0; } 25% { scale: 1.2; } 45% { scale: 1; } 100% { opacity: 0; translate: 0 -28px; } }
@keyframes result-in { from { opacity: 0; translate: 0 8px; } }
@keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
@keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
@keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
@keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
@keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
@keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
@keyframes potential-in { from { opacity: 0; translate: 0 6px; } to { opacity: 1; translate: 0 0; } }
@media (max-width: 350px) { .chapter-label { display: none; } .resource { padding: 4px; } .stage-header h2 { font-size: 19px; } .inventory-grid { grid-template-columns: repeat(5, minmax(44px, 1fr)); } .flip-inner { width: 72px; } .stage-controls { grid-template-columns: 1fr 1fr; } }
@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: .01s "'!important; transition-duration: .01s !important; } .unit-body { animation: none; } #toast.visible { animation-duration: 3.4s !important; } }
'"''',encoding='utf-8')
'@ | python -" in D:\Git\Inv\squad-idle
 succeeded in 130ms:
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();

codex
하늘색 배경·크림 패널·주황 버튼과 지역별 이미지 연결을 적용했습니다. 인벤토리는 시트 여백을 줄이니 375px에서도 기존 6열과 44px 터치 영역을 함께 유지할 수 있어 그대로 보존했습니다. 이제 브라우저에서 이미지 로딩, 공격 전환, 시트 넘침을 확인하고 있습니다.

apply patch
patch: completed
D:\Git\Inv\squad-idle\index.html
D:\Git\Inv\squad-idle\js\ui-expedition.js
D:\Git\Inv\squad-idle\js\ui.js
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem js/*.js | ForEach-Object { node --check $_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; node test/run.js; git diff --check' in D:\Git\Inv\squad-idle
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:BROWSER_NO_SANDBOX='"'1'; "'$env:BROWSER_FAST='"'1'; node test/browser.js" in D:\Git\Inv\squad-idle
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

 exited 1 in 12414ms:
Browser: Chrome/152.0.7977.76
PASS mobile 390×844 layout, tap sizes, initial auto-fight
PASS mercenary cards, equipment slots, placeholders, settings and reset confirmation
PASS real 100ms timer clears 1-1 and advances to 1-2 without input
PASS repeat, arrows, 320px no overflow and desktop 480px maximum width
PASS fresh 120s replay: 6 drops, pop animation and tab badge
PASS inventory grid/filter, equip CP animation and mercenary-slot equip mode at 375px
PASS single fusion T1→T2 flip, auto-fusion stagger/skip, lock and before/after potential reroll
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-wjP1Ww
[10232:23248:0907/200401.552:ERROR:chrome\installer\util\google_update_settings_win.cc:270] Failed opening key Software\Google\Update\ClientStateMedium\{8A69D345-D564-463c-AFF1-A69D9E530F96} to set usagestats; result: 5

DevTools listening on ws://127.0.0.1:6148/devtools/browser/647b3397-c498-4fd3-92ff-e465cdb9631f
[10232:39636:0907/200403.722:ERROR:services\device\public\cpp\geolocation\system_geolocation_source_win.cc:259] Failed to get location access status: 매개 변수가 틀립니다. (0x80070057)

AssertionError [ERR_ASSERTION]: The expression evaluated to a falsy value:

  assert.ok(await evaluate("!!document.querySelector('.rarity-upgrade .rarity-legendary') && document.querySelector('.battle-scene').classList.contains('loot-flash-strong')"))

    at module.exports (D:\Git\Inv\squad-idle\test\browser-equipment.js:74:10)
    at async main (D:\Git\Inv\squad-idle\test\browser.js:151:5) {
  generatedMessage: true,
  code: 'ERR_ASSERTION',
  actual: false,
  expected: true,
  operator: '==',
  diff: 'simple'
}

 exited 1 in 36413ms:
     Stage 1-1: 18.9s
PASS (a) fresh squad clears stage 1-1 within 60 seconds
PASS (b) stage 3-10 defeats a fresh level-1 squad, even with all classes
PASS (c) save/load preserves the entire state and ongoing battle
PASS (d) equal RNG seeds produce equal sequences (including seed zero)
PASS balance: unupgraded level-1 warrior clears 1-1 through 1-5 and fails 1-6
PASS P1 baseline without active skills first fails at 1-6, then gains XP on retry
PASS repeat keeps stage; first-clear coins cannot be farmed; challenge advances
PASS archer unlocks only on 1-6 clear; mage only on 2-6 clear
PASS wave, hit, kill, death, clear and fail events; boss is the last wave
PASS frontline priority survives reversed squad order and switches after death
PASS defense, forced critical damage, dead units and exact 60s timeout
PASS failure restores squad HP and retries the same stage without currency penalty
PASS catchUp below 60 seconds preserves deterministic fast-forward
PASS XP raises level and CP; the rolling five-minute window expires kills
PASS locked stages and invalid modes are rejected; corrupted saves preserve live state
PASS runtime installs only 100ms combat and 3s autosave timers; pause clears both
PASS all 30 stages, equipment data and P3 default skills are defined
PASS P2: 2,000 rolls per stage band respect tier bounds, boss tiers, rarity lines and ranges
     Fresh 120s drops: 3
PASS P2: every real boss kill drops its guaranteed tier, and fresh play drops in two minutes
PASS P2: fusion validates three distinct matching unlocked unequipped materials and gold
     Fusion bump rate: 0.1554
PASS P2: rarity bumps at 0.15 ± 0.02 over 5,000 actual fusions; highest rarity is retained
PASS P2: autoFuse cascades lowest tier first, respects protection, gold and tier 8
PASS P2: equip raises CP immediately, preserves HP ratio and unequip restores CP
PASS P2: sell is atomic, lock protects materials, reroll charges 20 and works while locked/equipped
PASS P2: schema v1 migration and populated v2 round-trip; bad equipment saves rejected
PASS P2: inventory cap sells lowest eligible rare; fully protected bag monetizes incoming drop
PASS P2: all combat potentials apply, gold/drop bonuses work and invincibility is enabled
PASS P2: transferring gear clears old owner, and changing gear never revives a downed ally
PASS Art: DATA.assets matches all 28 manifest keys and files exist
PASS P3: report math, 24h cap, no advance before harvest, exact-once persistence
PASS P3: fallback rates, 60s boundary, stage-specific statistics and boss XP
PASS P3: ready skills respect priority and independent cooldowns; book costs and slots
PASS P3: all twelve effects, DoT/HoT duration, aggro, shields and invincibility
PASS P3: revive restores 30% HP exactly once per battle, including across save/load
PASS P3: chaos unlock, separate progress, HP/ATK/gold multipliers and triple first-clear coins
PASS P3: v2→v3 and v1→v3 migrations; JSON import/export is atomic and round-trips
PASS P3: 3% class books persist on kill; corrupt skill/effect/report imports stay atomic
     Seed 271828: 5.50h, stage 30, levels 25,25,24, 248 fusions, 6 sessions
     Seed 7919: 5.10h, stage 30, levels 24,24,24, 209 fusions, 6 sessions
     Seed 42: 5.72h, stage 30, levels 25,25,25, 234 fusions, 6 sessions
PASS P3: full normal run clears 3-10 in six simulated hours with equipment, fusion and skills

38 tests passed.
index.html:1: trailing whitespace.
+<!doctype html>
index.html:2: trailing whitespace.
+<html lang="ko">
index.html:3: trailing whitespace.
+<head>
index.html:4: trailing whitespace.
+  <meta charset="utf-8">
index.html:5: trailing whitespace.
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
index.html:6: trailing whitespace.
+  <meta name="theme-color" content="#8FD3FF">
index.html:7: trailing whitespace.
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
index.html:8: trailing whitespace.
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
index.html:9: trailing whitespace.
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
index.html:10: trailing whitespace.
+  <link rel="stylesheet" href="style.css">
index.html:11: trailing whitespace.
+  <title>용병단 · 첫 번째 원정</title>
index.html:12: trailing whitespace.
+</head>
index.html:13: trailing whitespace.
+<body>
index.html:14: trailing whitespace.
+  <main id="app">
index.html:15: trailing whitespace.
+    <header class="hud">
index.html:16: trailing whitespace.
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
index.html:17: trailing whitespace.
+      <div class="resources">
index.html:21: trailing whitespace.
+      </div>
index.html:22: trailing whitespace.
+    </header>
index.html:23: trailing whitespace.
+    <section class="expedition" aria-label="자동 전투">
index.html:24: trailing whitespace.
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
index.html:25: trailing whitespace.
+      <div class="time-track"><div id="time-fill"></div></div>
index.html:26: trailing whitespace.
+      <div class="battle-scene">
index.html:27: trailing whitespace.
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
index.html:28: trailing whitespace.
+        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
index.html:29: trailing whitespace.
+          <g id="scene-background"><g class="asset-fallback">
index.html:30: trailing whitespace.
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
index.html:31: trailing whitespace.
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
index.html:32: trailing whitespace.
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
index.html:33: trailing whitespace.
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
index.html:34: trailing whitespace.
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
index.html:35: trailing whitespace.
+          </g></g>
index.html:36: trailing whitespace.
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
index.html:37: trailing whitespace.
+        </svg>
index.html:38: trailing whitespace.
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
index.html:39: trailing whitespace.
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
index.html:40: trailing whitespace.
+      </div>
index.html:41: trailing whitespace.
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
index.html:42: trailing whitespace.
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
index.html:43: trailing whitespace.
+    </section>
index.html:44: trailing whitespace.
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
index.html:45: trailing whitespace.
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
index.html:46: trailing whitespace.
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
index.html:47: trailing whitespace.
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
index.html:48: trailing whitespace.
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
index.html:49: trailing whitespace.
+    </section>
index.html:50: trailing whitespace.
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
index.html:51: trailing whitespace.
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
index.html:53: trailing whitespace.
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
index.html:55: trailing whitespace.
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
index.html:56: trailing whitespace.
+    </nav>
index.html:57: trailing whitespace.
+    <div id="toast" role="status"></div>
index.html:58: trailing whitespace.
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
index.html:59: trailing whitespace.
+  </main>
index.html:60: trailing whitespace.
+  <script src="js/data.js"></script>
index.html:61: trailing whitespace.
+  <script src="js/battle.js"></script>
index.html:62: trailing whitespace.
+  <script src="js/game.js"></script>
index.html:63: trailing whitespace.
+  <script src="js/game-equipment.js"></script>
index.html:64: trailing whitespace.
+  <script src="js/game-expedition.js"></script>
index.html:65: trailing whitespace.
+  <script src="js/ui.js"></script>
index.html:66: trailing whitespace.
+  <script src="js/ui-art.js"></script>
index.html:67: trailing whitespace.
+  <script src="js/ui-equipment.js"></script>
index.html:68: trailing whitespace.
+  <script src="js/ui-expedition.js"></script>
index.html:69: trailing whitespace.
+</body>
index.html:70: trailing whitespace.
+</html>
js/data.js:1: trailing whitespace.
+"use strict";
js/data.js:2: trailing whitespace.
+
js/data.js:3: trailing whitespace.
+var DATA = {
js/data.js:4: trailing whitespace.
+  // DECISION: Inline manifest; static hosting and offline play need no JSON request.
js/data.js:5: trailing whitespace.
+  assets: {
js/data.js:6: trailing whitespace.
+    "merc.warrior.idle": "assets/merc/warrior_idle.png",
js/data.js:7: trailing whitespace.
+    "merc.archer.idle": "assets/merc/archer_idle.png",
js/data.js:8: trailing whitespace.
+    "merc.mage.idle": "assets/merc/mage_idle.png",
js/data.js:9: trailing whitespace.
+    "mon.slime": "assets/mon/slime.png",
js/data.js:10: trailing whitespace.
+    "mon.goblin": "assets/mon/goblin.png",
js/data.js:11: trailing whitespace.
+    "mon.mushroom": "assets/mon/mushroom.png",
js/data.js:12: trailing whitespace.
+    "mon.boss_treant": "assets/mon/boss_treant.png",
js/data.js:13: trailing whitespace.
+    "bg.forest": "assets/bg/forest.png",
js/data.js:14: trailing whitespace.
+    "icon.weapon": "assets/icon/weapon.png",
js/data.js:15: trailing whitespace.
+    "icon.hat": "assets/icon/hat.png",
js/data.js:16: trailing whitespace.
+    "icon.gloves": "assets/icon/gloves.png",
js/data.js:17: trailing whitespace.
+    "icon.shoes": "assets/icon/shoes.png",
js/data.js:18: trailing whitespace.
+    "icon.skillbook": "assets/icon/skillbook.png",
js/data.js:19: trailing whitespace.
+    "icon.gold": "assets/icon/gold.png",
js/data.js:20: trailing whitespace.
+    "icon.coin": "assets/icon/coin.png",
js/data.js:21: trailing whitespace.
+    "merc.warrior.attack": "assets/merc/warrior_attack.png",
js/data.js:22: trailing whitespace.
+    "merc.archer.attack": "assets/merc/archer_attack.png",
js/data.js:23: trailing whitespace.
+    "merc.mage.attack": "assets/merc/mage_attack.png",
js/data.js:24: trailing whitespace.
+    "mon.ice_bat": "assets/mon/ice_bat.png",
js/data.js:25: trailing whitespace.
+    "mon.yeti": "assets/mon/yeti.png",
js/data.js:26: trailing whitespace.
+    "mon.frost_wolf": "assets/mon/frost_wolf.png",
js/data.js:27: trailing whitespace.
+    "mon.boss_ice_golem": "assets/mon/boss_ice_golem.png",
js/data.js:28: trailing whitespace.
+    "bg.peak": "assets/bg/peak.png",
js/data.js:29: trailing whitespace.
+    "mon.lava_slime": "assets/mon/lava_slime.png",
js/data.js:30: trailing whitespace.
+    "mon.imp": "assets/mon/imp.png",
js/data.js:31: trailing whitespace.
+    "mon.salamander": "assets/mon/salamander.png",
js/data.js:32: trailing whitespace.
+    "mon.boss_dragon": "assets/mon/boss_dragon.png",
js/data.js:33: trailing whitespace.
+    "bg.volcano": "assets/bg/volcano.png"
js/data.js:34: trailing whitespace.
+} ,
js/data.js:35: trailing whitespace.
+  version: "0.3.0 · P3",
js/data.js:36: trailing whitespace.
+  schemaVersion: 3,
js/data.js:37: trailing whitespace.
+  offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
js/data.js:38: trailing whitespace.
+  chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
js/data.js:39: trailing whitespace.
+  skillBookChance: 0.03,
js/data.js:40: trailing whitespace.
+  invincibleChance: 0.02,
js/data.js:41: trailing whitespace.
+  invincibleDuration: 1,
js/data.js:42: trailing whitespace.
+  skillBookCost: function (level) { return level; },
js/data.js:43: trailing whitespace.
+  defaultSkills: { warrior: ["taunt", "guard", "smash"], archer: ["volley", "snipe", "poison"], mage: ["heal", "meteor", "resurrect"] },
js/data.js:44: trailing whitespace.
+  // DECISION: Slot order is priority. Power/duration scales 50% per level; revival scales its cooldown instead.
js/data.js:45: trailing whitespace.
+  skills: [
js/data.js:46: trailing whitespace.
+    { id: "taunt", owner: "warrior", name: "도발", desc: "4초간 적의 공격을 자신에게 집중시킵니다.", cooldown: 12, effect: { type: "taunt", duration: 4 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:47: trailing whitespace.
+    { id: "guard", owner: "warrior", name: "방어태세", desc: "5초간 방어력이 100% 증가합니다.", cooldown: 12, effect: { type: "guard", duration: 5, power: 1 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:48: trailing whitespace.
+    { id: "smash", owner: "warrior", name: "강타", desc: "공격력 300%의 일격을 가합니다.", cooldown: 6, effect: { type: "hit", power: 3 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:49: trailing whitespace.
+    { id: "regen", owner: "warrior", name: "재생", desc: "5초간 매초 최대 HP의 4%를 회복합니다.", cooldown: 14, effect: { type: "regen", duration: 5, power: 0.04 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:50: trailing whitespace.
+    { id: "volley", owner: "archer", name: "연사", desc: "공격력 90%로 세 번 연속 공격합니다.", cooldown: 5, effect: { type: "multi", power: 0.9, hits: 3 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:51: trailing whitespace.
+    { id: "snipe", owner: "archer", name: "저격", desc: "공격력 200%의 확정 치명타를 가합니다.", cooldown: 9, effect: { type: "crit", power: 2 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:52: trailing whitespace.
+    { id: "poison", owner: "archer", name: "독화살", desc: "일격 후 6초간 매초 공격력 45%의 독 피해를 줍니다.", cooldown: 10, effect: { type: "dot", power: 0.45, duration: 6 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:53: trailing whitespace.
+    { id: "dodge", owner: "archer", name: "회피", desc: "4초간 50% 확률로 적의 공격을 회피합니다.", cooldown: 12, effect: { type: "dodge", power: 0.5, duration: 4 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:54: trailing whitespace.
+    { id: "heal", owner: "mage", name: "힐", desc: "모든 생존 동료의 최대 HP를 25% 회복합니다.", cooldown: 8, effect: { type: "heal", power: 0.25 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:55: trailing whitespace.
+    { id: "meteor", owner: "mage", name: "메테오", desc: "모든 적에게 공격력 250%의 피해를 줍니다.", cooldown: 10, effect: { type: "aoe", power: 2.5 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:56: trailing whitespace.
+    { id: "resurrect", owner: "mage", name: "천국의 문", desc: "전투당 한 번, 쓰러진 동료 한 명을 HP 30%로 부활시킵니다.", cooldown: 45, effect: { type: "resurrect", power: 0.3 }, levelScale: 0.5, maxLevel: 10 },
js/data.js:57: trailing whitespace.
+    { id: "shield", owner: "mage", name: "마나실드", desc: "6초간 최대 HP의 40%만큼 피해를 흡수합니다.", cooldown: 12, effect: { type: "shield", duration: 6, power: 0.4 }, levelScale: 0.5, maxLevel: 10 }
js/data.js:58: trailing whitespace.
+  ],
js/data.js:59: trailing whitespace.
+  tickMs: 100,
js/data.js:60: trailing whitespace.
+  autosaveMs: 3000,
js/data.js:61: trailing whitespace.
+  catchUpMaxMs: 60000,
js/data.js:62: trailing whitespace.
+  statsWindowMs: 300000,
js/data.js:63: trailing whitespace.
+  resultTicks: 15,
js/data.js:64: trailing whitespace.
+  defaultSeed: 271828,
js/data.js:65: trailing whitespace.
+  // DECISION: Unspecified base stats favor a durable, slow frontline warrior.
js/data.js:66: trailing whitespace.
+  mercenaries: [
js/data.js:67: trailing whitespace.
+    { id: "warrior", name: "전사", role: "선봉 · 탱커", unlockStage: null, position: 0,
js/data.js:68: trailing whitespace.
+      color: "#e4af65", base: { hp: 170, atk: 14, def: 12, attackSpeed: 0.85, critChance: 0.08, critDamage: 1.5 }, growth: 0.05 },
js/data.js:69: trailing whitespace.
+    { id: "archer", name: "궁수", role: "중위 · 딜러", unlockStage: 5, position: 1,
js/data.js:70: trailing whitespace.
+      color: "#88c8a2", base: { hp: 140, atk: 12, def: 5, attackSpeed: 1.4, critChance: 0.15, critDamage: 1.75 }, growth: 0.05 },
js/data.js:71: trailing whitespace.
+    { id: "mage", name: "마법사", role: "후위 · 서포터", unlockStage: 15, position: 2,
js/data.js:72: trailing whitespace.
+      color: "#b7a0ee", base: { hp: 115, atk: 23, def: 3, attackSpeed: 0.7, critChance: 0.1, critDamage: 1.5 }, growth: 0.05 }
js/data.js:73: trailing whitespace.
+  ],
js/data.js:74: trailing whitespace.
+  balance: { enemyHp: 20, enemyHpGrowth: 1.22, enemyAtk: 3, enemyAtkGrowth: 1.18,
js/data.js:75: trailing whitespace.
+    enemyDef: 1, enemyDefGrowth: 1.12, defenseConstant: 20, goldBase: 5, goldGrowth: 1.2,
js/data.js:76: trailing whitespace.
+    xpBase: 50, xpGrowth: 1.15, xpPerKill: 2, bossXpMultiplier: 5,
js/data.js:77: trailing whitespace.
+    firstClearCoins: 10, bossFirstClearCoins: 30, bossRepeatCoins: 2 },
js/data.js:78: trailing whitespace.
+  cpWeights: { hp: 0.2, atk: 5, def: 2, attackSpeed: 20, critChance: 100, critDamage: 10 },
js/data.js:79: trailing whitespace.
+  monsterTypes: {
js/data.js:80: trailing whitespace.
+    slime: { name: "슬라임", hp: 1, atk: 0.9, def: 1, attackSpeed: 0.65 },
js/data.js:81: trailing whitespace.
+    goblin: { name: "척후병", hp: 0.85, atk: 1, def: 0.8, attackSpeed: 0.8 },
js/data.js:82: trailing whitespace.
+    brute: { name: "파수꾼", hp: 1.4, atk: 1.25, def: 1.5, attackSpeed: 0.6 },
js/data.js:83: trailing whitespace.
+    boss: { name: "수호자", hp: 7, atk: 2.5, def: 2, attackSpeed: 0.75 }
js/data.js:84: trailing whitespace.
+  },
js/data.js:85: trailing whitespace.
+  regions: [
js/data.js:86: trailing whitespace.
+    { name: "잊힌 숲", subtitle: "안개 너머, 첫 번째 원정", palette: { slime: "#91b69b", goblin: "#9ba979", brute: "#b09b7d", boss: "#c5b88d" },
js/data.js:87: trailing whitespace.
+      stages: ["숲의 입구", "이끼 오솔길", "버려진 야영지", "뒤틀린 뿌리", "안개 공터", "궁수의 흔적", "가시 덤불", "오래된 제단", "수호자의 길", "고목의 심장"], bossName: "고목 수호자" },
js/data.js:88: trailing whitespace.
+    { name: "잿빛 협곡", subtitle: "꺼지지 않는 불씨를 따라", palette: { slime: "#c69169", goblin: "#c27b68", brute: "#a58a81", boss: "#e09a68" },
js/data.js:89: trailing whitespace.
+      stages: ["재의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "무너진 광산", "마법사의 봉화", "검은 용광로", "불씨의 다리", "왕의 무덤", "잿불 왕좌"], bossName: "잿불 거인" },
js/data.js:90: trailing whitespace.
+    { name: "서리 성채", subtitle: "마지막 성문을 향하여", palette: { slime: "#8fbdd1", goblin: "#91a2c9", brute: "#a9a3c8", boss: "#c2dded" },
js/data.js:91: trailing whitespace.
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "침묵의 초소", "푸른 회랑", "망각의 정원", "서리 감옥", "백야의 탑", "봉인된 서고", "최후의 성문", "영원의 군주"], bossName: "서리 군주" }
js/data.js:92: trailing whitespace.
+  ],
js/data.js:93: trailing whitespace.
+  // DECISION: Fixed waves keep balance reproducible; the boss replaces the last wave.
js/data.js:94: trailing whitespace.
+  waves: [["slime", "goblin"], ["slime", "goblin", "slime"], ["goblin", "slime", "brute"]],
js/data.js:95: trailing whitespace.
+  bossWaves: [["goblin", "brute"], ["slime", "goblin", "brute"], ["boss"]],
js/data.js:96: trailing whitespace.
+  stages: [],
js/data.js:97: trailing whitespace.
+  equipmentSlots: [
js/data.js:98: trailing whitespace.
+    { id: "weapon", name: "무기", icon: "⚔", base: { atk: 4 } },
js/data.js:99: trailing whitespace.
+    { id: "hat", name: "모자", icon: "♜", base: { hp: 30 } },
js/data.js:100: trailing whitespace.
+    { id: "gloves", name: "장갑", icon: "✧", base: { atk: 2, critChance: 0.01 } },
js/data.js:101: trailing whitespace.
+    { id: "shoes", name: "신발", icon: "♧", base: { hp: 15, def: 3 } }
js/data.js:102: trailing whitespace.
+  ],
js/data.js:103: trailing whitespace.
+  equipmentTiers: [
js/data.js:104: trailing whitespace.
+    { tier: 1, multiplier: 1 }, { tier: 2, multiplier: 1.6 },
js/data.js:105: trailing whitespace.
+    { tier: 3, multiplier: 2.56 }, { tier: 4, multiplier: 4.096 },
js/data.js:106: trailing whitespace.
+    { tier: 5, multiplier: 6.5536 }, { tier: 6, multiplier: 10.48576 },
js/data.js:107: trailing whitespace.
+    { tier: 7, multiplier: 16.777216 }, { tier: 8, multiplier: 26.8435456 }
js/data.js:108: trailing whitespace.
+  ],
js/data.js:109: trailing whitespace.
+  equipmentRarities: {
js/data.js:110: trailing whitespace.
+    rare: { name: "레어", multiplier: 1, lines: 1, color: "#659bff" },
js/data.js:111: trailing whitespace.
+    epic: { name: "에픽", multiplier: 1.15, lines: 2, color: "#b789f5" },
js/data.js:112: trailing whitespace.
+    unique: { name: "유니크", multiplier: 1.35, lines: 3, color: "#edd26a" },
js/data.js:113: trailing whitespace.
+    legendary: { name: "레전더리", multiplier: 1.6, lines: 3, color: "#6cdc99" }
js/data.js:114: trailing whitespace.
+  },
js/data.js:115: trailing whitespace.
+  inventoryCap: 60,
js/data.js:116: trailing whitespace.
+  fuseUpgradeChance: 0.15,
js/data.js:117: trailing whitespace.
+  potentialRerollCost: 20,
js/data.js:118: trailing whitespace.
+  rarityOrder: ["rare", "epic", "unique", "legendary"],
js/data.js:119: trailing whitespace.
+  sellMultipliers: { rare: 1, epic: 2, unique: 4, legendary: 8 },
js/data.js:120: trailing whitespace.
+  // DECISION: Boss-band trash uses normal chance; only the boss guarantees the exact tier.
js/data.js:121: trailing whitespace.
+  dropTables: [
js/data.js:122: trailing whitespace.
+    { min: 0, max: 8, chance: 0.08, tierWeights: { 1: 85, 2: 15 }, rarityWeights: [80, 17, 2.7, 0.3] },
js/data.js:123: trailing whitespace.
+    { min: 9, max: 9, chance: 0.08, bossChance: 1, guaranteedTier: 2, tierWeights: { 2: 100 }, rarityWeights: [65, 28, 6, 1] },
js/data.js:124: trailing whitespace.
+    { min: 10, max: 18, chance: 0.08, tierWeights: { 2: 65, 3: 30, 4: 5 }, rarityWeights: [74, 21, 4.4, 0.6] },
js/data.js:125: trailing whitespace.
+    { min: 19, max: 19, chance: 0.08, bossChance: 1, guaranteedTier: 3, tierWeights: { 3: 100 }, rarityWeights: [55, 34, 9, 2] },
js/data.js:126: trailing whitespace.
+    { min: 20, max: 28, chance: 0.08, tierWeights: { 3: 60, 4: 32, 5: 8 }, rarityWeights: [68, 25, 6, 1] },
js/data.js:127: trailing whitespace.
+    { min: 29, max: 29, chance: 0.08, bossChance: 1, guaranteedTier: 4, tierWeights: { 4: 100 }, rarityWeights: [45, 39, 13, 3] }
js/data.js:128: trailing whitespace.
+  ],
js/data.js:129: trailing whitespace.
+  // DECISION: Chaos bosses use the same T5–8 table without a normal-mode forced tier.
js/data.js:130: trailing whitespace.
+  chaosDropTable: { chance: 0.08, bossChance: 1, tierWeights: { 5: 55, 6: 30, 7: 12, 8: 3 }, rarityWeights: [40, 40, 16, 4] },
js/data.js:131: trailing whitespace.
+  fusionCost: function (tier) { return 100 * 2 ** (tier - 1); },
js/data.js:132: trailing whitespace.
+  sellPrice: function (tier, rarity) { return 20 * 2 ** (tier - 1) * DATA.sellMultipliers[rarity]; },
js/data.js:133: trailing whitespace.
+  // DECISION: Option values are fractions; each rarity lists [min, max] and selection weight.
js/data.js:134: trailing whitespace.
+  potentialPool: [
js/data.js:135: trailing whitespace.
+    { id: "atkPct", name: "공격력", ranges: [[0.02, 0.05], [0.04, 0.08], [0.06, 0.12], [0.08, 0.2]], weights: [25, 25, 25, 25] },
js/data.js:136: trailing whitespace.
+    { id: "hpPct", name: "HP", ranges: [[0.03, 0.06], [0.05, 0.1], [0.08, 0.15], [0.1, 0.25]], weights: [25, 25, 20, 20] },
js/data.js:137: trailing whitespace.
+    { id: "defPct", name: "방어력", ranges: [[0.03, 0.06], [0.05, 0.1], [0.08, 0.15], [0.1, 0.25]], weights: [15, 15, 15, 15] },
js/data.js:138: trailing whitespace.
+    { id: "attackSpeedPct", name: "공격속도", ranges: [[0.02, 0.04], [0.03, 0.07], [0.05, 0.1], [0.07, 0.15]], weights: [15, 15, 15, 15] },
js/data.js:139: trailing whitespace.
+    { id: "critChance", name: "치명타 확률", ranges: [[0.01, 0.03], [0.02, 0.05], [0.03, 0.08], [0.05, 0.12]], weights: [10, 10, 12, 12] },
js/data.js:140: trailing whitespace.
+    { id: "critDamage", name: "치명타 피해", ranges: [[0.05, 0.1], [0.08, 0.15], [0.1, 0.25], [0.15, 0.4]], weights: [10, 10, 12, 12] },
js/data.js:141: trailing whitespace.
+    { id: "goldPct", name: "골드 획득", ranges: [[0.03, 0.08], [0.05, 0.12], [0.08, 0.2], [0.12, 0.3]], weights: [10, 10, 8, 8] },
js/data.js:142: trailing whitespace.
+    { id: "dropPct", name: "드롭률", ranges: [[0.01, 0.03], [0.02, 0.05], [0.03, 0.08], [0.05, 0.12]], weights: [5, 4, 5, 5] },
js/data.js:143: trailing whitespace.
+    { id: "invincibleOnHit", name: "피격 시 1초 무적", duration: 1, ranges: [[0.01, 0.01], [0.01, 0.02], [0.02, 0.04], [0.03, 0.06]], weights: [0, 1, 3, 3] }
js/data.js:144: trailing whitespace.
+  ],
js/data.js:145: trailing whitespace.
+  enemyScale: function (stageIndex) {
js/data.js:146: trailing whitespace.
+    var b = DATA.balance;
js/data.js:147: trailing whitespace.
+    return { hp: b.enemyHp * b.enemyHpGrowth ** stageIndex,
js/data.js:148: trailing whitespace.
+      atk: b.enemyAtk * b.enemyAtkGrowth ** stageIndex,
js/data.js:149: trailing whitespace.
+      def: b.enemyDef * b.enemyDefGrowth ** stageIndex };
js/data.js:150: trailing whitespace.
+  },
js/data.js:151: trailing whitespace.
+  goldPerKill: function (stageIndex) { return Math.round(DATA.balance.goldBase * DATA.balance.goldGrowth ** stageIndex); },
js/data.js:152: trailing whitespace.
+  xpToNext: function (level) { return Math.ceil(DATA.balance.xpBase * DATA.balance.xpGrowth ** level); },
js/data.js:153: trailing whitespace.
+  mercenaryStats: function (id, level) {
js/data.js:154: trailing whitespace.
+    var merc = DATA.mercenaries.find(function (m) { return m.id === id; });
js/data.js:155: trailing whitespace.
+    var growth = (1 + merc.growth) ** (level - 1);
js/data.js:156: trailing whitespace.
+    // DECISION: HP/ATK/DEF grow 5% compounded; speed and critical stats stay at base values.
js/data.js:157: trailing whitespace.
+    return { hp: Math.round(merc.base.hp * growth), atk: Math.round(merc.base.atk * growth),
js/data.js:158: trailing whitespace.
+      def: Math.round(merc.base.def * growth), attackSpeed: merc.base.attackSpeed,
js/data.js:159: trailing whitespace.
+      critChance: merc.base.critChance, critDamage: merc.base.critDamage };
js/data.js:160: trailing whitespace.
+  }
js/data.js:161: trailing whitespace.
+};
js/data.js:162: trailing whitespace.
+
js/data.js:163: trailing whitespace.
+DATA.regions.forEach(function (region, regionIndex) {
js/data.js:164: trailing whitespace.
+  region.stages.forEach(function (name, localIndex) {
js/data.js:165: trailing whitespace.
+    DATA.stages.push({ index: regionIndex * 10 + localIndex, id: (regionIndex + 1) + "-" + (localIndex + 1),
js/data.js:166: trailing whitespace.
+      name: name, region: regionIndex, palette: region.palette, boss: localIndex === 9,
js/data.js:167: trailing whitespace.
+      bossName: region.bossName, timeLimit: 60, waves: localIndex === 9 ? DATA.bossWaves : DATA.waves,
js/data.js:168: trailing whitespace.
+      firstClearCoins: localIndex === 9 ? DATA.balance.bossFirstClearCoins : DATA.balance.firstClearCoins });
js/data.js:169: trailing whitespace.
+  });
js/data.js:170: trailing whitespace.
+});
js/ui-equipment.js:1: trailing whitespace.
+"use strict";
js/ui-equipment.js:2: trailing whitespace.
+
js/ui-equipment.js:3: trailing whitespace.
+(function () {
js/ui-equipment.js:4: trailing whitespace.
+  var filter = "all", sort = "tier", selected = null, equipFor = null, preview = null;
js/ui-equipment.js:5: trailing whitespace.
+  var unread = 0, currentSheet = null, revealQueue = [], shown = 0, revealFrame = null, lastReveal = 0, skipped = false;
js/ui-equipment.js:6: trailing whitespace.
+  var cpTarget = null, cpValue = 0, cpFrame = null;
js/ui-equipment.js:7: trailing whitespace.
+  var ranks = DATA.rarityOrder;
js/ui-equipment.js:8: trailing whitespace.
+  var labels = { hp: "HP", atk: "공격력", def: "방어력", critChance: "치명타 확률" };
js/ui-equipment.js:9: trailing whitespace.
+  function el(id) { return document.getElementById(id); }
js/ui-equipment.js:10: trailing whitespace.
+  function slot(id) { return DATA.equipmentSlots.find(function (s) { return s.id === id; }); }
js/ui-equipment.js:11: trailing whitespace.
+  function name(item) { return "T" + UI.fmt(item.tier) + " " + DATA.equipmentRarities[item.rarity].name + " " + slot(item.slot).name; }
js/ui-equipment.js:12: trailing whitespace.
+  function itemCard(item, extra) {
js/ui-equipment.js:13: trailing whitespace.
+    return '<span class="item-card rarity-' + item.rarity + ' ' + (extra || "") + '"><span class="item-icon">' + UI.icon(item.slot, slot(item.slot).icon) +
js/ui-equipment.js:14: trailing whitespace.
+      '</span><b>T' + UI.fmt(item.tier) + '</b><small>' + DATA.equipmentRarities[item.rarity].name + '</small></span>';
js/ui-equipment.js:15: trailing whitespace.
+  }
js/ui-equipment.js:16: trailing whitespace.
+  function lines(options) {
js/ui-equipment.js:17: trailing whitespace.
+    return '<ul class="potential-lines">' + options.map(function (p) {
js/ui-equipment.js:18: trailing whitespace.
+      var definition = DATA.potentialPool.find(function (d) { return d.id === p.id; });
js/ui-equipment.js:19: trailing whitespace.
+      return '<li><span>' + definition.name + '</span><b>' + (p.id === "invincibleOnHit" ? UI.fmt(DATA.invincibleChance * 100) : '+' + UI.fmt(p.value * 100)) + '%</b></li>';
js/ui-equipment.js:20: trailing whitespace.
+    }).join("") + '</ul>';
js/ui-equipment.js:21: trailing whitespace.
+  }
js/ui-equipment.js:22: trailing whitespace.
+  function badge() { el("equipment-badge").hidden = unread === 0; el("equipment-badge").textContent = UI.fmt(unread); }
js/ui-equipment.js:23: trailing whitespace.
+  UI.sheetOpened = function (sheet) {
js/ui-equipment.js:24: trailing whitespace.
+    currentSheet = sheet;
js/ui-equipment.js:25: trailing whitespace.
+    if (sheet === "equipment") { unread = 0; badge(); }
js/ui-equipment.js:26: trailing whitespace.
+  };
js/ui-equipment.js:27: trailing whitespace.
+  UI.equipmentSignature = function (sheet, state) {
js/ui-equipment.js:28: trailing whitespace.
+    if (sheet !== "equipment" && sheet !== "fusion") return "";
js/ui-equipment.js:29: trailing whitespace.
+    return JSON.stringify([filter, sort, selected, equipFor, preview, state.inventory, state.gold, state.squadCoins, state.mercenaries]);
js/ui-equipment.js:30: trailing whitespace.
+  };
js/ui-equipment.js:31: trailing whitespace.
+  UI.equipmentSlot = function (merc, definition, state) {
js/ui-equipment.js:32: trailing whitespace.
+    var item = state.inventory.find(function (i) { return i.uid === merc.equipment[definition.id]; });
js/ui-equipment.js:33: trailing whitespace.
+    return '<button class="equipment-slot ' + (item ? 'rarity-' + item.rarity : '') + '" data-merc-slot="' + definition.id +
js/ui-equipment.js:34: trailing whitespace.
+      '" data-merc="' + merc.id + '" ' + (merc.unlocked ? '' : 'disabled') + ' aria-label="' + definition.name + ' ' +
js/ui-equipment.js:35: trailing whitespace.
+      (item ? name(item) : '빈 슬롯') + '"><span>' + UI.icon(definition.id, definition.icon) + '</span><small>' + definition.name + '</small><em>' +
js/ui-equipment.js:36: trailing whitespace.
+      (item ? 'T' + UI.fmt(item.tier) + ' · ' + DATA.equipmentRarities[item.rarity].name : '비어 있음') + '</em></button>';
js/ui-equipment.js:37: trailing whitespace.
+  };
js/ui-equipment.js:38: trailing whitespace.
+  function detail(item, state) {
js/ui-equipment.js:39: trailing whitespace.
+    return '<section class="item-detail rarity-' + item.rarity + '" aria-label="장비 상세"><div class="detail-heading">' + itemCard(item) +
js/ui-equipment.js:40: trailing whitespace.
+      '<div><h3>' + name(item) + '</h3><p>' + (item.equippedBy ? DATA.mercenaries.find(function (m) { return m.id === item.equippedBy; }).name + ' 장착 중' : '장착 가능한 장비') +
js/ui-equipment.js:41: trailing whitespace.
+      '</p></div><button data-action="back" aria-label="목록으로">←</button></div><dl class="base-stats">' + Object.keys(item.base).map(function (key) {
js/ui-equipment.js:42: trailing whitespace.
+        return '<div><dt>' + labels[key] + '</dt><dd>+' + UI.fmt(item.base[key] * (key === "critChance" ? 100 : 1)) + (key === "critChance" ? '%' : '') + '</dd></div>';
js/ui-equipment.js:43: trailing whitespace.
+      }).join("") + '</dl><h4>잠재옵션</h4>' + lines(item.potentials) +
js/ui-equipment.js:44: trailing whitespace.
+      (preview && preview.uid === item.uid ? '<div class="reroll-preview"><div><h4>재설정 전</h4>' + lines(preview.before) + '</div><div class="reroll-after"><h4>재설정 후 ✦</h4>' + lines(preview.after) + '</div></div>' : '') +
js/ui-equipment.js:45: trailing whitespace.
+      '<h4>' + (equipFor ? DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + '에게 장착' : '장착할 용병') + '</h4><div class="equip-actions">' + state.mercenaries.map(function (m, i) {
js/ui-equipment.js:46: trailing whitespace.
+        return '<button data-equip="' + m.id + '" ' + (!m.unlocked || item.equippedBy === m.id ? 'disabled' : '') +
js/ui-equipment.js:47: trailing whitespace.
+          ' class="' + (equipFor === m.id ? 'primary' : '') + '">' + DATA.mercenaries[i].name + (m.unlocked ? ' 장착' : ' 미합류') + '</button>';
js/ui-equipment.js:48: trailing whitespace.
+      }).join("") + '</div><div class="detail-actions">' + (item.equippedBy ? '<button data-action="unequip">해제</button>' : '') +
js/ui-equipment.js:49: trailing whitespace.
+      '<button data-action="lock">' + (item.locked ? '🔒 잠금 해제' : '잠금') + '</button><button data-action="sell" ' + (item.locked || item.equippedBy ? 'disabled' : '') +
js/ui-equipment.js:50: trailing whitespace.
+      '>판매 · ' + UI.fmt(DATA.sellPrice(item.tier, item.rarity)) + ' 골드</button><button data-action="reroll" ' + (state.squadCoins < DATA.potentialRerollCost ? 'disabled' : '') +
js/ui-equipment.js:51: trailing whitespace.
+      '>잠재 재설정 · 코인 ' + UI.fmt(DATA.potentialRerollCost) + '</button></div></section>';
js/ui-equipment.js:52: trailing whitespace.
+  }
js/ui-equipment.js:53: trailing whitespace.
+  function inventory(state) {
js/ui-equipment.js:54: trailing whitespace.
+    var items = state.inventory.filter(function (i) { return filter === "all" || i.slot === filter; }).slice();
js/ui-equipment.js:55: trailing whitespace.
+    items.sort(function (a, b) {
js/ui-equipment.js:56: trailing whitespace.
+      var recent = Number(b.uid.slice(5)) - Number(a.uid.slice(5));
js/ui-equipment.js:57: trailing whitespace.
+      return sort === "recent" ? recent : sort === "rarity" ? ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || b.tier - a.tier || recent :
js/ui-equipment.js:58: trailing whitespace.
+        b.tier - a.tier || ranks.indexOf(b.rarity) - ranks.indexOf(a.rarity) || recent;
js/ui-equipment.js:59: trailing whitespace.
+    });
js/ui-equipment.js:60: trailing whitespace.
+    var item = state.inventory.find(function (i) { return i.uid === selected; });
js/ui-equipment.js:61: trailing whitespace.
+    return '<div class="inventory-heading"><p>전장에서 찾은 가능성</p><strong id="inventory-count">' + UI.fmt(state.inventory.length) + '/' + UI.fmt(DATA.inventoryCap) + '</strong></div>' +
js/ui-equipment.js:62: trailing whitespace.
+      (equipFor ? '<div class="equip-mode">' + DATA.mercenaries.find(function (m) { return m.id === equipFor; }).name + ' · 이 용병에게 장착<button data-action="end-mode">모드 해제</button></div>' : '') +
js/ui-equipment.js:63: trailing whitespace.
+      '<div class="inventory-filters" role="group" aria-label="장비 슬롯 필터">' + [{ id: "all", name: "전체" }].concat(DATA.equipmentSlots).map(function (s) {
js/ui-equipment.js:64: trailing whitespace.
+        return '<button data-filter="' + s.id + '" class="' + (filter === s.id ? 'active' : '') + '" aria-pressed="' + (filter === s.id) + '">' + s.name + '</button>';
js/ui-equipment.js:65: trailing whitespace.
+      }).join("") + '</div><div class="inventory-sort"><span>' + UI.fmt(items.length) + '개 장비</span><label>정렬 <select id="equipment-sort">' +
js/ui-equipment.js:66: trailing whitespace.
+      [['tier', '티어↓'], ['rarity', '등급↓'], ['recent', '최근']].map(function (s) { return '<option value="' + s[0] + '" ' + (sort === s[0] ? 'selected' : '') + '>' + s[1] + '</option>'; }).join('') +
js/ui-equipment.js:67: trailing whitespace.
+      '</select></label></div>' + (item ? detail(item, state) : '') + '<div class="inventory-grid">' + items.map(function (i) {
js/ui-equipment.js:68: trailing whitespace.
+        return '<button class="inventory-cell rarity-' + i.rarity + (selected === i.uid ? ' selected-item' : '') + '" data-item="' + i.uid + '" aria-label="' + name(i) +
js/ui-equipment.js:69: trailing whitespace.
+          (i.locked ? ' 잠금' : '') + (i.equippedBy ? ' 장착 중' : '') + '"><span>' + UI.icon(i.slot, slot(i.slot).icon) + '</span><b>T' + UI.fmt(i.tier) +
js/ui-equipment.js:70: trailing whitespace.
+          '</b>' + (i.locked ? '<em>🔒</em>' : '') + (i.equippedBy ? '<i>장착</i>' : '') + '</button>';
js/ui-equipment.js:71: trailing whitespace.
+      }).join("") + '</div>' + (!items.length ? '<p class="equipment-empty">적을 처치하면 장비가 떨어집니다.<br>같은 슬롯·티어 세 개를 모아 합성해 보세요.</p>' : '') +
js/ui-equipment.js:72: trailing whitespace.
+      '<p class="equipment-note">가득 차면 잠금·장착을 제외한 최저 티어 레어를 자동 판매합니다. 판매할 레어가 없으면 새 드롭을 골드로 받습니다.</p>';
js/ui-equipment.js:73: trailing whitespace.
+  }
js/ui-equipment.js:74: trailing whitespace.
+  function fusion(state) {
js/ui-equipment.js:75: trailing whitespace.
+    var rows = '';
js/ui-equipment.js:76: trailing whitespace.
+    DATA.equipmentSlots.forEach(function (s) {
js/ui-equipment.js:77: trailing whitespace.
+      for (var tier = 1; tier <= 8; tier++) {
js/ui-equipment.js:78: trailing whitespace.
+        var count = state.inventory.filter(function (i) { return i.slot === s.id && i.tier === tier && !i.locked && !i.equippedBy; }).length;
js/ui-equipment.js:79: trailing whitespace.
+        rows += '<tr><th>' + UI.icon(s.id, s.icon) + ' ' + s.name + '</th><td>T' + UI.fmt(tier) + '</td><td>' + UI.fmt(count) + '개</td><td>' + (tier === 8 ? '<small>최대 티어</small>' :
js/ui-equipment.js:80: trailing whitespace.
+          '<button data-fuse-slot="' + s.id + '" data-tier="' + tier + '" ' + (count < 3 || state.gold < DATA.fusionCost(tier) ? 'disabled' : '') +
js/ui-equipment.js:81: trailing whitespace.
+          '>합성 ×' + UI.fmt(1) + '<small>' + UI.fmt(DATA.fusionCost(tier)) + ' 골드</small></button>') + '</td></tr>';
js/ui-equipment.js:82: trailing whitespace.
+      }
js/ui-equipment.js:83: trailing whitespace.
+    });
js/ui-equipment.js:84: trailing whitespace.
+    return '<div class="fusion-heading"><div><p>셋을 모아, 한 단계 더.</p><small>보유 골드 ' + UI.fmt(state.gold) + '</small></div><button id="auto-fuse" class="primary">자동 합성</button></div>' +
js/ui-equipment.js:85: trailing whitespace.
+      '<p class="equipment-note">잠금·장착 장비 제외 · 최저 티어부터 합성<br>최고 재료 등급 유지 · ' + UI.fmt(DATA.fuseUpgradeChance * 100) + '% 확률로 등급 상승</p>' +
js/ui-equipment.js:86: trailing whitespace.
+      '<table class="fusion-table"><thead><tr><th>슬롯</th><th>티어</th><th>재료</th><th>세 개 합성</th></tr></thead><tbody>' + rows + '</tbody></table>';
js/ui-equipment.js:87: trailing whitespace.
+  }
js/ui-equipment.js:88: trailing whitespace.
+  UI.renderEquipmentSheet = function (sheet, state) { el("sheet-content").innerHTML = sheet === "equipment" ? inventory(state) : fusion(state); };
js/ui-equipment.js:89: trailing whitespace.
+  UI.updateCP = function (value) {
js/ui-equipment.js:90: trailing whitespace.
+    if (value === cpTarget) return;
js/ui-equipment.js:91: trailing whitespace.
+    if (cpTarget === null) { cpTarget = cpValue = value; el("cp").textContent = UI.fmt(value); return; }
js/ui-equipment.js:92: trailing whitespace.
+    cpTarget = value;
js/ui-equipment.js:93: trailing whitespace.
+    cancelAnimationFrame(cpFrame);
js/ui-equipment.js:94: trailing whitespace.
+    var from = cpValue, start = performance.now();
js/ui-equipment.js:95: trailing whitespace.
+    el("cp").classList.remove("cp-jump");
js/ui-equipment.js:96: trailing whitespace.
+    void el("cp").offsetWidth;
js/ui-equipment.js:97: trailing whitespace.
+    el("cp").classList.add("cp-jump");
js/ui-equipment.js:98: trailing whitespace.
+    function frame(now) {
js/ui-equipment.js:99: trailing whitespace.
+      var t = Math.min(1, (now - start) / 300);
js/ui-equipment.js:100: trailing whitespace.
+      cpValue = from + (value - from) * (1 - (1 - t) ** 3);
js/ui-equipment.js:101: trailing whitespace.
+      el("cp").textContent = UI.fmt(Math.round(cpValue));
js/ui-equipment.js:102: trailing whitespace.
+      if (t < 1) cpFrame = requestAnimationFrame(frame);
js/ui-equipment.js:103: trailing whitespace.
+    }
js/ui-equipment.js:104: trailing whitespace.
+    cpFrame = requestAnimationFrame(frame);
js/ui-equipment.js:105: trailing whitespace.
+  };
js/ui-equipment.js:106: trailing whitespace.
+  function flash(strong) {
js/ui-equipment.js:107: trailing whitespace.
+    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
js/ui-equipment.js:108: trailing whitespace.
+    scene.classList.remove("loot-flash", "loot-flash-strong");
js/ui-equipment.js:109: trailing whitespace.
+    void scene.offsetWidth;
js/ui-equipment.js:110: trailing whitespace.
+    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
js/ui-equipment.js:111: trailing whitespace.
+  }
js/ui-equipment.js:112: trailing whitespace.
+  function drop(item) {
js/ui-equipment.js:113: trailing whitespace.
+    if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
js/ui-equipment.js:114: trailing whitespace.
+    if (item.enemyId === null) return; // Already revealed in the expedition report.
js/ui-equipment.js:115: trailing whitespace.
+    var source = el("sprite-" + item.enemyId);
js/ui-equipment.js:116: trailing whitespace.
+    var svg = el("battle-svg");
js/ui-equipment.js:117: trailing whitespace.
+    var point = svg.createSVGPoint();
js/ui-equipment.js:118: trailing whitespace.
+    point.x = source ? Number(source.dataset.x) : 284;
js/ui-equipment.js:119: trailing whitespace.
+    point.y = source ? Number(source.dataset.y) - 50 : 190;
js/ui-equipment.js:120: trailing whitespace.
+    var start = point.matrixTransform(svg.getScreenCTM());
js/ui-equipment.js:121: trailing whitespace.
+    var target = document.querySelector('[data-tab="equipment"] span').getBoundingClientRect();
js/ui-equipment.js:122: trailing whitespace.
+    var card = document.createElement("div");
js/ui-equipment.js:123: trailing whitespace.
+    card.className = "drop-card";
js/ui-equipment.js:124: trailing whitespace.
+    card.innerHTML = itemCard(item);
js/ui-equipment.js:125: trailing whitespace.
+    card.style.left = start.x - 25 + "px"; card.style.top = start.y - 30 + "px";
js/ui-equipment.js:126: trailing whitespace.
+    card.style.setProperty("--drop-x", target.x + target.width / 2 - start.x + "px");
js/ui-equipment.js:127: trailing whitespace.
+    card.style.setProperty("--drop-y", target.y + target.height / 2 - start.y + "px");
js/ui-equipment.js:128: trailing whitespace.
+    el("app").appendChild(card);
js/ui-equipment.js:129: trailing whitespace.
+    card.addEventListener("animationend", function (e) { if (e.target === card) card.remove(); });
js/ui-equipment.js:130: trailing whitespace.
+    if (ranks.indexOf(item.rarity) >= 2) { flash(false); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
js/ui-equipment.js:131: trailing whitespace.
+  }
js/ui-equipment.js:132: trailing whitespace.
+  var reveal = document.createElement("dialog");
js/ui-equipment.js:133: trailing whitespace.
+  reveal.id = "fusion-reveal";
js/ui-equipment.js:134: trailing whitespace.
+  reveal.setAttribute("aria-label", "합성 결과");
js/ui-equipment.js:135: trailing whitespace.
+  reveal.innerHTML = '<h2>합성의 순간</h2><p id="reveal-progress" aria-live="polite"></p><div id="reveal-cards"></div><button id="reveal-skip">탭하면 모두 공개</button>';
js/ui-equipment.js:136: trailing whitespace.
+  el("app").appendChild(reveal);
js/ui-equipment.js:137: trailing whitespace.
+  function revealOne(event) {
js/ui-equipment.js:138: trailing whitespace.
+    var card = document.createElement("div");
js/ui-equipment.js:139: trailing whitespace.
+    card.className = "fusion-card" + (event.upgraded ? " rarity-upgrade" : "");
js/ui-equipment.js:140: trailing whitespace.
+    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + itemCard(event.item) +
js/ui-equipment.js:141: trailing whitespace.
+      '</div></div><p>' + (event.upgraded ? '✦ 등급 상승!' : slot(event.item.slot).name) + '</p>';
js/ui-equipment.js:142: trailing whitespace.
+    el("reveal-cards").appendChild(card);
js/ui-equipment.js:143: trailing whitespace.
+    if (event.upgraded || ranks.indexOf(event.item.rarity) >= 2) flash(true);
js/ui-equipment.js:144: trailing whitespace.
+  }
js/ui-equipment.js:145: trailing whitespace.
+  function revealTick(now) {
js/ui-equipment.js:146: trailing whitespace.
+    if (!reveal.open) { revealFrame = null; return; }
js/ui-equipment.js:147: trailing whitespace.
+    if (shown < revealQueue.length && now - lastReveal >= 250) {
js/ui-equipment.js:148: trailing whitespace.
+      revealOne(revealQueue[shown++]); lastReveal = now;
js/ui-equipment.js:149: trailing whitespace.
+      el("reveal-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(revealQueue.length) + ' 공개';
js/ui-equipment.js:150: trailing whitespace.
+      el("reveal-cards").lastElementChild.scrollIntoView({ block: "nearest" });
js/ui-equipment.js:151: trailing whitespace.
+    }
js/ui-equipment.js:152: trailing whitespace.
+    if (shown < revealQueue.length) revealFrame = requestAnimationFrame(revealTick);
js/ui-equipment.js:153: trailing whitespace.
+    else { revealFrame = null; el("reveal-skip").textContent = '탭하면 모두 공개 · 다시 탭하면 닫기'; }
js/ui-equipment.js:154: trailing whitespace.
+  }
js/ui-equipment.js:155: trailing whitespace.
+  function enqueue(event) {
js/ui-equipment.js:156: trailing whitespace.
+    if (!reveal.open) {
js/ui-equipment.js:157: trailing whitespace.
+      revealQueue = []; shown = 0; skipped = false; lastReveal = -Infinity;
js/ui-equipment.js:158: trailing whitespace.
+      el("reveal-cards").replaceChildren(); reveal.classList.remove("reveal-skipped"); reveal.showModal();
js/ui-equipment.js:159: trailing whitespace.
+    }
js/ui-equipment.js:160: trailing whitespace.
+    revealQueue.push(event);
js/ui-equipment.js:161: trailing whitespace.
+    if (revealFrame === null) revealFrame = requestAnimationFrame(revealTick);
js/ui-equipment.js:162: trailing whitespace.
+  }
js/ui-equipment.js:163: trailing whitespace.
+  reveal.addEventListener("click", function () {
js/ui-equipment.js:164: trailing whitespace.
+    if (skipped) { reveal.close(); return; }
js/ui-equipment.js:165: trailing whitespace.
+    cancelAnimationFrame(revealFrame); revealFrame = null;
js/ui-equipment.js:166: trailing whitespace.
+    while (shown < revealQueue.length) revealOne(revealQueue[shown++]);
js/ui-equipment.js:167: trailing whitespace.
+    skipped = true; reveal.classList.add("reveal-skipped");
js/ui-equipment.js:168: trailing whitespace.
+    el("reveal-progress").textContent = UI.fmt(shown) + '개 합성 완료';
js/ui-equipment.js:169: trailing whitespace.
+    el("reveal-skip").textContent = '닫기';
js/ui-equipment.js:170: trailing whitespace.
+  });
js/ui-equipment.js:171: trailing whitespace.
+  reveal.addEventListener("close", function () { if (!reveal.open) { cancelAnimationFrame(revealFrame); revealFrame = null; } });
js/ui-equipment.js:172: trailing whitespace.
+  el("sheet").addEventListener("close", function () { if (!el("sheet").open) { currentSheet = null; equipFor = null; } });
js/ui-equipment.js:173: trailing whitespace.
+  el("sheet-content").addEventListener("change", function (event) {
js/ui-equipment.js:174: trailing whitespace.
+    if (event.target.id === "equipment-sort") { sort = event.target.value; UI.refreshSheet(); }
js/ui-equipment.js:175: trailing whitespace.
+  });
js/ui-equipment.js:176: trailing whitespace.
+  el("sheet-content").addEventListener("click", function (event) {
js/ui-equipment.js:177: trailing whitespace.
+    var button = event.target.closest("button");
js/ui-equipment.js:178: trailing whitespace.
+    if (!button || button.disabled) return;
js/ui-equipment.js:179: trailing whitespace.
+    var d = button.dataset, state = Game.getState(), item = state.inventory.find(function (i) { return i.uid === selected; });
js/ui-equipment.js:180: trailing whitespace.
+    if (d.mercSlot) { filter = d.mercSlot; equipFor = d.merc; selected = null; preview = null; UI.openSheet("equipment"); }
js/ui-equipment.js:181: trailing whitespace.
+    else if (d.filter) { filter = d.filter; selected = null; preview = null; UI.refreshSheet(); }
js/ui-equipment.js:182: trailing whitespace.
+    else if (d.item) { selected = d.item; preview = null; UI.refreshSheet(); el("sheet").scrollTop = 0; }
js/ui-equipment.js:183: trailing whitespace.
+    else if (d.equip && item) { Game.equip(item.uid, d.equip); UI.toast('장착 완료 · CP ' + UI.fmt(Game.getCP())); }
js/ui-equipment.js:184: trailing whitespace.
+    else if (d.fuseSlot) {
js/ui-equipment.js:185: trailing whitespace.
+      var materials = state.inventory.filter(function (i) { return i.slot === d.fuseSlot && i.tier === Number(d.tier) && !i.locked && !i.equippedBy; });
js/ui-equipment.js:186: trailing whitespace.
+      if (!Game.fuse(materials.slice(0, 3).map(function (i) { return i.uid; }))) UI.toast('재료 또는 골드가 부족합니다');
js/ui-equipment.js:187: trailing whitespace.
+    } else if (button.id === "auto-fuse") { if (!Game.autoFuse().length) UI.toast('합성할 재료 또는 골드가 부족합니다'); }
js/ui-equipment.js:188: trailing whitespace.
+    else if (d.action === "back") { selected = null; preview = null; UI.refreshSheet(); }
js/ui-equipment.js:189: trailing whitespace.
+    else if (d.action === "end-mode") { equipFor = null; UI.refreshSheet(); }
js/ui-equipment.js:190: trailing whitespace.
+    else if (item) {
js/ui-equipment.js:191: trailing whitespace.
+      if (d.action === "unequip") Game.unequip(item.equippedBy, item.slot);
js/ui-equipment.js:192: trailing whitespace.
+      if (d.action === "lock") Game.toggleLock(item.uid);
js/ui-equipment.js:193: trailing whitespace.
+      if (d.action === "sell") {
js/ui-equipment.js:194: trailing whitespace.
+        var amount = Game.sell([item.uid]);
js/ui-equipment.js:195: trailing whitespace.
+        if (amount !== false) { selected = null; preview = null; UI.refreshSheet(); UI.toast(UI.fmt(amount) + ' 골드에 판매했습니다'); }
js/ui-equipment.js:196: trailing whitespace.
+      }
js/ui-equipment.js:197: trailing whitespace.
+      if (d.action === "reroll") {
js/ui-equipment.js:198: trailing whitespace.
+        if (!Game.rerollPotentials(item.uid)) UI.toast('스쿼드 코인이 부족합니다');
js/ui-equipment.js:199: trailing whitespace.
+        else document.querySelector('.reroll-preview').scrollIntoView({ block: 'center' });
js/ui-equipment.js:200: trailing whitespace.
+      }
js/ui-equipment.js:201: trailing whitespace.
+    }
js/ui-equipment.js:202: trailing whitespace.
+  });
js/ui-equipment.js:203: trailing whitespace.
+  Game.on("itemDrop", drop);
js/ui-equipment.js:204: trailing whitespace.
+  Game.on("fuseResult", enqueue);
js/ui-equipment.js:205: trailing whitespace.
+  Game.on("potentialReroll", function (e) { preview = { uid: e.item.uid, before: e.before, after: e.after }; });
js/ui-equipment.js:206: trailing whitespace.
+  Game.on("inventoryFull", function (e) {
js/ui-equipment.js:207: trailing whitespace.
+    UI.toast('장비함 가득 참 · ' + name(e.sold) + ' 자동 판매 +' + UI.fmt(e.gold) + ' 골드');
js/ui-equipment.js:208: trailing whitespace.
+  });
js/ui-equipment.js:209: trailing whitespace.
+  Game.on("stageStart", function (state) {
js/ui-equipment.js:210: trailing whitespace.
+    if (state.inventory.length === 0) { unread = 0; selected = null; preview = null; badge(); }
js/ui-equipment.js:211: trailing whitespace.
+  });
js/ui-equipment.js:212: trailing whitespace.
+  UI.itemCard = itemCard;
js/ui-equipment.js:213: trailing whitespace.
+  UI.lootFlash = flash;
js/ui-equipment.js:214: trailing whitespace.
+})();
js/ui-expedition.js:1: trailing whitespace.
+"use strict";
js/ui-expedition.js:2: trailing whitespace.
+
js/ui-expedition.js:3: trailing whitespace.
+(function () {
js/ui-expedition.js:4: trailing whitespace.
+  var owner = "warrior", report = null, frame = null, started = 0, shown = 0;
js/ui-expedition.js:5: trailing whitespace.
+  function el(id) { return document.getElementById(id); }
js/ui-expedition.js:6: trailing whitespace.
+  function mercName(id) { return DATA.mercenaries.find(function (m) { return m.id === id; }).name; }
js/ui-expedition.js:7: trailing whitespace.
+  UI.updateExpedition = function (state) {
js/ui-expedition.js:8: trailing whitespace.
+    el("difficulty-toggle").hidden = !state.clearedStages.includes(29);
js/ui-expedition.js:9: trailing whitespace.
+    document.querySelectorAll("[data-difficulty]").forEach(function (button) {
js/ui-expedition.js:10: trailing whitespace.
+      button.classList.toggle("active", button.dataset.difficulty === state.difficulty);
js/ui-expedition.js:11: trailing whitespace.
+      button.setAttribute("aria-pressed", String(button.dataset.difficulty === state.difficulty));
js/ui-expedition.js:12: trailing whitespace.
+    });
js/ui-expedition.js:13: trailing whitespace.
+  };
js/ui-expedition.js:14: trailing whitespace.
+  document.querySelectorAll("[data-difficulty]").forEach(function (button) {
js/ui-expedition.js:15: trailing whitespace.
+    button.addEventListener("click", function () { Game.setDifficulty(button.dataset.difficulty); });
js/ui-expedition.js:16: trailing whitespace.
+  });
js/ui-expedition.js:17: trailing whitespace.
+  UI.renderSkills = function (state) {
js/ui-expedition.js:18: trailing whitespace.
+    var merc = state.mercenaries.find(function (m) { return m.id === owner; });
js/ui-expedition.js:19: trailing whitespace.
+    el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
js/ui-expedition.js:20: trailing whitespace.
+      '<div class="skill-selector">' + state.mercenaries.map(function (m) {
js/ui-expedition.js:21: trailing whitespace.
+        return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
js/ui-expedition.js:23: trailing whitespace.
+      '<div class="active-skills">' + [0, 1, 2].map(function (index) {
js/ui-expedition.js:24: trailing whitespace.
+        var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
js/ui-expedition.js:25: trailing whitespace.
+        return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
js/ui-expedition.js:26: trailing whitespace.
+      }).join('') + '</div>' + DATA.skills.filter(function (s) { return s.owner === owner; }).map(function (s) {
js/ui-expedition.js:27: trailing whitespace.
+        var index = merc.skills.indexOf(s.id), level = merc.skillLevels[s.id], cost = DATA.skillBookCost(level);
js/ui-expedition.js:28: trailing whitespace.
+        return '<article class="skill-card ' + (index >= 0 ? 'equipped-skill' : '') + '"><button class="skill-toggle" data-skill="' + s.id + '" aria-pressed="' + (index >= 0) + '" ' + (!merc.unlocked ? 'disabled' : '') +
js/ui-expedition.js:29: trailing whitespace.
+          '><span><strong>' + s.name + '</strong> <small>Lv. ' + UI.fmt(level) + '</small></span><b>' + (index >= 0 ? '슬롯 ' + UI.fmt(index + 1) : '배치 +') + '</b></button>' +
js/ui-expedition.js:30: trailing whitespace.
+          '<p>' + s.desc + '</p><small>쿨타임 ' + UI.fmt(s.cooldown / (s.effect.type === "resurrect" ? 1 + (level - 1) * s.levelScale : 1)) + '초 · 성장 배율 ' + UI.fmt((1 + (level - 1) * s.levelScale) * 100) + '%</small>' +
js/ui-expedition.js:31: trailing whitespace.
+          '<button class="skill-level" data-level-skill="' + s.id + '" ' + (!merc.unlocked || level >= s.maxLevel || state.skillBooks[owner] < cost ? 'disabled' : '') + '>' +
js/ui-expedition.js:32: trailing whitespace.
+          (level >= s.maxLevel ? '최대 레벨' : '레벨업 (스킬북 ' + UI.fmt(cost) + ')') + '</button></article>';
js/ui-expedition.js:33: trailing whitespace.
+      }).join('') + (!merc.unlocked ? '<p class="equipment-note">이 용병이 합류하면 스킬을 변경할 수 있습니다.</p>' : '');
js/ui-expedition.js:34: trailing whitespace.
+  };
js/ui-expedition.js:35: trailing whitespace.
+  UI.renderSaveControls = function () {
js/ui-expedition.js:36: trailing whitespace.
+    var panel = document.createElement("section");
js/ui-expedition.js:37: trailing whitespace.
+    panel.className = "save-controls";
js/ui-expedition.js:38: trailing whitespace.
+    panel.innerHTML = '<h3>원정 기록 보관</h3><p>JSON으로 진행 상황을 옮기거나 백업하세요.</p>' +
js/ui-expedition.js:39: trailing whitespace.
+      '<div class="save-buttons"><button id="export-save">세이브 내보내기</button><button id="import-save">세이브 불러오기</button></div>' +
js/ui-expedition.js:40: trailing whitespace.
+      '<div id="save-editor" hidden><label for="save-json" id="save-json-label">세이브 JSON</label><textarea id="save-json" spellcheck="false" rows="6"></textarea>' +
js/ui-expedition.js:41: trailing whitespace.
+      '<button id="copy-save" hidden>복사</button><button id="apply-save" hidden>적용</button><p id="save-message" role="status"></p></div>';
js/ui-expedition.js:42: trailing whitespace.
+    el("sheet-content").insertBefore(panel, el("sheet-content").querySelector(".reset-panel"));
js/ui-expedition.js:43: trailing whitespace.
+  };
js/ui-expedition.js:44: trailing whitespace.
+  el("sheet-content").addEventListener("click", async function (event) {
js/ui-expedition.js:45: trailing whitespace.
+    var b = event.target.closest("button");
js/ui-expedition.js:46: trailing whitespace.
+    if (!b || b.disabled) return;
js/ui-expedition.js:47: trailing whitespace.
+    if (b.dataset.skillOwner) { owner = b.dataset.skillOwner; UI.refreshSheet(); }
js/ui-expedition.js:48: trailing whitespace.
+    if (b.dataset.skill && !Game.toggleSkill(owner, b.dataset.skill)) UI.toast("먼저 슬롯의 스킬 하나를 해제하세요");
js/ui-expedition.js:49: trailing whitespace.
+    if (b.dataset.levelSkill) Game.levelSkill(b.dataset.levelSkill);
js/ui-expedition.js:50: trailing whitespace.
+    if (b.id === "export-save" || b.id === "import-save") {
js/ui-expedition.js:51: trailing whitespace.
+      var exporting = b.id === "export-save";
js/ui-expedition.js:52: trailing whitespace.
+      el("save-editor").hidden = false;
js/ui-expedition.js:53: trailing whitespace.
+      el("save-json").value = exporting ? Game.exportSave() : "";
js/ui-expedition.js:54: trailing whitespace.
+      el("save-json").readOnly = exporting;
js/ui-expedition.js:55: trailing whitespace.
+      el("copy-save").hidden = !exporting; el("apply-save").hidden = exporting;
js/ui-expedition.js:56: trailing whitespace.
+      el("save-message").textContent = exporting ? "복사한 JSON을 안전한 곳에 보관하세요." : "불러올 JSON을 붙여 넣으세요.";
js/ui-expedition.js:57: trailing whitespace.
+    }
js/ui-expedition.js:58: trailing whitespace.
+    if (b.id === "copy-save") {
js/ui-expedition.js:59: trailing whitespace.
+      try {
js/ui-expedition.js:60: trailing whitespace.
+        if (!navigator.clipboard) throw new Error("clipboard unavailable");
js/ui-expedition.js:61: trailing whitespace.
+        await navigator.clipboard.writeText(el("save-json").value);
js/ui-expedition.js:62: trailing whitespace.
+        if (el("save-message")) el("save-message").textContent = "복사했습니다.";
js/ui-expedition.js:63: trailing whitespace.
+      } catch (error) {
js/ui-expedition.js:64: trailing whitespace.
+        if (el("save-json")) {
js/ui-expedition.js:65: trailing whitespace.
+          el("save-json").focus(); el("save-json").select();
js/ui-expedition.js:66: trailing whitespace.
+          el("save-message").textContent = "텍스트를 선택했습니다. 복사 메뉴 또는 Ctrl+C를 사용하세요.";
js/ui-expedition.js:67: trailing whitespace.
+        }
js/ui-expedition.js:68: trailing whitespace.
+      }
js/ui-expedition.js:69: trailing whitespace.
+    }
js/ui-expedition.js:70: trailing whitespace.
+    if (b.id === "apply-save") {
js/ui-expedition.js:71: trailing whitespace.
+      var json = el("save-json").value;
js/ui-expedition.js:72: trailing whitespace.
+      try { JSON.parse(json); } catch (error) { el("save-message").textContent = "올바른 JSON을 입력하세요."; return; }
js/ui-expedition.js:73: trailing whitespace.
+      if (!Game.validateSave(json)) { el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다."; return; }
js/ui-expedition.js:74: trailing whitespace.
+      if (!window.confirm("현재 진행 상황을 이 세이브로 교체할까요?")) return;
js/ui-expedition.js:75: trailing whitespace.
+      if (Game.importSave(json)) { el("sheet").close(); UI.toast("세이브를 불러왔습니다"); }
js/ui-expedition.js:76: trailing whitespace.
+      else el("save-message").textContent = "저장 형식이나 값이 올바르지 않습니다. 현재 진행은 유지됩니다.";
js/ui-expedition.js:77: trailing whitespace.
+    }
js/ui-expedition.js:78: trailing whitespace.
+  });
js/ui-expedition.js:79: trailing whitespace.
+  var overlay = document.createElement("dialog");
js/ui-expedition.js:80: trailing whitespace.
+  overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
js/ui-expedition.js:81: trailing whitespace.
+  overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
js/ui-expedition.js:83: trailing whitespace.
+    '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
js/ui-expedition.js:84: trailing whitespace.
+    '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
js/ui-expedition.js:85: trailing whitespace.
+  el("app").appendChild(overlay);
js/ui-expedition.js:86: trailing whitespace.
+  function revealOne(item) {
js/ui-expedition.js:87: trailing whitespace.
+    var card = document.createElement("div");
js/ui-expedition.js:88: trailing whitespace.
+    card.className = "fusion-card";
js/ui-expedition.js:90: trailing whitespace.
+    card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
js/ui-expedition.js:91: trailing whitespace.
+    el("report-items").appendChild(card);
js/ui-expedition.js:92: trailing whitespace.
+    if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
js/ui-expedition.js:93: trailing whitespace.
+  }
js/ui-expedition.js:94: trailing whitespace.
+  function progress() { el("report-progress").textContent = UI.fmt(shown) + ' / ' + UI.fmt(report.items.length) + ' 공개 · 탭하면 모두 공개'; }
js/ui-expedition.js:95: trailing whitespace.
+  function tick(now) {
js/ui-expedition.js:96: trailing whitespace.
+    if (!overlay.open) return;
js/ui-expedition.js:97: trailing whitespace.
+    var elapsed = now - started;
js/ui-expedition.js:98: trailing whitespace.
+    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 300)));
js/ui-expedition.js:99: trailing whitespace.
+    var count = Math.min(report.items.length, Math.max(0, Math.floor((elapsed - 300) / 200) + 1));
js/ui-expedition.js:100: trailing whitespace.
+    while (shown < count) revealOne(report.items[shown++]);
js/ui-expedition.js:101: trailing whitespace.
+    progress();
js/ui-expedition.js:102: trailing whitespace.
+    if (elapsed < 300 || shown < report.items.length) frame = requestAnimationFrame(tick);
js/ui-expedition.js:103: trailing whitespace.
+    else frame = null;
js/ui-expedition.js:104: trailing whitespace.
+  }
js/ui-expedition.js:105: trailing whitespace.
+  function skip() {
js/ui-expedition.js:106: trailing whitespace.
+    cancelAnimationFrame(frame); frame = null;
js/ui-expedition.js:107: trailing whitespace.
+    while (shown < report.items.length) revealOne(report.items[shown++]);
js/ui-expedition.js:108: trailing whitespace.
+    el("report-gold").textContent = UI.fmt(report.gold);
js/ui-expedition.js:109: trailing whitespace.
+    overlay.classList.add("reveal-skipped"); progress();
js/ui-expedition.js:110: trailing whitespace.
+  }
js/ui-expedition.js:111: trailing whitespace.
+  Game.on("offlineReport", function (reward) {
js/ui-expedition.js:112: trailing whitespace.
+    if (overlay.open) return;
js/ui-expedition.js:113: trailing whitespace.
+    report = reward; shown = 0; started = performance.now();
js/ui-expedition.js:114: trailing whitespace.
+    el("report-items").replaceChildren(); overlay.classList.remove("reveal-skipped");
js/ui-expedition.js:115: trailing whitespace.
+    var minutes = Math.floor(reward.elapsedMs / 60000);
js/ui-expedition.js:116: trailing whitespace.
+    el("report-duration").textContent = UI.fmt(Math.floor(minutes / 60)) + '시간 ' + UI.fmt(minutes % 60) + '분 동안 용병단이 싸웠습니다';
js/ui-expedition.js:117: trailing whitespace.
+    el("report-stats").textContent = UI.fmt(reward.kills) + ' 처치 · 용병별 경험치 +' + UI.fmt(reward.xp);
js/ui-expedition.js:118: trailing whitespace.
+    el("report-gold").textContent = '0';
js/ui-expedition.js:119: trailing whitespace.
+    overlay.showModal(); frame = requestAnimationFrame(tick);
js/ui-expedition.js:120: trailing whitespace.
+  });
js/ui-expedition.js:121: trailing whitespace.
+  overlay.addEventListener("cancel", function (event) { event.preventDefault(); skip(); });
js/ui-expedition.js:122: trailing whitespace.
+  overlay.addEventListener("click", function (event) {
js/ui-expedition.js:123: trailing whitespace.
+    if (event.target.closest("#harvest-report")) {
js/ui-expedition.js:124: trailing whitespace.
+      skip(); overlay.close(); Game.harvest(); report = null;
js/ui-expedition.js:125: trailing whitespace.
+    } else skip();
js/ui-expedition.js:126: trailing whitespace.
+  });
js/ui-expedition.js:127: trailing whitespace.
+  Game.on("skill", function (event) {
js/ui-expedition.js:128: trailing whitespace.
+    var sprite = el("sprite-" + event.id);
js/ui-expedition.js:129: trailing whitespace.
+    if (!sprite) return;
js/ui-expedition.js:130: trailing whitespace.
+    UI.attackSprite(sprite);
js/ui-expedition.js:131: trailing whitespace.
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
js/ui-expedition.js:132: trailing whitespace.
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 94);
js/ui-expedition.js:133: trailing whitespace.
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "skill-label"); label.textContent = event.name;
js/ui-expedition.js:134: trailing whitespace.
+    el("effect-layer").appendChild(label);
js/ui-expedition.js:135: trailing whitespace.
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
js/ui-expedition.js:136: trailing whitespace.
+  });
js/ui-expedition.js:137: trailing whitespace.
+  Game.on("skillBook", function (event) { UI.toast(mercName(event.owner) + ' 스킬북 획득'); });
js/ui-expedition.js:138: trailing whitespace.
+  UI.init();
js/ui-expedition.js:139: trailing whitespace.
+})();
js/ui.js:1: trailing whitespace.
+"use strict";
js/ui.js:2: trailing whitespace.
+
js/ui.js:3: trailing whitespace.
+var UI = (function () {
js/ui.js:4: trailing whitespace.
+  var svgNS = "http://www.w3.org/2000/svg";
js/ui.js:5: trailing whitespace.
+  var activeSheet = null;
js/ui.js:6: trailing whitespace.
+  var hiddenAt = null;
js/ui.js:7: trailing whitespace.
+  var sheetSignature = "";
js/ui.js:8: trailing whitespace.
+  var latestState;
js/ui.js:9: trailing whitespace.
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
js/ui.js:10: trailing whitespace.
+  function el(id) { return document.getElementById(id); }
js/ui.js:11: trailing whitespace.
+  function fmt(n) {
js/ui.js:12: trailing whitespace.
+    if (!Number.isFinite(n)) return "0";
js/ui.js:13: trailing whitespace.
+    var abs = Math.abs(n);
js/ui.js:14: trailing whitespace.
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
js/ui.js:15: trailing whitespace.
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
js/ui.js:16: trailing whitespace.
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
js/ui.js:17: trailing whitespace.
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
js/ui.js:18: trailing whitespace.
+  }
js/ui.js:19: trailing whitespace.
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
js/ui.js:20: trailing whitespace.
+  function svgNode(tag, attrs, content) {
js/ui.js:21: trailing whitespace.
+    var node = document.createElementNS(svgNS, tag);
js/ui.js:22: trailing whitespace.
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
js/ui.js:23: trailing whitespace.
+    if (content !== undefined) node.textContent = content;
js/ui.js:24: trailing whitespace.
+    return node;
js/ui.js:25: trailing whitespace.
+  }
js/ui.js:26: trailing whitespace.
+  function figure(type, color) {
js/ui.js:27: trailing whitespace.
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
js/ui.js:28: trailing whitespace.
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
js/ui.js:29: trailing whitespace.
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
js/ui.js:30: trailing whitespace.
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
js/ui.js:31: trailing whitespace.
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
js/ui.js:32: trailing whitespace.
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
js/ui.js:33: trailing whitespace.
+  }
js/ui.js:34: trailing whitespace.
+  function position(unit) {
js/ui.js:35: trailing whitespace.
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
js/ui.js:36: trailing whitespace.
+    if (unit.type === "boss") return { x: 284, y: 245 };
js/ui.js:37: trailing whitespace.
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
js/ui.js:38: trailing whitespace.
+  }
js/ui.js:39: trailing whitespace.
+  function drawUnit(unit, color, locked) {
js/ui.js:40: trailing whitespace.
+    var p = position(unit);
js/ui.js:41: trailing whitespace.
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
js/ui.js:42: trailing whitespace.
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
js/ui.js:43: trailing whitespace.
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
js/ui.js:44: trailing whitespace.
+    var body = svgNode("g", { class: "unit-body" });
js/ui.js:45: trailing whitespace.
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
js/ui.js:46: trailing whitespace.
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
js/ui.js:47: trailing whitespace.
+    var region = DATA.stages[Game.getState().currentStage].region;
js/ui.js:48: trailing whitespace.
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
js/ui.js:49: trailing whitespace.
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
js/ui.js:50: trailing whitespace.
+    body.addEventListener("animationend", function (event) {
js/ui.js:51: trailing whitespace.
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
js/ui.js:52: trailing whitespace.
+    });
js/ui.js:53: trailing whitespace.
+    group.appendChild(body);
js/ui.js:54: trailing whitespace.
+    if (!locked) {
js/ui.js:55: trailing whitespace.
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
js/ui.js:56: trailing whitespace.
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
js/ui.js:57: trailing whitespace.
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
js/ui.js:58: trailing whitespace.
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
js/ui.js:59: trailing whitespace.
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
js/ui.js:60: trailing whitespace.
+    return group;
js/ui.js:61: trailing whitespace.
+  }
js/ui.js:62: trailing whitespace.
+  function drawScene(state, clearEffects) {
js/ui.js:63: trailing whitespace.
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
js/ui.js:64: trailing whitespace.
+    if (clearEffects) el("effect-layer").replaceChildren();
js/ui.js:65: trailing whitespace.
+    el("mercenary-layer").replaceChildren();
js/ui.js:66: trailing whitespace.
+    el("enemy-layer").replaceChildren();
js/ui.js:67: trailing whitespace.
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
js/ui.js:68: trailing whitespace.
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
js/ui.js:69: trailing whitespace.
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
js/ui.js:70: trailing whitespace.
+    });
js/ui.js:71: trailing whitespace.
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
js/ui.js:72: trailing whitespace.
+  }
js/ui.js:73: trailing whitespace.
+  function animate(node, name) {
js/ui.js:74: trailing whitespace.
+    node.classList.remove(name);
js/ui.js:75: trailing whitespace.
+    void node.getBoundingClientRect();
js/ui.js:76: trailing whitespace.
+    node.classList.add(name);
js/ui.js:77: trailing whitespace.
+  }
js/ui.js:78: trailing whitespace.
+  function hit(event) {
js/ui.js:79: trailing whitespace.
+    var attacker = el("sprite-" + event.attackerId);
js/ui.js:80: trailing whitespace.
+    var target = el("sprite-" + event.targetId);
js/ui.js:81: trailing whitespace.
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
js/ui.js:82: trailing whitespace.
+    if (!target) return;
js/ui.js:83: trailing whitespace.
+    animate(target.querySelector(".unit-body"), "hit-flash");
js/ui.js:84: trailing whitespace.
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
js/ui.js:85: trailing whitespace.
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
js/ui.js:86: trailing whitespace.
+    (event.crit ? "✦ " : "") + fmt(event.amount));
js/ui.js:87: trailing whitespace.
+    el("effect-layer").appendChild(text);
js/ui.js:88: trailing whitespace.
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
js/ui.js:89: trailing whitespace.
+  }
js/ui.js:90: trailing whitespace.
+  function death(event) {
js/ui.js:91: trailing whitespace.
+    var target = el("sprite-" + event.id);
js/ui.js:92: trailing whitespace.
+    if (!target) return;
js/ui.js:93: trailing whitespace.
+    var ghost = target.cloneNode(true);
js/ui.js:94: trailing whitespace.
+    ghost.removeAttribute("id");
js/ui.js:95: trailing whitespace.
+    ghost.classList.add("death-fade");
js/ui.js:96: trailing whitespace.
+    el("effect-layer").appendChild(ghost);
js/ui.js:97: trailing whitespace.
+    target.classList.add("fallen");
js/ui.js:98: trailing whitespace.
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
js/ui.js:99: trailing whitespace.
+  }
js/ui.js:100: trailing whitespace.
+  function toast(message) {
js/ui.js:101: trailing whitespace.
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
js/ui.js:102: trailing whitespace.
+    surface.appendChild(el("toast"));
js/ui.js:103: trailing whitespace.
+    el("toast").textContent = message;
js/ui.js:104: trailing whitespace.
+    animate(el("toast"), "visible");
js/ui.js:105: trailing whitespace.
+  }
js/ui.js:106: trailing whitespace.
+  function update(state) {
js/ui.js:107: trailing whitespace.
+    latestState = state;
js/ui.js:108: trailing whitespace.
+    var stage = DATA.stages[state.currentStage];
js/ui.js:109: trailing whitespace.
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
js/ui.js:110: trailing whitespace.
+    if (UI.updateCP) UI.updateCP(Game.getCP());
js/ui.js:111: trailing whitespace.
+    else el("cp").textContent = fmt(Game.getCP());
js/ui.js:112: trailing whitespace.
+    UI.countUp("gold", state.gold);
js/ui.js:113: trailing whitespace.
+    UI.countUp("coins", state.squadCoins);
js/ui.js:114: trailing whitespace.
+    el("region-name").textContent = UI.artRegion(stage.region).name;
js/ui.js:115: trailing whitespace.
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
js/ui.js:116: trailing whitespace.
+    el("stage-id").textContent = stageLabel(state.currentStage);
js/ui.js:117: trailing whitespace.
+    el("stage-name").textContent = stage.name;
js/ui.js:118: trailing whitespace.
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
js/ui.js:119: trailing whitespace.
+    el("timer").classList.toggle("urgent", remaining <= 10);
js/ui.js:120: trailing whitespace.
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
js/ui.js:121: trailing whitespace.
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
js/ui.js:122: trailing whitespace.
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
js/ui.js:123: trailing whitespace.
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
js/ui.js:124: trailing whitespace.
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
js/ui.js:125: trailing whitespace.
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
js/ui.js:126: trailing whitespace.
+    if (UI.updateExpedition) UI.updateExpedition(state);
js/ui.js:127: trailing whitespace.
+    ["repeat", "challenge"].forEach(function (mode) {
js/ui.js:128: trailing whitespace.
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
js/ui.js:129: trailing whitespace.
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
js/ui.js:130: trailing whitespace.
+    });
js/ui.js:131: trailing whitespace.
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
js/ui.js:132: trailing whitespace.
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
js/ui.js:133: trailing whitespace.
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
js/ui.js:134: trailing whitespace.
+    var result = el("battle-result");
js/ui.js:135: trailing whitespace.
+    result.classList.toggle("shown", state.battle.status !== "fighting");
js/ui.js:136: trailing whitespace.
+    result.classList.toggle("failed", state.battle.status === "fail");
js/ui.js:137: trailing whitespace.
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
js/ui.js:138: trailing whitespace.
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
js/ui.js:139: trailing whitespace.
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
js/ui.js:140: trailing whitespace.
+      var sprite = el("sprite-" + unit.id);
js/ui.js:141: trailing whitespace.
+      if (sprite && sprite.querySelector(".hp-fill")) {
js/ui.js:142: trailing whitespace.
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
js/ui.js:143: trailing whitespace.
+        sprite.classList.toggle("fallen", unit.hp <= 0);
js/ui.js:144: trailing whitespace.
+      }
js/ui.js:145: trailing whitespace.
+    });
js/ui.js:146: trailing whitespace.
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
js/ui.js:147: trailing whitespace.
+      var definition = DATA.mercenaries[index];
js/ui.js:148: trailing whitespace.
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
js/ui.js:149: trailing whitespace.
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
js/ui.js:150: trailing whitespace.
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
js/ui.js:151: trailing whitespace.
+    }).join("");
js/ui.js:152: trailing whitespace.
+    if (activeSheet) renderSheet(state);
js/ui.js:153: trailing whitespace.
+  }
js/ui.js:154: trailing whitespace.
+  function mercenaryCards(state) {
js/ui.js:155: trailing whitespace.
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
js/ui.js:156: trailing whitespace.
+      var def = DATA.mercenaries[index];
js/ui.js:157: trailing whitespace.
+      var stats = Game.mercenaryStats(merc.id);
js/ui.js:158: trailing whitespace.
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
js/ui.js:159: trailing whitespace.
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
js/ui.js:160: trailing whitespace.
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
js/ui.js:161: trailing whitespace.
+          return UI.equipmentSlot(merc, slot, state);
js/ui.js:162: trailing whitespace.
+        }).join("") + '</div></article>';
js/ui.js:163: trailing whitespace.
+    }).join("");
js/ui.js:164: trailing whitespace.
+  }
js/ui.js:165: trailing whitespace.
+  function renderSheet(state) {
js/ui.js:166: trailing whitespace.
+    if (!activeSheet) return;
js/ui.js:167: trailing whitespace.
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
js/ui.js:168: trailing whitespace.
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
js/ui.js:169: trailing whitespace.
+    if (signature === sheetSignature) return;
js/ui.js:170: trailing whitespace.
+    sheetSignature = signature;
js/ui.js:171: trailing whitespace.
+    el("sheet-title").textContent = titles[activeSheet];
js/ui.js:172: trailing whitespace.
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
js/ui.js:173: trailing whitespace.
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
js/ui.js:174: trailing whitespace.
+    else if (activeSheet === "skills") UI.renderSkills(state);
js/ui.js:175: trailing whitespace.
+    else if (activeSheet === "settings") {
js/ui.js:176: trailing whitespace.
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
js/ui.js:177: trailing whitespace.
+      el("reset-game").addEventListener("click", function () {
js/ui.js:178: trailing whitespace.
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
js/ui.js:179: trailing whitespace.
+          Game.reset();
js/ui.js:180: trailing whitespace.
+          el("sheet").close();
js/ui.js:181: trailing whitespace.
+          toast("새로운 원정이 시작되었습니다");
js/ui.js:182: trailing whitespace.
+        }
js/ui.js:183: trailing whitespace.
+      });
js/ui.js:184: trailing whitespace.
+      UI.renderSaveControls();
js/ui.js:185: trailing whitespace.
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
js/ui.js:186: trailing whitespace.
+  }
js/ui.js:187: trailing whitespace.
+  function openSheet(name) {
js/ui.js:188: trailing whitespace.
+    activeSheet = name;
js/ui.js:189: trailing whitespace.
+    if (UI.sheetOpened) UI.sheetOpened(name);
js/ui.js:190: trailing whitespace.
+    sheetSignature = "";
js/ui.js:191: trailing whitespace.
+    renderSheet(latestState);
js/ui.js:192: trailing whitespace.
+    if (!el("sheet").open) el("sheet").showModal();
js/ui.js:193: trailing whitespace.
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
js/ui.js:194: trailing whitespace.
+  }
js/ui.js:195: trailing whitespace.
+  function init() {
js/ui.js:196: trailing whitespace.
+    Game.on("update", update);
js/ui.js:197: trailing whitespace.
+    Game.on("stageStart", function (state) { drawScene(state, true); });
js/ui.js:198: trailing whitespace.
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
js/ui.js:199: trailing whitespace.
+    Game.on("hit", hit);
js/ui.js:200: trailing whitespace.
+    Game.on("unitDeath", death);
js/ui.js:202: trailing whitespace.
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
js/ui.js:203: trailing whitespace.
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
js/ui.js:204: trailing whitespace.
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
js/ui.js:205: trailing whitespace.
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
js/ui.js:206: trailing whitespace.
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
js/ui.js:207: trailing whitespace.
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
js/ui.js:208: trailing whitespace.
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
js/ui.js:209: trailing whitespace.
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
js/ui.js:210: trailing whitespace.
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
js/ui.js:211: trailing whitespace.
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
js/ui.js:212: trailing whitespace.
+    el("sheet").addEventListener("close", function () {
js/ui.js:213: trailing whitespace.
+      if (el("sheet").open) return;
js/ui.js:214: trailing whitespace.
+      activeSheet = null;
js/ui.js:215: trailing whitespace.
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
js/ui.js:216: trailing whitespace.
+    });
js/ui.js:217: trailing whitespace.
+    document.addEventListener("visibilitychange", function () {
js/ui.js:218: trailing whitespace.
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
js/ui.js:219: trailing whitespace.
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
js/ui.js:220: trailing whitespace.
+    });
js/ui.js:221: trailing whitespace.
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
js/ui.js:222: trailing whitespace.
+    window.addEventListener("pageshow", function (event) {
js/ui.js:223: trailing whitespace.
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
js/ui.js:224: trailing whitespace.
+    });
js/ui.js:225: trailing whitespace.
+    Game.init();
js/ui.js:226: trailing whitespace.
+    if (!document.hidden) Game.resume();
js/ui.js:227: trailing whitespace.
+    else hiddenAt = Date.now();
js/ui.js:228: trailing whitespace.
+  }
js/ui.js:229: trailing whitespace.
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
js/ui.js:230: trailing whitespace.
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
js/ui.js:231: trailing whitespace.
+})();
style.css:1: trailing whitespace.
+@charset "UTF-8";
style.css:2: trailing whitespace.
+:root {
style.css:3: trailing whitespace.
+  color-scheme: light; font-family: "Jua", "Gothic A1", system-ui, sans-serif; font-synthesis: none;
style.css:4: trailing whitespace.
+  --bg-top: #8FD3FF; --bg-bottom: #EAF7FF; --panel: #FFF8E7; --panel-2: #FFE9B8;
style.css:5: trailing whitespace.
+  --ink: #4A2C12; --ink-soft: #8A6A4A; --primary: #FF9F1C; --primary-end: #FF6B00;
style.css:6: trailing whitespace.
+  --accent: #3DDC97; --danger: #FF5C5C; --gold: #FFC93C; --coin: #B388FF;
style.css:7: trailing whitespace.
+  --hp-ally: #5CE07A; --hp-enemy: #FF6B6B;
style.css:8: trailing whitespace.
+  --r-rare: #3B82F6; --r-epic: #A855F7; --r-unique: #FACC15; --r-legendary: #22C55E;
style.css:9: trailing whitespace.
+  color: var(--ink); background: linear-gradient(var(--bg-top), var(--bg-bottom)) fixed;
style.css:10: trailing whitespace.
+  font-size: 15px; font-variant-numeric: tabular-nums;
style.css:11: trailing whitespace.
+}
style.css:12: trailing whitespace.
+* { box-sizing: border-box; }
style.css:13: trailing whitespace.
+body { margin: 0; min-width: 280px; -webkit-tap-highlight-color: transparent; }
style.css:14: trailing whitespace.
+[hidden] { display: none !important; }
style.css:15: trailing whitespace.
+h1, h2, h3, h4, p { margin: 0; }
style.css:16: trailing whitespace.
+h1, h2 { font-size: 22px; } h3 { font-size: 18px; } small { font-size: 12px; }
style.css:17: trailing whitespace.
+button, select, textarea { font: inherit; color: inherit; }
style.css:18: trailing whitespace.
+button { cursor: pointer; touch-action: manipulation; min-width: 44px; min-height: 44px; padding: 8px;
style.css:19: trailing whitespace.
+  border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink);
style.css:20: trailing whitespace.
+  background: linear-gradient(var(--primary), var(--primary-end)); color: white; font-weight: 700;
style.css:21: trailing whitespace.
+  text-shadow: 0 2px 0 rgba(0,0,0,.25); transition: transform .12s, box-shadow .12s; }
style.css:22: trailing whitespace.
+button:active:not(:disabled) { transform: translateY(3px); box-shadow: none; }
style.css:23: trailing whitespace.
+button:disabled { cursor: default; opacity: .55; }
style.css:24: trailing whitespace.
+button:focus-visible, select:focus-visible, textarea:focus-visible { outline: 3px solid var(--r-rare); outline-offset: 3px; }
style.css:25: trailing whitespace.
+button, .battle-scene { -webkit-user-select: none; user-select: none; }
style.css:26: trailing whitespace.
+#app { max-width: 480px; margin: auto; min-height: 100vh; min-height: 100dvh; display: flex; flex-direction: column;
style.css:27: trailing whitespace.
+  position: relative; background: linear-gradient(var(--bg-top), var(--bg-bottom)); overflow: clip; }
style.css:28: trailing whitespace.
+.hud { margin: 10px 10px 0; padding: calc(10px + env(safe-area-inset-top)) 10px 12px; background: var(--panel);
style.css:29: trailing whitespace.
+  border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink); }
style.css:30: trailing whitespace.
+.brand-row { display: flex; align-items: center; gap: 8px; margin-bottom: 10px; }
style.css:31: trailing whitespace.
+.brand-mark { color: var(--primary-end); } .chapter-label { font-size: 12px; color: var(--ink-soft); }
style.css:32: trailing whitespace.
+.save-state { margin-left: auto; font-size: 12px; white-space: nowrap; color: var(--ink); }
style.css:33: trailing whitespace.
+.save-state::before { content: ''; display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
style.css:34: trailing whitespace.
+.resources { display: grid; grid-template-columns: 1.15fr 1fr 1fr; gap: 6px; }
style.css:35: trailing whitespace.
+.resource { min-width: 0; padding: 6px; border: 3px solid var(--ink); border-radius: 18px; background: var(--panel-2); }
style.css:36: trailing whitespace.
+.resource-label { display: flex; align-items: center; gap: 3px; font-size: 12px; white-space: nowrap; }
style.css:37: trailing whitespace.
+.resource strong { display: block; font-size: 23px; line-height: 1.1; margin-top: 2px; overflow-wrap: anywhere; }
style.css:38: trailing whitespace.
+.power strong { text-shadow: 0 0 8px var(--gold), 0 2px var(--gold); font-size: 26px; }
style.css:39: trailing whitespace.
+.asset-icon { display: inline-grid; place-items: center; position: relative; width: 30px; height: 30px; vertical-align: middle; flex: 0 0 auto; }
style.css:40: trailing whitespace.
+.asset-icon img, .asset-icon .asset-fallback { grid-area: 1 / 1; width: 100%; height: 100%; object-fit: contain; }
style.css:41: trailing whitespace.
+.asset-icon .asset-fallback { display: grid; place-items: center; }
style.css:42: trailing whitespace.
+.resource .asset-icon { width: 24px; height: 24px; }
style.css:43: trailing whitespace.
+.expedition { display: flex; flex: 1; flex-direction: column; min-height: 0; }
style.css:44: trailing whitespace.
+.stage-header { display: flex; justify-content: space-between; align-items: center; gap: 6px; padding: 16px 16px 9px; }
style.css:45: trailing whitespace.
+.stage-header h2 { font-size: 22px; } .stage-header > div:first-child { min-width: 0; }
style.css:46: trailing whitespace.
+.eyebrow { font-size: 13px; color: var(--ink-soft); margin-bottom: 4px; }
style.css:47: trailing whitespace.
+#stage-id { color: var(--ink); } .title-divider { margin: 0 5px; color: var(--ink-soft); }
style.css:48: trailing whitespace.
+.clock { text-align: right; flex-shrink: 0; } .clock > span { display: block; font-size: 12px; }
style.css:49: trailing whitespace.
+.clock strong { font-size: 26px; } .clock small { padding-left: 3px; }
style.css:50: trailing whitespace.
+.urgent { color: #B52B20; }
style.css:51: trailing whitespace.
+.time-track { height: 8px; background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; margin: 0 12px 9px; overflow: hidden; }
style.css:52: trailing whitespace.
+#time-fill { height: 100%; background: var(--gold); transition: width .1s linear; }
style.css:53: trailing whitespace.
+.battle-scene { position: relative; flex: 1; display: flex; flex-direction: column; justify-content: center; min-height: 290px;
style.css:54: trailing whitespace.
+  margin: 0 10px; border: 3px solid var(--ink); border-radius: 18px; box-shadow: 0 4px 0 var(--ink); background: var(--bg-top); isolation: isolate; overflow: hidden; }
style.css:55: trailing whitespace.
+.scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
style.css:56: trailing whitespace.
+.scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
style.css:57: trailing whitespace.
+.auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
style.css:58: trailing whitespace.
+#battle-svg { width: 100%; display: block; max-height: 430px; }
style.css:59: trailing whitespace.
+.scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
style.css:60: trailing whitespace.
+#region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
style.css:61: trailing whitespace.
+.locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
style.css:62: trailing whitespace.
+.hp-fill { transition: width .1s linear; } .fallen { visibility: hidden; }
style.css:63: trailing whitespace.
+.unit-body { transform-box: fill-box; transform-origin: center bottom; animation: breathe 2s ease-in-out infinite; }
style.css:64: trailing whitespace.
+.lunge-right { animation: lunge-right .12s ease-out; } .lunge-left { animation: lunge-left .12s ease-out; }
style.css:65: trailing whitespace.
+.hit-flash { animation: hit-flash .18s ease-out; }
style.css:66: trailing whitespace.
+.death-fade { animation: death-fade .45s ease-in forwards; pointer-events: none; } .death-fade .unit-body { animation: none; }
style.css:67: trailing whitespace.
+.damage-number, .skill-label, .level-up-label { fill: white; stroke: var(--ink); stroke-width: 3px; paint-order: stroke; font-size: 18px; font-weight: 800; animation: float-damage .8s ease-out forwards; pointer-events: none; }
style.css:68: trailing whitespace.
+.damage-number.critical { fill: var(--gold); font-size: 23px; } .damage-number.incoming { fill: var(--hp-enemy); font-size: 16px; }
style.css:69: trailing whitespace.
+.skill-label { fill: var(--panel); font-size: 13px; animation: skill-rise .8s ease-out both; }
style.css:70: trailing whitespace.
+.level-up-label { fill: var(--gold); font-size: 19px; transform-box: fill-box; transform-origin: center; animation: level-pop .85s ease-out both; }
style.css:71: trailing whitespace.
+.battle-result { display: none; position: absolute; z-index: 3; top: 28%; left: 10%; right: 10%; padding: 16px 10px; text-align: center;
style.css:72: trailing whitespace.
+  background: var(--panel); border: 3px solid var(--ink); box-shadow: 0 4px 0 var(--ink); border-radius: 18px; }
style.css:73: trailing whitespace.
+.battle-result.shown { display: grid; gap: 5px; animation: result-in .3s ease-out; }
style.css:74: trailing whitespace.
+.battle-result > span { color: var(--primary-end); font-size: 22px; } .battle-result strong { font-size: 22px; }
style.css:75: trailing whitespace.
+.battle-result.failed strong { color: #B52B20; }
style.css:76: trailing whitespace.
+.squad-strip { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 5px; padding: 12px 10px 4px; }
style.css:77: trailing whitespace.
+.squad-member { display: grid; grid-template-columns: 5px 1fr; column-gap: 5px; padding: 6px; font-size: 13px;
style.css:78: trailing whitespace.
+  border: 3px solid var(--ink); border-radius: 18px; background: var(--panel); }
style.css:79: trailing whitespace.
+.squad-member small { grid-column: 2; font-size: 12px; white-space: nowrap; } .member-dot { width: 5px; height: 14px; border-radius: 3px; }
style.css:80: trailing whitespace.
+.not-recruited { opacity: .7; }
style.css:81: trailing whitespace.
+.harvest-row { display: flex; justify-content: space-between; gap: 6px; padding: 6px 13px 8px; font-size: 12px; }
style.css:82: trailing whitespace.
+.harvest-row > span:first-child { display: none; } .harvest-row > span:last-child { margin: auto; }
style.css:83: trailing whitespace.
+.rate-divider { margin: 0 4px; }
style.css:84: trailing whitespace.
+.stage-controls { display: grid; grid-template-columns: 1fr 1.1fr; gap: 8px; padding: 9px 10px 12px; }
style.css:85: trailing whitespace.
+.stage-navigation { display: flex; align-items: center; justify-content: space-between; gap: 3px; }
style.css:86: trailing whitespace.
+.stage-navigation button { font-size: 24px; padding: 0; }
style.css:87: trailing whitespace.
+.stage-navigation div { text-align: center; } .stage-navigation div span { display: block; font-size: 12px; }
style.css:88: trailing whitespace.
+.stage-navigation strong { font-size: 17px; }
style.css:89: trailing whitespace.
+.mode-toggle { display: flex; gap: 5px; } .mode-toggle button { flex: 1; padding: 5px 2px; font-size: 13px; white-space: nowrap; }
style.css:90: trailing whitespace.
+#repeat-mode.active { background: var(--accent); } #challenge-mode.active { outline: 3px solid var(--gold); outline-offset: 1px; }
style.css:91: trailing whitespace.
+#mode-hint { grid-column: 1/-1; text-align: center; font-size: 12px; margin-top: 2px; }
style.css:92: trailing whitespace.
+.bottom-tabs { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 5px; padding: 10px 10px calc(10px + env(safe-area-inset-bottom)); background: var(--panel); border-top: 3px solid var(--ink); }
style.css:93: trailing whitespace.
+.bottom-tabs button { position: relative; background: var(--panel-2); color: var(--ink); text-shadow: none; display: flex; flex-direction: column; align-items: center; gap: 2px; font-size: 13px; padding: 3px 0; min-height: 64px; }
style.css:94: trailing whitespace.
+.bottom-tabs button > span { font-size: 25px; height: 30px; line-height: 30px; }
style.css:95: trailing whitespace.
+.bottom-tabs button.selected { transform: translateY(-6px); background: linear-gradient(var(--primary), var(--primary-end)); color: white; text-shadow: 0 2px 0 #0004; }
style.css:96: trailing whitespace.
+#equipment-badge { position: absolute; right: -4px; top: -9px; display: grid; place-items: center; border: 3px solid var(--ink); border-radius: 50%; min-width: 25px; height: 25px; padding: 0 3px; font-size: 12px; background: var(--danger); color: white; text-shadow: none; }
style.css:97: trailing whitespace.
+#toast { position: fixed; z-index: 20; width: max-content; max-width: min(90vw, 430px); left: 50%; bottom: 110px; transform: translateX(-50%); pointer-events: none; opacity: 0;
style.css:98: trailing whitespace.
+  background: var(--panel-2); color: var(--ink); padding: 12px 16px; border: 3px solid var(--ink); border-radius: 18px; font-size: 13px; box-shadow: 0 4px 0 var(--ink); }
style.css:99: trailing whitespace.
+#toast.visible { animation: toast 3.4s ease both; }
style.css:100: trailing whitespace.
+dialog { color: var(--ink); background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; margin: auto auto 6px; padding: 0 12px calc(20px + env(safe-area-inset-bottom));
style.css:101: trailing whitespace.
+  width: calc(100% - 12px); max-width: 468px; max-height: 78vh; max-height: 78dvh; overflow-y: auto; overscroll-behavior: contain; box-shadow: 0 4px 0 var(--ink); }
style.css:102: trailing whitespace.
+dialog::backdrop { background: #8FD3FF80; backdrop-filter: blur(3px); } dialog[open] { animation: sheet-in .22s ease-out; }
style.css:103: trailing whitespace.
+.sheet-grip { height: 5px; width: 38px; background: var(--ink-soft); border-radius: 18px; margin: 9px auto 12px; }
style.css:104: trailing whitespace.
+.sheet-heading { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin-bottom: 16px; }
style.css:105: trailing whitespace.
+.sheet-heading h2, #fusion-reveal h2, .report-heading h2 { display: inline-block; background: linear-gradient(var(--primary), var(--primary-end)); color: white;
style.css:106: trailing whitespace.
+  text-shadow: 0 2px #0004; padding: 7px 20px; border: 3px solid var(--ink); border-radius: 5px 18px 18px 5px; box-shadow: 0 4px 0 var(--ink); font-size: 22px; }
style.css:107: trailing whitespace.
+.sheet-heading button { font-size: 24px; padding: 0; } .sheet-intro { font-size: 13px; line-height: 1.6; margin-bottom: 16px; }
style.css:108: trailing whitespace.
+.merc-card, .skill-card, .reset-panel { background: var(--panel-2); border: 3px solid var(--ink); border-radius: 18px; padding: 12px; margin-bottom: 15px; box-shadow: 0 4px 0 var(--ink); }
style.css:109: trailing whitespace.
+.merc-card-heading { display: flex; gap: 10px; align-items: center; margin-bottom: 12px; }
style.css:110: trailing whitespace.
+.merc-card-heading svg { width: 64px; height: 70px; background: var(--panel); border-radius: 18px; }
style.css:111: trailing whitespace.
+.merc-card-heading p { font-size: 12px; } .merc-card-heading > strong { margin-left: auto; font-size: 17px; }
style.css:112: trailing whitespace.
+.xp-label { display: flex; justify-content: space-between; font-size: 12px; margin-bottom: 5px; }
style.css:113: trailing whitespace.
+.xp-track { height: 8px; background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; overflow: hidden; }
style.css:114: trailing whitespace.
+.xp-track > div { height: 100%; background: var(--accent); }
style.css:115: trailing whitespace.
+.merc-stats { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 10px 5px; margin: 14px 0; }
style.css:116: trailing whitespace.
+.merc-stats dt { font-size: 12px; margin-bottom: 3px; } .merc-stats dd { margin: 0; font-size: 15px; }
style.css:117: trailing whitespace.
+.equipment-slots { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 5px; }
style.css:118: trailing whitespace.
+.equipment-slot { min-width: 44px; min-height: 92px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px; padding: 4px 1px; background: var(--panel); color: var(--ink); text-shadow: none; }
style.css:119: trailing whitespace.
+.equipment-slot em { font-size: 12px; font-style: normal; overflow-wrap: anywhere; }
style.css:120: trailing whitespace.
+.locked-card { opacity: .65; } .unlock-note { font-size: 13px; padding: 7px 0; }
style.css:121: trailing whitespace.
+.placeholder, .equipment-empty { text-align: center; padding: 30px 5px; line-height: 1.8; } .placeholder > span { font-size: 40px; }
style.css:122: trailing whitespace.
+.settings-info { font-size: 13px; } .settings-info > div { display: flex; justify-content: space-between; gap: 8px; padding: 12px 0; border-bottom: 3px solid var(--panel-2); }
style.css:123: trailing whitespace.
+.settings-info dd { margin: 0; } .reset-panel { margin-top: 22px; } .reset-panel p { font-size: 13px; line-height: 1.7; margin: 8px 0 12px; }
style.css:124: trailing whitespace.
+.danger-button, button[data-action="sell"] { background: var(--danger); }
style.css:125: trailing whitespace.
+.rarity-rare { --rarity: var(--r-rare); } .rarity-epic { --rarity: var(--r-epic); } .rarity-unique { --rarity: var(--r-unique); } .rarity-legendary { --rarity: var(--r-legendary); }
style.css:126: trailing whitespace.
+.equipment-slot[class*="rarity-"], .inventory-cell, .item-detail, .item-card { border-color: var(--rarity); background: color-mix(in srgb, var(--rarity) 12%, var(--panel)); }
style.css:127: trailing whitespace.
+.inventory-heading, .inventory-sort, .fusion-heading, .equip-mode { display: flex; align-items: center; justify-content: space-between; gap: 8px; margin-bottom: 14px; }
style.css:128: trailing whitespace.
+.inventory-heading p, .fusion-heading p { font-size: 15px; } .inventory-heading strong { font-size: 17px; }
style.css:129: trailing whitespace.
+.inventory-filters { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 5px; margin-bottom: 14px; }
style.css:130: trailing whitespace.
+.inventory-filters button, .equip-actions button, .fusion-table button { font-size: 13px; padding: 5px 2px; }
style.css:131: trailing whitespace.
+.inventory-filters button.active, .skill-selector .active, .difficulty-toggle .active { background: var(--accent); }
style.css:132: trailing whitespace.
+.inventory-sort { font-size: 13px; } select { min-height: 44px; background: var(--panel-2); border: 3px solid var(--ink); border-radius: 18px; padding: 0 8px; }
style.css:133: trailing whitespace.
+/* DECISION: Compact sheet padding keeps all six existing inventory columns at least 44px at 375px. */
style.css:134: trailing whitespace.
+.inventory-grid { display: grid; grid-template-columns: repeat(6, minmax(44px, 1fr)); gap: 5px; }
style.css:135: trailing whitespace.
+.inventory-cell { position: relative; min-width: 44px; min-height: 80px; padding: 4px 0 16px; display: flex; flex-direction: column; align-items: center; justify-content: center; color: var(--ink); text-shadow: none; }
style.css:136: trailing whitespace.
+.inventory-cell b { font-size: 13px; } .inventory-cell em { position: absolute; right: 0; top: 0; font-size: 12px; font-style: normal; }
style.css:137: trailing whitespace.
+.inventory-cell i { position: absolute; bottom: 1px; font-size: 12px; font-style: normal; }
style.css:138: trailing whitespace.
+.selected-item { outline: 3px solid var(--ink); outline-offset: 1px; }
style.css:139: trailing whitespace.
+.equipment-note { font-size: 13px; line-height: 1.7; margin: 14px 0; } .equip-mode { font-size: 13px; padding: 8px; background: var(--panel-2); border-radius: 18px; }
style.css:140: trailing whitespace.
+.item-detail { border: 3px solid var(--rarity); border-radius: 18px; padding: 10px; margin-bottom: 16px; box-shadow: 0 4px 0 var(--ink); }
style.css:141: trailing whitespace.
+.detail-heading { display: flex; gap: 8px; align-items: center; } .detail-heading > div { flex: 1; min-width: 0; }
style.css:142: trailing whitespace.
+.detail-heading h3 { font-size: 17px; overflow-wrap: anywhere; } .detail-heading p { font-size: 12px; margin-top: 5px; }
style.css:143: trailing whitespace.
+.item-card { width: 62px; height: 88px; flex: 0 0 auto; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px;
style.css:144: trailing whitespace.
+  border: 3px solid var(--rarity); border-radius: 18px; color: var(--ink); box-shadow: 0 4px 0 var(--ink); }
style.css:145: trailing whitespace.
+.item-card .asset-icon { width: 36px; height: 36px; } .item-card b { font-size: 15px; } .item-card small { font-size: 12px; }
style.css:146: trailing whitespace.
+.base-stats { display: flex; flex-wrap: wrap; gap: 15px; margin: 16px 0; } .base-stats dt { font-size: 12px; margin-bottom: 3px; } .base-stats dd { margin: 0; }
style.css:147: trailing whitespace.
+.item-detail h4 { font-size: 15px; margin: 14px 0 8px; } .potential-lines { list-style: none; padding: 0; margin: 0; display: grid; gap: 8px; font-size: 13px; }
style.css:148: trailing whitespace.
+.potential-lines li { display: flex; justify-content: space-between; gap: 8px; } .potential-lines b { white-space: nowrap; }
style.css:149: trailing whitespace.
+.equip-actions { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 5px; } .detail-actions { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 12px; }
style.css:150: trailing whitespace.
+.detail-actions button { flex: 1 1 42%; font-size: 13px; }
style.css:151: trailing whitespace.
+.reroll-preview { margin-top: 15px; padding: 0 8px 12px; border: 3px solid var(--r-epic); border-radius: 18px; background: var(--panel); }
style.css:152: trailing whitespace.
+.reroll-after { animation: potential-in .4s ease-out both; }
style.css:153: trailing whitespace.
+.fusion-heading small { display: block; margin-top: 6px; } .fusion-table { width: 100%; border-collapse: collapse; font-size: 13px; text-align: center; table-layout: fixed; }
style.css:154: trailing whitespace.
+.fusion-table td, .fusion-table th { padding: 8px 2px; border-bottom: 3px solid var(--panel-2); } .fusion-table th:last-child { width: 32%; }
style.css:155: trailing whitespace.
+.fusion-table button { width: 100%; } .fusion-table small { display: block; font-size: 12px; margin-top: 2px; } .fusion-table .asset-icon { width: 25px; height: 25px; }
style.css:156: trailing whitespace.
+.drop-card { position: fixed; pointer-events: none; z-index: 9; animation: drop-flight 1.25s both; }
style.css:157: trailing whitespace.
+.drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
style.css:158: trailing whitespace.
+.battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
style.css:159: trailing whitespace.
+#fusion-reveal::after, #offline-report::after { position: fixed; }
style.css:160: trailing whitespace.
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
style.css:161: trailing whitespace.
+#cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
style.css:162: trailing whitespace.
+#fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
style.css:163: trailing whitespace.
+#reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
style.css:164: trailing whitespace.
+#reveal-cards:has(.fusion-card:only-child) { grid-template-columns: 1fr; } .fusion-card { min-width: 0; perspective: 600px; }
style.css:165: trailing whitespace.
+.flip-inner { position: relative; width: 82px; height: 110px; margin: auto; transform-style: preserve-3d; animation: item-flip .5s ease-out both; }
style.css:166: trailing whitespace.
+.card-back, .card-front { position: absolute; inset: 0; backface-visibility: hidden; border-radius: 18px; }
style.css:167: trailing whitespace.
+.card-back { display: grid; place-items: center; border: 3px solid var(--ink); color: white; font-size: 40px;
style.css:168: trailing whitespace.
+  background: repeating-linear-gradient(45deg, var(--primary), var(--primary) 8px, var(--gold) 8px, var(--gold) 16px); transform: rotateY(180deg); box-shadow: 0 4px 0 var(--ink); }
style.css:169: trailing whitespace.
+.card-front .item-card { width: 100%; height: 100%; } .card-front .asset-icon { width: 44px; height: 44px; }
style.css:170: trailing whitespace.
+.fusion-card p { margin-top: 8px; font-size: 13px; animation: potential-in .5s both; }
style.css:171: trailing whitespace.
+.rarity-upgrade .card-front { animation: fusion-flash .4s .2s both; } #reveal-skip { margin-top: 20px; width: 100%; }
style.css:172: trailing whitespace.
+.reveal-skipped .flip-inner, .reveal-skipped .fusion-card p, .reveal-skipped .card-front { animation: none; }
style.css:173: trailing whitespace.
+.difficulty-toggle { grid-column: 1/-1; display: flex; gap: 8px; } .difficulty-toggle button, .skill-selector button { flex: 1; }
style.css:174: trailing whitespace.
+.skill-selector, .active-skills, .save-buttons { display: flex; gap: 6px; } .skill-books { margin: 16px 0 12px; }
style.css:175: trailing whitespace.
+.active-skills span { flex: 1; min-width: 0; background: var(--panel-2); padding: 8px 3px; border: 3px solid var(--ink); border-radius: 18px; text-align: center; font-size: 12px; }
style.css:176: trailing whitespace.
+.skill-card { margin-top: 14px; } .equipped-skill { border-color: var(--accent); }
style.css:177: trailing whitespace.
+.skill-toggle { display: flex; width: 100%; justify-content: space-between; align-items: center; gap: 6px; text-align: left; }
style.css:178: trailing whitespace.
+.skill-toggle strong { font-size: 17px; } .skill-toggle b { font-size: 12px; white-space: nowrap; }
style.css:179: trailing whitespace.
+.skill-card p { font-size: 15px; line-height: 1.6; margin: 10px 0; } .skill-level { display: block; width: 100%; margin-top: 12px; }
style.css:180: trailing whitespace.
+.save-controls { margin-top: 20px; } .save-controls p { font-size: 13px; line-height: 1.7; margin: 8px 0; } .save-buttons button { flex: 1; font-size: 13px; }
style.css:181: trailing whitespace.
+#save-editor label { display: block; margin: 16px 0 8px; } #save-json { display: block; width: 100%; min-width: 0; max-width: 100%; padding: 10px;
style.css:182: trailing whitespace.
+  border: 3px solid var(--ink); border-radius: 18px; background: white; font: 13px monospace; resize: vertical; overflow-wrap: anywhere; }
style.css:183: trailing whitespace.
+#save-editor > button { margin-top: 10px; width: 100%; }
style.css:184: trailing whitespace.
+#offline-report { margin: auto; width: calc(100% - 8px); max-width: 480px; height: calc(100dvh - 8px); max-height: calc(100dvh - 8px); padding: 24px 12px 0; text-align: center; background: linear-gradient(var(--panel), var(--panel-2)); }
style.css:185: trailing whitespace.
+.report-heading h2 { margin: 10px 0 16px; } #report-duration { font-size: 15px; line-height: 1.7; }
style.css:186: trailing whitespace.
+.report-gold { padding: 22px 0 14px; } .report-gold > span { display: block; } .report-gold strong { display: block; margin-top: 10px; font-size: 44px; text-shadow: 0 0 8px var(--gold); }
style.css:187: trailing whitespace.
+#report-stats, #report-progress { font-size: 13px; margin-bottom: 16px; } #report-items { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; padding: 8px 0 24px; }
style.css:188: trailing whitespace.
+.book-card { --rarity: var(--coin); } .harvest-footer { position: sticky; bottom: 0; background: var(--panel); padding: 12px 0 max(20px, env(safe-area-inset-bottom)); z-index: 2; }
style.css:189: trailing whitespace.
+.harvest-footer p { font-size: 12px; margin-bottom: 10px; } #harvest-report { width: 100%; min-height: 58px; font-size: 18px; }
style.css:190: trailing whitespace.
+@keyframes breathe { 50% { translate: 0 -3px; } }
style.css:191: trailing whitespace.
+@keyframes lunge-right { 50% { translate: 10px 0; rotate: 4deg; } }
style.css:192: trailing whitespace.
+@keyframes lunge-left { 50% { translate: -8px 0; rotate: -4deg; } }
style.css:193: trailing whitespace.
+@keyframes hit-flash { 20%, 60% { filter: brightness(0) invert(1); } }
style.css:194: trailing whitespace.
+@keyframes death-fade { to { opacity: 0; translate: 0 10px; } }
style.css:195: trailing whitespace.
+@keyframes float-damage { 0% { opacity: 0; translate: 0 5px; } 15% { opacity: 1; } 100% { opacity: 0; translate: 0 -34px; } }
style.css:196: trailing whitespace.
+@keyframes skill-rise { to { opacity: 0; translate: 0 -25px; } }
style.css:197: trailing whitespace.
+@keyframes level-pop { 0% { scale: 0; } 25% { scale: 1.2; } 45% { scale: 1; } 100% { opacity: 0; translate: 0 -28px; } }
style.css:198: trailing whitespace.
+@keyframes result-in { from { opacity: 0; translate: 0 8px; } }
style.css:199: trailing whitespace.
+@keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
style.css:200: trailing whitespace.
+@keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
style.css:201: trailing whitespace.
+@keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
style.css:202: trailing whitespace.
+@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
style.css:203: trailing whitespace.
+@keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
style.css:204: trailing whitespace.
+@keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
style.css:205: trailing whitespace.
+@keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
style.css:206: trailing whitespace.
+@keyframes potential-in { from { opacity: 0; translate: 0 6px; } to { opacity: 1; translate: 0 0; } }
style.css:207: trailing whitespace.
+@media (max-width: 350px) { .chapter-label { display: none; } .resource { padding: 4px; } .stage-header h2 { font-size: 19px; } .inventory-grid { grid-template-columns: repeat(5, minmax(44px, 1fr)); } .flip-inner { width: 72px; } .stage-controls { grid-template-columns: 1fr 1fr; } }
style.css:208: trailing whitespace.
+@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: .01s !important; transition-duration: .01s !important; } .unit-body { animation: none; } #toast.visible { animation-duration: 3.4s !important; } }
test/run.js:1: trailing whitespace.
+"use strict";
test/run.js:2: trailing whitespace.
+
test/run.js:3: trailing whitespace.
+const assert = require("node:assert/strict");
test/run.js:4: trailing whitespace.
+const fs = require("node:fs");
test/run.js:5: trailing whitespace.
+const path = require("node:path");
test/run.js:6: trailing whitespace.
+const vm = require("node:vm");
test/run.js:7: trailing whitespace.
+
test/run.js:8: trailing whitespace.
+function context() {
test/run.js:9: trailing whitespace.
+  const storage = new Map();
test/run.js:10: trailing whitespace.
+  const intervals = new Map();
test/run.js:11: trailing whitespace.
+  let timerId = 0;
test/run.js:12: trailing whitespace.
+  const sandbox = vm.createContext({
test/run.js:13: trailing whitespace.
+    localStorage: { getItem: key => storage.get(key) || null, setItem: (key, value) => storage.set(key, value) },
test/run.js:14: trailing whitespace.
+    setInterval: (fn, ms) => { intervals.set(++timerId, { fn, ms }); return timerId; },
test/run.js:15: trailing whitespace.
+    clearInterval: id => intervals.delete(id)
test/run.js:16: trailing whitespace.
+  });
test/run.js:17: trailing whitespace.
+  for (const file of ["data", "battle", "game", "game-equipment", "game-expedition"]) {
test/run.js:18: trailing whitespace.
+    vm.runInContext(fs.readFileSync(path.join(__dirname, "../js/" + file + ".js"), "utf8"), sandbox, { filename: file + ".js" });
test/run.js:19: trailing whitespace.
+  }
test/run.js:20: trailing whitespace.
+  return { ...sandbox, storage, intervals };
test/run.js:21: trailing whitespace.
+}
test/run.js:22: trailing whitespace.
+function plain(value) { return JSON.parse(JSON.stringify(value)); }
test/run.js:23: trailing whitespace.
+function simulate(c, stage, squad = [{ id: "warrior", level: 1 }], emit) {
test/run.js:24: trailing whitespace.
+  const sim = c.Battle.start(stage, squad, emit);
test/run.js:25: trailing whitespace.
+  while (sim.status === "fighting") c.Battle.tick(sim, emit);
test/run.js:26: trailing whitespace.
+  return sim;
test/run.js:27: trailing whitespace.
+}
test/run.js:28: trailing whitespace.
+function untilResult(c) {
test/run.js:29: trailing whitespace.
+  for (let i = 0; i < 601 && c.Game.getState().battle.status === "fighting"; i++) c.Game.step();
test/run.js:30: trailing whitespace.
+  return c.Game.getState();
test/run.js:31: trailing whitespace.
+}
test/run.js:32: trailing whitespace.
+function fixture(c, stage, level = 1) {
test/run.js:33: trailing whitespace.
+  c.Game.reset();
test/run.js:34: trailing whitespace.
+  const state = c.Game.getState();
test/run.js:35: trailing whitespace.
+  state.currentStage = stage;
test/run.js:36: trailing whitespace.
+  state.unlockedStages = Array.from({ length: stage + 1 }, (_, i) => i);
test/run.js:37: trailing whitespace.
+  state.clearedStages = Array.from({ length: stage }, (_, i) => i);
test/run.js:38: trailing whitespace.
+  state.mercenaries.forEach((merc, i) => {
test/run.js:39: trailing whitespace.
+    merc.level = level;
test/run.js:40: trailing whitespace.
+    merc.unlocked = i === 0 || c.DATA.mercenaries[i].unlockStage < stage;
test/run.js:41: trailing whitespace.
+  });
test/run.js:42: trailing whitespace.
+  state.battle = c.Battle.start(stage, state.mercenaries.filter(m => m.unlocked));
test/run.js:43: trailing whitespace.
+  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
test/run.js:44: trailing whitespace.
+  assert.equal(c.Game.load(), true);
test/run.js:45: trailing whitespace.
+}
test/run.js:46: trailing whitespace.
+let passed = 0;
test/run.js:47: trailing whitespace.
+function test(name, fn) {
test/run.js:48: trailing whitespace.
+  try { fn(); console.log("PASS " + name); passed++; }
test/run.js:49: trailing whitespace.
+  catch (error) { console.error("FAIL " + name); console.error(error); process.exitCode = 1; }
test/run.js:50: trailing whitespace.
+}
test/run.js:51: trailing whitespace.
+
test/run.js:52: trailing whitespace.
+test("(a) fresh squad clears stage 1-1 within 60 seconds", () => {
test/run.js:53: trailing whitespace.
+  const c = context();
test/run.js:54: trailing whitespace.
+  const sim = simulate(c, 0);
test/run.js:55: trailing whitespace.
+  assert.equal(sim.status, "clear");
test/run.js:56: trailing whitespace.
+  assert.ok(sim.ticks <= 600);
test/run.js:57: trailing whitespace.
+  console.log("     Stage 1-1: " + sim.ticks / 10 + "s");
test/run.js:58: trailing whitespace.
+});
test/run.js:59: trailing whitespace.
+test("(b) stage 3-10 defeats a fresh level-1 squad, even with all classes", () => {
test/run.js:60: trailing whitespace.
+  const c = context();
test/run.js:61: trailing whitespace.
+  assert.equal(simulate(c, 29).status, "fail");
test/run.js:62: trailing whitespace.
+  assert.equal(simulate(c, 29, c.DATA.mercenaries.map(m => ({ id: m.id, level: 1 }))).status, "fail");
test/run.js:63: trailing whitespace.
+});
test/run.js:64: trailing whitespace.
+test("(c) save/load preserves the entire state and ongoing battle", () => {
test/run.js:65: trailing whitespace.
+  const c = context();
test/run.js:66: trailing whitespace.
+  for (let i = 0; i < 211; i++) c.Game.step();
test/run.js:67: trailing whitespace.
+  c.Game.setMode("repeat");
test/run.js:68: trailing whitespace.
+  const before = plain(c.Game.getState());
test/run.js:69: trailing whitespace.
+  c.Game.save();
test/run.js:70: trailing whitespace.
+  const stored = c.storage.get("squad_v1");
test/run.js:71: trailing whitespace.
+  c.Game.step();
test/run.js:72: trailing whitespace.
+  assert.equal(c.Game.load(), true);
test/run.js:73: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:74: trailing whitespace.
+  const other = context();
test/run.js:75: trailing whitespace.
+  other.storage.set("squad_v1", stored);
test/run.js:76: trailing whitespace.
+  assert.equal(other.Game.load(), true);
test/run.js:77: trailing whitespace.
+  for (let i = 0; i < 70; i++) { c.Game.step(); other.Game.step(); }
test/run.js:78: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState()), plain(other.Game.getState()));
test/run.js:79: trailing whitespace.
+});
test/run.js:80: trailing whitespace.
+test("(d) equal RNG seeds produce equal sequences (including seed zero)", () => {
test/run.js:81: trailing whitespace.
+  const c = context();
test/run.js:82: trailing whitespace.
+  for (const seed of [0, 42, 4294967295]) {
test/run.js:83: trailing whitespace.
+    c.Game.reset(seed);
test/run.js:84: trailing whitespace.
+    const first = Array.from({ length: 100 }, () => c.Game.rng());
test/run.js:85: trailing whitespace.
+    c.Game.reset(seed);
test/run.js:86: trailing whitespace.
+    assert.deepEqual(Array.from({ length: 100 }, () => c.Game.rng()), first);
test/run.js:87: trailing whitespace.
+    assert.ok(first.every(n => n >= 0 && n < 1));
test/run.js:88: trailing whitespace.
+  }
test/run.js:89: trailing whitespace.
+});
test/run.js:90: trailing whitespace.
+test("balance: unupgraded level-1 warrior clears 1-1 through 1-5 and fails 1-6", () => {
test/run.js:91: trailing whitespace.
+  const c = context();
test/run.js:92: trailing whitespace.
+  for (let stage = 0; stage < 6; stage++) {
test/run.js:93: trailing whitespace.
+    c.Game.reset();
test/run.js:94: trailing whitespace.
+    const sim = simulate(c, stage);
test/run.js:95: trailing whitespace.
+    assert.equal(sim.status, stage < 5 ? "clear" : "fail", "stage " + (stage + 1));
test/run.js:96: trailing whitespace.
+  }
test/run.js:97: trailing whitespace.
+});
test/run.js:98: trailing whitespace.
+test("P1 baseline without active skills first fails at 1-6, then gains XP on retry", () => {
test/run.js:99: trailing whitespace.
+  const c = context();
test/run.js:100: trailing whitespace.
+  c.DATA.defaultSkills.warrior = []; c.Game.reset();
test/run.js:101: trailing whitespace.
+  let failure;
test/run.js:102: trailing whitespace.
+  c.Game.on("stageFail", e => { failure = e; });
test/run.js:103: trailing whitespace.
+  for (let i = 0; i < 2400 && !failure; i++) c.Game.step();
test/run.js:104: trailing whitespace.
+  assert.equal(failure.stageIndex, 5);
test/run.js:105: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState().clearedStages), [0, 1, 2, 3, 4]);
test/run.js:106: trailing whitespace.
+  const xp = c.Game.getState().mercenaries[0].xp;
test/run.js:107: trailing whitespace.
+  for (let i = 0; i < 100; i++) c.Game.step();
test/run.js:108: trailing whitespace.
+  assert.notEqual(c.Game.getState().mercenaries[0].xp, xp);
test/run.js:109: trailing whitespace.
+});
test/run.js:110: trailing whitespace.
+test("repeat keeps stage; first-clear coins cannot be farmed; challenge advances", () => {
test/run.js:111: trailing whitespace.
+  const c = context();
test/run.js:112: trailing whitespace.
+  c.Game.setMode("repeat");
test/run.js:113: trailing whitespace.
+  untilResult(c);
test/run.js:114: trailing whitespace.
+  assert.equal(c.Game.getState().squadCoins, 10);
test/run.js:115: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState().unlockedStages), [0, 1]);
test/run.js:116: trailing whitespace.
+  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
test/run.js:117: trailing whitespace.
+  assert.equal(c.Game.getState().currentStage, 0);
test/run.js:118: trailing whitespace.
+  untilResult(c);
test/run.js:119: trailing whitespace.
+  assert.equal(c.Game.getState().squadCoins, 10);
test/run.js:120: trailing whitespace.
+  c.Game.setMode("challenge");
test/run.js:121: trailing whitespace.
+  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
test/run.js:122: trailing whitespace.
+  assert.equal(c.Game.getState().currentStage, 1);
test/run.js:123: trailing whitespace.
+});
test/run.js:124: trailing whitespace.
+test("archer unlocks only on 1-6 clear; mage only on 2-6 clear", () => {
test/run.js:125: trailing whitespace.
+  const c = context();
test/run.js:126: trailing whitespace.
+  fixture(c, 5, 50);
test/run.js:127: trailing whitespace.
+  assert.equal(c.Game.getState().mercenaries[1].unlocked, false);
test/run.js:128: trailing whitespace.
+  untilResult(c);
test/run.js:129: trailing whitespace.
+  assert.equal(c.Game.getState().mercenaries[1].unlocked, true);
test/run.js:130: trailing whitespace.
+  assert.equal(c.Game.getState().mercenaries[2].unlocked, false);
test/run.js:131: trailing whitespace.
+  fixture(c, 15, 100);
test/run.js:132: trailing whitespace.
+  untilResult(c);
test/run.js:133: trailing whitespace.
+  assert.equal(c.Game.getState().mercenaries[2].unlocked, true);
test/run.js:134: trailing whitespace.
+});
test/run.js:135: trailing whitespace.
+test("wave, hit, kill, death, clear and fail events; boss is the last wave", () => {
test/run.js:136: trailing whitespace.
+  const c = context();
test/run.js:137: trailing whitespace.
+  const events = [];
test/run.js:138: trailing whitespace.
+  simulate(c, 9, [{ id: "warrior", level: 150 }], (name, data) => events.push({ name, data }));
test/run.js:139: trailing whitespace.
+  assert.deepEqual(events.filter(e => e.name === "wave").map(e => e.data.wave), [1, 2, 3]);
test/run.js:140: trailing whitespace.
+  assert.equal(events.filter(e => e.name === "kill").length, 6);
test/run.js:141: trailing whitespace.
+  assert.equal(events.filter(e => e.name === "unitDeath").length, 6);
test/run.js:142: trailing whitespace.
+  assert.ok(events.some(e => e.name === "kill" && e.data.type === "boss"));
test/run.js:143: trailing whitespace.
+  assert.equal(events.at(-1).name, "stageClear");
test/run.js:144: trailing whitespace.
+  const fails = [];
test/run.js:145: trailing whitespace.
+  simulate(c, 29, undefined, (name, data) => { if (name === "stageFail") fails.push(data); });
test/run.js:146: trailing whitespace.
+  assert.equal(fails.length, 1);
test/run.js:147: trailing whitespace.
+});
test/run.js:148: trailing whitespace.
+test("frontline priority survives reversed squad order and switches after death", () => {
test/run.js:149: trailing whitespace.
+  const c = context();
test/run.js:150: trailing whitespace.
+  const sim = c.Battle.start(0, [{ id: "mage", level: 1 }, { id: "archer", level: 1 }, { id: "warrior", level: 1 }]);
test/run.js:151: trailing whitespace.
+  sim.units.forEach(u => { u.cooldown = 999; });
test/run.js:152: trailing whitespace.
+  sim.units[0].hp = 1;
test/run.js:153: trailing whitespace.
+  sim.enemies.forEach(u => { u.cooldown = 0; });
test/run.js:154: trailing whitespace.
+  const targets = [];
test/run.js:155: trailing whitespace.
+  c.Battle.tick(sim, (name, event) => { if (name === "hit" && event.side === "enemy") targets.push(event.targetId); });
test/run.js:156: trailing whitespace.
+  assert.deepEqual(targets, ["warrior", "archer"]);
test/run.js:157: trailing whitespace.
+});
test/run.js:158: trailing whitespace.
+test("defense, forced critical damage, dead units and exact 60s timeout", () => {
test/run.js:159: trailing whitespace.
+  const c = context();
test/run.js:160: trailing whitespace.
+  const sim = c.Battle.start(0, [{ id: "warrior", level: 1 }]);
test/run.js:161: trailing whitespace.
+  sim.units[0].critChance = 1;
test/run.js:162: trailing whitespace.
+  sim.units[0].atk = 10;
test/run.js:163: trailing whitespace.
+  sim.units[0].critDamage = 2;
test/run.js:164: trailing whitespace.
+  sim.enemies[0].def = 20;
test/run.js:165: trailing whitespace.
+  let hit;
test/run.js:166: trailing whitespace.
+  c.Battle.tick(sim, (name, event) => { if (name === "hit") hit = event; });
test/run.js:167: trailing whitespace.
+  assert.equal(hit.amount, 10);
test/run.js:168: trailing whitespace.
+  assert.equal(hit.crit, true);
test/run.js:169: trailing whitespace.
+  sim.units.concat(sim.enemies).forEach(u => { u.cooldown = 999; });
test/run.js:170: trailing whitespace.
+  let reason;
test/run.js:171: trailing whitespace.
+  while (sim.status === "fighting") c.Battle.tick(sim, (name, e) => { if (name === "stageFail") reason = e.reason; });
test/run.js:172: trailing whitespace.
+  assert.equal(sim.ticks, 600);
test/run.js:173: trailing whitespace.
+  assert.equal(reason, "timeout");
test/run.js:174: trailing whitespace.
+  c.Battle.tick(sim, () => assert.fail("Finished battle emitted again"));
test/run.js:175: trailing whitespace.
+});
test/run.js:176: trailing whitespace.
+test("failure restores squad HP and retries the same stage without currency penalty", () => {
test/run.js:177: trailing whitespace.
+  const c = context();
test/run.js:178: trailing whitespace.
+  fixture(c, 29);
test/run.js:179: trailing whitespace.
+  const before = c.Game.getState().gold;
test/run.js:180: trailing whitespace.
+  assert.equal(untilResult(c).battle.status, "fail");
test/run.js:181: trailing whitespace.
+  for (let i = 0; i < c.DATA.resultTicks; i++) c.Game.step();
test/run.js:182: trailing whitespace.
+  const state = c.Game.getState();
test/run.js:183: trailing whitespace.
+  assert.equal(state.currentStage, 29);
test/run.js:184: trailing whitespace.
+  assert.equal(state.battle.status, "fighting");
test/run.js:185: trailing whitespace.
+  assert.ok(state.battle.units.every(u => u.hp === u.maxHp));
test/run.js:186: trailing whitespace.
+  assert.ok(state.gold >= before);
test/run.js:187: trailing whitespace.
+});
test/run.js:188: trailing whitespace.
+test("catchUp below 60 seconds preserves deterministic fast-forward", () => {
test/run.js:189: trailing whitespace.
+  const a = context(); const b = context();
test/run.js:190: trailing whitespace.
+  assert.equal(a.Game.catchUp(59999), 599);
test/run.js:191: trailing whitespace.
+  for (let i = 0; i < 599; i++) b.Game.step();
test/run.js:192: trailing whitespace.
+  assert.deepEqual(plain(a.Game.getState()), plain(b.Game.getState()));
test/run.js:193: trailing whitespace.
+  assert.equal(a.Game.catchUp(-1), 0);
test/run.js:194: trailing whitespace.
+  assert.equal(a.Game.catchUp(NaN), 0);
test/run.js:195: trailing whitespace.
+  assert.equal(a.Game.catchUp(Infinity), 0);
test/run.js:196: trailing whitespace.
+  assert.equal(a.Game.catchUp(99), 0);
test/run.js:197: trailing whitespace.
+});
test/run.js:198: trailing whitespace.
+test("XP raises level and CP; the rolling five-minute window expires kills", () => {
test/run.js:199: trailing whitespace.
+  const c = context();
test/run.js:200: trailing whitespace.
+  const cp = c.Game.getCP();
test/run.js:201: trailing whitespace.
+  for (let i = 0; i < 1800; i++) c.Game.step();
test/run.js:202: trailing whitespace.
+  assert.ok(c.Game.getState().mercenaries[0].level > 1);
test/run.js:203: trailing whitespace.
+  assert.ok(c.Game.getCP() > cp);
test/run.js:204: trailing whitespace.
+  assert.ok(c.Game.getState().stats.goldPerSec > 0);
test/run.js:205: trailing whitespace.
+  fixture(c, 29);
test/run.js:206: trailing whitespace.
+  const state = c.Game.getState();
test/run.js:207: trailing whitespace.
+  state.stats = { elapsedMs: 300000, samples: [{ at: 0, gold: 5 }], goldPerSec: 1, killsPerSec: 1 };
test/run.js:208: trailing whitespace.
+  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
test/run.js:209: trailing whitespace.
+  assert.equal(c.Game.load(), true);
test/run.js:210: trailing whitespace.
+  c.Game.step();
test/run.js:211: trailing whitespace.
+  assert.equal(c.Game.getState().stats.goldPerSec, 0);
test/run.js:212: trailing whitespace.
+  assert.equal(c.Game.getState().stats.killsPerSec, 0);
test/run.js:213: trailing whitespace.
+});
test/run.js:214: trailing whitespace.
+test("locked stages and invalid modes are rejected; corrupted saves preserve live state", () => {
test/run.js:215: trailing whitespace.
+  const c = context();
test/run.js:216: trailing whitespace.
+  assert.equal(c.Game.selectStage(1), false);
test/run.js:217: trailing whitespace.
+  assert.equal(c.Game.selectStage(-1), false);
test/run.js:218: trailing whitespace.
+  assert.equal(c.Game.setMode("chaos"), false);
test/run.js:219: trailing whitespace.
+  const before = plain(c.Game.getState());
test/run.js:220: trailing whitespace.
+  for (const value of ["{", "null", '{"schemaVersion":2}', '{"schemaVersion":1,"state":{}}']) {
test/run.js:221: trailing whitespace.
+    c.storage.set("squad_v1", value);
test/run.js:222: trailing whitespace.
+    assert.equal(c.Game.load(), false);
test/run.js:223: trailing whitespace.
+    assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:224: trailing whitespace.
+  }
test/run.js:225: trailing whitespace.
+  const corrupt = plain(before);
test/run.js:226: trailing whitespace.
+  corrupt.battle.enemies[0].position = 99;
test/run.js:227: trailing whitespace.
+  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: 1, savedAt: Date.now(), state: corrupt }));
test/run.js:228: trailing whitespace.
+  assert.equal(c.Game.load(), false);
test/run.js:229: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:230: trailing whitespace.
+});
test/run.js:231: trailing whitespace.
+test("runtime installs only 100ms combat and 3s autosave timers; pause clears both", () => {
test/run.js:232: trailing whitespace.
+  const c = context();
test/run.js:233: trailing whitespace.
+  c.Game.resume(); c.Game.resume();
test/run.js:234: trailing whitespace.
+  assert.deepEqual([...c.intervals.values()].map(t => t.ms), [100, 3000]);
test/run.js:235: trailing whitespace.
+  c.Game.pause();
test/run.js:236: trailing whitespace.
+  assert.equal(c.intervals.size, 0);
test/run.js:237: trailing whitespace.
+  assert.equal(JSON.parse(c.storage.get("squad_v1")).schemaVersion, 3);
test/run.js:238: trailing whitespace.
+});
test/run.js:239: trailing whitespace.
+test("all 30 stages, equipment data and P3 default skills are defined", () => {
test/run.js:240: trailing whitespace.
+  const c = context();
test/run.js:241: trailing whitespace.
+  assert.equal(c.DATA.stages.length, 30);
test/run.js:242: trailing whitespace.
+  assert.equal(c.DATA.equipmentTiers.length, 8);
test/run.js:243: trailing whitespace.
+  c.DATA.stages.forEach((stage, i) => {
test/run.js:244: trailing whitespace.
+    assert.equal(stage.index, i);
test/run.js:245: trailing whitespace.
+    assert.equal(stage.timeLimit, 60);
test/run.js:246: trailing whitespace.
+    assert.equal(stage.boss, i % 10 === 9);
test/run.js:247: trailing whitespace.
+    assert.equal(stage.waves.length, 3);
test/run.js:248: trailing whitespace.
+    stage.waves.forEach((wave, n) => assert.ok(stage.boss && n === 2 ? wave.length === 1 : wave.length >= 2 && wave.length <= 4));
test/run.js:249: trailing whitespace.
+  });
test/run.js:250: trailing whitespace.
+  assert.equal(c.Game.getState().inventory.length, 0);
test/run.js:251: trailing whitespace.
+  assert.ok(c.Game.getState().battle.units.every(u => u.skills.length === 3 && u.skills.every(s => c.DATA.skills.some(d => d.id === s))));
test/run.js:252: trailing whitespace.
+});
test/run.js:253: trailing whitespace.
+function setState(c, state) {
test/run.js:254: trailing whitespace.
+  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
test/run.js:255: trailing whitespace.
+  assert.equal(c.Game.load(), true);
test/run.js:256: trailing whitespace.
+}
test/run.js:257: trailing whitespace.
+function itemsOf(c, count, slot = "weapon", tier = 1, rarity = "rare") {
test/run.js:258: trailing whitespace.
+  const items = [];
test/run.js:259: trailing whitespace.
+  while (items.length < count) {
test/run.js:260: trailing whitespace.
+    const item = c.Game.rollItem(0, { forcedTier: tier });
test/run.js:261: trailing whitespace.
+    if (item.slot === slot && item.rarity === rarity) items.push(item);
test/run.js:262: trailing whitespace.
+  }
test/run.js:263: trailing whitespace.
+  return items;
test/run.js:264: trailing whitespace.
+}
test/run.js:265: trailing whitespace.
+function stock(c, items, gold = 1000000, coins = 100) {
test/run.js:266: trailing whitespace.
+  const state = c.Game.getState();
test/run.js:267: trailing whitespace.
+  state.inventory = items;
test/run.js:268: trailing whitespace.
+  state.gold = gold; state.squadCoins = coins;
test/run.js:269: trailing whitespace.
+  state.mercenaries.forEach(m => { m.equipment = { weapon: null, hat: null, gloves: null, shoes: null }; });
test/run.js:270: trailing whitespace.
+  setState(c, state);
test/run.js:271: trailing whitespace.
+}
test/run.js:272: trailing whitespace.
+test("P2: 2,000 rolls per stage band respect tier bounds, boss tiers, rarity lines and ranges", () => {
test/run.js:273: trailing whitespace.
+  const c = context();
test/run.js:274: trailing whitespace.
+  for (const band of c.DATA.dropTables) {
test/run.js:275: trailing whitespace.
+    for (let n = 0; n < 2000; n++) {
test/run.js:276: trailing whitespace.
+      const item = c.Game.rollItem(n % 2 ? band.min : band.max);
test/run.js:277: trailing whitespace.
+      assert.ok(band.tierWeights[item.tier] > 0);
test/run.js:278: trailing whitespace.
+      if (band.guaranteedTier) assert.equal(item.tier, band.guaranteedTier);
test/run.js:279: trailing whitespace.
+      assert.equal(item.potentials.length, c.DATA.equipmentRarities[item.rarity].lines);
test/run.js:280: trailing whitespace.
+      const rank = c.DATA.rarityOrder.indexOf(item.rarity);
test/run.js:281: trailing whitespace.
+      for (const p of item.potentials) {
test/run.js:282: trailing whitespace.
+        const option = c.DATA.potentialPool.find(o => o.id === p.id);
test/run.js:283: trailing whitespace.
+        assert.ok(option.weights[rank] > 0 && p.value >= option.ranges[rank][0] && p.value <= option.ranges[rank][1]);
test/run.js:284: trailing whitespace.
+      }
test/run.js:285: trailing whitespace.
+    }
test/run.js:286: trailing whitespace.
+  }
test/run.js:287: trailing whitespace.
+  for (let tier = 1; tier <= 8; tier++) assert.equal(c.Game.rollItem(0, { forcedTier: tier }).tier, tier);
test/run.js:288: trailing whitespace.
+  assert.equal(c.Game.rollItem(-1), null);
test/run.js:289: trailing whitespace.
+  assert.equal(c.Game.rollItem(30), null);
test/run.js:290: trailing whitespace.
+  assert.equal(c.Game.rollItem(0, { forcedTier: 9 }), null);
test/run.js:291: trailing whitespace.
+});
test/run.js:292: trailing whitespace.
+test("P2: every real boss kill drops its guaranteed tier, and fresh play drops in two minutes", () => {
test/run.js:293: trailing whitespace.
+  const c = context();
test/run.js:294: trailing whitespace.
+  for (const stage of [9, 19, 29]) {
test/run.js:295: trailing whitespace.
+    fixture(c, stage, 150);
test/run.js:296: trailing whitespace.
+    const drops = [];
test/run.js:297: trailing whitespace.
+    const off = c.Game.on("itemDrop", item => drops.push(item));
test/run.js:298: trailing whitespace.
+    untilResult(c); off();
test/run.js:299: trailing whitespace.
+    assert.ok(drops.some(i => i.enemyId === "enemy-2-0" && i.tier === Math.floor(stage / 10) + 2));
test/run.js:300: trailing whitespace.
+  }
test/run.js:301: trailing whitespace.
+  c.Game.reset();
test/run.js:302: trailing whitespace.
+  for (let n = 0; n < 1200; n++) c.Game.step();
test/run.js:303: trailing whitespace.
+  console.log("     Fresh 120s drops: " + c.Game.getState().inventory.length);
test/run.js:304: trailing whitespace.
+  assert.ok(c.Game.getState().inventory.length >= 3);
test/run.js:305: trailing whitespace.
+});
test/run.js:306: trailing whitespace.
+test("P2: fusion validates three distinct matching unlocked unequipped materials and gold", () => {
test/run.js:307: trailing whitespace.
+  const c = context();
test/run.js:308: trailing whitespace.
+  const items = itemsOf(c, 3).concat(itemsOf(c, 1, "hat"), itemsOf(c, 1, "weapon", 2));
test/run.js:309: trailing whitespace.
+  stock(c, items, 99);
test/run.js:310: trailing whitespace.
+  const uids = items.slice(0, 3).map(i => i.uid);
test/run.js:311: trailing whitespace.
+  assert.equal(c.Game.fuse(uids), null);
test/run.js:312: trailing whitespace.
+  stock(c, items);
test/run.js:313: trailing whitespace.
+  const before = plain(c.Game.getState());
test/run.js:314: trailing whitespace.
+  for (const bad of [[], uids.slice(0, 2), [uids[0], uids[0], uids[1]], [uids[0], uids[1], "missing"],
test/run.js:315: trailing whitespace.
+    [uids[0], uids[1], items[3].uid], [uids[0], uids[1], items[4].uid]]) assert.equal(c.Game.fuse(bad), null);
test/run.js:316: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:317: trailing whitespace.
+  c.Game.toggleLock(uids[0]); assert.equal(c.Game.fuse(uids), null); c.Game.toggleLock(uids[0]);
test/run.js:318: trailing whitespace.
+  c.Game.equip(uids[0], "warrior"); assert.equal(c.Game.fuse(uids), null); c.Game.unequip("warrior", "weapon");
test/run.js:319: trailing whitespace.
+  const result = c.Game.fuse(uids);
test/run.js:320: trailing whitespace.
+  assert.equal(result.tier, 2); assert.equal(result.slot, "weapon");
test/run.js:321: trailing whitespace.
+  assert.equal(c.Game.getState().gold, 999900);
test/run.js:322: trailing whitespace.
+  assert.ok(!c.Game.getState().inventory.some(i => uids.includes(i.uid)));
test/run.js:323: trailing whitespace.
+  stock(c, itemsOf(c, 3, "weapon", 8));
test/run.js:324: trailing whitespace.
+  assert.equal(c.Game.fuse(c.Game.getState().inventory.map(i => i.uid)), null);
test/run.js:325: trailing whitespace.
+});
test/run.js:326: trailing whitespace.
+test("P2: rarity bumps at 0.15 ± 0.02 over 5,000 actual fusions; highest rarity is retained", () => {
test/run.js:327: trailing whitespace.
+  const c = context();
test/run.js:328: trailing whitespace.
+  let bumps = 0;
test/run.js:329: trailing whitespace.
+  for (let n = 0; n < 5000; n++) {
test/run.js:330: trailing whitespace.
+    const items = itemsOf(c, 3);
test/run.js:331: trailing whitespace.
+    stock(c, items);
test/run.js:332: trailing whitespace.
+    const item = c.Game.fuse(items.map(i => i.uid));
test/run.js:333: trailing whitespace.
+    if (item.rarity === "epic") bumps++;
test/run.js:334: trailing whitespace.
+    else assert.equal(item.rarity, "rare");
test/run.js:335: trailing whitespace.
+  }
test/run.js:336: trailing whitespace.
+  console.log("     Fusion bump rate: " + bumps / 5000);
test/run.js:337: trailing whitespace.
+  assert.ok(Math.abs(bumps / 5000 - 0.15) <= 0.02);
test/run.js:338: trailing whitespace.
+  const mixed = itemsOf(c, 2).concat(itemsOf(c, 1, "weapon", 1, "legendary"));
test/run.js:339: trailing whitespace.
+  stock(c, mixed);
test/run.js:340: trailing whitespace.
+  assert.equal(c.Game.fuse(mixed.map(i => i.uid)).rarity, "legendary");
test/run.js:341: trailing whitespace.
+});
test/run.js:342: trailing whitespace.
+test("P2: autoFuse cascades lowest tier first, respects protection, gold and tier 8", () => {
test/run.js:343: trailing whitespace.
+  const c = context();
test/run.js:344: trailing whitespace.
+  const items = itemsOf(c, 27).concat(itemsOf(c, 6, "hat"), itemsOf(c, 3, "shoes", 8));
test/run.js:345: trailing whitespace.
+  items[27].locked = true;
test/run.js:346: trailing whitespace.
+  stock(c, items);
test/run.js:347: trailing whitespace.
+  c.Game.equip(items[28].uid, "warrior");
test/run.js:348: trailing whitespace.
+  const results = c.Game.autoFuse();
test/run.js:349: trailing whitespace.
+  assert.equal(results.at(-1).tier, 4);
test/run.js:350: trailing whitespace.
+  assert.ok(results.slice(0, 10).every(i => i.tier === 2));
test/run.js:351: trailing whitespace.
+  const state = c.Game.getState();
test/run.js:352: trailing whitespace.
+  assert.equal(state.inventory.find(i => i.uid === items[27].uid).locked, true);
test/run.js:353: trailing whitespace.
+  assert.equal(state.inventory.find(i => i.uid === items[28].uid).equippedBy, "warrior");
test/run.js:354: trailing whitespace.
+  for (const slot of c.DATA.equipmentSlots) for (let t = 1; t < 8; t++) {
test/run.js:355: trailing whitespace.
+    assert.ok(state.inventory.filter(i => i.slot === slot.id && i.tier === t && !i.locked && !i.equippedBy).length < 3);
test/run.js:356: trailing whitespace.
+  }
test/run.js:357: trailing whitespace.
+  c.Game.unequip("warrior", "hat");
test/run.js:358: trailing whitespace.
+  stock(c, itemsOf(c, 9), 100);
test/run.js:359: trailing whitespace.
+  assert.equal(c.Game.autoFuse().length, 1);
test/run.js:360: trailing whitespace.
+  assert.equal(c.Game.getState().gold, 0);
test/run.js:361: trailing whitespace.
+});
test/run.js:362: trailing whitespace.
+test("P2: equip raises CP immediately, preserves HP ratio and unequip restores CP", () => {
test/run.js:363: trailing whitespace.
+  const c = context();
test/run.js:364: trailing whitespace.
+  const items = itemsOf(c, 1).concat(itemsOf(c, 1, "hat"));
test/run.js:365: trailing whitespace.
+  stock(c, items);
test/run.js:366: trailing whitespace.
+  for (let i = 0; i < 20; i++) c.Game.step();
test/run.js:367: trailing whitespace.
+  const cp = c.Game.getCP(), unit = c.Game.getState().battle.units[0];
test/run.js:368: trailing whitespace.
+  assert.equal(c.Game.equip(items[0].uid, "archer"), false);
test/run.js:369: trailing whitespace.
+  c.Game.equip(items[0].uid, "warrior"); c.Game.equip(items[1].uid, "warrior");
test/run.js:370: trailing whitespace.
+  assert.ok(c.Game.getCP() > cp);
test/run.js:371: trailing whitespace.
+  const after = c.Game.getState().battle.units[0];
test/run.js:372: trailing whitespace.
+  assert.ok(after.atk > unit.atk && after.maxHp > unit.maxHp);
test/run.js:373: trailing whitespace.
+  assert.ok(Math.abs(after.hp / after.maxHp - unit.hp / unit.maxHp) < 1e-12);
test/run.js:374: trailing whitespace.
+  c.Game.unequip("warrior", "weapon"); c.Game.unequip("warrior", "hat");
test/run.js:375: trailing whitespace.
+  assert.equal(c.Game.getCP(), cp);
test/run.js:376: trailing whitespace.
+  assert.equal(c.Game.getState().inventory[0].equippedBy, null);
test/run.js:377: trailing whitespace.
+});
test/run.js:378: trailing whitespace.
+test("P2: sell is atomic, lock protects materials, reroll charges 20 and works while locked/equipped", () => {
test/run.js:379: trailing whitespace.
+  const c = context();
test/run.js:380: trailing whitespace.
+  const items = itemsOf(c, 3);
test/run.js:381: trailing whitespace.
+  stock(c, items, 0, 20);
test/run.js:382: trailing whitespace.
+  c.Game.toggleLock(items[0].uid);
test/run.js:383: trailing whitespace.
+  assert.equal(c.Game.sell([items[0].uid, items[1].uid]), false);
test/run.js:384: trailing whitespace.
+  c.Game.equip(items[1].uid, "warrior");
test/run.js:385: trailing whitespace.
+  assert.equal(c.Game.sell([items[1].uid]), false);
test/run.js:386: trailing whitespace.
+  assert.equal(c.Game.sell([items[2].uid, items[2].uid]), false);
test/run.js:387: trailing whitespace.
+  assert.equal(c.Game.sell(["missing"]), false);
test/run.js:388: trailing whitespace.
+  assert.equal(c.Game.sell([items[2].uid]), c.DATA.sellPrice(1, "rare"));
test/run.js:389: trailing whitespace.
+  c.Game.equip(items[0].uid, "warrior");
test/run.js:390: trailing whitespace.
+  let event; c.Game.on("potentialReroll", e => { event = e; });
test/run.js:391: trailing whitespace.
+  const rolled = c.Game.rerollPotentials(items[0].uid);
test/run.js:392: trailing whitespace.
+  assert.equal(rolled.locked, true); assert.equal(rolled.equippedBy, "warrior");
test/run.js:393: trailing whitespace.
+  assert.equal(event.before.length, event.after.length);
test/run.js:394: trailing whitespace.
+  assert.equal(c.Game.getState().squadCoins, 0);
test/run.js:395: trailing whitespace.
+  assert.equal(c.Game.rerollPotentials(items[0].uid), null);
test/run.js:396: trailing whitespace.
+});
test/run.js:397: trailing whitespace.
+test("P2: schema v1 migration and populated v2 round-trip; bad equipment saves rejected", () => {
test/run.js:398: trailing whitespace.
+  const c = context();
test/run.js:399: trailing whitespace.
+  const legacy = c.Game.getState();
test/run.js:400: trailing whitespace.
+  legacy.schemaVersion = 1;
test/run.js:401: trailing whitespace.
+  delete legacy.inventory; delete legacy.overflow; delete legacy.nextItemUid;
test/run.js:402: trailing whitespace.
+  legacy.mercenaries.forEach(m => { delete m.equipment; });
test/run.js:403: trailing whitespace.
+  c.storage.set("squad_v1", JSON.stringify({ schemaVersion: 1, savedAt: Date.now(), state: legacy }));
test/run.js:404: trailing whitespace.
+  assert.equal(c.Game.load(), true);
test/run.js:405: trailing whitespace.
+  assert.equal(c.Game.getState().schemaVersion, 3);
test/run.js:406: trailing whitespace.
+  assert.deepEqual(plain(c.Game.getState().inventory), []);
test/run.js:407: trailing whitespace.
+  const item = itemsOf(c, 1)[0]; stock(c, [item]); c.Game.equip(item.uid, "warrior");
test/run.js:408: trailing whitespace.
+  c.Game.save(); const before = plain(c.Game.getState());
test/run.js:409: trailing whitespace.
+  assert.equal(c.Game.load(), true); assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:410: trailing whitespace.
+  for (const corrupt of [s => { s.inventory.push(s.inventory[0]); }, s => { s.inventory[0].base.atk = 999; },
test/run.js:411: trailing whitespace.
+    s => { s.mercenaries[0].equipment.weapon = null; }, s => { s.inventory[0].potentials[0].value = 500; }]) {
test/run.js:412: trailing whitespace.
+    const state = plain(before); corrupt(state);
test/run.js:413: trailing whitespace.
+    c.storage.set("squad_v1", JSON.stringify({ schemaVersion: c.DATA.schemaVersion, savedAt: Date.now(), state }));
test/run.js:414: trailing whitespace.
+    assert.equal(c.Game.load(), false); assert.deepEqual(plain(c.Game.getState()), before);
test/run.js:415: trailing whitespace.
+  }
test/run.js:416: trailing whitespace.
+});
test/run.js:417: trailing whitespace.
+test("P2: inventory cap sells lowest eligible rare; fully protected bag monetizes incoming drop", () => {
test/run.js:418: trailing whitespace.
+  const c = context();
test/run.js:419: trailing whitespace.
+  fixture(c, 9, 150);
test/run.js:420: trailing whitespace.
+  const items = itemsOf(c, 60, "weapon", 3);
test/run.js:421: trailing whitespace.
+  items[0].locked = true;
test/run.js:422: trailing whitespace.
+  stock(c, items);
test/run.js:423: trailing whitespace.
+  c.Game.equip(items[1].uid, "warrior");
test/run.js:424: trailing whitespace.
+  const events = [], drops = [];
test/run.js:425: trailing whitespace.
+  c.Game.on("inventoryFull", e => events.push(e)); c.Game.on("itemDrop", e => drops.push(e));
test/run.js:426: trailing whitespace.
+  untilResult(c);
test/run.js:427: trailing whitespace.
+  assert.ok(events.length >= 1); assert.equal(events.length, drops.length);
test/run.js:428: trailing whitespace.
+  assert.equal(c.Game.getState().inventory.length, 60);
test/run.js:429: trailing whitespace.
+  assert.ok(events.every(e => e.sold.tier === 2 && !e.kept || e.sold.tier === 3 && e.kept));
test/run.js:430: trailing whitespace.
+  assert.ok(c.Game.getState().inventory.some(i => i.uid === items[0].uid));
test/run.js:431: trailing whitespace.
+  assert.ok(c.Game.getState().inventory.some(i => i.uid === items[1].uid));
test/run.js:432: trailing whitespace.
+  fixture(c, 9, 150);
test/run.js:433: trailing whitespace.
+  const protectedItems = itemsOf(c, 60); protectedItems.forEach(i => { i.locked = true; }); stock(c, protectedItems);
test/run.js:434: trailing whitespace.
+  events.length = 0; drops.length = 0; untilResult(c);
test/run.js:435: trailing whitespace.
+  assert.ok(events.length >= 1 && events.every(e => !e.kept && e.gold > 0));
test/run.js:436: trailing whitespace.
+  assert.equal(c.Game.getState().overflow, events.length);
test/run.js:437: trailing whitespace.
+  assert.equal(c.Game.getState().inventory.length, 60);
test/run.js:438: trailing whitespace.
+});
test/run.js:439: trailing whitespace.
+test("P2: all combat potentials apply, gold/drop bonuses work and invincibility is enabled", () => {
test/run.js:440: trailing whitespace.
+  const c = context();
test/run.js:441: trailing whitespace.
+  const items = c.DATA.equipmentSlots.map(s => itemsOf(c, 1, s.id, 1, "legendary")[0]);
test/run.js:442: trailing whitespace.
+  items[0].potentials = [{ id: "atkPct", value: .2 }, { id: "hpPct", value: .25 }, { id: "defPct", value: .25 }];
test/run.js:443: trailing whitespace.
+  items[1].potentials = [{ id: "attackSpeedPct", value: .15 }, { id: "critChance", value: .12 }, { id: "critDamage", value: .4 }];
test/run.js:444: trailing whitespace.
+  items[2].potentials = [{ id: "goldPct", value: .3 }, { id: "dropPct", value: .12 }, { id: "invincibleOnHit", value: .06 }];
test/run.js:445: trailing whitespace.
+  items[3].potentials = [{ id: "atkPct", value: .2 }, { id: "hpPct", value: .25 }, { id: "defPct", value: .25 }];
test/run.js:446: trailing whitespace.
+  stock(c, items, 0);
test/run.js:447: trailing whitespace.
+  items.forEach(i => c.Game.equip(i.uid, "warrior"));
test/run.js:448: trailing whitespace.
+  const base = c.DATA.mercenaryStats("warrior", 1), stats = c.Game.mercenaryStats("warrior");
test/run.js:449: trailing whitespace.
+  for (const key of ["atk", "hp", "def", "attackSpeed", "critChance", "critDamage"]) {
test/run.js:450: trailing whitespace.
+    const flat = base[key] + items.reduce((sum, i) => sum + (i.base[key] || 0), 0);
test/run.js:451: trailing whitespace.
+    const expected = ({ atk: flat * 1.4, hp: flat * 1.5, def: flat * 1.5, attackSpeed: flat * 1.15,
test/run.js:452: trailing whitespace.
+      critChance: flat + .12, critDamage: flat + .4 })[key];
test/run.js:453: trailing whitespace.
+    assert.ok(Math.abs(stats[key] - expected) < 1e-10, key);
test/run.js:454: trailing whitespace.
+  }
test/run.js:455: trailing whitespace.
+  assert.equal(stats.invincibleOnHit, true);
test/run.js:456: trailing whitespace.
+  const save = JSON.parse(c.Game.save());
test/run.js:457: trailing whitespace.
+  save.state.battle.enemies[0].hp = 1; save.state.battle.units[0].skills = [];
test/run.js:458: trailing whitespace.
+  setState(c, save.state);
test/run.js:459: trailing whitespace.
+  c.Game.rng = () => .085; // Above 8%, below the equipped 8.96% drop threshold.
test/run.js:460: trailing whitespace.
+  c.Game.step();
test/run.js:461: trailing whitespace.
+  assert.equal(c.Game.getState().gold, 7);
test/run.js:462: trailing whitespace.
+  assert.equal(c.Game.getState().inventory.length, 5);
test/run.js:463: trailing whitespace.
+  const plainGame = context();
test/run.js:464: trailing whitespace.
+  const plainState = plainGame.Game.getState(); plainState.battle.enemies[0].hp = 1; plainState.battle.units[0].skills = []; setState(plainGame, plainState);
test/run.js:465: trailing whitespace.
+  plainGame.Game.rng = () => .085;
test/run.js:466: trailing whitespace.
+  plainGame.Game.step();
test/run.js:467: trailing whitespace.
+  assert.equal(plainGame.Game.getState().inventory.length, 0);
test/run.js:468: trailing whitespace.
+  assert.equal(plainGame.Game.getState().gold, 5);
test/run.js:469: trailing whitespace.
+});
test/run.js:470: trailing whitespace.
+test("P2: transferring gear clears old owner, and changing gear never revives a downed ally", () => {
test/run.js:471: trailing whitespace.
+  const c = context(); fixture(c, 6, 1);
test/run.js:472: trailing whitespace.
+  const item = itemsOf(c, 1, "hat")[0]; stock(c, [item]);
test/run.js:473: trailing whitespace.
+  const before = c.Game.getCP(); c.Game.equip(item.uid, "warrior"); c.Game.equip(item.uid, "archer");
test/run.js:474: trailing whitespace.
+  let state = c.Game.getState();
test/run.js:475: trailing whitespace.
+  assert.equal(state.mercenaries[0].equipment.hat, null);
test/run.js:476: trailing whitespace.
+  assert.equal(state.mercenaries[1].equipment.hat, item.uid);
test/run.js:477: trailing whitespace.
+  assert.equal(state.inventory[0].equippedBy, "archer");
test/run.js:478: trailing whitespace.
+  c.Game.unequip("archer", "hat"); assert.equal(c.Game.getCP(), before);
test/run.js:479: trailing whitespace.
+  state = c.Game.getState(); state.battle.units[0].hp = 0; setState(c, state);
test/run.js:480: trailing whitespace.
+  c.Game.equip(item.uid, "warrior");
test/run.js:481: trailing whitespace.
+  assert.equal(c.Game.getState().battle.units[0].hp, 0);
test/run.js:482: trailing whitespace.
+  c.Game.unequip("warrior", "hat");
test/run.js:483: trailing whitespace.
+  assert.equal(c.Game.getState().battle.units[0].hp, 0);
test/run.js:484: trailing whitespace.
+});
test/run.js:485: trailing whitespace.
+test("Art: DATA.assets matches all 28 manifest keys and files exist", () => {
test/run.js:486: trailing whitespace.
+  const c = context();
test/run.js:487: trailing whitespace.
+  const manifest = JSON.parse(fs.readFileSync(path.join(__dirname, "../assets/manifest.json"), "utf8"));
test/run.js:488: trailing whitespace.
+  assert.equal(Object.keys(manifest).length, 28);
test/run.js:489: trailing whitespace.
+  assert.deepEqual(plain(c.DATA.assets), manifest);
test/run.js:490: trailing whitespace.
+  for (const file of Object.values(c.DATA.assets)) assert.ok(fs.statSync(path.join(__dirname, "..", file)).isFile(), file);
test/run.js:491: trailing whitespace.
+});
test/run.js:492: trailing whitespace.
+require("./p3")({ test, context, plain, setState, fixture, untilResult, assert });
test/run.js:493: trailing whitespace.
+console.log("\n" + passed + " tests passed.");

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content test/browser.js | Select-Object -First 20; git diff --stat; Get-Content .gitignore' in D:\Git\Inv\squad-idle
 succeeded in 116ms:
"use strict";

// Optional integration smoke test. Node 22+ and an existing Chrome/Edge install; no packages.
const assert = require("node:assert/strict");
const fs = require("node:fs");
const os = require("node:os");
const path = require("node:path");
const http = require("node:http");
const { spawn } = require("node:child_process");
const { once } = require("node:events");
const root = path.resolve(__dirname, "..");
const delay = ms => new Promise(resolve => setTimeout(resolve, ms));
const browserPath = [process.env.CHROME_PATH,
  "C:/Program Files/Google/Chrome/Application/chrome.exe",
  "C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe",
  "/usr/bin/google-chrome", "/usr/bin/chromium", "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
].filter(Boolean).find(file => fs.existsSync(file));
if (!browserPath) throw new Error("Set CHROME_PATH to an existing Chrome or Edge executable.");

async function main() {
 index.html          | 144 ++++----
 js/data.js          | 309 +++++++++--------
 js/ui-equipment.js  | 428 +++++++++++------------
 js/ui-expedition.js | 277 +++++++--------
 js/ui.js            | 457 ++++++++++++------------
 style.css           | 475 +++++++++++--------------
 test/run.js         | 979 ++++++++++++++++++++++++++--------------------------
 7 files changed, 1525 insertions(+), 1544 deletions(-)
node_modules/
.claude/*.log

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
for name in ['index.html','js/data.js','js/ui.js','js/ui-equipment.js','js/ui-expedition.js','test/run.js','style.css']:
 p=Path(name);p.write_bytes(p.read_text(encoding='utf-8').encode('utf-8'))
'@ | python -
git diff --stat" in D:\Git\Inv\squad-idle
 succeeded in 122ms:
 index.html          |  36 ++---
 js/data.js          |  31 ++++
 js/ui-equipment.js  |  14 +-
 js/ui-expedition.js |  13 +-
 js/ui.js            |  39 +++--
 style.css           | 453 +++++++++++++++++++++++-----------------------------
 test/run.js         |   7 +
 7 files changed, 287 insertions(+), 306 deletions(-)

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

apply patch
patch: completed
D:\Git\Inv\squad-idle\js\ui-equipment.js
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:BROWSER_NO_SANDBOX='"'1'; "'$env:BROWSER_FAST='"'1'; node test/browser.js" in D:\Git\Inv\squad-idle
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git diff --check; git diff -- js/ui.js js/ui-equipment.js js/ui-expedition.js; Get-ChildItem js/*.js | ForEach-Object { '{0}: {1}' -f "'$_.Name,(Get-Content $_.FullName).Length }' in D:\Git\Inv\squad-idle
 succeeded in 146ms:
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index 8ffb97e..69a552f 100644
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -10,7 +10,7 @@
   function slot(id) { return DATA.equipmentSlots.find(function (s) { return s.id === id; }); }
   function name(item) { return "T" + UI.fmt(item.tier) + " " + DATA.equipmentRarities[item.rarity].name + " " + slot(item.slot).name; }
   function itemCard(item, extra) {
-    return '<span class="item-card rarity-' + item.rarity + ' ' + (extra || "") + '"><span class="item-icon">' + slot(item.slot).icon +
+    return '<span class="item-card rarity-' + item.rarity + ' ' + (extra || "") + '"><span class="item-icon">' + UI.icon(item.slot, slot(item.slot).icon) +
       '</span><b>T' + UI.fmt(item.tier) + '</b><small>' + DATA.equipmentRarities[item.rarity].name + '</small></span>';
   }
   function lines(options) {
@@ -32,7 +32,7 @@
     var item = state.inventory.find(function (i) { return i.uid === merc.equipment[definition.id]; });
     return '<button class="equipment-slot ' + (item ? 'rarity-' + item.rarity : '') + '" data-merc-slot="' + definition.id +
       '" data-merc="' + merc.id + '" ' + (merc.unlocked ? '' : 'disabled') + ' aria-label="' + definition.name + ' ' +
-      (item ? name(item) : '빈 슬롯') + '"><span>' + definition.icon + '</span><small>' + definition.name + '</small><em>' +
+      (item ? name(item) : '빈 슬롯') + '"><span>' + UI.icon(definition.id, definition.icon) + '</span><small>' + definition.name + '</small><em>' +
       (item ? 'T' + UI.fmt(item.tier) + ' · ' + DATA.equipmentRarities[item.rarity].name : '비어 있음') + '</em></button>';
   };
   function detail(item, state) {
@@ -66,7 +66,7 @@
       [['tier', '티어↓'], ['rarity', '등급↓'], ['recent', '최근']].map(function (s) { return '<option value="' + s[0] + '" ' + (sort === s[0] ? 'selected' : '') + '>' + s[1] + '</option>'; }).join('') +
       '</select></label></div>' + (item ? detail(item, state) : '') + '<div class="inventory-grid">' + items.map(function (i) {
         return '<button class="inventory-cell rarity-' + i.rarity + (selected === i.uid ? ' selected-item' : '') + '" data-item="' + i.uid + '" aria-label="' + name(i) +
-          (i.locked ? ' 잠금' : '') + (i.equippedBy ? ' 장착 중' : '') + '"><span>' + slot(i.slot).icon + '</span><b>T' + UI.fmt(i.tier) +
+          (i.locked ? ' 잠금' : '') + (i.equippedBy ? ' 장착 중' : '') + '"><span>' + UI.icon(i.slot, slot(i.slot).icon) + '</span><b>T' + UI.fmt(i.tier) +
           '</b>' + (i.locked ? '<em>🔒</em>' : '') + (i.equippedBy ? '<i>장착</i>' : '') + '</button>';
       }).join("") + '</div>' + (!items.length ? '<p class="equipment-empty">적을 처치하면 장비가 떨어집니다.<br>같은 슬롯·티어 세 개를 모아 합성해 보세요.</p>' : '') +
       '<p class="equipment-note">가득 차면 잠금·장착을 제외한 최저 티어 레어를 자동 판매합니다. 판매할 레어가 없으면 새 드롭을 골드로 받습니다.</p>';
@@ -76,7 +76,7 @@
     DATA.equipmentSlots.forEach(function (s) {
       for (var tier = 1; tier <= 8; tier++) {
         var count = state.inventory.filter(function (i) { return i.slot === s.id && i.tier === tier && !i.locked && !i.equippedBy; }).length;
-        rows += '<tr><th>' + s.icon + ' ' + s.name + '</th><td>T' + UI.fmt(tier) + '</td><td>' + UI.fmt(count) + '개</td><td>' + (tier === 8 ? '<small>최대 티어</small>' :
+        rows += '<tr><th>' + UI.icon(s.id, s.icon) + ' ' + s.name + '</th><td>T' + UI.fmt(tier) + '</td><td>' + UI.fmt(count) + '개</td><td>' + (tier === 8 ? '<small>최대 티어</small>' :
           '<button data-fuse-slot="' + s.id + '" data-tier="' + tier + '" ' + (count < 3 || state.gold < DATA.fusionCost(tier) ? 'disabled' : '') +
           '>합성 ×' + UI.fmt(1) + '<small>' + UI.fmt(DATA.fusionCost(tier)) + ' 골드</small></button>') + '</td></tr>';
       }
@@ -96,7 +96,7 @@
     void el("cp").offsetWidth;
     el("cp").classList.add("cp-jump");
     function frame(now) {
-      var t = Math.min(1, (now - start) / 500);
+      var t = Math.min(1, (now - start) / 300);
       cpValue = from + (value - from) * (1 - (1 - t) ** 3);
       el("cp").textContent = UI.fmt(Math.round(cpValue));
       if (t < 1) cpFrame = requestAnimationFrame(frame);
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
@@ -140,7 +144,7 @@
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + itemCard(event.item) +
       '</div></div><p>' + (event.upgraded ? '✦ 등급 상승!' : slot(event.item.slot).name) + '</p>';
     el("reveal-cards").appendChild(card);
-    if (event.upgraded) flash(true);
+    if (event.upgraded || ranks.indexOf(event.item.rarity) >= 2) flash(true);
   }
   function revealTick(now) {
     if (!reveal.open) { revealFrame = null; return; }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 1ea87ff..d5b3ff5 100644
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
@@ -95,11 +95,11 @@
   function tick(now) {
     if (!overlay.open) return;
     var elapsed = now - started;
-    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 1000)));
-    var count = Math.min(report.items.length, Math.max(0, Math.floor((elapsed - 1000) / 200) + 1));
+    el("report-gold").textContent = UI.fmt(Math.floor(report.gold * Math.min(1, elapsed / 300)));
+    var count = Math.min(report.items.length, Math.max(0, Math.floor((elapsed - 300) / 200) + 1));
     while (shown < count) revealOne(report.items[shown++]);
     progress();
-    if (elapsed < 1000 || shown < report.items.length) frame = requestAnimationFrame(tick);
+    if (elapsed < 300 || shown < report.items.length) frame = requestAnimationFrame(tick);
     else frame = null;
   }
   function skip() {
@@ -127,6 +127,7 @@
   Game.on("skill", function (event) {
     var sprite = el("sprite-" + event.id);
     if (!sprite) return;
+    UI.attackSprite(sprite);
     var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
     label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 94);
     label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "skill-label"); label.textContent = event.name;
diff --git a/js/ui.js b/js/ui.js
index ebb1e17..a69681b 100644
--- a/js/ui.js
+++ b/js/ui.js
@@ -24,12 +24,12 @@ var UI = (function () {
     return node;
   }
   function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#674e38"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#abb5b0" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#b8c2bb"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#89968a" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#335e49"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#b3ad8f" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#638e6d"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#6f619b"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#8f7dbb"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#18251f" stroke-width="4"/><path d="M-7-6h8" stroke="#334b3d" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#5b625b" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#526357"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#657461" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#26392c" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#716550" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#9da69a"/>';
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
   }
   function position(unit) {
     if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
@@ -40,22 +40,27 @@ var UI = (function () {
     var p = position(unit);
     var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
       "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#07110d", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body", transform: unit.type === "boss" ? "scale(1.2)" : "scale(1)" });
-    body.innerHTML = figure(unit.type, color);
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
     body.addEventListener("animationend", function (event) {
       if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
     });
     group.appendChild(body);
     if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#07130e" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
       group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#c78778" : "#accb9b", class: "hp-fill" }));
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
     } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
     if (unit.hp <= 0 && !locked) group.classList.add("fallen");
     return group;
   }
   function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
     if (clearEffects) el("effect-layer").replaceChildren();
     el("mercenary-layer").replaceChildren();
     el("enemy-layer").replaceChildren();
@@ -73,7 +78,7 @@ var UI = (function () {
   function hit(event) {
     var attacker = el("sprite-" + event.attackerId);
     var target = el("sprite-" + event.targetId);
-    if (attacker) animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right");
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
     if (!target) return;
     animate(target.querySelector(".unit-body"), "hit-flash");
     var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
@@ -104,10 +109,10 @@ var UI = (function () {
     var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
     if (UI.updateCP) UI.updateCP(Game.getCP());
     else el("cp").textContent = fmt(Game.getCP());
-    el("gold").textContent = fmt(state.gold);
-    el("coins").textContent = fmt(state.squadCoins);
-    el("region-name").textContent = DATA.regions[stage.region].name;
-    el("region-subtitle").textContent = DATA.regions[stage.region].subtitle;
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
     el("stage-id").textContent = stageLabel(state.currentStage);
     el("stage-name").textContent = stage.name;
     el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
@@ -193,7 +198,7 @@ var UI = (function () {
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
battle.js: 167
data.js: 170
game-equipment.js: 234
game-expedition.js: 185
game.js: 286
ui-art.js: 93
ui-equipment.js: 218
ui-expedition.js: 139
ui.js: 231

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

 exited 1 in 16047ms:
Browser: Chrome/152.0.7977.76
PASS mobile 390×844 layout, tap sizes, initial auto-fight
PASS mercenary cards, equipment slots, placeholders, settings and reset confirmation
PASS real 100ms timer clears 1-1 and advances to 1-2 without input
PASS repeat, arrows, 320px no overflow and desktop 480px maximum width
PASS fresh 120s replay: 6 drops, pop animation and tab badge
PASS inventory grid/filter, equip CP animation and mercenary-slot equip mode at 375px
PASS single fusion T1→T2 flip, auto-fusion stagger/skip, lock and before/after potential reroll
PASS unique→legendary fusion with stronger flash
PASS P3 3h reload report, count-up, 200ms flips, strong flash, skip and harvest overflow
PASS P3 visibilitychange return after 61s opens a harvest report
PASS P3 visible skill casts, slot edits, chaos toggle only after 3-10 and 375px layout
PASS P3 JSON export/copy, invalid import validation, confirmed import round-trip
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-dgohM2
[33844:34832:0907/200548.706:ERROR:chrome\installer\util\google_update_settings_win.cc:270] Failed opening key Software\Google\Update\ClientStateMedium\{8A69D345-D564-463c-AFF1-A69D9E530F96} to set usagestats; result: 5

DevTools listening on ws://127.0.0.1:12920/devtools/browser/aabd381a-4b7f-4cf8-83ac-95c309ad800a
[33844:15256:0907/200550.891:ERROR:services\device\public\cpp\geolocation\system_geolocation_source_win.cc:259] Failed to get location access status: 매개 변수가 틀립니다. (0x80070057)

AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal:
+ actual - expected

+ [
+   {
+     method: 'Log.entryAdded',
+     params: {
+       entry: {
+         level: 'error',
+         networkRequestId: '9180.32',
+         source: 'network',
+         text: 'Failed to load resource: net::ERR_NETWORK_ACCESS_DENIED',
+         timestamp: 1788779155036.848,
+         url: 'https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&family=Jua&display=swap'
+       }
+     },
+     sessionId: 'B1CA0727C149492EC30168F073DA6CC1'
+   },
+   {
+     method: 'Log.entryAdded',
+     params: {
+       entry: {
+         level: 'error',
+         networkRequestId: '9180.916',
+         source: 'network',
+         text: 'Failed to load resource: net::ERR_NETWORK_ACCESS_DENIED',
+         timestamp: 1788779160285.311,
+         url: 'https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&family=Jua&display=swap'
+       }
+     },
+     sessionId: 'B1CA0727C149492EC30168F073DA6CC1'
+   },
+   {
+     method: 'Runtime.exceptionThrown',
+     params: {
+       exceptionDetails: {
+         columnNumber: 35,
+         exception: {
+           className: 'TypeError',
+           description: "TypeError: Cannot read properties of null (reading 'querySelector')\n" +
+             '    at UI.assetLoaded (http://127.0.0.1:12919/js/ui-art.js:20:36)\n' +
+             '    at SVGImageElement.<anonymous> (http://127.0.0.1:12919/js/ui-art.js:36:53)',
+           objectId: '-2047513221711475040.4.1',
+           preview: {
+             description: "TypeError: Cannot read properties of null (reading 'querySelector')\n" +
+               '    at UI.assetLoaded (http://127.0.0.1:12919/js/ui-art.js:20:36)\n' +
+               '    at SVGImageElement.<anonymous> (http://127.0.0.1:12919/js/ui-art.js:36:53)',
+             overflow: false,
+             properties: [
+               {
+                 name: 'stack',
+                 type: 'string',
+                 value: 'TypeError: Cannot read properties of null (reading…mous> (http://127.0.0.1:12919/js/ui-art.js:36:53)'
+               },
+               {
+                 name: 'message',
+                 type: 'string',
+                 value: "Cannot read properties of null (reading 'querySelector')"
+               }
+             ],
+             subtype: 'error',
+             type: 'object'
+           },
+           subtype: 'error',
+           type: 'object'
+         },
+         exceptionId: 1,
+         executionContextId: 4,
+         lineNumber: 19,
+         scriptId: '23',
+         stackTrace: {
+           callFrames: [
+             {
+               columnNumber: 35,
+               functionName: 'UI.assetLoaded',
+               lineNumber: 19,
+               scriptId: '23',
+               url: 'http://127.0.0.1:12919/js/ui-art.js'
+             },
+             {
+               columnNumber: 52,
+               functionName: '',
+               lineNumber: 35,
+               scriptId: '23',
+               url: 'http://127.0.0.1:12919/js/ui-art.js'
+             }
+           ]
+         },
+         text: 'Uncaught',
+         url: 'http://127.0.0.1:12919/js/ui-art.js'
+       },
+       timestamp: 1788779162846.773
+     },
+     sessionId: 'B1CA0727C149492EC30168F073DA6CC1'
+   },
+   {
+     method: 'Log.entryAdded',
+     params: {
+       entry: {
+         level: 'error',
+         networkRequestId: '9180.1552',
+         source: 'network',
+         text: 'Failed to load resource: net::ERR_NETWORK_ACCESS_DENIED',
+         timestamp: 1788779163523.787,
+         url: 'https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&family=Jua&display=swap'
+       }
+     },
+     sessionId: 'B1CA0727C149492EC30168F073DA6CC1'
+   }
+ ]
- []

    at main (D:\Git\Inv\squad-idle\test\browser.js:165:12) {
  generatedMessage: true,
  code: 'ERR_ASSERTION',
  actual: [Array],
  expected: [],
  operator: 'deepStrictEqual',
  diff: 'simple'
}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content test/browser-p3.js | Select-Object -First 65; Get-Content -Encoding utf8 js/data.js | Select-Object -First 38' in D:\Git\Inv\squad-idle
 succeeded in 112ms:
"use strict";
const assert = require("node:assert/strict");

module.exports = async function ({ evaluate, command, screenshot, delay, events }) {
  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
  await evaluate(`Game.pause(); document.querySelectorAll('dialog[open]').forEach(d=>d.close()); Game.reset();
    window.p3Save = JSON.parse(Game.save());
    p3Save.state.stats = {goldPerSec:20,killsPerSec:2,elapsedMs:10000,samples:[{at:10000,gold:20}]};
    p3Save.savedAt = Date.now()-3*3600000;
    localStorage.setItem('squad_v1',JSON.stringify(p3Save));`);
  // Navigation reload triggers pagehide save; set the old timestamp in the next document before UI.init.
  const injected = await command("Page.addScriptToEvaluateOnNewDocument", { source: `
    const stored = JSON.parse(localStorage.getItem('squad_v1'));
    stored.savedAt=Date.now()-3*3600000;
    stored.state.stats={goldPerSec:20,killsPerSec:2,elapsedMs:10000,samples:[{at:10000,gold:20}]};
    localStorage.setItem('squad_v1',JSON.stringify(stored));` });
  await command("Page.reload");
  for (let i = 0; i < 50; i++) {
    if (await evaluate("!!document.getElementById('offline-report')?.open")) break;
    await delay(50);
  }
  await command("Page.removeScriptToEvaluateOnNewDocument", { identifier: injected.identifier });
  assert.ok(await evaluate("document.getElementById('offline-report').open"));
  assert.ok(await evaluate("document.getElementById('report-duration').textContent.includes('3?쒓컙 0遺?)"));
  const goldEarly = await evaluate("document.getElementById('report-gold').textContent");
  await delay(250);
  assert.notEqual(await evaluate("document.getElementById('report-gold').textContent"), goldEarly);
  assert.equal(await evaluate("Game.getState().gold"), 0);
  await screenshot("p3-report-countup");
  await delay(900);
  const before = await evaluate("document.querySelectorAll('#report-items .fusion-card').length");
  await delay(450);
  assert.ok(await evaluate("document.querySelectorAll('#report-items .fusion-card').length > " + before));
  assert.ok(await evaluate("getComputedStyle(document.querySelector('#report-items .flip-inner')).animationName === 'item-flip'"));
  await screenshot("p3-report-flips");
  await evaluate("document.getElementById('report-title').click()");
  assert.ok(await evaluate("document.querySelectorAll('#report-items .fusion-card').length === Game.getState().pendingReport.items.length"));
  assert.ok(await evaluate("document.getElementById('offline-report').classList.contains('loot-flash-strong')"));
  assert.ok(await evaluate("document.documentElement.scrollWidth <= 375 && document.getElementById('offline-report').scrollWidth <= 375"));
  await screenshot("p3-report-revealed");
  const reward = await evaluate("Game.getState().pendingReport.gold");
  await evaluate("document.getElementById('harvest-report').click(); Game.pause()");
  assert.ok(await evaluate("!document.getElementById('offline-report').open && Game.getState().pendingReport === null && Game.getState().gold >= " + reward));
  assert.ok(await evaluate("Game.getState().inventory.length === 60 && Game.getState().overflow > 0"));
  console.log("PASS P3 3h reload report, count-up, 200ms flips, strong flash, skip and harvest overflow");

  await evaluate(`Game.reset(); window.p3Now=Date.now;
    Object.defineProperty(document,'hidden',{configurable:true,value:true});
    document.dispatchEvent(new Event('visibilitychange'));
    Date.now=()=>p3Now()+61000;
    Object.defineProperty(document,'hidden',{configurable:true,value:false});
    document.dispatchEvent(new Event('visibilitychange'));
    Date.now=p3Now; delete document.hidden;`);
  assert.ok(await evaluate("document.getElementById('offline-report').open && Game.getState().pendingReport.elapsedMs >= 61000"));
  await evaluate("document.getElementById('harvest-report').click(); Game.pause()");
  console.log("PASS P3 visibilitychange return after 61s opens a harvest report");

  await evaluate("Game.reset(); Game.pause(); Game.step(); document.querySelector('[data-tab=skills]').click()");
  assert.ok(await evaluate("!!document.querySelector('.skill-label') && document.querySelectorAll('.skill-card').length === 4"));
  await delay(300);
  await screenshot("p3-skills");
  await evaluate("document.querySelector('[data-skill=taunt]').click(); document.querySelector('[data-skill=regen]').click()");
  assert.ok(await evaluate("Game.getState().mercenaries[0].skills.join(',') === 'guard,smash,regen'"));
  await evaluate("document.getElementById('close-sheet').click()");
  assert.ok(await evaluate("document.getElementById('difficulty-toggle').hidden"));
"use strict";

var DATA = {
  // DECISION: Inline manifest; static hosting and offline play need no JSON request.
  assets: {
    "merc.warrior.idle": "assets/merc/warrior_idle.png",
    "merc.archer.idle": "assets/merc/archer_idle.png",
    "merc.mage.idle": "assets/merc/mage_idle.png",
    "mon.slime": "assets/mon/slime.png",
    "mon.goblin": "assets/mon/goblin.png",
    "mon.mushroom": "assets/mon/mushroom.png",
    "mon.boss_treant": "assets/mon/boss_treant.png",
    "bg.forest": "assets/bg/forest.png",
    "icon.weapon": "assets/icon/weapon.png",
    "icon.hat": "assets/icon/hat.png",
    "icon.gloves": "assets/icon/gloves.png",
    "icon.shoes": "assets/icon/shoes.png",
    "icon.skillbook": "assets/icon/skillbook.png",
    "icon.gold": "assets/icon/gold.png",
    "icon.coin": "assets/icon/coin.png",
    "merc.warrior.attack": "assets/merc/warrior_attack.png",
    "merc.archer.attack": "assets/merc/archer_attack.png",
    "merc.mage.attack": "assets/merc/mage_attack.png",
    "mon.ice_bat": "assets/mon/ice_bat.png",
    "mon.yeti": "assets/mon/yeti.png",
    "mon.frost_wolf": "assets/mon/frost_wolf.png",
    "mon.boss_ice_golem": "assets/mon/boss_ice_golem.png",
    "bg.peak": "assets/bg/peak.png",
    "mon.lava_slime": "assets/mon/lava_slime.png",
    "mon.imp": "assets/mon/imp.png",
    "mon.salamander": "assets/mon/salamander.png",
    "mon.boss_dragon": "assets/mon/boss_dragon.png",
    "bg.volcano": "assets/bg/volcano.png"
} ,
  version: "0.3.0 · P3",
  schemaVersion: 3,
  offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
  chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..1d84a20452a86d69ed9d3e4e901a012b4a42cf70
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,93 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  // DECISION: Asset selection is presentation-only; region indices and combat types stay intact.
+  var regions = [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ];
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

apply patch
patch: completed
D:\Git\Inv\squad-idle\js\data.js
D:\Git\Inv\squad-idle\js\ui-art.js
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..2cd289224d03c315a855d3cc7a02a6fe877b7057
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,88 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..2cd289224d03c315a855d3cc7a02a6fe877b7057
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,88 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

codex
기존 로직 테스트 37개와 새 에셋 테스트가 모두 통과했습니다. 브라우저에서는 기존 장비·합성·P3 흐름도 정상이며, 빠른 지역 전환 때 제거된 이미지의 로드 콜백이 발생하는 오류를 찾아 수정했습니다. Google Fonts는 이 실행 환경의 네트워크 정책에 차단되어 현재 시스템 폰트로 정상 표시되고 있습니다.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n 'function load|stageStart|function notify|emit\\(\"levelUp' js/game.js; Get-Content -Encoding utf8 test/browser.js | Select-Object -Skip 18 -First 80" in D:\Git\Inv\squad-idle
 succeeded in 111ms:

async function main() {
  const profile = fs.mkdtempSync(path.join(os.tmpdir(), "p1-browser-"));
  const artifacts = fs.mkdtempSync(path.join(os.tmpdir(), "p1-screenshots-"));
  const server = http.createServer((req, res) => {
    const pathname = decodeURIComponent(new URL(req.url, "http://localhost").pathname);
    const file = path.resolve(root, "." + (pathname === "/" ? "/index.html" : pathname));
    if (!file.startsWith(root + path.sep)) { res.writeHead(403); res.end(); return; }
    fs.readFile(file, (error, data) => {
      if (error) { res.writeHead(404); res.end(); return; }
      const types = { ".html": "text/html", ".css": "text/css", ".js": "application/javascript" };
      res.setHeader("Content-Type", (types[path.extname(file)] || "application/octet-stream") + "; charset=utf-8");
      res.end(data);
    });
  });
  server.listen(0, "127.0.0.1");
  await once(server, "listening");
  const browser = spawn(browserPath, ["--headless=new", "--remote-debugging-port=0", "--user-data-dir=" + profile,
    "--no-first-run", "--no-default-browser-check", "--disable-gpu", "--in-process-gpu",
    ...(process.env.BROWSER_NO_SANDBOX === "1" ? ["--no-sandbox"] : []),
    ...(process.env.BROWSER_SINGLE_PROCESS === "1" ? ["--single-process"] : []),
    "about:blank"], { stdio: ["ignore", "ignore", "pipe"], windowsHide: true });
  let browserLog = "";
  browser.stderr.on("data", data => { browserLog += data.toString(); });
  let socket;
  try {
    const portFile = path.join(profile, "DevToolsActivePort");
    for (let i = 0; i < 100 && !fs.existsSync(portFile); i++) await delay(100);
    assert.ok(fs.existsSync(portFile), "Browser did not start its debugging endpoint");
    const endpoint = fs.readFileSync(portFile, "utf8").trim().split(/\r?\n/);
    socket = new WebSocket("ws://127.0.0.1:" + endpoint[0] + endpoint[1]);
    await Promise.race([once(socket, "open"), delay(10000).then(() => { if (socket.readyState !== WebSocket.OPEN) throw new Error("Browser WebSocket did not open"); })]);
    let id = 0;
    let sessionId;
    const pending = new Map();
    const events = [];
    socket.addEventListener("message", event => {
      const message = JSON.parse(event.data);
      if (message.id) {
        const task = pending.get(message.id);
        pending.delete(message.id);
        if (message.error) task.reject(new Error(JSON.stringify(message.error)));
        else task.resolve(message.result);
      } else events.push(message);
    });
    function command(method, params = {}) {
      return new Promise((resolve, reject) => {
        const key = ++id;
        const timeout = setTimeout(() => { pending.delete(key); reject(new Error("Timed out: " + method)); }, 10000);
        pending.set(key, { resolve: data => { clearTimeout(timeout); resolve(data); }, reject: error => { clearTimeout(timeout); reject(error); } });
        socket.send(JSON.stringify({ id: key, method, params, sessionId }));
      });
    }
    async function evaluate(expression) {
      const result = await command("Runtime.evaluate", { expression, returnByValue: true, awaitPromise: true });
      assert.equal(result.exceptionDetails, undefined, JSON.stringify(result.exceptionDetails));
      return result.result.value;
    }
    async function screenshot(name) {
      const result = await command("Page.captureScreenshot", { format: "png" });
      fs.writeFileSync(path.join(artifacts, name + ".png"), Buffer.from(result.data, "base64"));
    }
    const browserVersion = await command("Browser.getVersion");
    console.log("Browser: " + browserVersion.product);
    const target = await command("Target.createTarget", { url: "http://127.0.0.1:" + server.address().port });
    const session = await command("Target.attachToTarget", { targetId: target.targetId, flatten: true });
    sessionId = session.sessionId;
    await command("Page.enable");
    await command("Page.navigate", { url: "http://127.0.0.1:" + server.address().port });
    await command("Runtime.enable");
    await command("Page.enable");
    await command("Log.enable");
    await command("Emulation.setDeviceMetricsOverride", { width: 390, height: 844, deviceScaleFactor: 1, mobile: true });
    const url = "http://127.0.0.1:" + server.address().port;
    await command("Page.navigate", { url });
    for (let i = 0; i < 100; i++) {
      if (await evaluate("document.readyState === 'complete' && typeof UI !== 'undefined'")) break;
      await delay(100);
    }
    const initial = await evaluate("({stage:Game.getState().currentStage,status:Game.getState().battle.status,mode:Game.getState().mode})");

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..2cd289224d03c315a855d3cc7a02a6fe877b7057
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,88 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

apply patch
patch: completed
D:\Git\Inv\squad-idle\js\ui-art.js
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
p=Path('js/ui.js');s=p.read_text(encoding='utf-8');s=s.replace(\"'<svg viewBox=\", \"'<svg viewBox=\")
s=s.replace('><svg viewBox=\"-42 -88 96 104\" aria-hidden=\"true\">\\' + figure(merc.id, def.color) + \\'</svg><div>', \">\\' + UI.portrait(merc.id, figure(merc.id, def.color)) + \\'<div>\")
p.write_bytes(s.encode('utf-8'))
'@ | python -
Get-ChildItem js/*.js | ForEach-Object { node --check "'$_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }' in D:\Git\Inv\squad-idle
 succeeded in 587ms:
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });

apply patch
patch: completed
D:\Git\Inv\squad-idle\test\art-checks.js
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
s=Path('test/browser.js').read_text(encoding='utf-8')
s=s.replace('    const initial = await evaluate(', '    await require(\"./art-checks\")({evaluate, command, screenshot, delay});\\n    await evaluate(\"Game.reset(); Game.resume()\");\\n    const initial = await evaluate(')
s=s.replace('    await command(\"Page.enable\");\\n    await command(\"Page.navigate\",', '''    if (process.env.FONT_FALLBACK === \"1\") {
      await command(\"Fetch.enable\", {patterns:[{urlPattern:\"https://fonts.googleapis.com/*\"},{urlPattern:\"https://fonts.gstatic.com/*\"}]});
      socket.addEventListener(\"message\", async event => {
        const message=JSON.parse(event.data);
        if(message.method===\"Fetch.requestPaused\") await command(\"Fetch.fulfillRequest\", {
          requestId:message.params.requestId,responseCode:200,responseHeaders:[{name:\"Content-Type\",value:\"text/css\"}],body:\"\"
        });
      });
      console.log(\"Simulated unavailable web fonts: empty stylesheet, system font fallback\");
    }
    await command(\"Page.enable\");
    await command(\"Page.navigate\",''',1)
Path('test/.restyle-browser.js').write_bytes(s.encode('utf-8'))
'@ | python -
"'$env:FONT_FALLBACK='"'1'; "'$env:BROWSER_NO_SANDBOX='"'1'; "'$env:BROWSER_FAST='"'1'; node test/.restyle-browser.js" in D:\Git\Inv\squad-idle
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

 exited 1 in 10663ms:
Browser: Chrome/152.0.7977.76
Simulated unavailable web fonts: empty stylesheet, system font fallback
PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter
PASS three regional backgrounds, all regional type mappings and 140px bosses
PASS all five sheets: 375px no overflow, 44px buttons and raised active tab
PASS missing mapping, actual image decode failures and detached-image callbacks
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-1MT49B
[33812:37996:0907/200955.630:ERROR:chrome\installer\util\google_update_settings_win.cc:270] Failed opening key Software\Google\Update\ClientStateMedium\{8A69D345-D564-463c-AFF1-A69D9E530F96} to set usagestats; result: 5

DevTools listening on ws://127.0.0.1:11728/devtools/browser/75f3a6dc-fb26-48cf-814a-b3e561a9004e
[33812:39292:0907/200957.808:ERROR:services\device\public\cpp\geolocation\system_geolocation_source_win.cc:259] Failed to get location access status: 매개 변수가 틀립니다. (0x80070057)

AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:

375 !== 390

    at main (D:\Git\Inv\squad-idle\test\.restyle-browser.js:116:12) {
  generatedMessage: true,
  code: 'ERR_ASSERTION',
  actual: 375,
  expected: 390,
  operator: 'strictEqual',
  diff: 'simple'
}

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
p=Path('test/.restyle-browser.js');s=p.read_text(encoding='utf-8').replace('    await evaluate(\"Game.reset(); Game.resume()\");','    await evaluate(\"Game.reset(); Game.resume()\");\\n    await command(\"Emulation.setDeviceMetricsOverride\", {width:390,height:844,deviceScaleFactor:1,mobile:true});');p.write_bytes(s.encode('utf-8'))
# Standalone visual checks retain the existing dependency-free Chrome harness without editing regression tests.
a=s.index('    await require(\"./art-checks\")');b=s.index('    const errors = events.filter',a)
s=s[:a]+'    await require(\"./art-checks\")({evaluate, command, screenshot, delay});\\n'+s[b:]
s=s.replace('PASS reload persistence; zero browser console/runtime errors','PASS zero browser console/runtime/network errors')
Path('test/browser-art.js').write_bytes(s.encode('utf-8'))
'@ | python -
"'$env:FONT_FALLBACK='"'1'; "'$env:BROWSER_NO_SANDBOX='"'1'; "'$env:BROWSER_FAST='"'1'; node test/.restyle-browser.js" in D:\Git\Inv\squad-idle
 succeeded in 14448ms:
Browser: Chrome/152.0.7977.76
Simulated unavailable web fonts: empty stylesheet, system font fallback
PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter
PASS three regional backgrounds, all regional type mappings and 140px bosses
PASS all five sheets: 375px no overflow, 44px buttons and raised active tab
PASS missing mapping, actual image decode failures and detached-image callbacks
PASS mobile 390×844 layout, tap sizes, initial auto-fight
PASS mercenary cards, equipment slots, placeholders, settings and reset confirmation
PASS real 100ms timer clears 1-1 and advances to 1-2 without input
PASS repeat, arrows, 320px no overflow and desktop 480px maximum width
PASS fresh 120s replay: 6 drops, pop animation and tab badge
PASS inventory grid/filter, equip CP animation and mercenary-slot equip mode at 375px
PASS single fusion T1→T2 flip, auto-fusion stagger/skip, lock and before/after potential reroll
PASS unique→legendary fusion with stronger flash
PASS P3 3h reload report, count-up, 200ms flips, strong flash, skip and harvest overflow
PASS P3 visibilitychange return after 61s opens a harvest report
PASS P3 visible skill casts, slot edits, chaos toggle only after 3-10 and 375px layout
PASS P3 JSON export/copy, invalid import validation, confirmed import round-trip
PASS reload persistence; zero browser console/runtime errors
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-wKJxyV

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

codex
375px에서 세 지역 배경, 지역별 몬스터·보스, 120ms 공격 전환, 모든 시트의 터치 영역을 확인했습니다. 누락된 매핑과 손상된 PNG도 벡터 폴백으로 전환됩니다. 새 아트와 어긋나는 기존 지역·보스 표시는 화면용 데이터로 맞추고, 마지막 회귀 검증을 진행하겠습니다.

apply patch
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..b34023e78f2d89bc8b1781d10fc7bf592000dad5
--- a/index.html
+++ b/index.html
@@ -15,9 +15,9 @@
     <header class="hud">
       <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
       <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
       </div>
     </header>
     <section class="expedition" aria-label="자동 전투">
@@ -49,9 +49,9 @@
     </section>
     <nav class="bottom-tabs" aria-label="게임 메뉴">
       <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
       <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
       <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
     </nav>
     <div id="toast" role="status"></div>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..720d32de98651d8d7033bb21160a5b14e1952279
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,13 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..6eb0ff4a8257f8db1eecc0555aa4e590d752031f
--- a/js/ui.js
+++ b/js/ui.js
@@ -198,7 +198,7 @@
     Game.on("wave", function () { drawScene(Game.getState(), false); });
     Game.on("hit", hit);
     Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
     Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
     Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
     Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

patch: completed
D:\Git\Inv\squad-idle\index.html
D:\Git\Inv\squad-idle\js\data.js
D:\Git\Inv\squad-idle\js\ui.js
D:\Git\Inv\squad-idle\style.css
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..5c1e73ab8187981713bf77a5b9d6f662a782bfa5
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,18 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..fd1b373cd02a84bb77295ca19b7bdbd1d8f5acee
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:FONT_FALLBACK='"'1'; "'$env:BROWSER_NO_SANDBOX='"'1'; node test/browser-art.js" in D:\Git\Inv\squad-idle
 succeeded in 10559ms:
Browser: Chrome/152.0.7977.76
Simulated unavailable web fonts: empty stylesheet, system font fallback
PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter
PASS three regional backgrounds, all regional type mappings and 140px bosses
PASS all five sheets: 375px no overflow, 44px buttons and raised active tab
PASS missing mapping, actual image decode failures and detached-image callbacks
PASS zero browser console/runtime/network errors
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-DXAhqH

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..5c1e73ab8187981713bf77a5b9d6f662a782bfa5
--- a/js/data.js
+++ b/js/data.js
@@ -31,7 +31,18 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
   version: "0.3.0 · P3",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..fd1b373cd02a84bb77295ca19b7bdbd1d8f5acee
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

apply patch
patch: completed
D:\Git\Inv\squad-idle\js\data.js
D:\Git\Inv\squad-idle\style.css
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:FONT_FALLBACK='"'1'; "'$env:BROWSER_NO_SANDBOX='"'1'; "'$env:BROWSER_FAST='"'1'; node test/.restyle-browser.js" in D:\Git\Inv\squad-idle
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem js/*.js | ForEach-Object { node --check $_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; node test/run.js' in D:\Git\Inv\squad-idle
 succeeded in 14366ms:
Browser: Chrome/152.0.7977.76
Simulated unavailable web fonts: empty stylesheet, system font fallback
PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter
PASS three regional backgrounds, all regional type mappings and 140px bosses
PASS all five sheets: 375px no overflow, 44px buttons and raised active tab
PASS missing mapping, actual image decode failures and detached-image callbacks
PASS mobile 390×844 layout, tap sizes, initial auto-fight
PASS mercenary cards, equipment slots, placeholders, settings and reset confirmation
PASS real 100ms timer clears 1-1 and advances to 1-2 without input
PASS repeat, arrows, 320px no overflow and desktop 480px maximum width
PASS fresh 120s replay: 6 drops, pop animation and tab badge
PASS inventory grid/filter, equip CP animation and mercenary-slot equip mode at 375px
PASS single fusion T1→T2 flip, auto-fusion stagger/skip, lock and before/after potential reroll
PASS unique→legendary fusion with stronger flash
PASS P3 3h reload report, count-up, 200ms flips, strong flash, skip and harvest overflow
PASS P3 visibilitychange return after 61s opens a harvest report
PASS P3 visible skill casts, slot edits, chaos toggle only after 3-10 and 375px layout
PASS P3 JSON export/copy, invalid import validation, confirmed import round-trip
PASS reload persistence; zero browser console/runtime errors
Screenshots: C:\Users\dklee\AppData\Local\Temp\p1-screenshots-udcbTv

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

 succeeded in 35747ms:
     Stage 1-1: 18.9s
PASS (a) fresh squad clears stage 1-1 within 60 seconds
PASS (b) stage 3-10 defeats a fresh level-1 squad, even with all classes
PASS (c) save/load preserves the entire state and ongoing battle
PASS (d) equal RNG seeds produce equal sequences (including seed zero)
PASS balance: unupgraded level-1 warrior clears 1-1 through 1-5 and fails 1-6
PASS P1 baseline without active skills first fails at 1-6, then gains XP on retry
PASS repeat keeps stage; first-clear coins cannot be farmed; challenge advances
PASS archer unlocks only on 1-6 clear; mage only on 2-6 clear
PASS wave, hit, kill, death, clear and fail events; boss is the last wave
PASS frontline priority survives reversed squad order and switches after death
PASS defense, forced critical damage, dead units and exact 60s timeout
PASS failure restores squad HP and retries the same stage without currency penalty
PASS catchUp below 60 seconds preserves deterministic fast-forward
PASS XP raises level and CP; the rolling five-minute window expires kills
PASS locked stages and invalid modes are rejected; corrupted saves preserve live state
PASS runtime installs only 100ms combat and 3s autosave timers; pause clears both
PASS all 30 stages, equipment data and P3 default skills are defined
PASS P2: 2,000 rolls per stage band respect tier bounds, boss tiers, rarity lines and ranges
     Fresh 120s drops: 3
PASS P2: every real boss kill drops its guaranteed tier, and fresh play drops in two minutes
PASS P2: fusion validates three distinct matching unlocked unequipped materials and gold
     Fusion bump rate: 0.1554
PASS P2: rarity bumps at 0.15 ± 0.02 over 5,000 actual fusions; highest rarity is retained
PASS P2: autoFuse cascades lowest tier first, respects protection, gold and tier 8
PASS P2: equip raises CP immediately, preserves HP ratio and unequip restores CP
PASS P2: sell is atomic, lock protects materials, reroll charges 20 and works while locked/equipped
PASS P2: schema v1 migration and populated v2 round-trip; bad equipment saves rejected
PASS P2: inventory cap sells lowest eligible rare; fully protected bag monetizes incoming drop
PASS P2: all combat potentials apply, gold/drop bonuses work and invincibility is enabled
PASS P2: transferring gear clears old owner, and changing gear never revives a downed ally
PASS Art: DATA.assets matches all 28 manifest keys and files exist
PASS P3: report math, 24h cap, no advance before harvest, exact-once persistence
PASS P3: fallback rates, 60s boundary, stage-specific statistics and boss XP
PASS P3: ready skills respect priority and independent cooldowns; book costs and slots
PASS P3: all twelve effects, DoT/HoT duration, aggro, shields and invincibility
PASS P3: revive restores 30% HP exactly once per battle, including across save/load
PASS P3: chaos unlock, separate progress, HP/ATK/gold multipliers and triple first-clear coins
PASS P3: v2→v3 and v1→v3 migrations; JSON import/export is atomic and round-trips
PASS P3: 3% class books persist on kill; corrupt skill/effect/report imports stay atomic
     Seed 271828: 5.50h, stage 30, levels 25,25,24, 248 fusions, 6 sessions
     Seed 7919: 5.10h, stage 30, levels 24,24,24, 209 fusions, 6 sessions
     Seed 42: 5.72h, stage 30, levels 25,25,25, 234 fusions, 6 sessions
PASS P3: full normal run clears 3-10 in six simulated hours with equipment, fusion and skills

38 tests passed.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git diff --check; git status --short; Get-Content -Encoding utf8 CHANGELOG.md | Select-Object -Last 3; Get-ChildItem test/*.js | ForEach-Object { '{0}: {1}' -f "'$_.Name,(Get-Content $_.FullName).Length }' in D:\Git\Inv\squad-idle
 succeeded in 178ms:
 M index.html
 M js/data.js
 M js/ui-equipment.js
 M js/ui-expedition.js
 M js/ui.js
 M style.css
 M test/run.js
?? .claude/restyle-log.txt
?? js/ui-art.js
?? test/.restyle-browser.js
?? test/art-checks.js
?? test/browser-art.js
- 장기 진행: 10초마다 장비 평가/장착·자동 합성·스킬북 사용, 두 번 실패하면 세 스테이지 아래에서 10분 반복 후 재도전, 매시간 저장/로드. 무료 장비/XP 없이 시드 **271828 / 7919 / 42**가 각각 **5.50 / 5.10 / 5.72시간**에 일반 3-10 클리어(각 6개 세션, 합성 248 / 209 / 234회). 모두 6시간 제한 통과.
- Chrome 152: 기존 P1/P2 회귀와 P3 브라우저 검증 통과. savedAt을 3시간 전으로 저장한 재로딩에서 카운트업·200ms 뒤집기·강한 플래시·탭 공개·수확/포화 판매, 61초 숨김 후 visibilitychange 복귀, 전투 스킬 이름/슬롯 변경, 3-10 전후 카오스 토글, JSON 복사/잘못된 입력 거부/확인 후 왕복을 확인했다. 375px 가로 넘침 없음, 콘솔·런타임 오류 0. 실행은 기존 격리 headless 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; P2 120초 전투는 1,200틱 재생, P3 연출은 실제 프레임으로 검증했다.
- 동시 작업의 `assets/` 파일에는 접근하지 않았다. 변경 이력은 직전 내용을 다시 읽어 단일 원자적 교체로 이 절을 덧붙였다.
.restyle-browser.js: 196
art-checks.js: 92
balance.js: 40
browser-art.js: 130
browser-equipment.js: 78
browser-p3.js: 98
browser.js: 183
long-run.js: 57
p3.js: 216
run.js: 493

diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};

codex
최종 검증에서 38개 로직 테스트와 기존 P1/P2/P3 브라우저 회귀가 모두 통과했습니다. 375px 가로 넘침과 44px 미만 버튼도 없었습니다. 브라우저 오류 0 검증은 Google Fonts 응답을 빈 CSS로 대체한 폰트 폴백 환경에서 통과했으며, 실제 외부 폰트 접근 제한은 변경 이력에 명시하겠습니다.

apply patch
patch: completed
D:\Git\Inv\squad-idle\CHANGELOG.md
D:\Git\Inv\squad-idle\test\browser-art.js
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 42d6957603e2df6983e1819dadf0719b17cdc2d8..2e9471b574fc78c62c47407cc57caef02a81cf13
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -93,3 +93,26 @@
 - 장기 진행: 10초마다 장비 평가/장착·자동 합성·스킬북 사용, 두 번 실패하면 세 스테이지 아래에서 10분 반복 후 재도전, 매시간 저장/로드. 무료 장비/XP 없이 시드 **271828 / 7919 / 42**가 각각 **5.50 / 5.10 / 5.72시간**에 일반 3-10 클리어(각 6개 세션, 합성 248 / 209 / 234회). 모두 6시간 제한 통과.
 - Chrome 152: 기존 P1/P2 회귀와 P3 브라우저 검증 통과. savedAt을 3시간 전으로 저장한 재로딩에서 카운트업·200ms 뒤집기·강한 플래시·탭 공개·수확/포화 판매, 61초 숨김 후 visibilitychange 복귀, 전투 스킬 이름/슬롯 변경, 3-10 전후 카오스 토글, JSON 복사/잘못된 입력 거부/확인 후 왕복을 확인했다. 375px 가로 넘침 없음, 콘솔·런타임 오류 0. 실행은 기존 격리 headless 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; P2 120초 전투는 1,200틱 재생, P3 연출은 실제 프레임으로 검증했다.
 - 동시 작업의 `assets/` 파일에는 접근하지 않았다. 변경 이력은 직전 내용을 다시 읽어 단일 원자적 교체로 이 절을 덧붙였다.
+
+## 0.4.0 · Restyle — 2026-09-07
+
+- `style.css` 전면 교체: 하늘 그라디언트, 크림 패널, 3px 갈색 테두리·18px 모서리·하단 그림자, 주황 버튼, 리본 제목, 올라오는 활성 탭, 빨간 장비 배지, 등급 변수. 기본 15px/보조 13px/최소 12px와 44px 터치 영역을 적용했다.
+- `index.html`: Jua/Gothic A1 Google Fonts 링크와 시스템 폴백, theme-color `#8FD3FF`, 무기 PNG 파비콘, HUD/탭 아이콘, 밝은 SVG 배경 폴백, `ui-art.js` 순서 로딩.
+- `js/data.js`: 28개 manifest 매핑을 `DATA.assets`에 인라인, 화면용 `artRegions`와 버전 0.4.0 추가. 게임 로직·전투 수치·저장 schemaVersion·기존 공개 함수 이름은 유지했다.
+- `js/ui-art.js`: `assetPath/assetLoaded/assetError`, `icon/portrait/spriteImage/sceneBackground`, `attackSprite`, `countUp`, `levelUp`. SVG 이미지 96px(보스 140px), 하단 5% 발 정렬, 공격 프레임 사전 로딩·120ms 복귀, 2초/3px 호흡, 흰 피격 필터·사망 페이드. 매핑 누락/이미지 로드 실패는 기존 벡터로 전환하며 실패 키는 세션 동안 재요청하지 않는다. 제거된 이미지의 늦은 콜백도 처리한다.
+- `js/ui.js`, `js/ui-equipment.js`, `js/ui-expedition.js`: 지역별 이미지·용병 초상·장비 4종/골드/코인/스킬북 아이콘 연결. 드롭 0→1.2→1 팝·회전·등급 글로우 후 0.9초부터 장비 탭으로 이동, 합성 뒤집기, 유니크 이상 0.4초 무지개 테두리, LEVEL UP! 라벨, CP/재화/보고서 골드 300ms 카운트업.
+- `test/run.js`: 기존 37개 테스트의 검증을 그대로 두고 manifest 28개 키·값 일치 및 실제 파일 존재 테스트 하나 추가. `test/art-checks.js`, `test/browser-art.js`: 설치된 Chrome/Edge로 실행하는 의존성 없는 별도 아트 검증.
+
+### DECISION: 화면과 게임 규칙의 분리
+
+- ASSET_SPEC의 숲→눈 봉우리→화산 순서는 기존 P3 지역 순서와 다르므로, 화면용 이름·스테이지 문구·보스 이름·몬스터 키만 별도 매핑했다. 저장된 스테이지 번호, 해금, 웨이브 및 몬스터 전투 타입은 보존한다.
+- 전투는 기존 SVG 좌표를 유지하고 `<image>`를 사용한다. 배경은 `preserveAspectRatio="xMidYMid slice"`로 프레임을 채운다. 용병 카드의 기존 벡터도 이미지 아래 폴백으로 보존했다.
+- 375px에서는 시트 여백을 줄여 기존 인벤토리 6열과 44px 터치 영역을 함께 유지한다. 350px 미만은 5열. 크림색 장비/비활성 탭에는 갈색 글자를 사용해 대비를 확보한다.
+- 합성 플래시는 기존 전투 씬 이벤트 표시를 유지하며 실제 보이는 최상위 dialog에도 적용한다. UI 모션에는 RNG나 전투 대기를 추가하지 않았고, 모션 감소 설정도 지원한다.
+
+### 검증
+
+- 모든 `js/*.js` 파일에 `node --check` 실행(Windows glob 미확장 대응), `node test/run.js`: **38 PASS**, 종료 코드 0. 기존 3개 장기 플레이 시드의 클리어 시간·성장·합성 횟수 동일. `git diff --check` 통과, 모든 변경 코드/테스트 파일 600줄 미만.
+- Chrome 152, 375×844: 28개 PNG 디코딩, 3지역 배경과 몬스터/보스, 96/140 크기, 120ms 공격 복귀, 2초 호흡, 300ms 카운트업, 레벨 라벨, 5개 시트와 탭 활성 이동, 최소 44px 버튼, 문서/시트 가로 넘침 없음 확인. 매핑 삭제·실제 PNG 디코딩 실패·분리된 이미지 콜백에서 폴백과 오류 없음 확인.
+- 기존 P1/P2/P3 브라우저 검증도 변경하지 않고 임시 실행 래퍼로 함께 실행하여 통과했다. 임시 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; 120초 전투는 1,200틱 재생, 이미지/모션은 실제 브라우저 프레임으로 검사했다.
+- **환경 제한:** 실제 Google Fonts 요청은 관리 환경에서 `ERR_NETWORK_ACCESS_DENIED` 네트워크 진단을 남긴다. 시스템 폰트로 정상 표시되는 것은 직접 확인했다. 오류 0 브라우저 회귀는 `FONT_FALLBACK=1`로 외부 폰트 CSS만 빈 응답으로 대체한 명시적 폴백 환경에서 통과했으며, 앱의 콘솔·런타임·에셋 오류는 0이었다. 원격 Google Fonts 다운로드 성공까지 검증한 것은 아니다. 별도 아트 검증 명령: `$env:FONT_FALLBACK='1'; $env:BROWSER_NO_SANDBOX='1'; node test/browser-art.js`.
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};
diff --git a/test/browser-art.js b/test/browser-art.js
index 41706225e45b31c80b9542c3eb946562a53ffe97..16cbd68da4a63055c1d2ffc509faeaabbcce751d
--- a/test/browser-art.js
+++ b/test/browser-art.js
@@ -1,6 +1,7 @@
 "use strict";
 
-// Optional integration smoke test. Node 22+ and an existing Chrome/Edge install; no packages.
+// Standalone art checks. Node 22+ and an existing Chrome/Edge install; no packages.
+// FONT_FALLBACK=1 explicitly substitutes empty remote font CSS to verify the system fallback.
 const assert = require("node:assert/strict");
 const fs = require("node:fs");
 const os = require("node:os");

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Remove-Item -LiteralPath 'D:\\Git\\Inv\\squad-idle\\test\\.restyle-browser.js'; git diff --check; git status --short; Get-ChildItem js/*.js | ForEach-Object { node --check "'$_.FullName; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; node --check test/browser-art.js; node --check test/art-checks.js' in D:\Git\Inv\squad-idle
 succeeded in 567ms:
 M CHANGELOG.md
 M index.html
 M js/data.js
 M js/ui-equipment.js
 M js/ui-expedition.js
 M js/ui.js
 M style.css
 M test/run.js
?? .claude/restyle-log.txt
?? js/ui-art.js
?? test/art-checks.js
?? test/browser-art.js

diff --git a/CHANGELOG.md b/CHANGELOG.md
index 42d6957603e2df6983e1819dadf0719b17cdc2d8..2e9471b574fc78c62c47407cc57caef02a81cf13
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -93,3 +93,26 @@
 - 장기 진행: 10초마다 장비 평가/장착·자동 합성·스킬북 사용, 두 번 실패하면 세 스테이지 아래에서 10분 반복 후 재도전, 매시간 저장/로드. 무료 장비/XP 없이 시드 **271828 / 7919 / 42**가 각각 **5.50 / 5.10 / 5.72시간**에 일반 3-10 클리어(각 6개 세션, 합성 248 / 209 / 234회). 모두 6시간 제한 통과.
 - Chrome 152: 기존 P1/P2 회귀와 P3 브라우저 검증 통과. savedAt을 3시간 전으로 저장한 재로딩에서 카운트업·200ms 뒤집기·강한 플래시·탭 공개·수확/포화 판매, 61초 숨김 후 visibilitychange 복귀, 전투 스킬 이름/슬롯 변경, 3-10 전후 카오스 토글, JSON 복사/잘못된 입력 거부/확인 후 왕복을 확인했다. 375px 가로 넘침 없음, 콘솔·런타임 오류 0. 실행은 기존 격리 headless 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; P2 120초 전투는 1,200틱 재생, P3 연출은 실제 프레임으로 검증했다.
 - 동시 작업의 `assets/` 파일에는 접근하지 않았다. 변경 이력은 직전 내용을 다시 읽어 단일 원자적 교체로 이 절을 덧붙였다.
+
+## 0.4.0 · Restyle — 2026-09-07
+
+- `style.css` 전면 교체: 하늘 그라디언트, 크림 패널, 3px 갈색 테두리·18px 모서리·하단 그림자, 주황 버튼, 리본 제목, 올라오는 활성 탭, 빨간 장비 배지, 등급 변수. 기본 15px/보조 13px/최소 12px와 44px 터치 영역을 적용했다.
+- `index.html`: Jua/Gothic A1 Google Fonts 링크와 시스템 폴백, theme-color `#8FD3FF`, 무기 PNG 파비콘, HUD/탭 아이콘, 밝은 SVG 배경 폴백, `ui-art.js` 순서 로딩.
+- `js/data.js`: 28개 manifest 매핑을 `DATA.assets`에 인라인, 화면용 `artRegions`와 버전 0.4.0 추가. 게임 로직·전투 수치·저장 schemaVersion·기존 공개 함수 이름은 유지했다.
+- `js/ui-art.js`: `assetPath/assetLoaded/assetError`, `icon/portrait/spriteImage/sceneBackground`, `attackSprite`, `countUp`, `levelUp`. SVG 이미지 96px(보스 140px), 하단 5% 발 정렬, 공격 프레임 사전 로딩·120ms 복귀, 2초/3px 호흡, 흰 피격 필터·사망 페이드. 매핑 누락/이미지 로드 실패는 기존 벡터로 전환하며 실패 키는 세션 동안 재요청하지 않는다. 제거된 이미지의 늦은 콜백도 처리한다.
+- `js/ui.js`, `js/ui-equipment.js`, `js/ui-expedition.js`: 지역별 이미지·용병 초상·장비 4종/골드/코인/스킬북 아이콘 연결. 드롭 0→1.2→1 팝·회전·등급 글로우 후 0.9초부터 장비 탭으로 이동, 합성 뒤집기, 유니크 이상 0.4초 무지개 테두리, LEVEL UP! 라벨, CP/재화/보고서 골드 300ms 카운트업.
+- `test/run.js`: 기존 37개 테스트의 검증을 그대로 두고 manifest 28개 키·값 일치 및 실제 파일 존재 테스트 하나 추가. `test/art-checks.js`, `test/browser-art.js`: 설치된 Chrome/Edge로 실행하는 의존성 없는 별도 아트 검증.
+
+### DECISION: 화면과 게임 규칙의 분리
+
+- ASSET_SPEC의 숲→눈 봉우리→화산 순서는 기존 P3 지역 순서와 다르므로, 화면용 이름·스테이지 문구·보스 이름·몬스터 키만 별도 매핑했다. 저장된 스테이지 번호, 해금, 웨이브 및 몬스터 전투 타입은 보존한다.
+- 전투는 기존 SVG 좌표를 유지하고 `<image>`를 사용한다. 배경은 `preserveAspectRatio="xMidYMid slice"`로 프레임을 채운다. 용병 카드의 기존 벡터도 이미지 아래 폴백으로 보존했다.
+- 375px에서는 시트 여백을 줄여 기존 인벤토리 6열과 44px 터치 영역을 함께 유지한다. 350px 미만은 5열. 크림색 장비/비활성 탭에는 갈색 글자를 사용해 대비를 확보한다.
+- 합성 플래시는 기존 전투 씬 이벤트 표시를 유지하며 실제 보이는 최상위 dialog에도 적용한다. UI 모션에는 RNG나 전투 대기를 추가하지 않았고, 모션 감소 설정도 지원한다.
+
+### 검증
+
+- 모든 `js/*.js` 파일에 `node --check` 실행(Windows glob 미확장 대응), `node test/run.js`: **38 PASS**, 종료 코드 0. 기존 3개 장기 플레이 시드의 클리어 시간·성장·합성 횟수 동일. `git diff --check` 통과, 모든 변경 코드/테스트 파일 600줄 미만.
+- Chrome 152, 375×844: 28개 PNG 디코딩, 3지역 배경과 몬스터/보스, 96/140 크기, 120ms 공격 복귀, 2초 호흡, 300ms 카운트업, 레벨 라벨, 5개 시트와 탭 활성 이동, 최소 44px 버튼, 문서/시트 가로 넘침 없음 확인. 매핑 삭제·실제 PNG 디코딩 실패·분리된 이미지 콜백에서 폴백과 오류 없음 확인.
+- 기존 P1/P2/P3 브라우저 검증도 변경하지 않고 임시 실행 래퍼로 함께 실행하여 통과했다. 임시 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; 120초 전투는 1,200틱 재생, 이미지/모션은 실제 브라우저 프레임으로 검사했다.
+- **환경 제한:** 실제 Google Fonts 요청은 관리 환경에서 `ERR_NETWORK_ACCESS_DENIED` 네트워크 진단을 남긴다. 시스템 폰트로 정상 표시되는 것은 직접 확인했다. 오류 0 브라우저 회귀는 `FONT_FALLBACK=1`로 외부 폰트 CSS만 빈 응답으로 대체한 명시적 폴백 환경에서 통과했으며, 앱의 콘솔·런타임·에셋 오류는 0이었다. 원격 Google Fonts 다운로드 성공까지 검증한 것은 아니다. 별도 아트 검증 명령: `$env:FONT_FALLBACK='1'; $env:BROWSER_NO_SANDBOX='1'; node test/browser-art.js`.
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};
diff --git a/test/browser-art.js b/test/browser-art.js
index 41706225e45b31c80b9542c3eb946562a53ffe97..16cbd68da4a63055c1d2ffc509faeaabbcce751d
--- a/test/browser-art.js
+++ b/test/browser-art.js
@@ -1,6 +1,7 @@
 "use strict";
 
-// Optional integration smoke test. Node 22+ and an existing Chrome/Edge install; no packages.
+// Standalone art checks. Node 22+ and an existing Chrome/Edge install; no packages.
+// FONT_FALLBACK=1 explicitly substitutes empty remote font CSS to verify the system fallback.
 const assert = require("node:assert/strict");
 const fs = require("node:fs");
 const os = require("node:os");

codex
Implemented the full restyle, image sprites, icons, animations, and changelog while preserving gameplay.

- **38 tests pass**; JavaScript syntax checks clean.
- **375px verified**: no horizontal overflow, ≥44px buttons.
- Regional assets, attack timing, and vector fallbacks verified.

Google Fonts is blocked by this environment. System-font fallback works; zero-error browser checks passed with remote font CSS stubbed.
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 42d6957603e2df6983e1819dadf0719b17cdc2d8..2e9471b574fc78c62c47407cc57caef02a81cf13
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -93,3 +93,26 @@
 - 장기 진행: 10초마다 장비 평가/장착·자동 합성·스킬북 사용, 두 번 실패하면 세 스테이지 아래에서 10분 반복 후 재도전, 매시간 저장/로드. 무료 장비/XP 없이 시드 **271828 / 7919 / 42**가 각각 **5.50 / 5.10 / 5.72시간**에 일반 3-10 클리어(각 6개 세션, 합성 248 / 209 / 234회). 모두 6시간 제한 통과.
 - Chrome 152: 기존 P1/P2 회귀와 P3 브라우저 검증 통과. savedAt을 3시간 전으로 저장한 재로딩에서 카운트업·200ms 뒤집기·강한 플래시·탭 공개·수확/포화 판매, 61초 숨김 후 visibilitychange 복귀, 전투 스킬 이름/슬롯 변경, 3-10 전후 카오스 토글, JSON 복사/잘못된 입력 거부/확인 후 왕복을 확인했다. 375px 가로 넘침 없음, 콘솔·런타임 오류 0. 실행은 기존 격리 headless 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; P2 120초 전투는 1,200틱 재생, P3 연출은 실제 프레임으로 검증했다.
 - 동시 작업의 `assets/` 파일에는 접근하지 않았다. 변경 이력은 직전 내용을 다시 읽어 단일 원자적 교체로 이 절을 덧붙였다.
+
+## 0.4.0 · Restyle — 2026-09-07
+
+- `style.css` 전면 교체: 하늘 그라디언트, 크림 패널, 3px 갈색 테두리·18px 모서리·하단 그림자, 주황 버튼, 리본 제목, 올라오는 활성 탭, 빨간 장비 배지, 등급 변수. 기본 15px/보조 13px/최소 12px와 44px 터치 영역을 적용했다.
+- `index.html`: Jua/Gothic A1 Google Fonts 링크와 시스템 폴백, theme-color `#8FD3FF`, 무기 PNG 파비콘, HUD/탭 아이콘, 밝은 SVG 배경 폴백, `ui-art.js` 순서 로딩.
+- `js/data.js`: 28개 manifest 매핑을 `DATA.assets`에 인라인, 화면용 `artRegions`와 버전 0.4.0 추가. 게임 로직·전투 수치·저장 schemaVersion·기존 공개 함수 이름은 유지했다.
+- `js/ui-art.js`: `assetPath/assetLoaded/assetError`, `icon/portrait/spriteImage/sceneBackground`, `attackSprite`, `countUp`, `levelUp`. SVG 이미지 96px(보스 140px), 하단 5% 발 정렬, 공격 프레임 사전 로딩·120ms 복귀, 2초/3px 호흡, 흰 피격 필터·사망 페이드. 매핑 누락/이미지 로드 실패는 기존 벡터로 전환하며 실패 키는 세션 동안 재요청하지 않는다. 제거된 이미지의 늦은 콜백도 처리한다.
+- `js/ui.js`, `js/ui-equipment.js`, `js/ui-expedition.js`: 지역별 이미지·용병 초상·장비 4종/골드/코인/스킬북 아이콘 연결. 드롭 0→1.2→1 팝·회전·등급 글로우 후 0.9초부터 장비 탭으로 이동, 합성 뒤집기, 유니크 이상 0.4초 무지개 테두리, LEVEL UP! 라벨, CP/재화/보고서 골드 300ms 카운트업.
+- `test/run.js`: 기존 37개 테스트의 검증을 그대로 두고 manifest 28개 키·값 일치 및 실제 파일 존재 테스트 하나 추가. `test/art-checks.js`, `test/browser-art.js`: 설치된 Chrome/Edge로 실행하는 의존성 없는 별도 아트 검증.
+
+### DECISION: 화면과 게임 규칙의 분리
+
+- ASSET_SPEC의 숲→눈 봉우리→화산 순서는 기존 P3 지역 순서와 다르므로, 화면용 이름·스테이지 문구·보스 이름·몬스터 키만 별도 매핑했다. 저장된 스테이지 번호, 해금, 웨이브 및 몬스터 전투 타입은 보존한다.
+- 전투는 기존 SVG 좌표를 유지하고 `<image>`를 사용한다. 배경은 `preserveAspectRatio="xMidYMid slice"`로 프레임을 채운다. 용병 카드의 기존 벡터도 이미지 아래 폴백으로 보존했다.
+- 375px에서는 시트 여백을 줄여 기존 인벤토리 6열과 44px 터치 영역을 함께 유지한다. 350px 미만은 5열. 크림색 장비/비활성 탭에는 갈색 글자를 사용해 대비를 확보한다.
+- 합성 플래시는 기존 전투 씬 이벤트 표시를 유지하며 실제 보이는 최상위 dialog에도 적용한다. UI 모션에는 RNG나 전투 대기를 추가하지 않았고, 모션 감소 설정도 지원한다.
+
+### 검증
+
+- 모든 `js/*.js` 파일에 `node --check` 실행(Windows glob 미확장 대응), `node test/run.js`: **38 PASS**, 종료 코드 0. 기존 3개 장기 플레이 시드의 클리어 시간·성장·합성 횟수 동일. `git diff --check` 통과, 모든 변경 코드/테스트 파일 600줄 미만.
+- Chrome 152, 375×844: 28개 PNG 디코딩, 3지역 배경과 몬스터/보스, 96/140 크기, 120ms 공격 복귀, 2초 호흡, 300ms 카운트업, 레벨 라벨, 5개 시트와 탭 활성 이동, 최소 44px 버튼, 문서/시트 가로 넘침 없음 확인. 매핑 삭제·실제 PNG 디코딩 실패·분리된 이미지 콜백에서 폴백과 오류 없음 확인.
+- 기존 P1/P2/P3 브라우저 검증도 변경하지 않고 임시 실행 래퍼로 함께 실행하여 통과했다. 임시 프로필에서 `BROWSER_NO_SANDBOX=1`, `BROWSER_FAST=1` 사용; 120초 전투는 1,200틱 재생, 이미지/모션은 실제 브라우저 프레임으로 검사했다.
+- **환경 제한:** 실제 Google Fonts 요청은 관리 환경에서 `ERR_NETWORK_ACCESS_DENIED` 네트워크 진단을 남긴다. 시스템 폰트로 정상 표시되는 것은 직접 확인했다. 오류 0 브라우저 회귀는 `FONT_FALLBACK=1`로 외부 폰트 CSS만 빈 응답으로 대체한 명시적 폴백 환경에서 통과했으며, 앱의 콘솔·런타임·에셋 오류는 0이었다. 원격 Google Fonts 다운로드 성공까지 검증한 것은 아니다. 별도 아트 검증 명령: `$env:FONT_FALLBACK='1'; $env:BROWSER_NO_SANDBOX='1'; node test/browser-art.js`.
diff --git a/index.html b/index.html
index 48a6f0bbbe41503af93519bee0447e4ab4ec7d26..ca1e0067e94f4b11a27cc1e763139058e5eaa6ef
--- a/index.html
+++ b/index.html
@@ -1,70 +1,70 @@
-<!doctype html>
-<html lang="ko">
-<head>
-  <meta charset="utf-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
-  <meta name="theme-color" content="#8FD3FF">
-  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
-  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
-  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
-  <link rel="stylesheet" href="style.css">
-  <title>용병단 · 첫 번째 원정</title>
-</head>
-<body>
-  <main id="app">
-    <header class="hud">
-      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
-      <div class="resources">
-        <div class="resource power"><span class="resource-label">전투력 <small>CP</small></span><strong id="cp">0</strong></div>
-        <div class="resource"><span class="resource-label"><span data-icon="gold">??</span> 골드</span><strong id="gold">0</strong></div>
-        <div class="resource"><span class="resource-label"><i class="coin-gem">◆</i> 코인</span><strong id="coins">0</strong></div>
-      </div>
-    </header>
-    <section class="expedition" aria-label="자동 전투">
-      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
-      <div class="time-track"><div id="time-fill"></div></div>
-      <div class="battle-scene">
-        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
-        <svg id="battle-svg" viewBox="0 0 390 340" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
-          <g id="scene-background"><g class="asset-fallback">
-            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
-            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
-            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
-            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
-            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
-          </g></g>
-          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
-        </svg>
-        <div id="battle-result" class="battle-result" aria-live="polite"></div>
-        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
-      </div>
-      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
-      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
-    </section>
-    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
-      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
-      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
-      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
-      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
-    </section>
-    <nav class="bottom-tabs" aria-label="게임 메뉴">
-      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
-      <button data-tab="equipment" aria-haspopup="dialog"><span>♜</span>장비<b id="equipment-badge" hidden>0</b></button>
-      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
-      <button data-tab="skills" aria-haspopup="dialog"><span>✧</span>스킬</button>
-      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
-    </nav>
-    <div id="toast" role="status"></div>
-    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
-  </main>
-  <script src="js/data.js"></script>
-  <script src="js/battle.js"></script>
-  <script src="js/game.js"></script>
-  <script src="js/game-equipment.js"></script>
-  <script src="js/game-expedition.js"></script>
-  <script src="js/ui.js"></script>
-  <script src="js/ui-art.js"></script>
-  <script src="js/ui-equipment.js"></script>
-  <script src="js/ui-expedition.js"></script>
-</body>
-</html>
+<!doctype html>
+<html lang="ko">
+<head>
+  <meta charset="utf-8">
+  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
+  <meta name="theme-color" content="#8FD3FF">
+  <meta name="description" content="안개 너머로 나아가는 작은 용병단. 자동 전투와 성장의 첫 번째 원정.">
+  <link rel="icon" type="image/png" href="assets/icon/weapon.png">
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Gothic+A1:wght@400;700;800&amp;family=Jua&amp;display=swap">
+  <link rel="stylesheet" href="style.css">
+  <title>용병단 · 첫 번째 원정</title>
+</head>
+<body>
+  <main id="app">
+    <header class="hud">
+      <div class="brand-row"><h1><span class="brand-mark">⚔</span> 용병단</h1><span class="chapter-label">첫 번째 원정</span><span class="save-state" id="save-status">자동 저장</span></div>
+      <div class="resources">
+        <div class="resource power"><span class="resource-label"><span data-icon="weapon">⚔</span>전투력</span><strong id="cp">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="gold">🪙</span>골드</span><strong id="gold">0</strong></div>
+        <div class="resource"><span class="resource-label"><span data-icon="coin">◆</span>코인</span><strong id="coins">0</strong></div>
+      </div>
+    </header>
+    <section class="expedition" aria-label="자동 전투">
+      <div class="stage-header"><div><p id="region-name" class="eyebrow">잊힌 숲</p><h2><span id="stage-id">1-1</span><span class="title-divider">/</span><span id="stage-name">숲의 입구</span></h2></div><div class="clock"><span>남은 시간</span><strong id="timer">60<small>초</small></strong></div></div>
+      <div class="time-track"><div id="time-fill"></div></div>
+      <div class="battle-scene">
+        <div class="scene-topline"><span class="auto-badge"><i></i> 자동 전투 중</span><span id="wave-label">웨이브 1 / 3</span></div>
+        <svg id="battle-svg" viewBox="0 0 390 340" preserveAspectRatio="xMidYMid slice" role="img" aria-label="용병과 몬스터의 자동 전투 장면">
+          <g id="scene-background"><g class="asset-fallback">
+            <defs><linearGradient id="sky" x2="0" y2="1"><stop stop-color="#8FD3FF"/><stop offset="1" stop-color="#EAF7FF"/></linearGradient></defs>
+            <rect width="390" height="340" fill="url(#sky)"/><circle cx="300" cy="72" r="30" fill="#FFC93C"/>
+            <path d="M0 187Q80 150 160 185T390 175V340H0Z" fill="#95E888"/>
+            <path d="M140 340l30-145h50l75 145" fill="#FFE9B8"/>
+            <g fill="#3DDC97" stroke="#4A2C12" stroke-width="3"><path d="M20 180V65m-20 80Q-20 80 25 55q55 35 10 92zM365 180V50m-22 93q-40-55 18-95 60 35 25 93z"/></g>
+          </g></g>
+          <g id="mercenary-layer"></g><g id="enemy-layer"></g><g id="effect-layer" aria-hidden="true"></g>
+        </svg>
+        <div id="battle-result" class="battle-result" aria-live="polite"></div>
+        <div class="scene-caption"><span class="caption-line"></span><span id="region-subtitle">안개 너머, 첫 번째 원정</span><span class="caption-line"></span></div>
+      </div>
+      <div id="squad-strip" class="squad-strip" aria-label="용병 상태"></div>
+      <div class="harvest-row"><span>이번 원정의 발걸음</span><span><b id="gold-rate">0</b> 골드/초 <span class="rate-divider">·</span> <b id="kill-rate">0</b> 처치/초</span></div>
+    </section>
+    <section class="stage-controls" aria-label="스테이지 선택과 전투 모드">
+      <div id="difficulty-toggle" class="difficulty-toggle" role="group" aria-label="원정 난이도" hidden><button data-difficulty="normal" aria-pressed="true">일반</button><button data-difficulty="chaos" aria-pressed="false">카오스</button></div>
+      <div class="stage-navigation"><button id="previous-stage" aria-label="이전 스테이지">‹</button><div><span>스테이지</span><strong id="selected-stage">1-1</strong></div><button id="next-stage" aria-label="다음 스테이지">›</button></div>
+      <div class="mode-toggle" role="group" aria-label="전투 모드"><button id="repeat-mode" aria-pressed="false">↻ 반복</button><button id="challenge-mode" class="active" aria-pressed="true">도전 →</button></div>
+      <p id="mode-hint">클리어하면 다음 스테이지로 나아갑니다</p>
+    </section>
+    <nav class="bottom-tabs" aria-label="게임 메뉴">
+      <button data-tab="mercenaries" aria-haspopup="dialog"><span>⚔</span>용병</button>
+      <button data-tab="equipment" aria-haspopup="dialog"><span data-icon="hat">♜</span>장비<b id="equipment-badge" hidden>0</b></button>
+      <button data-tab="fusion" aria-haspopup="dialog"><span>◇</span>합성</button>
+      <button data-tab="skills" aria-haspopup="dialog"><span data-icon="skillbook">✧</span>스킬</button>
+      <button data-tab="settings" aria-haspopup="dialog"><span>⚙</span>설정</button>
+    </nav>
+    <div id="toast" role="status"></div>
+    <dialog id="sheet" aria-labelledby="sheet-title"><div class="sheet-grip"></div><div class="sheet-heading"><div><p class="eyebrow">용병단의 기록</p><h2 id="sheet-title">용병</h2></div><button id="close-sheet" aria-label="닫기">×</button></div><div id="sheet-content"></div></dialog>
+  </main>
+  <script src="js/data.js"></script>
+  <script src="js/battle.js"></script>
+  <script src="js/game.js"></script>
+  <script src="js/game-equipment.js"></script>
+  <script src="js/game-expedition.js"></script>
+  <script src="js/ui.js"></script>
+  <script src="js/ui-art.js"></script>
+  <script src="js/ui-equipment.js"></script>
+  <script src="js/ui-expedition.js"></script>
+</body>
+</html>
diff --git a/js/data.js b/js/data.js
index 4bfeb61b37d9c65790be19aa85be51c0b3e8a296..68e15e2de274ac448bd59776b285c606aa2a8354
--- a/js/data.js
+++ b/js/data.js
@@ -31,8 +31,19 @@
     "mon.salamander": "assets/mon/salamander.png",
     "mon.boss_dragon": "assets/mon/boss_dragon.png",
     "bg.volcano": "assets/bg/volcano.png"
-} ,
-  version: "0.3.0 · P3",
+  },
+  // DECISION: ASSET_SPEC region order affects only art; stage balance and saved indices stay intact.
+  artRegions: [
+    { bg: "forest", name: "잊힌 숲", subtitle: "햇살 아래, 첫 번째 원정", bossName: "고목 수호자",
+      monsters: { slime: "slime", goblin: "goblin", brute: "mushroom", boss: "boss_treant" } },
+    { bg: "peak", name: "얼어붙은 봉우리", subtitle: "반짝이는 눈길을 따라", bossName: "얼음 골렘",
+      stages: ["눈 덮인 고개", "얼어붙은 호수", "눈꽃 초소", "푸른 회랑", "얼음 정원", "마법사의 봉화", "백야의 탑", "서리 다리", "눈보라 성문", "얼음 왕좌"],
+      monsters: { slime: "ice_bat", goblin: "yeti", brute: "frost_wolf", boss: "boss_ice_golem" } },
+    { bg: "volcano", name: "잿빛 화산", subtitle: "뜨거운 모험이 기다려요", bossName: "아기 드래곤",
+      stages: ["불꽃의 경계", "메마른 강", "갈라진 절벽", "붉은 모래", "용암 광산", "불꽃 감옥", "황금 용광로", "불씨의 다리", "용의 둥지", "드래곤 봉우리"],
+      monsters: { slime: "lava_slime", goblin: "imp", brute: "salamander", boss: "boss_dragon" } }
+  ],
+  version: "0.4.0 · Restyle",
   schemaVersion: 3,
   offline: { thresholdMs: 60000, maxMs: 86400000, efficiency: 0.7, fallbackKillsPerSec: 0.1, maxRolls: 500 },
   chaos: { hp: 8, atk: 5, gold: 4, firstClearCoins: 3 },
diff --git a/js/ui-art.js b/js/ui-art.js
new file mode 100644
index 0000000000000000000000000000000000000000..3d42ea762a8b17840670e5c4b31e2589caf826bb
--- /dev/null
+++ b/js/ui-art.js
@@ -0,0 +1,100 @@
+"use strict";
+
+(function () {
+  var failed = new Set(), counters = new Map();
+  var regions = DATA.artRegions;
+  UI.artRegion = function (index) { return regions[index]; };
+  UI.assetPath = function (key) { return failed.has(key) ? null : DATA.assets[key]; };
+  UI.assetError = function (node) {
+    failed.add(node.dataset.asset);
+    node.removeAttribute("href"); node.removeAttribute("src"); node.style.display = "none";
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "";
+  };
+  UI.assetLoaded = function (node) {
+    var fallback = node.parentNode?.querySelector(".asset-fallback");
+    if (fallback) fallback.style.display = "none";
+  };
+  UI.icon = function (key, fallback) {
+    var path = UI.assetPath("icon." + key);
+    return '<span class="asset-icon" aria-hidden="true"><span class="asset-fallback">' + fallback + '</span>' +
+      (path ? '<img alt="" src="' + path + '" data-asset="icon.' + key + '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)">' : '') + '</span>';
+  };
+  UI.portrait = function (id, fallback) {
+    var key = "merc." + id + ".idle", path = UI.assetPath(key);
+    return '<svg viewBox="-48 -92 96 100" aria-hidden="true"><g class="asset-fallback">' + fallback + '</g>' +
+      (path ? '<image x="-48" y="-91.2" width="96" height="96" href="' + path + '" data-asset="' + key +
+        '" onload="UI.assetLoaded(this)" onerror="UI.assetError(this)"/>' : '') + '</svg>';
+  };
+  UI.spriteImage = function (parent, key, size) {
+    var path = UI.assetPath(key);
+    if (!path) return null;
+    var image = document.createElementNS("http://www.w3.org/2000/svg", "image");
+    image.setAttribute("class", "sprite-image"); image.dataset.asset = key;
+    image.setAttribute("x", -size / 2); image.setAttribute("y", -size * .95);
+    image.setAttribute("width", size); image.setAttribute("height", size);
+    image.addEventListener("error", function () { UI.assetError(image); });
+    image.addEventListener("load", function () { UI.assetLoaded(image); });
+    parent.appendChild(image); image.setAttribute("href", path);
+    return image;
+  };
+  UI.sceneBackground = function (region) {
+    var parent = document.getElementById("scene-background"), key = "bg." + regions[region].bg;
+    if (parent.dataset.asset === key) return;
+    parent.dataset.asset = key;
+    parent.querySelector("image")?.remove();
+    parent.querySelector(".asset-fallback").style.display = "";
+    var image = UI.spriteImage(parent, key, 390);
+    if (image) {
+      image.setAttribute("x", 0); image.setAttribute("y", 0);
+      image.setAttribute("width", 390); image.setAttribute("height", 340);
+      image.setAttribute("preserveAspectRatio", "xMidYMid slice"); image.setAttribute("class", "region-image");
+    }
+  };
+  UI.attackSprite = function (sprite) {
+    var image = sprite.querySelector(".sprite-image");
+    if (!image || !image.dataset.asset.startsWith("merc.")) return;
+    var idle = image.dataset.asset.replace(".attack", ".idle"), attack = idle.replace(".idle", ".attack");
+    if (!UI.assetPath(attack)) return;
+    cancelAnimationFrame(image.attackFrame);
+    image.dataset.asset = attack; image.setAttribute("href", UI.assetPath(attack));
+    var start = performance.now();
+    function frame(now) {
+      if (!image.isConnected) return;
+      if (now - start < 120) { image.attackFrame = requestAnimationFrame(frame); return; }
+      image.dataset.asset = idle;
+      if (UI.assetPath(idle)) { image.style.display = ""; image.setAttribute("href", UI.assetPath(idle)); }
+    }
+    image.attackFrame = requestAnimationFrame(frame);
+  };
+  UI.countUp = function (id, value) {
+    var node = document.getElementById(id), counter = counters.get(id);
+    if (!counter) { counters.set(id, { target: value, value: value }); node.textContent = UI.fmt(value); return; }
+    if (counter.target === value) return;
+    cancelAnimationFrame(counter.frame);
+    var from = counter.value, start = performance.now(); counter.target = value;
+    function frame(now) {
+      var t = Math.min(1, (now - start) / 300);
+      counter.value = from + (value - from) * (1 - (1 - t) ** 3);
+      node.textContent = UI.fmt(Math.round(counter.value));
+      if (t < 1) counter.frame = requestAnimationFrame(frame);
+    }
+    counter.frame = requestAnimationFrame(frame);
+  };
+  UI.levelUp = function (event) {
+    var sprite = document.getElementById("sprite-" + event.id);
+    if (!sprite) return;
+    var label = document.createElementNS("http://www.w3.org/2000/svg", "text");
+    label.setAttribute("x", sprite.dataset.x); label.setAttribute("y", Number(sprite.dataset.y) - 104);
+    label.setAttribute("text-anchor", "middle"); label.setAttribute("class", "level-up-label");
+    label.textContent = "LEVEL UP!"; document.getElementById("effect-layer").appendChild(label);
+    label.addEventListener("animationend", function () { label.remove(); }, { once: true });
+  };
+  document.querySelectorAll("[data-icon]").forEach(function (node) { node.innerHTML = UI.icon(node.dataset.icon, node.textContent); });
+  // DECISION: Warm attack frames before the first combat tick; failed frames are never retried this session.
+  DATA.mercenaries.forEach(function (merc) {
+    var key = "merc." + merc.id + ".attack", path = UI.assetPath(key);
+    if (!path) return;
+    var frame = new Image(); frame.onerror = function () { failed.add(key); }; frame.src = path;
+  });
+})();
diff --git a/js/ui-equipment.js b/js/ui-equipment.js
index b39d3f0dbc5000220061ef02963466534d2504d5..69a552fb5664f2af5595eaf683604181ef59027e
--- a/js/ui-equipment.js
+++ b/js/ui-equipment.js
@@ -104,10 +104,14 @@
     cpFrame = requestAnimationFrame(frame);
   };
   function flash(strong) {
-    var scene = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector(".battle-scene");
-    scene.classList.remove("loot-flash", "loot-flash-strong");
-    void scene.offsetWidth;
-    scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    // DECISION: Keep the scene's existing feedback hook; also flash the visible top-layer sheet.
+    var surfaces = [document.querySelector(".battle-scene"),
+      document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]")];
+    surfaces.filter(Boolean).forEach(function (scene) {
+      scene.classList.remove("loot-flash", "loot-flash-strong");
+      void scene.offsetWidth;
+      scene.classList.add(strong ? "loot-flash-strong" : "loot-flash");
+    });
   }
   function drop(item) {
     if (!item.autoSold && currentSheet !== "equipment") { unread++; badge(); }
diff --git a/js/ui-expedition.js b/js/ui-expedition.js
index 14015794c03fcf079698793e22e42452094ce5e8..6f0d10537a0987522b389b5ea860931f7b4554b0
--- a/js/ui-expedition.js
+++ b/js/ui-expedition.js
@@ -19,7 +19,7 @@
     el("sheet-content").innerHTML = '<p class="sheet-intro">슬롯 순서대로 자동 발동합니다. 해제 후 탭하면 마지막 슬롯에 배치됩니다.</p>' +
       '<div class="skill-selector">' + state.mercenaries.map(function (m) {
         return '<button data-skill-owner="' + m.id + '" aria-pressed="' + (owner === m.id) + '" class="' + (owner === m.id ? 'active' : '') + '">' + mercName(m.id) + '</button>';
-      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '?') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
+      }).join('') + '</div><p class="skill-books">' + UI.icon('skillbook', '▤') + mercName(owner) + ' 스킬북 <b>' + UI.fmt(state.skillBooks[owner]) + '</b></p>' +
       '<div class="active-skills">' + [0, 1, 2].map(function (index) {
         var skill = DATA.skills.find(function (s) { return s.id === merc.skills[index]; });
         return '<span>' + UI.fmt(index + 1) + ' · ' + (skill ? skill.name : '빈 슬롯') + '</span>';
@@ -79,14 +79,14 @@
   var overlay = document.createElement("dialog");
   overlay.id = "offline-report"; overlay.setAttribute("aria-labelledby", "report-title");
   overlay.innerHTML = '<div class="report-heading"><p class="eyebrow">용병단이 돌아왔습니다</p><h2 id="report-title">원정 보고서</h2><p id="report-duration"></p></div>' +
-    '<div class="report-gold"><span>획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
+    '<div class="report-gold"><span>' + UI.icon("gold", "🪙") + '획득 골드</span><strong id="report-gold">0</strong></div><p id="report-stats"></p>' +
     '<p id="report-progress" aria-live="polite">탭하면 모두 공개</p><div id="report-items"></div>' +
     '<div class="harvest-footer"><p>장비함이 가득 차면 자동 판매 규칙이 적용됩니다.</p><button id="harvest-report" class="primary">수확하기</button></div>';
   el("app").appendChild(overlay);
   function revealOne(item) {
     var card = document.createElement("div");
     card.className = "fusion-card";
-    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">▤</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
+    var content = item.kind === "skillBook" ? '<span class="item-card book-card"><span class="item-icon">' + UI.icon("skillbook", "▤") + '</span><b>' + mercName(item.owner) + '</b><small>스킬북 ×' + UI.fmt(item.count) + '</small></span>' : UI.itemCard(item);
     card.innerHTML = '<div class="flip-inner"><div class="card-back">◇</div><div class="card-front">' + content + '</div></div>';
     el("report-items").appendChild(card);
     if (DATA.rarityOrder.indexOf(item.rarity) >= 2) { card.classList.add("rarity-upgrade"); UI.lootFlash(true); if (navigator.userActivation?.hasBeenActive) navigator.vibrate?.(30); }
diff --git a/js/ui.js b/js/ui.js
index 728aaaf535ae20da9bc044647f6a7c3dccc8a39f..ff8b2a719b0b13bb9c38a6dde4677e509d814ad5
--- a/js/ui.js
+++ b/js/ui.js
@@ -1,231 +1,231 @@
-"use strict";
-
-var UI = (function () {
-  var svgNS = "http://www.w3.org/2000/svg";
-  var activeSheet = null;
-  var hiddenAt = null;
-  var sheetSignature = "";
-  var latestState;
-  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
-  function el(id) { return document.getElementById(id); }
-  function fmt(n) {
-    if (!Number.isFinite(n)) return "0";
-    var abs = Math.abs(n);
-    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
-    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
-    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
-    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
-  }
-  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
-  function svgNode(tag, attrs, content) {
-    var node = document.createElementNS(svgNS, tag);
-    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
-    if (content !== undefined) node.textContent = content;
-    return node;
-  }
-  function figure(type, color) {
-    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
-    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
-    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
-    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
-    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
-    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
-  }
-  function position(unit) {
-    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
-    if (unit.type === "boss") return { x: 284, y: 245 };
-    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
-  }
-  function drawUnit(unit, color, locked) {
-    var p = position(unit);
-    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
-      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
-    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
-    var body = svgNode("g", { class: "unit-body" });
-    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
-    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
-    var region = DATA.stages[Game.getState().currentStage].region;
-    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
-    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
-    body.addEventListener("animationend", function (event) {
-      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
-    });
-    group.appendChild(body);
-    if (!locked) {
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
-      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
-        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
-    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
-    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
-    return group;
-  }
-  function drawScene(state, clearEffects) {
-    UI.sceneBackground(DATA.stages[state.currentStage].region);
-    if (clearEffects) el("effect-layer").replaceChildren();
-    el("mercenary-layer").replaceChildren();
-    el("enemy-layer").replaceChildren();
-    DATA.mercenaries.slice().reverse().forEach(function (merc) {
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
-    });
-    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
-  }
-  function animate(node, name) {
-    node.classList.remove(name);
-    void node.getBoundingClientRect();
-    node.classList.add(name);
-  }
-  function hit(event) {
-    var attacker = el("sprite-" + event.attackerId);
-    var target = el("sprite-" + event.targetId);
-    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
-    if (!target) return;
-    animate(target.querySelector(".unit-body"), "hit-flash");
-    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
-      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
-    (event.crit ? "✦ " : "") + fmt(event.amount));
-    el("effect-layer").appendChild(text);
-    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
-  }
-  function death(event) {
-    var target = el("sprite-" + event.id);
-    if (!target) return;
-    var ghost = target.cloneNode(true);
-    ghost.removeAttribute("id");
-    ghost.classList.add("death-fade");
-    el("effect-layer").appendChild(ghost);
-    target.classList.add("fallen");
-    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
-  }
-  function toast(message) {
-    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
-    surface.appendChild(el("toast"));
-    el("toast").textContent = message;
-    animate(el("toast"), "visible");
-  }
-  function update(state) {
-    latestState = state;
-    var stage = DATA.stages[state.currentStage];
-    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
-    if (UI.updateCP) UI.updateCP(Game.getCP());
-    else el("cp").textContent = fmt(Game.getCP());
-    UI.countUp("gold", state.gold);
-    UI.countUp("coins", state.squadCoins);
-    el("region-name").textContent = UI.artRegion(stage.region).name;
-    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
-    el("stage-id").textContent = stageLabel(state.currentStage);
-    el("stage-name").textContent = stage.name;
-    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
-    el("timer").classList.toggle("urgent", remaining <= 10);
-    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
-    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + stage.bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
-    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
-    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
-    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
-    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
-    if (UI.updateExpedition) UI.updateExpedition(state);
-    ["repeat", "challenge"].forEach(function (mode) {
-      el(mode + "-mode").classList.toggle("active", state.mode === mode);
-      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
-    });
-    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
-    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
-    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
-    var result = el("battle-result");
-    result.classList.toggle("shown", state.battle.status !== "fighting");
-    result.classList.toggle("failed", state.battle.status === "fail");
-    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
-      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
-    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
-      var sprite = el("sprite-" + unit.id);
-      if (sprite && sprite.querySelector(".hp-fill")) {
-        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
-        sprite.classList.toggle("fallen", unit.hp <= 0);
-      }
-    });
-    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
-      var definition = DATA.mercenaries[index];
-      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
-      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
-        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
-    }).join("");
-    if (activeSheet) renderSheet(state);
-  }
-  function mercenaryCards(state) {
-    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
-      var def = DATA.mercenaries[index];
-      var stats = Game.mercenaryStats(merc.id);
-      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading"><svg viewBox="-42 -88 96 104" aria-hidden="true">' + figure(merc.id, def.color) + '</svg><div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
-        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
-        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
-          return UI.equipmentSlot(merc, slot, state);
-        }).join("") + '</div></article>';
-    }).join("");
-  }
-  function renderSheet(state) {
-    if (!activeSheet) return;
-    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
-      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
-    if (signature === sheetSignature) return;
-    sheetSignature = signature;
-    el("sheet-title").textContent = titles[activeSheet];
-    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
-    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
-    else if (activeSheet === "skills") UI.renderSkills(state);
-    else if (activeSheet === "settings") {
-      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
-      el("reset-game").addEventListener("click", function () {
-        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
-          Game.reset();
-          el("sheet").close();
-          toast("새로운 원정이 시작되었습니다");
-        }
-      });
-      UI.renderSaveControls();
-    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
-  }
-  function openSheet(name) {
-    activeSheet = name;
-    if (UI.sheetOpened) UI.sheetOpened(name);
-    sheetSignature = "";
-    renderSheet(latestState);
-    if (!el("sheet").open) el("sheet").showModal();
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
-  }
-  function init() {
-    Game.on("update", update);
-    Game.on("stageStart", function (state) { drawScene(state, true); });
-    Game.on("wave", function () { drawScene(Game.getState(), false); });
-    Game.on("hit", hit);
-    Game.on("unitDeath", death);
-    Game.on("levelUp", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
-    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
-    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
-    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
-    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
-    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
-    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
-    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
-    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
-    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
-    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
-    el("sheet").addEventListener("close", function () {
-      if (el("sheet").open) return;
-      activeSheet = null;
-      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
-    });
-    document.addEventListener("visibilitychange", function () {
-      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
-      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
-    window.addEventListener("pageshow", function (event) {
-      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
-    });
-    Game.init();
-    if (!document.hidden) Game.resume();
-    else hiddenAt = Date.now();
-  }
-  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
-    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
-})();
+"use strict";
+
+var UI = (function () {
+  var svgNS = "http://www.w3.org/2000/svg";
+  var activeSheet = null;
+  var hiddenAt = null;
+  var sheetSignature = "";
+  var latestState;
+  var titles = { mercenaries: "용병", equipment: "장비", fusion: "합성", skills: "스킬", settings: "설정" };
+  function el(id) { return document.getElementById(id); }
+  function fmt(n) {
+    if (!Number.isFinite(n)) return "0";
+    var abs = Math.abs(n);
+    if (abs >= 1e9) return (n / 1e9).toFixed(1).replace(/\.0$/, "") + "B";
+    if (abs >= 1e6) return (n / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
+    if (abs >= 1e4) return (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K";
+    return n.toLocaleString("ko-KR", { maximumFractionDigits: abs < 10 ? 2 : 1 });
+  }
+  function stageLabel(index) { return fmt(Math.floor(index / 10) + 1) + "-" + fmt(index % 10 + 1); }
+  function svgNode(tag, attrs, content) {
+    var node = document.createElementNS(svgNS, tag);
+    Object.keys(attrs || {}).forEach(function (key) { node.setAttribute(key, attrs[key]); });
+    if (content !== undefined) node.textContent = content;
+    return node;
+  }
+  function figure(type, color) {
+    if (type === "warrior") return '<path d="M-18-39L-25-3 14-2 17-39" fill="#FF9F1C"/><path d="M-10-13l-3 15m23-15 4 15" stroke="#FFC93C" stroke-width="9"/><path d="M-17-45h30l8 28-36 3z" fill="' + color + '"/><path d="M-9-44l6 26 12-3 1-23" fill="#ede0b4" opacity=".6"/><circle cy="-57" r="12" fill="#d2b69a"/><path d="M-13-55v-8l13-8 13 8v8z" fill="#FFC93C"/><path d="M-11-60h22m-11-9v14" stroke="#e4e5c8" stroke-width="3"/><path d="M20-37l14-20" stroke="#ceb9a0" stroke-width="7"/><path d="M32-52l15-29 5 2-10 32z" fill="#dce9df"/><path d="M30-54l15 7" stroke="#bd924e" stroke-width="4"/><path d="M-24-37l17 5-2 23-12 7-13-12 1-21z" fill="#FF9F1C" stroke="#d6c3a0" stroke-width="2"/><path d="M-23-31v19m-6-10h13" stroke="#d6c3a0" stroke-width="2"/>';
+    if (type === "archer") return '<path d="M-15-43l-9 41h30l8-41z" fill="#22C55E"/><path d="M-6-14l-3 17m14-17 3 17" stroke="#FFE9B8" stroke-width="6"/><path d="M-12-44h21l5 28-29-1z" fill="' + color + '"/><circle cy="-54" r="10" fill="#d5b9a0"/><path d="M-14-51q0-34 19-17l10 21-15-9z" fill="#3DDC97"/><path d="M-8-42l14 26" stroke="#bfa47a" stroke-width="4"/><path d="M5-36l20 5" stroke="#d5b9a0" stroke-width="6"/><path d="M23-59q30 27 0 50" fill="none" stroke="#cfad70" stroke-width="3"/><path d="M23-59v50m-12-24h30" stroke="#e6d6ac"/><path d="M40-36l6 3-6 3" fill="#e6d6ac"/>';
+    if (type === "mage") return '<path d="M-11-44L-24 2h46L10-44z" fill="' + color + '"/><path d="M-4-36l-8 35h23L4-36z" fill="#A855F7"/><circle cy="-52" r="10" fill="#d5b9a0"/><path d="M-23-56L-7-82 13-58l12 7z" fill="#B388FF"/><path d="M-14-60l20 4" stroke="#d6bc81" stroke-width="3"/><path d="M9-35l16 3" stroke="#b7a0ee" stroke-width="7"/><path d="M27-57L23 2" stroke="#bfa77b" stroke-width="4"/><path d="M28-72l8 9-9 10-7-11z" fill="#d2ccfa"/><circle cx="27" cy="-63" r="14" fill="#b7a0ee" opacity=".15"/>';
+    if (type === "slime") return '<path d="M-25-4q-4-24 11-31 9-12 19-2 22 3 23 28Q12 6-25-4" fill="' + color + '"/><ellipse cx="-10" cy="-27" rx="6" ry="3" fill="#eef5cf" opacity=".3"/><path d="M-12-17v5m15-5v5" stroke="#4A2C12" stroke-width="4"/><path d="M-7-6h8" stroke="#4A2C12" stroke-width="2"/>';
+    if (type === "boss") return '<path d="M-23-15L-29 2m47-17 9 17" stroke="#FF9F1C" stroke-width="15"/><path d="M-32-56l-8 41 28 9 40-6 8-39-29-19z" fill="' + color + '"/><path d="M-26-54l-16-23m62 25 19-24" stroke="' + color + '" stroke-width="10"/><path d="M-16-52l10 4m13 0 10-4" stroke="#fbe0a2" stroke-width="5"/><path d="M-3-35l-9 21h22L4-37" fill="#3DDC97"/><path d="M-38-39l-15 21m84-20 17 19" stroke="' + color + '" stroke-width="12"/>';
+    return '<path d="M-9-17l-6 20m22-20 7 20" stroke="#3DDC97" stroke-width="7"/><path d="M-17-42h30l7 28-39 2z" fill="' + color + '"/><path d="M-13-58l-15-6 8 17m29-12 17-6-8 19" fill="' + color + '"/><ellipse cy="-49" rx="17" ry="14" fill="' + color + '"/><path d="M-11-50l7 2m8 0 7-2" stroke="#4A2C12" stroke-width="3"/><path d="M-7-41h12" stroke="#f1dbb2" stroke-width="3"/><path d="M-20-31l-10 11" stroke="' + color + '" stroke-width="7"/><path d="M-31-39v39" stroke="#8A6A4A" stroke-width="5"/><path d="M-38-41h13l4 12-16 2z" fill="#FFC93C"/>';
+  }
+  function position(unit) {
+    if (unit.side === "mercenary") return [{ x: 143, y: 236 }, { x: 91, y: 255 }, { x: 47, y: 224 }][unit.position];
+    if (unit.type === "boss") return { x: 284, y: 245 };
+    return [{ x: 252, y: 242 }, { x: 313, y: 216 }, { x: 335, y: 271 }, { x: 274, y: 289 }][unit.position];
+  }
+  function drawUnit(unit, color, locked) {
+    var p = position(unit);
+    var group = svgNode("g", { id: "sprite-" + unit.id, transform: "translate(" + p.x + " " + p.y + ")",
+      "data-x": p.x, "data-y": p.y, class: "unit " + (locked ? "locked-unit" : unit.side) });
+    group.appendChild(svgNode("ellipse", { cx: 0, cy: 4, rx: unit.type === "boss" ? 40 : 25, ry: 7, fill: "#4A2C12", opacity: ".4" }));
+    var body = svgNode("g", { class: "unit-body" });
+    var fallback = svgNode("g", { class: "asset-fallback", transform: unit.type === "boss" ? "scale(1.6)" : "scale(1.15)" });
+    fallback.innerHTML = figure(unit.type, color); body.appendChild(fallback);
+    var region = DATA.stages[Game.getState().currentStage].region;
+    var key = unit.side === "mercenary" ? "merc." + unit.type + ".idle" : "mon." + UI.artRegion(region).monsters[unit.type];
+    UI.spriteImage(body, key, unit.type === "boss" ? 140 : 96);
+    body.addEventListener("animationend", function (event) {
+      if (event.target === body) body.classList.remove("lunge-left", "lunge-right", "hit-flash");
+    });
+    group.appendChild(body);
+    if (!locked) {
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44, height: 4, rx: 2, fill: "#FFE9B8" }));
+      group.appendChild(svgNode("rect", { x: -22, y: 13, width: 44 * unit.hp / unit.maxHp, height: 4, rx: 2,
+        fill: unit.side === "enemy" ? "#FF6B6B" : "#5CE07A", class: "hp-fill" }));
+    } else group.appendChild(svgNode("text", { x: 0, y: 20, "text-anchor": "middle", class: "locked-label" }, "미합류"));
+    if (unit.hp <= 0 && !locked) group.classList.add("fallen");
+    return group;
+  }
+  function drawScene(state, clearEffects) {
+    UI.sceneBackground(DATA.stages[state.currentStage].region);
+    if (clearEffects) el("effect-layer").replaceChildren();
+    el("mercenary-layer").replaceChildren();
+    el("enemy-layer").replaceChildren();
+    DATA.mercenaries.slice().reverse().forEach(function (merc) {
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      el("mercenary-layer").appendChild(drawUnit(unit || { id: merc.id, type: merc.id, side: "mercenary", position: merc.position }, merc.color, !unit));
+    });
+    state.battle.enemies.forEach(function (unit) { el("enemy-layer").appendChild(drawUnit(unit, DATA.stages[state.currentStage].palette[unit.type], false)); });
+  }
+  function animate(node, name) {
+    node.classList.remove(name);
+    void node.getBoundingClientRect();
+    node.classList.add(name);
+  }
+  function hit(event) {
+    var attacker = el("sprite-" + event.attackerId);
+    var target = el("sprite-" + event.targetId);
+    if (attacker) { UI.attackSprite(attacker); animate(attacker.querySelector(".unit-body"), event.side === "enemy" ? "lunge-left" : "lunge-right"); }
+    if (!target) return;
+    animate(target.querySelector(".unit-body"), "hit-flash");
+    var text = svgNode("text", { x: target.dataset.x, y: Number(target.dataset.y) - 78,
+      "text-anchor": "middle", class: "damage-number " + (event.crit ? "critical" : event.side === "enemy" ? "incoming" : "") },
+    (event.crit ? "✦ " : "") + fmt(event.amount));
+    el("effect-layer").appendChild(text);
+    text.addEventListener("animationend", function () { text.remove(); }, { once: true });
+  }
+  function death(event) {
+    var target = el("sprite-" + event.id);
+    if (!target) return;
+    var ghost = target.cloneNode(true);
+    ghost.removeAttribute("id");
+    ghost.classList.add("death-fade");
+    el("effect-layer").appendChild(ghost);
+    target.classList.add("fallen");
+    ghost.addEventListener("animationend", function (e) { if (e.target === ghost) ghost.remove(); });
+  }
+  function toast(message) {
+    var surface = document.querySelector("#offline-report[open]") || document.querySelector("#fusion-reveal[open]") || document.querySelector("#sheet[open]") || el("app");
+    surface.appendChild(el("toast"));
+    el("toast").textContent = message;
+    animate(el("toast"), "visible");
+  }
+  function update(state) {
+    latestState = state;
+    var stage = DATA.stages[state.currentStage];
+    var remaining = Math.max(0, stage.timeLimit - state.battle.ticks / 10);
+    if (UI.updateCP) UI.updateCP(Game.getCP());
+    else el("cp").textContent = fmt(Game.getCP());
+    UI.countUp("gold", state.gold);
+    UI.countUp("coins", state.squadCoins);
+    el("region-name").textContent = UI.artRegion(stage.region).name;
+    el("region-subtitle").textContent = UI.artRegion(stage.region).subtitle;
+    el("stage-id").textContent = stageLabel(state.currentStage);
+    el("stage-name").textContent = UI.artRegion(stage.region).stages?.[state.currentStage % 10] || stage.name;
+    el("timer").innerHTML = fmt(Math.ceil(remaining)) + "<small>초</small>";
+    el("timer").classList.toggle("urgent", remaining <= 10);
+    el("time-fill").style.width = (remaining / stage.timeLimit * 100) + "%";
+    el("wave-label").textContent = stage.boss && state.battle.waveIndex === 2 ? "보스 · " + UI.artRegion(stage.region).bossName : "웨이브 " + fmt(state.battle.waveIndex + 1) + " / " + fmt(stage.waves.length);
+    el("selected-stage").textContent = (state.difficulty === "chaos" ? "카오스 " : "") + stageLabel(state.currentStage);
+    var unlocks = state.difficulty === "chaos" ? state.chaosUnlockedStages : state.unlockedStages;
+    el("previous-stage").disabled = !unlocks.includes(state.currentStage - 1);
+    el("next-stage").disabled = !unlocks.includes(state.currentStage + 1);
+    if (UI.updateExpedition) UI.updateExpedition(state);
+    ["repeat", "challenge"].forEach(function (mode) {
+      el(mode + "-mode").classList.toggle("active", state.mode === mode);
+      el(mode + "-mode").setAttribute("aria-pressed", String(state.mode === mode));
+    });
+    el("mode-hint").textContent = state.mode === "repeat" ? "현재 스테이지에서 전투와 성장을 반복합니다" : "클리어하면 다음 스테이지로 나아갑니다";
+    el("gold-rate").textContent = fmt(state.stats.goldPerSec);
+    el("kill-rate").textContent = fmt(state.stats.killsPerSec);
+    var result = el("battle-result");
+    result.classList.toggle("shown", state.battle.status !== "fighting");
+    result.classList.toggle("failed", state.battle.status === "fail");
+    result.innerHTML = state.battle.status === "clear" ? "<span>✦</span><strong>스테이지 클리어</strong><small>다음 전투를 준비합니다</small>" :
+      "<span>↻</span><strong>" + (remaining <= 0 ? "시간 초과" : "용병단 전멸") + "</strong><small>체력을 회복하고 다시 도전합니다</small>";
+    state.battle.units.concat(state.battle.enemies).forEach(function (unit) {
+      var sprite = el("sprite-" + unit.id);
+      if (sprite && sprite.querySelector(".hp-fill")) {
+        sprite.querySelector(".hp-fill").setAttribute("width", 44 * unit.hp / unit.maxHp);
+        sprite.classList.toggle("fallen", unit.hp <= 0);
+      }
+    });
+    el("squad-strip").innerHTML = state.mercenaries.map(function (merc, index) {
+      var definition = DATA.mercenaries[index];
+      var unit = state.battle.units.find(function (u) { return u.id === merc.id; });
+      return '<div class="squad-member ' + (merc.unlocked ? "" : "not-recruited") + '"><span class="member-dot" style="background:' + definition.color + '"></span><span>' + definition.name + '</span><small>' +
+        (merc.unlocked ? (unit && unit.hp <= 0 ? "전투 불능" : "Lv. " + fmt(merc.level)) : stageLabel(definition.unlockStage) + " 해금") + "</small></div>";
+    }).join("");
+    if (activeSheet) renderSheet(state);
+  }
+  function mercenaryCards(state) {
+    return '<p class="sheet-intro">함께 싸우고, 함께 성장합니다.</p>' + state.mercenaries.map(function (merc, index) {
+      var def = DATA.mercenaries[index];
+      var stats = Game.mercenaryStats(merc.id);
+      return '<article class="merc-card ' + (merc.unlocked ? "" : "locked-card") + '"><div class="merc-card-heading">' + UI.portrait(merc.id, figure(merc.id, def.color)) + '<div><p>' + def.role + '</p><h3>' + def.name + '</h3></div><strong>' + (merc.unlocked ? 'Lv. ' + fmt(merc.level) : '미합류') + '</strong></div>' +
+        (merc.unlocked ? '<div class="xp-label"><span>경험치</span><span>' + fmt(merc.xp) + ' / ' + fmt(DATA.xpToNext(merc.level)) + '</span></div><div class="xp-track"><div style="width:' + (merc.xp / DATA.xpToNext(merc.level) * 100) + '%"></div></div>' : '<p class="unlock-note">' + stageLabel(def.unlockStage) + ' 클리어 시 합류</p>') +
+        '<dl class="merc-stats"><div><dt>HP</dt><dd>' + fmt(stats.hp) + '</dd></div><div><dt>공격력</dt><dd>' + fmt(stats.atk) + '</dd></div><div><dt>방어력</dt><dd>' + fmt(stats.def) + '</dd></div><div><dt>공격속도</dt><dd>' + fmt(stats.attackSpeed) + '/초</dd></div><div><dt>치명타 확률</dt><dd>' + fmt(stats.critChance * 100) + '%</dd></div><div><dt>치명타 피해</dt><dd>' + fmt(stats.critDamage * 100) + '%</dd></div></dl><div class="equipment-slots">' + DATA.equipmentSlots.map(function (slot) {
+          return UI.equipmentSlot(merc, slot, state);
+        }).join("") + '</div></article>';
+    }).join("");
+  }
+  function renderSheet(state) {
+    if (!activeSheet) return;
+    var signature = activeSheet + (activeSheet === "skills" ? JSON.stringify([state.mercenaries, state.skillBooks]) : activeSheet === "mercenaries" ? JSON.stringify([state.mercenaries, state.inventory]) :
+      UI.equipmentSignature ? UI.equipmentSignature(activeSheet, state) : "");
+    if (signature === sheetSignature) return;
+    sheetSignature = signature;
+    el("sheet-title").textContent = titles[activeSheet];
+    if (activeSheet === "mercenaries") el("sheet-content").innerHTML = mercenaryCards(state);
+    else if (activeSheet === "equipment" || activeSheet === "fusion") UI.renderEquipmentSheet(activeSheet, state);
+    else if (activeSheet === "skills") UI.renderSkills(state);
+    else if (activeSheet === "settings") {
+      el("sheet-content").innerHTML = '<p class="sheet-intro">진행 상황은 자동으로 저장됩니다.</p><dl class="settings-info"><div><dt>버전</dt><dd>' + DATA.version + '</dd></div><div><dt>저장 형식 · schemaVersion</dt><dd>' + fmt(state.schemaVersion) + '</dd></div><div><dt>자동 저장 간격</dt><dd>' + fmt(DATA.autosaveMs / 1000) + '초</dd></div></dl><div class="reset-panel"><h3>새로운 원정</h3><p>모든 용병의 성장과 보유 재화를 초기화합니다.</p><button id="reset-game" class="danger-button">게임 초기화</button></div>';
+      el("reset-game").addEventListener("click", function () {
+        if (window.confirm("모든 진행 상황과 재화를 초기화할까요? 이 작업은 되돌릴 수 없습니다.")) {
+          Game.reset();
+          el("sheet").close();
+          toast("새로운 원정이 시작되었습니다");
+        }
+      });
+      UI.renderSaveControls();
+    } else el("sheet-content").innerHTML = '<div class="placeholder"><span>◇</span><h3>준비 중</h3><p>지금은 용병단과 첫 원정을 떠나 보세요.</p><small>이 기능은 이후 업데이트에서 열립니다.</small></div>';
+  }
+  function openSheet(name) {
+    activeSheet = name;
+    if (UI.sheetOpened) UI.sheetOpened(name);
+    sheetSignature = "";
+    renderSheet(latestState);
+    if (!el("sheet").open) el("sheet").showModal();
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.toggle("selected", button.dataset.tab === name); });
+  }
+  function init() {
+    Game.on("update", update);
+    Game.on("stageStart", function (state) { drawScene(state, true); });
+    Game.on("wave", function () { drawScene(Game.getState(), false); });
+    Game.on("hit", hit);
+    Game.on("unitDeath", death);
+    Game.on("levelUp", function (event) { UI.levelUp(event); toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + " 레벨 " + fmt(event.level) + " 달성"); });
+    Game.on("mercenaryUnlock", function (event) { toast(DATA.mercenaries.find(function (m) { return m.id === event.id; }).name + "가 용병단에 합류했습니다"); });
+    Game.on("storageError", function (event) { el("save-status").textContent = "저장 확인 필요"; el("save-status").title = event.message; toast(event.message); });
+    Game.on("saved", function () { el("save-status").textContent = "저장됨"; });
+    el("previous-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage - 1); });
+    el("next-stage").addEventListener("click", function () { Game.selectStage(latestState.currentStage + 1); });
+    el("repeat-mode").addEventListener("click", function () { Game.setMode("repeat"); });
+    el("challenge-mode").addEventListener("click", function () { Game.setMode("challenge"); });
+    document.querySelectorAll("[data-tab]").forEach(function (button) { button.addEventListener("click", function () { openSheet(button.dataset.tab); }); });
+    el("close-sheet").addEventListener("click", function () { el("sheet").close(); });
+    el("sheet").addEventListener("click", function (event) { if (event.target === el("sheet") && event.clientY < el("sheet").getBoundingClientRect().top) el("sheet").close(); });
+    el("sheet").addEventListener("close", function () {
+      if (el("sheet").open) return;
+      activeSheet = null;
+      document.querySelectorAll("[data-tab]").forEach(function (button) { button.classList.remove("selected"); });
+    });
+    document.addEventListener("visibilitychange", function () {
+      if (document.hidden) { hiddenAt = Date.now(); Game.pause(); }
+      else { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    window.addEventListener("pagehide", function () { hiddenAt = Date.now(); Game.pause(); });
+    window.addEventListener("pageshow", function (event) {
+      if (event.persisted && !document.hidden) { if (hiddenAt !== null) Game.catchUp(Date.now() - hiddenAt); hiddenAt = null; Game.resume(); }
+    });
+    Game.init();
+    if (!document.hidden) Game.resume();
+    else hiddenAt = Date.now();
+  }
+  return { fmt: fmt, init: init, openSheet: openSheet, toast: toast,
+    refreshSheet: function () { sheetSignature = ""; renderSheet(Game.getState()); } };
+})();
diff --git a/style.css b/style.css
index 2ed660fedb4746f9054fdaac656a630420c8b83d..5044a7b4f3a97bddebe0c28df495c39ea7d8cb4f
--- a/style.css
+++ b/style.css
@@ -55,7 +55,7 @@
 .scene-topline { position: absolute; top: 10px; left: 9px; right: 9px; display: flex; justify-content: space-between; gap: 4px; z-index: 1; font-size: 12px; }
 .scene-topline > span { background: var(--panel); border: 3px solid var(--ink); border-radius: 18px; padding: 4px 6px; }
 .auto-badge i { display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: var(--accent); margin-right: 4px; }
-#battle-svg { width: 100%; display: block; max-height: 430px; }
+#battle-svg { position: absolute; inset: 0; width: 100%; height: 100%; display: block; }
 .scene-caption { position: absolute; bottom: 10px; left: 12px; right: 12px; text-align: center; font-size: 12px; }
 #region-subtitle { background: var(--panel); padding: 3px 10px; border-radius: 18px; }
 .locked-unit { opacity: .3; } .locked-label { fill: var(--ink); font-size: 12px; font-weight: bold; }
@@ -157,7 +157,9 @@
 .drop-card .item-card { box-shadow: 0 0 22px 6px color-mix(in srgb, var(--rarity) 65%, transparent); }
 .battle-scene::after, #fusion-reveal::after, #offline-report::after { content: ''; position: absolute; inset: 0; border: 6px solid transparent; border-radius: inherit; pointer-events: none; z-index: 10; }
 #fusion-reveal::after, #offline-report::after { position: fixed; }
-.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; }
+.loot-flash::after, .loot-flash-strong::after { animation: loot-border .4s ease-out; padding: 7px;
+  background: conic-gradient(var(--danger), var(--gold), var(--accent), var(--bg-top), var(--r-epic), var(--danger));
+  mask: linear-gradient(white 0 0) content-box, linear-gradient(white 0 0); mask-composite: exclude; opacity: 0; }
 #cp.cp-jump { animation: cp-jump .3s ease-out; transform-origin: left center; }
 #fusion-reveal { margin: auto; width: calc(100% - 24px); max-width: 430px; padding: 20px 12px; text-align: center; }
 #reveal-progress { margin: 14px 0 18px; font-size: 13px; } #reveal-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 8px; }
@@ -199,7 +201,7 @@
 @keyframes sheet-in { from { opacity: 0; translate: 0 30px; } }
 @keyframes toast { 0%, 100% { opacity: 0; translate: 0 8px; } 10%, 85% { opacity: 1; translate: 0 0; } }
 @keyframes drop-flight { 0% { transform: scale(0) rotate(-15deg); opacity: 0; } 20% { transform: translateY(-35px) scale(1.2) rotate(8deg); opacity: 1; } 40%, 72% { transform: translateY(-24px) scale(1) rotate(-3deg); opacity: 1; } 100% { transform: translate(var(--drop-x), var(--drop-y)) scale(0); opacity: 0; } }
-@keyframes loot-border { 15%, 60% { border-color: var(--gold); box-shadow: inset 0 0 20px var(--gold), inset 0 0 40px var(--danger), inset 0 0 60px var(--r-epic), inset 0 0 80px #8FD3FF; } 35% { border-color: var(--accent); } 100% { border-color: transparent; } }
+@keyframes loot-border { 15%, 60% { opacity: 1; } 100% { opacity: 0; } }
 @keyframes cp-jump { 40% { scale: 1.1; text-shadow: 0 0 16px var(--gold); } }
 @keyframes item-flip { 0%, 25% { transform: rotateY(180deg) scale(.85); } 75% { transform: rotateY(-12deg) scale(1.05); } 100% { transform: rotateY(0); } }
 @keyframes fusion-flash { 35% { box-shadow: 0 0 28px 8px var(--gold); filter: brightness(1.3); } }
diff --git a/test/art-checks.js b/test/art-checks.js
new file mode 100644
index 0000000000000000000000000000000000000000..160bf3084c88e467544b0767f28d3e2e84197c66
--- /dev/null
+++ b/test/art-checks.js
@@ -0,0 +1,92 @@
+"use strict";
+const assert = require("node:assert/strict");
+
+module.exports = async function ({ evaluate, command, screenshot, delay }) {
+  await command("Emulation.setDeviceMetricsOverride", { width: 375, height: 844, deviceScaleFactor: 1, mobile: true });
+  await evaluate("Game.reset(); Game.pause()");
+  assert.equal(await evaluate(`Promise.all(Object.values(DATA.assets).map(src => {
+    const image = new Image(); image.src = src; return image.decode().then(() => true);
+  })).then(images => images.length)`), 28);
+  await delay(300);
+  await screenshot("restyle-375-battle");
+  const layout = await evaluate(`({ width: document.documentElement.scrollWidth,
+    small: [...document.querySelectorAll('button')].filter(b => {
+      const r = b.getBoundingClientRect(); return r.height > 0 && (r.width < 44 || r.height < 44);
+    }).map(b => b.outerHTML),
+    palette: getComputedStyle(document.documentElement).getPropertyValue('--panel').trim(),
+    sprites: document.querySelectorAll('#battle-svg .sprite-image').length,
+    breathing: getComputedStyle(document.querySelector('.unit-body')).animationDuration })`);
+  assert.ok(layout.width <= 375); assert.deepEqual(layout.small, []);
+  assert.equal(layout.palette, "#FFF8E7"); assert.equal(layout.sprites, 5); assert.equal(layout.breathing, "2s");
+  assert.ok(await evaluate(`new Promise(resolve => {
+    const sprite = document.getElementById('sprite-warrior'), image = sprite.querySelector('image');
+    UI.attackSprite(sprite); const start = performance.now();
+    const attack = image.getAttribute('href').endsWith('warrior_attack.png');
+    function sample(now) {
+      if (image.getAttribute('href').endsWith('warrior_idle.png')) resolve(attack && now-start >= 120 && now-start < 200);
+      else requestAnimationFrame(sample);
+    } requestAnimationFrame(sample);
+  })`));
+  await evaluate("UI.levelUp({id:'warrior',level:2})");
+  assert.equal(await evaluate("document.querySelector('.level-up-label').textContent"), "LEVEL UP!");
+  await evaluate("UI.countUp('gold', 1000)");
+  await delay(150);
+  assert.notEqual(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  await delay(200);
+  assert.equal(await evaluate("document.getElementById('gold').textContent"), "1,000");
+  console.log("PASS 28 PNG decodes, 375px, 44px targets, 2s idle, 120ms attack, level label and 300ms counter");
+
+  await evaluate(`window.artFixture = function(stage, boss) {
+    Game.reset(); Game.pause(); const save = JSON.parse(Game.save()), state = save.state;
+    state.currentStage = stage; state.unlockedStages = Array.from({length:30}, (_,i)=>i);
+    state.clearedStages = Array.from({length:29}, (_,i)=>i);
+    state.mercenaries.forEach(m => { m.unlocked = true; m.level = 150; });
+    state.battle = Battle.start(stage, state.mercenaries);
+    if (boss) while(state.battle.waveIndex < 2) Battle.tick(state.battle, ()=>{});
+    if (!Game.load(JSON.stringify(save))) throw new Error('Invalid art fixture');
+    Game.pause();
+  }`);
+  for (let region = 0; region < 3; region++) {
+    await evaluate("artFixture(" + (region * 10 + 9) + ", false)");
+    await delay(150);
+    const expected = ["forest", "peak", "volcano"][region];
+    assert.ok(await evaluate("document.querySelector('.region-image').getAttribute('href').endsWith('/" + expected + ".png')"));
+    assert.ok(await evaluate(`Array.from(document.querySelectorAll('#enemy-layer image')).every(i =>
+      Object.values(DATA.artRegions[${region}].monsters).some(name => i.getAttribute('href').endsWith('/'+name+'.png')))`));
+    await screenshot("restyle-region-" + expected);
+    await evaluate("artFixture(" + (region * 10 + 9) + ", true)");
+    await delay(150);
+    assert.equal(await evaluate("document.querySelector('#enemy-layer image').getAttribute('height')"), "140");
+    assert.ok(await evaluate(`document.querySelector('#enemy-layer image').getAttribute('href').endsWith('/'+DATA.artRegions[${region}].monsters.boss+'.png')`));
+    await screenshot("restyle-boss-" + expected);
+  }
+  console.log("PASS three regional backgrounds, all regional type mappings and 140px bosses");
+
+  for (const tab of ["mercenaries", "equipment", "fusion", "skills", "settings"]) {
+    await evaluate("UI.openSheet('" + tab + "')"); await delay(250);
+    assert.ok(await evaluate(`document.documentElement.scrollWidth <= 375 && document.getElementById('sheet').scrollWidth <= document.getElementById('sheet').clientWidth`));
+    assert.deepEqual(await evaluate(`Array.from(document.querySelectorAll('#sheet button')).filter(b => {
+      const r=b.getBoundingClientRect(); return r.height>0 && (r.width<44 || r.height<44);
+    }).map(b=>b.outerHTML)`), []);
+    assert.equal(await evaluate("getComputedStyle(document.querySelector('[data-tab=" + tab + "]')).transform"), "matrix(1, 0, 0, 1, 0, -6)");
+    await screenshot("restyle-sheet-" + tab);
+    await evaluate("document.getElementById('sheet').close()");
+  }
+  console.log("PASS all five sheets: 375px no overflow, 44px buttons and raised active tab");
+
+  // A missing manifest entry must issue no request. Invalid PNG bytes exercise real image onerror.
+  await evaluate(`window.artAssets = {...DATA.assets}; delete DATA.assets['merc.warrior.idle'];
+    DATA.assets['merc.archer.idle'] = 'data:image/png;base64,AAAA';
+    DATA.assets['bg.peak'] = 'data:image/png;base64,AAAA'; artFixture(19, false)`);
+  await delay(300);
+  assert.ok(await evaluate(`!document.querySelector('#sprite-warrior image') &&
+    getComputedStyle(document.querySelector('#sprite-warrior .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#sprite-archer .asset-fallback')).display !== 'none' &&
+    getComputedStyle(document.querySelector('#scene-background .asset-fallback')).display !== 'none'`));
+  await screenshot("restyle-fallbacks");
+  // Late load/error callbacks from a replaced region must also be harmless.
+  await evaluate(`const orphan = document.createElementNS('http://www.w3.org/2000/svg','image');
+    orphan.dataset.asset='test.orphan'; UI.assetLoaded(orphan); UI.assetError(orphan);
+    DATA.assets = artAssets; Game.reset(); Game.pause()`);
+  console.log("PASS missing mapping, actual image decode failures and detached-image callbacks");
+};
diff --git a/test/browser-art.js b/test/browser-art.js
index 41706225e45b31c80b9542c3eb946562a53ffe97..16cbd68da4a63055c1d2ffc509faeaabbcce751d
--- a/test/browser-art.js
+++ b/test/browser-art.js
@@ -1,6 +1,7 @@
 "use strict";
 
-// Optional integration smoke test. Node 22+ and an existing Chrome/Edge install; no packages.
+// Standalone art checks. Node 22+ and an existing Chrome/Edge install; no packages.
+// FONT_FALLBACK=1 explicitly substitutes empty remote font CSS to verify the system fallback.
 const assert = require("node:assert/strict");
 const fs = require("node:fs");
 const os = require("node:os");

tokens used
157,553
Implemented the full restyle, image sprites, icons, animations, and changelog while preserving gameplay.

- **38 tests pass**; JavaScript syntax checks clean.
- **375px verified**: no horizontal overflow, ≥44px buttons.
- Regional assets, attack timing, and vector fallbacks verified.

Google Fonts is blocked by this environment. System-font fallback works; zero-error browser checks passed with remote font CSS stubbed.
